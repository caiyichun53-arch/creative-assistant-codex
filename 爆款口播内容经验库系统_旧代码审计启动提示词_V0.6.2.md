# 爆款口播内容经验库系统｜旧代码审计启动提示词 V0.6.2

请先完整读取我上传的两项材料：

1.《爆款口播内容经验库系统_最终完整执行总控文档_V0.6.2_无损汇编版》
2. 现有旧代码仓库（完整文件夹或 ZIP，包含当前代码、配置、迁移、测试、脚本和文档）

## 文件定位与优先级

- V0.6.2 是当前唯一完整设计事实来源和最高级执行基线。
- 旧代码仓库只是待审计的现有实现，不得反向修改业务设计。
- 冲突优先级：我在本对话中的最新明确指令 ＞ V0.6.2 ＞ 旧代码现状。
- 不要默认全部保留，也不要默认全部推倒重建。
- 不要开发独立 Agent Runtime。
- Codex 只负责代码开发、审计、迁移和验证，不作为生产宿主。
- Hermes 是生产 Host；OpenClaw 只作为替代方案。
- 本对话只执行 GOAL-00 旧代码审计，不进入 GOAL-01，不开始大规模改代码。

## 本次唯一目标

对现有旧代码进行完整审计，判断每个模块应当：

- `keep`：直接保留；
- `adapt`：保留核心实现并改造；
- `wrap`：通过 Adapter / Port 隔离包装；
- `replace`：仅在与 V0.6.2 根本冲突、改造成本明显高于重写时替换。

不得只按文件名或目录结构判断，必须结合代码、依赖、测试、运行方式和真实副作用。

## 审计步骤

### 一、冻结当前仓库现场

先检查并记录：

- 当前 Git 分支、HEAD commit、tag；
- 未提交修改和未跟踪文件；
- 仓库目录结构；
- 依赖与运行环境；
- 数据库迁移现状；
- 已有配置、密钥占位和外部脚本；
- 现有测试、fixture、样本和验证工具；
- 当前是否能够启动、测试或部分运行。

不得执行：

- `git reset --hard`；
- 清空仓库；
- 删除旧代码；
- 批量格式化；
- 自动升级全部依赖；
- 创建新的平行系统；
- 覆盖现有迁移历史。

### 二、逐模块审计

至少覆盖：

- Core API、业务状态与数据库；
- 账号、视频、快照、D/P 时间模型；
- MediaCrawler 相关采集；
- 网易云独立采集脚本；
- ASR、评论、基线、候选与分析；
- 正式资料研究的 SearchProvider / Fetcher；
- 选题、研究、内容计划、稿件与审核；
- 经验、证据、P+、实验与纠错传播；
- ModelGateway、Prompt、配置和 Skill；
- 飞书、Hermes、OpenClaw、Codex 相关入口；
- Scheduler、Job、Worker、重试和恢复；
- Obsidian 派生；
- 测试、fixture、Replay、FakeClock 和故障注入；
- 部署、Docker、WSL2、脚本和运维工具。

每个模块必须给出：

1. 当前真实职责；
2. 主要入口、依赖和副作用；
3. 是否直接写数据库或业务状态；
4. 是否符合 V0.6.2 的能力边界；
5. 可复用部分；
6. 冲突部分；
7. 分类：`keep / adapt / wrap / replace`；
8. 分类依据；
9. 迁移风险；
10. 后续最小验证方式。

### 三、重点检查的禁止项

必须检查旧代码是否存在：

- 飞书直连替代 Hermes；
- 自建通用 Agent Runtime；
- Codex 被当作生产宿主；
- Adapter、Skill 或 Hermes 直接操作 ORM、状态机或数据库；
- 正式 Skill 自动静默修改或直接发布；
- 正式资料研究混入视频平台搜索、ASR、评论或视频分析；
- 稿件、经验、研究结果和发布稿原地覆盖；
- 只引用 current 对象而未冻结具体版本；
- 批准稿和实际发布稿混为一体；
- 缺少模型、Prompt、配置、输入和人工修改追溯；
- 验证代码和生产代码采用两套不同业务逻辑；
- 删除测试资产后生产系统无法运行；
- 为保留旧代码而修改已经冻结的业务边界；
- 不必要的 Redis、Celery、Temporal、向量库或多 Agent 体系。

### 四、运行与测试

在不破坏仓库的前提下，尽量运行：

- 现有单元测试；
- 现有集成测试；
- 静态检查；
- 数据库迁移检查；
- 关键模块的最小启动验证。

无法运行时，必须准确记录：

- 缺少什么；
- 是真实阻塞、外部门禁还是环境缺失；
- 是否影响模块分类；
- 后续怎样用 fixture / Replay 验证。

不得因为缺少外部凭证就把可通过 fixture 验证的模块判定为不可实施。

## 必须输出的文件

只生成仓库内的审计文件，不修改正式业务代码：

1. `CURRENT_REPOSITORY_BASELINE.md`
2. `LEGACY_CODE_AUDIT.md`
3. `MODULE_REUSE_MATRIX.yaml`
4. `implementation_progress/GOAL-00.md`
5. `GOAL-00_VALIDATION_REPORT.md`

### `MODULE_REUSE_MATRIX.yaml` 最低字段

```yaml
modules:
  - module_id:
    path:
    current_responsibility:
    decision: keep | adapt | wrap | replace
    reusable_parts: []
    conflicts: []
    migration_actions: []
    dependencies: []
    validation:
    risk_level: low | medium | high
    design_refs: []
```

## 输出结论格式

最终回复必须依次给出：

1. GOAL-00 审计是否完成；
2. 仓库当前是否可运行；
3. 模块分类统计；
4. 阻塞级问题；
5. 高风险迁移项；
6. 明确可直接复用的部分；
7. 明确必须替换的部分；
8. 是否存在默认全量重建的必要；
9. 是否可以进入 GOAL-01；
10. 需要我确认的事项。

## 停止条件

满足以下任一情况必须暂停，不得自行继续：

- V0.6.2 与代码事实出现真正不可实施的矛盾；
- 仓库并非完整版本或缺失关键目录；
- Git 现场无法安全冻结；
- 需要删除或覆盖大批旧代码；
- 需要改变业务边界、用户流程或数据口径；
- 模块应 `adapt` 还是 `replace` 的选择会显著改变成本或数据安全；
- 需要真实凭证、平台账号或不可替代外部依赖才能继续。

## 两轮审查要求

完成初步分类后，再执行第二轮复核：

- 检查是否过度重写；
- 检查是否为了复用而保留错误架构；
- 检查是否把领域差异写死；
- 检查是否增加无必要 Agent、中间件、状态或表；
- 检查是否遗漏历史数据迁移；
- 检查是否破坏 Portable Skill、Host Binding、Core/Materializer 的责任边界；
- 检查是否保留完整端到端追溯和不可变历史。

本轮结束后停止。等待我确认模块复用矩阵，再进入 GOAL-01。
