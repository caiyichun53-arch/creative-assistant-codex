# Validation Evidence Directory

This directory is the local evidence root for external live gate runs.

The directory ignores run output by default because evidence may contain
redacted request summaries, response hashes, local paths, provider metadata,
logs or other sensitive validation material.

Only this README and `.gitignore` should be tracked.
