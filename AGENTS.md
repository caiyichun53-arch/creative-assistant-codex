# 项目级硬边界

本文件只规定项目级硬边界，不是第二份完整设计文档。正式设计、当前决策状态、Stage 和 Skill 的细节以项目内对应文档为准；具体 cold-start 流程、Skill 字段、模型 Prompt、数据结构和当前 run 状态不在此重复。

- Creation Assistant Core 负责素材、正式业务数据、选题、研究、内容生成、人工决定和经验回流。
- 正式业务数据和正式状态只能通过 Core 的正式业务入口修改。
- 所有交互入口都必须回到同一条正式业务主线，不能产生第二套正式状态或正式资料。
- Hermes 负责交互和模型调用承载；Core 负责正式业务数据、正式状态及其变更。Hermes 不得绕过 Core 直接写入正式数据或建立平行业务状态。
- 外部采集和本地转写是被调用的外部能力，与内容业务核心分开管理，但必须保持可用。
- Agent 可以理解指令、整理材料、调用允许的业务入口和汇报结果，但不能替用户作出正式业务决定。
- Agent 不得跳过人工确认、擅自改变正式状态、替换正式流程或直接修改正式资料。
- 模型只能按明确配置运行；不得自动换模型、换服务、fallback 或降低要求。
- 测试通过只代表验证通过，不代表正式业务链路完成；正式完成必须有真实端到端业务结果，并能对应到正式数据和人工决定。
