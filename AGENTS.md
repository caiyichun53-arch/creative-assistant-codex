# 核心包规则

本项目只保留内容业务核心，以及真实采集和本地转写所需的外部依赖。

- 核心负责：素材采集、正式数据、选题、研究、内容生成、人工确认与经验回流。
- 模型只能按明确配置运行；不得自动换模型、换服务或降级。
- 正式数据只能经过核心业务入口写入。
- 不保留测试、验收、阶段控制、监控、定时启动器或旧式直连入口。
- 保留两层防跑偏钩子：实施代码变更时自动核对正式设计与可执行业务契约；真实运行和正式写入时自动核对同一契约并在不一致时阻断。它们只在实施或正式业务执行时生效，不介入普通讨论、方案澄清和日常问答。
- 两套外部采集工具和本地转写能力不是自研核心；它们必须保持可用，但应与核心代码分开管理。

# Agent 平台与设计颗粒度

- 本系统建立在 AI Agent 平台之上。Agent 应通过自然对话理解上下文、处理普通调整并推进任务；不能把每一种改口、补充、范围微调或材料取舍都固化成僵硬流程。
- Agent 的实际承载方式尚未定案：GPT 客户端、经 Hermes 调用的模型 API，或两者混合，以及飞书远程收发任务如何接入同一上下文，都属于必须先验证的架构选择。不得假定其中任何一种已经确定，也不得据此直接构建。
- 飞书可以同时支持自然对话和耗时任务的异步回传，不把交互形态写死。无论通过哪种形态进入，只要触发既定业务流程，就必须按已确认路径执行：Agent 只能理解指令、调用允许的业务入口和汇报结果，不能自行跳步、改状态、绕过人工确认、替换正式流程或直接写入正式数据。
- 只有会改变正式业务边界、人工决定权、正式留存内容、外部或不可逆动作，或需要让不同入口保持一致的事项，才细化成系统规则和后续构建内容。
- 其他日常协商、表达调整和任务内推进，交由 Agent 的自然对话处理。设计时必须主动判断颗粒度，优先保留足够粗的业务规则，避免为覆盖对话细节而过度设计。
