# 项目规则与执行边界

## 适用范围与依据

本文件继承全局要求，只补充项目内的适用边界，不重复完整设计。

- 当前业务设计以 `docs/EFFECTIVE_DESIGN_BASELINE.md` 为依据，运行说明见 `TECHNICAL_MANUAL.md`。设计目标和当前实现程度必须区分，不凭旧代码或旧记录替代有效设计。
- 只读取本次任务需要的设计章节、当前决定和相关 Skill。正式业务 Skill 位于 runtime_skills，由业务入口装配并经 MCP 提供给 Agent；沿目录、注册清单和实际调用关系识别，不以本文件是否点名为准，不扫描无关 Skill、凭据、会话记录或缓存。
- 历史迁移记录、阶段记录中的“本轮”限制和完成要求，只适用于对应工作；不能自动变成所有日常任务的前置条件。用户明确要求继续某阶段时，再核对该阶段的有效要求和完成标准。
- 有效要求发生实质冲突且现有依据无法裁决时，指出具体条款、影响和需决定的事项；只暂停依赖该决定的工作，不自行选择新的业务规则。

## 授权、推进与完成

- 解释、检查、审查和诊断默认只读，不自动授权修复；用户同时明确要求修改的部分，按实施任务处理。
- 已授权、范围和设计明确的实施工作，应自主完成修改及与影响相称的验证，不因涉及业务规则或机制重复请求授权。保留已有未提交改动，不覆盖、丢弃或顺手修改无关内容。
- 仅在缺少实质影响结果的信息、超出授权或遇到真实权限障碍时，针对该事项询问或报告。必需的业务行为、数据流或边界无法从有效设计和已有授权确定时，报告“设计缺口”；多种等效实现方式不构成缺口。只暂停受影响部分，其余已授权工作继续。
- 新增或改变执行控制仍须符合全局要求，有明确设计、外部硬约束或真实故障依据；不能自行选择重试、超时、降级等策略或数值。必要性证据不等于超范围修改的授权。
- 部署、发布、向他人发送消息、凭据处理和重要数据删除，继续遵守已有安全与授权边界；本地代码或文档修改的授权不自动覆盖这些操作。
- 开发与文档任务以所要求的修改及必要验证为完成标准；验证不自动授权启动正式业务、调用真实外部模型或操作正式数据。测试通过不代表正式业务完成；声称业务完成，须有对应正式数据和人工决定的真实端到端结果，并说明尚未接通、验收或启用的部分。
- 多模型、子 Agent、复杂流程和全量测试仅在用户要求、适用专项流程或实际改动需要时采用，遵守已有授权和模型路由。必要检查通过后，没有新改动、失败或未解决的具体风险就停止。
- 删除机制时，沿关联核查其定义、调用方、持久状态、配置、测试、文档和运行时副本中的适用部分，确认授权可访问范围内无活动引用。无法核实的范围如实说明，不能声称全部清除；若影响安全删除，只暂停相应部分，不扩大权限或扫描无关内容。

## 正式业务硬边界

- Creation Assistant Core 负责素材、正式业务数据、选题、研究、内容生成、人工决定和经验回流。
- 正式业务数据和正式状态只能通过 Core 的正式业务入口修改。所有交互入口都必须回到同一条正式业务主线，不能产生第二套正式状态或正式资料。
- Core 通过 MCP 对接 Agent，不绑定当前使用的 Hermes；用户可以选择 Codex 或其他支持该接口的 Agent。任何 Agent 都不得绕过 Core 写正式数据或另建业务状态；模型执行由当前接入的 Agent 负责，Core 不选择模型或服务。
- 外部采集和本地转写是被调用的外部能力，与内容业务核心分开管理，但必须保持可用。涉及这些能力的改动应验证受影响部分；不把检查、启动或修复外部环境作为无关任务的前置步骤，环境故障也不自动授权越界修复。
- Agent 可以理解指令、整理材料、调用允许的入口和汇报结果，不能替用户作正式业务决定、跳过规定的人工确认、擅改正式状态或替换正式流程。当前对象及适用版本已有明确决定时，不重复询问，仍通过正式入口记录并校验；开发授权不等于业务审批，一次确认不适用于新对象、版本或业务节点。
- 模型和服务由当前接入的 Agent 管理。项目只通过 MCP 提供任务材料、规则和正式结果入口，不保存或解析业务模型选择，不直接调用模型。维护、测试、临时脚本也不得恢复旧模型路由；更换模型须在 Agent 侧按用户授权处理。
