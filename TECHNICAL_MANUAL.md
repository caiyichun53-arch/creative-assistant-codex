# 核心包使用说明

使用前需要准备两类外部能力：真实来源采集，以及本地音频转写。

核心运行只接受明确的领域、操作者、幂等标识和运行模式。真实来源、模型和正式数据均不允许通过临时脚本或隐式替代方式绕过核心。

候选选题必须经过人工选择，未选择的候选不会自动进入研究或内容生产。

模型由当前接入的 Agent 执行。项目不配置业务模型、服务地址或模型凭据；不存在测试专用的旧模型入口。Core 通过 MCP 提供材料和规则、校验并保存 Agent 提交的结果。历史记录中的模型信息仅供追溯，不得用于恢复调用。

综合热点汇报使用 MCP 的 `creation_assistant_hotspot_report`，Skill 为 `runtime_skills/hotspot_report`。`collect` 按 TrendRadar 当前平台配置获取完整单次快照并返回任务；`prepare` 接受同结构原快照用于重放；Agent 完成全部事件的合并、保留/排除及理由后，使用 `render` 提交原 `snapshot`、结构化 `output`，以及用户要求的 `top_n`（未要求条数则省略）。不能自行提交分数或仅处理前几条。

`render` 返回榜内、保留但榜外、偏好排除三部分，附原始来源、源更新时间、规则版本、分数公式和排除理由。语义判断由当前 Agent 完成，原分由 TrendRadar Python 环境计算；采集只调用其 DataFetcher，不启动 AI、通知或旧日常候选流程。此按需报告与领域热点转选题独立；音乐停用领域热点转选题，泛科普等适用领域保留；报告不产生正式候选和业务状态，不写正式库。不将 prepare 传入的回放快照描述为新采集。偏好调整按用户明确反馈修改，不能由一次运行自学扩展。


### 来源理解、成题与候选排序

成题步骤使用 `runtime_skills/source_to_topic`，当前版本 2.0.0、输出 v3。Core 装配完整已存正文、来源关系和当前领域规则；Agent 默认搜索理解普通线索，已有完整题目不强制重做。当前 Agent 使用自身工具，项目不指定模型或搜索模型服务。输出保留实际已读来源与发现，区分候选阶段理解和后续正式研究。

MCP 的 `source_to_topic` 提交现在完成同一条 Core 写入链：保存成形候选、合并同题同角度的支持材料，或记录不转题原因。不是只记录模型执行后等待另一个入口造候选。提交响应的 `continuation` 给出本批下一条待处理来源、接下来的整组评分任务，或本批完成结果。普通线索未搜索、候选核心结构为空或旧版输出不能作为当前候选；旧输入装配不与新 Skill 混跑。

音乐的专业乐评和技术分析排除来自领域规则，成题和排序都读取同一份规则；不会把该限制推广到泛科普。当前泛科普正式配置尚无冻结生产边界，不能把隔离验收的讨论范围当成正式启用。

评分步骤只使用 `runtime_skills/candidate_priority`。本批来源处理完成、支持材料合并后，Core 返回 `requires_external_intelligence`，任务类型为 `candidate_priority`，身份为 `run_id`、`domain_label`。现有 MCP 获取和提交外部结果入口承接该任务；提交后继续原批次，不新建日报。项目不调用或选择评分模型。

输入为同领域全部当前候选及已有支持材料，输出完整 0—100 整数制作优先级、理由、来源引用和限制，携带原输入指纹。Core 校验实际候选集、材料引用、分数范围及输入一致性并保存，再按总分降序、形成时间、标识稳定排序。Top 10 仅展示优先项；完整评分仍保存在 Core。快照读取对应 run 的评分，不读取后来重评分。规则正文以有效设计 5.3 和 Skill 为准，热点分不参与候选评分。

升级由 Core 的 schema 安装入口移除废弃的两张候选评分表，候选、支持材料、正式选题与用户决定不删除；旧分无兼容读取路径。没有当前有效评分的候选需重评，不能默认补分。该迁移不是启动正式日报，也不运行任何模型。
