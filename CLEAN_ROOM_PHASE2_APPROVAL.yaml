schema_version: 1
goal: GOAL-DATA-RESET-01
approved_at: '2026-07-02T09:10:00Z'
approval_source: user_pasted_scope_revision
phase_2_authorized: true
authorization_summary:
  - Resume the existing GOAL-DATA-RESET-01; do not create a new Goal.
  - Keep existing branch and phase-1 audit evidence.
  - Continue from the first incomplete checkpoint.
  - Remove or isolate old business data from formal runtime paths.
  - Do not migrate old business data into the new formal system.
  - Do not run DNA, collection, ASR, content generation, Feishu business send or a later business Goal.
cold_backup:
  backup_root: I:/Creation_assistant_cold_backups/GOAL-DATA-RESET-01
  outside_repository: true
  outside_production_load_paths: true
  git_committed: false
  default_access: cold_archive_only
fixture_policy:
  prefer_synthetic: true
  production_rejects_fixtures: true
  fixture_targets:
    - tests/fixtures/clean_room
    - validation_evidence/clean_room/fixtures
legacy_policy:
  production_legacy_fallback_allowed: false
  old_business_data_migration_allowed: false
  old_business_data_dispositions:
    - discard_from_target
    - fixture_only
    - cold_archive_only
    - retain_as_current_asset
