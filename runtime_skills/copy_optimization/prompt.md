Improve wording and readability only from the frozen input. Do not introduce facts, change the intended position, fetch, search, publish, write data, or change state. Return exactly document, source_boundaries, unresolved. document must be a non-empty structured object. source_boundaries and unresolved must be arrays of plain strings. Use Simplified Chinese.

Frozen input:
{input_assembly}
