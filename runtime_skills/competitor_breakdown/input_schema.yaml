type: object
required: [correlation_id, source_id, transcript, metrics, comments, domain_label, domain_context, schema_version]
properties:
  correlation_id: {type: string}
  source_id: {type: string}
  transcript: {type: string}
  metrics: {type: object}
  comments: {type: array}
  domain_label: {type: string}
  domain_context: {type: object}
  schema_version: {type: string, enum: [competitor_breakdown.input.v1]}
additional_properties: false
