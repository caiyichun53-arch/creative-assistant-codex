type: object
required: [correlation_id, domain_label, content_plan_context, frozen_breakdowns, schema_version]
properties:
  correlation_id: {type: string}
  domain_label: {type: string}
  content_plan_context: {type: object}
  frozen_breakdowns: {type: array, minItems: 2, maxItems: 6}
  schema_version: {type: string, enum: [experience_candidate_propose.input.v1]}
additional_properties: false
