Return exactly one JSON object with keys decision, candidate, source_ids. The frozen_breakdowns are the existing one-video breakdown skill's formal outputs; use their recorded structure_assessment, structure_grasp, spoken_progression, recurring_evidence_patterns, audience_reactions, content_type_evidence, full_analysis, and cannot_infer fields as evidence. The full_analysis.adaptation_suggestions section is only a source-specific reference; do not copy it into an experience candidate or treat it as a confirmed rule. Do not require or invent another breakdown format. Use only the supplied frozen breakdowns and content context. The context may be a pre-topic experience review or an approved content plan. Do not fetch material, infer missing facts, claim a writing method is effective, call a skill, access memory, write data, or change state.

First decide whether these specific sources support one concrete reusable spoken-content observation that is relevant to the supplied content-plan context. Sources may discuss different subject types because reusable writing methods are grouped first by expression form and structure depth. Do not reject them merely because one discusses a person, another a work, and another a case; judge whether they share one substantive organization or expression method. The content context may include existing_experience_summaries. Do not repeat, paraphrase, or slightly broaden an existing accepted, pending, rejected, or previously proposed observation. If the evidence only reinforces an existing observation, or supports no genuinely different observation, return {{"decision":"no_proposal","candidate":null,"source_ids":[]}}.

If they do, return decision "proposal". candidate must contain exactly summary, experience_layer, use_positions, trigger_signals, applicable_when, method, boundary, not_applicable_when.

Classify the candidate into exactly one layer:
- structure: the whole spoken piece has a reusable mainline, such as a problem or situation moving through conflict, response, turn and outcome, or a question moving through claim, evidence, comparison and conclusion. Use this only when at least two cited breakdowns show a real change in the mainline through spoken_progression. A simple list, chronological introduction, biography recap, or repeated item delivery is not structure.
- section_method: one spoken section has a reusable organization, such as how an opening enters a question, how a body turns from surface to cause, how a transition changes the question, or how an ending returns to the starting issue. It may contain several consecutive sentences, but it must stay within one section and must not describe the whole script.
- local_detail: one sentence-level delivery action is reusable, such as a concrete example, wording move, rhythm, or one-sentence contrast. It must be expressible in one sentence or two adjacent sentences at most. If the observation needs a full item, several examples, a body paragraph, a transition sequence, or a whole opening, it is not local_detail; classify it as section_method or return no_proposal.

Use exactly one main layer and do not combine opening, body, item delivery and ending into one full-script template. For structure, use_positions must be exactly ["whole_content"] and the summary must describe the mainline, not a single opener or phrase. For section_method, use_positions must name one or two of opening, body, transition, or ending. For local_detail, use_positions must be exactly ["sentence"]. trigger_signals must state the visible problem or content condition that calls for this experience. applicable_when must describe the content situation in plain Chinese. method must describe the concrete action to take. boundary and not_applicable_when must state what this experience cannot solve or when it must not be forced. All these arrays must be non-empty plain Chinese strings.

Do not propose a candidate merely because several videos share a listicle format. The following are not experiences by themselves: saying the item number and title, reporting a ranking, inserting a lyric or clip after each item, adding a one-line item label, or ordering a playlist. If the candidate can be summarized as "先报名次/歌名，再补一句定位或歌词" without a deeper organization, contrast, causal explanation, scene relation, or meaning-making move, return no_proposal. A listicle may support an experience only when it contains a substantive reusable choice beyond item delivery, and the summary must name that choice rather than the list format.

Sources must contain only supplied source_id values, cite at least three sources, and cover at least two distinct supplied account_ref values. account_ref is only for source diversity and is not content evidence. Every concrete action, sequence, quantity, scope, or condition stated anywhere in the candidate must be directly supported by at least two of the cited breakdowns; do not combine a detail found in only one source into the shared observation, and do not make it more specific than the evidence. If this requirement cannot be met, return no_proposal. A shared number, item count, person, song, topic, format label, or other surface coincidence is not a reusable method by itself; a proposal must describe a substantive choice in how spoken content is organized or expressed. Do not say or imply that the method is effective, successful, verified, mature, popular, universal, proven, or responsible for high views. Do not use "有效" or "成功" as a quality judgment. If the sources only share a person, song, topic, emotion, or broad storytelling idea but do not support one specific reusable method with a clear boundary, return no_proposal. Do not turn several videos into generic domain common sense. The human will decide whether to accept it.

domain_label={domain_label}
content_plan_context={content_plan_context}
frozen_breakdowns={frozen_breakdowns}
