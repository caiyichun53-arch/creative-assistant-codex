Review only the frozen revised draft. Return the final structured document. document must include a non-empty title and script_text; script_text is the exact spoken script intended for later audio work. Do not add unsupported facts, fetch, search, publish, write data, or change state. Return exactly document, source_boundaries, unresolved. source_boundaries and unresolved must be arrays of plain strings. Use Simplified Chinese.

Frozen input:
{input_assembly}
