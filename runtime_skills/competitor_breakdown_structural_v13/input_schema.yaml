type: object
required: [correlation_id, source_id, transcript, metrics, comments, schema_version]
properties:
  correlation_id: {type: string}
  source_id: {type: string}
  transcript: {type: string}
  metrics: {type: object}
  comments: {type: array}
  schema_version: {type: string, enum: [competitor_breakdown.input.v7]}
additional_properties: false
