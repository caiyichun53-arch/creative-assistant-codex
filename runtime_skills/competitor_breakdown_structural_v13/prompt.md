只分析这一次输入的一条视频。模型只能使用带编号的完整口播原文、基础数据和带编号评论；不得搜索、采集、读取其他材料、写入数据、调用其他能力或使用聊天记忆。

这是单条视频的口播拆解记录，不是跨视频归纳，也不是领域经验。内容类型只用于整理和检索。不得声称任何说法导致了播放、点赞、评论或爆款结果；不得替视频核验事实。原文中的评价、因果、内幕、指控或情绪判断，应写成“口播称”“视频提出”或“原文把它说成”，不能改写成已核验事实。

原文可能有错字、断句或听不清。只引用主意思能可靠理解的段落；主意思根本无法判断的段落直接不用。错字、断句或个别词听不清，但主意思仍能理解时，可以按能可靠理解的主意思引用，但不能补写听不清的细节。拆解不要求覆盖原文的每一个编号。

所有口播依据只选 P 编号，评论依据只选 C 编号。只输出编号，不要转抄或改写原文作为依据。系统会负责还原原文、补上口播推进的顺序编号，并核验引用。

请返回且只返回一个 JSON 对象，不要 Markdown、解释或额外字段。下面是模型的精简交付格式：它只收集你的判断和编号；系统会把它整理成最终拆解记录。字段名必须原样复制，不能翻译、改名或增加字段。

格式不规定分析要写几条：tensions、highlights、recurring 和 reactions 都可以是 []；progression 只写实际改变理解、问题、信息、比较、情绪或判断的部分，段数不固定。

先做 structure_assessment：simple 表示原文只是开场承诺、重复交付清单项和简单收尾，没有原文依据能说明它另有筛选逻辑、明显反差或主线转折；这不是失败。expression_form 是 list 且判断为 simple 时，progression 必须是 []，重复的逐项交付写进 recurring。distinct 只在原文确有可指出的组织办法、反差或转折时使用；unclear 表示原文不足以判断。simple 的清单可以作为“主题、开场承诺、反复交付或收尾”的检索材料，但不能当作有独特口播结构或内容打法的例证。不要为了显得有结构，逐项拆清单。

目标是抓住口播主线，不是把原文重新缩写一遍。连续的同类盘点、歌曲举例、年份罗列或并列案例，如果只是重复同一个口播动作，就合并成一个推进；只有开场核心、关键反差、真正转折、论证换层或结尾收束才单独记录。口播为什么能自然进入下一段，直接写在该条 progression 的第三项“相对前文推进了什么”里；这是内容承接，不是对观众效果的断言。

当 expression_form 是 list 时，progression 写的是“这张清单怎样组织口播”，不是“清单里依次有哪些项目”。通常应抓：为什么要列这张清单、按什么标准或反差来组织、哪一处改变了前文的理解、最后如何回扣核心。连续的逐首/逐项介绍，如果都在支撑同一个判断，就合并成一条并可绑定多个 P 编号；只有某一项真的改变筛选标准、制造反差或扭转主线时，才单独进入 progression。逐项内容可放进 recurring，不要用多个 progression 重新列清单。

当 expression_form 是 profile 或 story 时，先找它真正要回答的核心变化，再处理履历。连续交代童年、训练、出道、工作经历或后续事件，如果都只是在铺同一个背景或证明同一件事，合并为一个推进，不能因为发生了新事件就新开一段。只有人物处境、核心冲突、应对方式、判断方向或结局真的改变时，才单独进入 progression。若原文只是按时间介绍人物，没有原文支持的冲突、转折、回应或收束，structure_assessment 选 simple，progression 必须是 []。

每个二维列表的第一个位置都是编号数组，后面的文字位置依次对应模板中的说明。progression 的排列顺序就是口播推进顺序，自己不要写序号；每一条 progression 必须正好有“依据、实际口播动作、相对前文推进、主线阶段”四项，无法写全四项就不要输出该条。第四项只能是 setup、conflict、response、turn、outcome 或 closure；它只供系统核对，不会显示在最终拆解里。同一阶段可以出现多次，但必须是另一条独立的人生线、关系线或事件线发生了新的变化；如果只是继续补同一个事件的背景、例子或感受，就合并。

- core：本条口播反复要说明的核心，必须绑定原文。
- tensions：原文实际呈现的反差、两难、冲突或错位；没有就返回 []。
- highlights：真正有辨识度的具体说法、转折或组织动作；没有就返回 []。
- progression：按原文顺序记录真正的口播推进；前三项分别是依据、实际说法和它相对前文推进了什么，第四项是主线阶段。
- recurring：只记录确实反复出现、但不推进主线的具体说法或组织方式；没有就返回 []。
- reactions：只记录评论实际表达的反应；没有可靠评论就返回 []。第二项关联口播依据可以是 []。
原文中的事实、因果、名次、数据、影响力或高下判断，只能写成“口播称”“视频提出”或“原文将其说成”；不能改写成已经核实的事实。你可以提炼口播怎样组织这些说法，但不能替它背书。

输出模板：
{{
  "source_id": "输入中的 source_id",
  "content_subject_type": "person|work|event|concept|case|method|collection|mixed|unclear",
  "expression_form": "story|profile|list|analysis|explanation|commentary|event_response|interview|mixed|unclear",
  "content_type_evidence": ["P001"],
  "structure_assessment": [["P001"], "simple|distinct|unclear", "本条口播结构判断及原文依据"],
  "core": [["P001"], "口播反复将什么说成核心"],
  "tensions": [[["P002"], "原文实际呈现的反差、两难、冲突或错位"]],
  "highlights": [[["P003"], "具体而有辨识度的说法、转折或组织动作"]],
  "progression": [[["P001"], "这段口播实际说了什么或做了什么", "相对前文实际推进了什么", "setup|conflict|response|turn|outcome|closure"]],
  "recurring": [[["P002"], "反复但不推进主线的具体说法或组织方式"]],
  "reactions": [[["C001"], ["P001"], "评论实际表达的反应"]]
}}

人物故事或人物履历的 progression，不能把“口播讲述／详述／介绍／回顾某段经历”当作口播动作。这是履历复述，不是结构拆解。连续经历如果共同服务同一个处境、冲突、应对、转折或结局，必须合并；每条都要写清口播如何组织这个结构变化。系统会拒绝大部分推进只是这种泛泛讲经历的输出。这里不是规定固定段数，复杂故事仍可有多个推进，但每个推进都必须是独立的结构变化。

source_id={source_id}
numbered_transcript={numbered_transcript}
metrics={metrics}
numbered_comments={numbered_comments}
