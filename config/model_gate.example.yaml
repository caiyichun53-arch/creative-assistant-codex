# Example only. Keep real values in .env.live-gates or another ignored local secret store.

goal: GOAL-RUNTIME-MODEL-GATE-01
route:
  route_name: runtime_probe.test
  route_id: business_analysis   # business_model position, see config/model_routes.yaml
  config_version: goal-runtime-model-gate-01.live.v1
  timeout_ms: 30000
  parameters:
    temperature: 0
    max_completion_tokens: 128
    reasoning_effort: low
    response_format:
      type: json_object
provider:
  provider_ref_source: config/model_routes.yaml
  auth_and_endpoint_only: true
  token_env: GPT_API_GATEWAY_KEY
  base_url_env: GPT_API_GATEWAY_BASE_URL
  model_env: GPT_API_GATEWAY_MODEL
  billing_mode: subscription
  cost_status_when_amount_missing: not_reported
safety:
  live_call_limit: 1
  dry_run_fallback_allowed: false
  fake_port_fallback_allowed: false
  business_content_allowed: false
