爆款口播内容经验库系统

最终完整执行总控文档 V0.6.2｜无损汇编版

无损汇编：V0.5＋CR-002＋R05＋R06＋R07＋跨窗口最终固化修订＋Codex R04
完整实施控制

唯一有效路线：Hermes 生产 Host｜四层学习治理｜Codex
仅负责代码｜现有仓库先审计后迁移

编制日期：2026 年 7 月 1 日

# V0.6.2 使用说明

本 V0.6.2
采用"完整原文汇编＋完整新增章节＋完整实施控制文本"的方式形成，目的在于消除多窗口、多个增量文件和摘要压缩造成的细节丢失风险。V0.5、CR-002、R05、R06、R07
的正文继续完整保留；新增第六编固化跨窗口确认结论，第七编完整收录 Codex
R04
的唯一实施控制内容。日常最终审核只需本文件；正式实施只需本文件和真实代码仓库。

  优先级   文档/指令
  -------- --------------------------------------------
  1        用户在当前对话中的最新明确指令
  2        本汇编中的 R07 学习治理与 Codex 续写固化版
  3        R06 TECH 完成与运行宿主固化版
  4        R05 完整无损继承版
  5        CR-002 R01
  6        V0.5 执行审计修订版
  7        更早内容仅作历史审计

  **当前关键覆盖**   **有效口径**
  ------------------ ------------------------------------------------------------------------------------
  运行 Host          Hermes production-host；不再飞书直连，不自建 Runtime。
  Codex              仅代码开发和维护，不进入生产内容链。
  正式 Skill         Core Runner/ModelGateway 独立执行；正式仓库只读；Hermes 可形成候选但不得静默发布。
  TECH               物理对象、状态、Job、Runner、纠错和验证全部完成。
  MediaCrawler       对标视频默认执行器，许可门禁豁免；网易云独立 Adapter。
  剩余事项           仅代码实施、待校准和外部门禁；Goal 中断按 R07 协议恢复。
  学习治理           Memory 保留；Core 版本化内容偏好；Skill 候选经回归后发布。
  Codex Goal         原子检查点、进度文件、Git 提交和中断对账；会话不是唯一恢复依据。

# 汇编目录

1.  第一编：V0.5 执行审计修订版（完整原文）

2.  第二编：CR-002 经验自动演进增量设计（完整原文）

3.  第三编：R05 完整无损继承版（完整原文）

4.  第四编：R06 TECH 完成与运行宿主固化版（完整原文）

5.  第五编：R07 学习治理与 Codex 续写固化版（完整原文，最高优先级增量）

6.  第六编：跨窗口最终固化修订与单文件治理（完整正式增量）

7.  第七编：Codex 可执行实施控制 R04（完整文本化汇编）

8.  附录 A---C：核对原文、历史说明与 source_docs 无损映射

本文件是唯一日常上位执行文档。旧版、增量文档和控制包保留归档，但不再要求用户同时管理或上传。

  ----------------------------------------------------------------------------------------------------------------------------------------------------------------
  **阅读建议\
  **日常实施先读 R06 的当前状态、TECH 和逐章继承清单；需要完整业务细节时再查 R05、CR-002 和 V0.5。不要按物理页数判断信息是否被压缩，本汇编完整保留所有来源正文。

  ----------------------------------------------------------------------------------------------------------------------------------------------------------------

**爆款口播内容经验库系统**

**当前阶段技术设计文档**

执行审计修订版 V0.5

  **项目**       **当前口径**
  -------------- -------------------------------------------------------------------------------------------------------------------------------------------------------------------
  文档状态       替代 V0.4 的当前有效执行基线；可直接启动核心开发，但生产采集上线受采集器许可与平台合规门禁约束。
  业务目标       两个自营账号均以流量为唯一目标，不设计成交、线索、私信等业务转化链路。
  初始领域       泛科普---社会生活；音乐娱乐。当前各绑定一个自营账号，数据结构支持以后一个领域多账号或一个来源账号关联多领域。
  主要平台       抖音；平台能力、字段、登录和风控变化通过适配器隔离。
  采集执行器     统一 CollectorAdapter。MediaCrawler 仅作为当前技术候选；生产使用前必须解决其非商业学习许可及平台合规问题。
  知识工作层     Obsidian 从 MVP 开始使用，但只承载阅读、分析、创作与复盘，不承担运行状态。
  交互入口       飞书官方开放平台直连；固定按钮和命令确定性处理，自然语言只有在必要时才调用低成本意图解析。Hermes/OpenClaw 仅作可替换扩展。
  核心技术选择   Python 3.12（锁定补丁版本）+ FastAPI + SQLAlchemy/Alembic + PostgreSQL 18.x；数据库任务队列；Docker Compose/WSL2；初期不启用 Redis、Celery、Temporal 或向量索引。
  LLM 运行原则   所有模型调用经 ModelGateway；按低成本/高质量分层、缓存、预算和可降级策略执行。采集、快照、基线、状态和日报模板在 LLM 故障时仍可运行。
  编制日期       2026 年 6 月 28 日

+----------------------------------------------------------------------+
| **文档定位**                                                         |
|                                                                      |
| **本文件只保留当前阶段有效口径。已确认规则写为实施约束；             |
| 可执行但需要真实数据校准的内容写为"当前默认"；尚未完成但不阻塞其他模 |
| 块的内容写为"未完成节点"。开发不得自行把模糊词扩展成新状态、新表或新 |
| Agent。**                                                            |
+----------------------------------------------------------------------+

# **文档说明**

V0.5 在 V0.4
基础上按"能否直接交付开发"为标准完成全量执行审计。除飞书直连、LLM
分层与成本治理、采集许可门禁、冷启动基线修正、多领域关系和证据模型外，本次最终复核进一步补齐：各来源进入选题状态的确定条件、历史高信号与冷启动粗略阈值、普通/低表现对照选择、日报展示上限、选题排序字段枚举、实验确定性评价、研究分阶段预算、来源发现持久化，以及减少重复
LLM 调用的执行规则。

本文件同时承担业务设计、技术边界和实施验收基线。凡是会影响数据口径、任务状态、模型调用、费用、权限或后续动作的规则，均给出确定性实现方式或明确列为上线门禁；仍需真实样本校准的数值全部配置化。开发不得把示例参数、领域示例或外部工具能力误写成不可变业务规则。

  **版本**   **日期**     **说明**
  ---------- ------------ ---------------------------------------------------------------------------------------------------------------------------------------------------------------------
  V0.1       2026-06-27   分别形成经验系统阶段方案与账号采集技术方案。
  V0.2       2026-06-27   统一术语、ID、时间口径、基线、样本接口和反馈流程。
  V0.3       2026-06-27   冻结 90 天历史窗口、D0---D7、20 条正式 D 基线、Obsidian 和经验自动维护；选题与创作仍为预留。
  V0.4       2026-06-28   形成可实施技术基线：两个领域、五类选题来源、提前候选与双阶段评论、选题流转、飞书 Agent、研究与创作路线、经验决策作用域、技术栈和分阶段验收。
  V0.5       2026-06-28   完成执行级审计：直连飞书、LLM 成本治理、采集许可门禁、冷启动与粗略信号修正、多领域关系、来源/选题状态、主张证据、对照选择、确定性实验评价、日报排序和数据模型精简。

## **决策状态说明**

  **状态**     **含义**                                         **实施处理**
  ------------ ------------------------------------------------ ------------------------------------------------------
  已确认       业务边界或不可混淆的数据口径已经明确。           写入代码约束、数据库唯一约束或正式配置。
  当前默认     已有低成本可执行方案，但数值需要真实运行校准。   先实现为配置项并记录版本，不能写死在代码中。
  技术选择     当前阶段已选定实现方案。                         直接实施；若替换，必须保持业务接口和数据对象不变。
  未完成节点   缺少外部数据源、样本或验证，无法可靠冻结。       明确是否阻塞；接口先建，禁止开发者自行补造业务规则。
  明确排除     当前阶段不做。                                   不建表、不建状态、不引入依赖，只保留必要扩展边界。

## **当前建设结论**

  **范围**                     **建设结论**
  ---------------------------- ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  可以直接开工                 数据库与任务基础、账号/视频/快照、D/P 时间模型、ASR/采集适配接口、基线、候选、评论、选题对象、飞书直连、生产任务、标准样本包、经验/研究数据模型与 Obsidian 单向同步。采集阶段可先用 fixtures、人工导入或合规数据源；语义阶段先建 Schema/回归框架。
  可先建接口后校准             候选倍数阈值、发现延迟上限、活跃标签、选题保留周期、经验等级阈值、自营 P 基线参数、模型与提示词版本、模型预算。
  生产上线门禁                 采集器许可与平台条款确认；飞书应用权限；运行模型/研究提供商评测；两个领域最小回归样本；实际产能配置。
  局部未完成但不阻塞核心开发   自动热点源、全自动网页研究、向量检索和后续新增领域。
  不在当前范围                 抖音自动发布、视频画面/表演/剪辑深度分析、专业音乐音频结构分析、多人权限后台、商业转化归因、多 Agent 团队。

# **大纲目录**

-   第一部分　系统现实、目标与总体闭环

-   第二部分　领域、账号与多领域扩展接口

-   第三部分　采集、时间模型、基线、候选与评论

-   第四部分　选题来源、选题流转、拓展调研与飞书汇报

-   第五部分　深度分析、经验原子与自营验证

-   第六部分　研究、内容策划、文案生成与审核

-   第七部分　LLM 成本治理、技术架构、飞书交互、数据模型与部署

-   第八部分　实施阶段、验收、未完成节点与简化审计

-   附录 A---K　参数、字段、领域配置、Skill
    > 契约、飞书模板、任务/API、LLM 调用矩阵、状态机与参考资料

**第一部分　系统现实、目标与总体闭环**

先确认系统真实能获得什么、要解决什么，以及哪些结论不能从公开数据中得出。

# **1 项目现实与系统定位**

## **1.1 现实使用场景**

-   系统由个人长期运营，不按企业多人协作平台设计。

-   当前平台为抖音，内容形态以一个人面对镜头的口播内容为主。

-   初始同时服务两个互相独立的自营账号：泛科普---社会生活账号、音乐娱乐账号。

-   两个账号均以流量为唯一目标；不设计成交、线索、私信、商品或咨询转化。

-   系统重点研究标题、口播文案、公开互动数据、热门评论、账号背景和外部事实资料；MVP
    > 不深入分析镜头、表演、剪辑、配乐和封面。

-   对标账号无法取得播放量、完播率、平均观看时长、首屏停留和后台分发数据，系统必须承认这一能力边界。

## **1.2 核心目标**

-   持续采集爆款、普通、低表现和对照样本，不只收藏爆款。

-   从单篇样本发现候选机制，再通过多篇独立内容、普通样本、反例和自营实验形成条件经验。

-   把经验拆成可执行、可证伪、带适用条件和失效边界的原子经验。

-   让选题来源、父视频、评论线索、调研证据、创作路线和最终文案可追溯。

-   每天通过飞书汇报可生产选题及来源数据，允许用户直接下达选择、暂停、换角度和查询任务。

-   正式选题进入资料研究、内容策划、文案生成和审核，并在自营发布后通过
    > P+ 数据验证。

-   系统自动维护运行状态和经验风险，人只处理低频边界决策和最终内容确认。

## **1.3 系统不等于什么**

-   不是爆款文案收藏夹，也不是每条视频都生成长报告的资料仓库。

-   不是固定开头、固定三段式和固定结尾的模板库。

-   不是逐句同义替换或直接洗稿工具。

-   不是仅凭点赞、评论、收藏、分享就做绝对因果归因的系统。

-   不是由一个全能 Agent 自由决定采集、基线、状态和数据库写入的黑盒。

-   不是为了满足日产量而强行制造低质量选题的排产系统。

# **2 数据能力边界与归因约束**

  **可直接观察**                                               **可以推断但必须标注不确定**                               **不能据此证明**
  ------------------------------------------------------------ ---------------------------------------------------------- ------------------------------------------------------
  点赞、评论、收藏、分享的累计值，以及相对同口径基线的表现。   某个开头可能形成信息缺口；某段可能促进收藏、讨论或转发。   真实曝光、留存、完播、点赞率、转粉率和平台分发因果。
  热门评论样本中的高热观点、争议、追问和记忆点。               高热观点可能反映主要共鸣、争议或未解决问题。               全部观众立场比例或评论区整体分布。
  同账号或跨账号公开表现差异。                                 选题、文案、人物、账号资产、热点和画面可能共同贡献。       单独由某个文案机制导致结果。

## **2.1 文案贡献置信度**

  **等级**   **判断含义**
  ---------- ------------------------------------------------------------------------------
  高         主要吸引力可以从选题、表达、结构和价值交付解释，热点、人物或视听依赖相对低。
  中         文案具备明显作用，但人物、账号、热点或画面也可能贡献较大。
  低         表现更可能依赖热点、人物知名度、画面或账号资产；文案结论只作弱证据。

# **3 设计原则与复杂度边界**

+----------------------------------------------------------------------+
| **复杂度准入标准**                                                   |
|                                                                      |
| **一个新表、新状态、新                                               |
| Agent、新字段或自动任务只有在"真实问题已经存在、会影响后             |
| 续动作或结果、简单方案不足以处理"三个条件同时成立时，才进入系统。**  |
+----------------------------------------------------------------------+

  **原则**                 **实施要求**
  ------------------------ --------------------------------------------------------------------------------------------------------------------------------------------------------------
  确定性优先               ID、时间点、基线、阈值、资格判断、任务状态、重试、排序硬规则和数据库写入由程序执行。
  语义能力版本化           分类、分析、选题成形、研究、策划和文案使用固定输入输出契约，记录模型、提示词、配置和哈希。
  LLM 非必需               采集、快照、基线、状态、飞书卡片和确定性命令不得依赖 LLM；模型不可用时只延迟语义任务。
  分层与预算               低成本任务使用本地/低价模型；高质量模型只服务 deep_eligibility=eligible 的样本、正式 production_task 和 blocking/disputed claim；按领域和 Skill 设置日预算。
  低成本完整、高成本筛选   基础数据与 raw ASR 尽量完整；LLM 清理、评论、深度分析和研究按资格触发。
  统一口径优先             成熟历史、D 序列、P 序列和外部快照不能混算；发现延迟不合格的视频不得进入正式 D 基线。
  数据不删除               关闭、淘汰、未采用和失败只改变状态；原始记录、来源和失败证据保留。
  配置代替写死             领域边界、标签、产能、TTL、阈值、风险、预算和模型版本必须配置化。
  依赖可替换               采集器、ASR、LLM、搜索提供商和对话网关均通过适配器接入。

## 3.1 LLM 非必需与成本边界

  **层级**       **默认能力**                                               **是否调用 LLM**                                                          **失败时处理**
  -------------- ---------------------------------------------------------- ------------------------------------------------------------------------- --------------------------------
  确定性层       采集调度、ID、快照、基线、过滤、状态、日报装配、按钮命令   否                                                                        继续运行并记录异常
  低成本语义层   轻量分类、选题签名、必要的转写修订、自然语言命令解析       按需                                                                      排队、降级为规则或要求用户确认
  高质量语义层   深度分析、拓展验证、正式研究、内容计划、文案、语义审核     仅 deep_eligibility=eligible、正式 production_task 或用户明确研究的对象   延迟，不阻塞数据采集
  禁止模式       每条搜索结果、每次日报、每个状态变化都调用 LLM             禁止                                                                      由代码审查和成本监控拦截

## 3.2 运行事实来源

  **对象**                           **唯一事实来源**                      **禁止做法**
  ---------------------------------- ------------------------------------- --------------------------------------------------------
  任务、状态、基线、产能、选择结果   PostgreSQL                            以飞书对话历史、Obsidian 文件或 Agent 记忆作为正式状态
  领域/Skill/提示词定义              版本控制仓库中的配置文件              在数据库和 Agent Skill 中各维护一份可变正文
  原始媒体与采集响应                 file_asset + collection_raw_payload   只保存模型摘要而丢弃原始来源
  知识阅读与人工创作                 Obsidian 派生笔记                     人工移动文件夹直接改变数据库状态

# **4 总体闭环**

## **4.1 外部学习闭环**

  -------------------- ------- ---------------- ------- --------------- ------- ---------------- ------- -------------- ------- --------------
  **账号/标签/热点**   **→**   **视频与快照**   **→**   **ASR与评论**   **→**   **候选与分析**   **→**   **原子经验**   **→**   **选题证据**
  -------------------- ------- ---------------- ------- --------------- ------- ---------------- ------- -------------- ------- --------------

外部学习闭环输出的是样本、选题来源、候选机制、条件经验、反例和领域差异，不直接输出"万能爆款公式"。

## **4.2 内部生产与验证闭环**

  -------------- ------- -------------- ------- -------------- ------- ---------------- ------- ---------- ------- ------------
  **飞书选题**   **→**   **资料研究**   **→**   **内容策划**   **→**   **文案与审核**   **→**   **发布**   **→**   **P+复盘**
  -------------- ------- -------------- ------- -------------- ------- ---------------- ------- ---------- ------- ------------

内部闭环输出实际使用的创作决策、自营支持或反对证据、经验边界变化和账号级打法。

## **4.3 核心概念**

  **术语**     **定义**
  ------------ -------------------------------------------------------------------------------------------------------------------
  原始样本     采集到的账号、视频、标题、公开数据、文案和评论，尚未做机制判断。
  观察事实     可以从原文、数据、评论或外部资料直接核对的现象。
  机制解释     观察事实为什么可能影响认知、情绪或行为的推断。
  经验原子     只解决一个明确内容决策问题、可独立理解和证伪的机制。
  选题线索     来源中出现的潜在内容方向，尚未通过完整证据和可生产性判断。
  可生产选题   stage=validated、validation_result=supported、角度明确、来源可追溯，且当前没有 known_risk_status=blocked 的选题。
  拓展问题     从爆款原文、评论或资料中发现、需要有限调研验证的问题，不等同于正式选题。
  标准样本包   固定某次分析使用的快照、基线、文案、评论、对照、领域和模型版本。

**第二部分　领域、账号与多领域扩展接口**

初始实现两个领域，但领域差异必须留在配置包中，不能写进核心流程。

# **5 初始领域与账号**

  **领域**            **绑定自营账号**   **当前内容范围**                                                                                           **明确排除**
  ------------------- ------------------ ---------------------------------------------------------------------------------------------------------- --------------------------------------------------------------------------------------------------
  泛科普---社会生活   泛科普自营账号     与大众生活直接相关的社会现象、生活问题、常见认知、规则、消费、职场、家庭、人际、教育和养老等解释型内容。   硬核科学技术教学、纯新闻搬运、与大众生活关系弱的知识、专业诊断和无证据的强因果。
  音乐娱乐            音乐自营账号       音乐人物传记、歌曲赏析、创作与传播背景、人物/歌曲/音乐现象盘点。                                           娱乐新闻、综艺八卦、粉圈争议、私生活猎奇、未证实传闻；MVP 不分析旋律、和声、编曲等专业音频结构。

## **5.1 两个领域的选题基本单位**

  **领域**            **原始对象**                                 **成为选题前必须补足**
  ------------------- -------------------------------------------- -------------------------------------------------------------------------------------
  泛科普---社会生活   社会现象、生活问题、规则、误区或大众处境。   具体问题、观众关系、信息增量、可解释原因和单条视频边界。
  音乐娱乐            人物、歌曲、专辑、时期或盘点主题。           明确叙事角度、冲突或变化、情绪/认知交付、可靠资料主线；人物或歌曲名称本身不是选题。

## **5.2 产能模型**

产能只限制进入高成本研究、策划和文案环节的数量，不限制来源采集与历史记录。系统支持"每日硬上限
+ 滚动软目标"，但用户此前仅讨论过 3/日、平均
1/日的假设，未正式冻结；V0.5
使用保守安全默认，实际值在生产阶段按账号配置。

  **参数**              **V0.5 安全默认**           **含义**
  --------------------- --------------------------- -----------------------------------------------------------------------
  max_daily             每个自营账号每天最多 1 条   防止未确认配置时一次创建过多高成本任务；可改为 2---3。
  rolling_target        未启用（null）              不强制平均日产；用户确认后可配置为 7 天 7 条等软目标。
  rolling_window_days   7                           用于展示近期实际产量；rolling_target 未启用时只展示，不参与建议数量。
  质量优先              固定                        没有合格选题可以生产 0 条；任何软目标都不得产生"欠产追债"。

  -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **重要边界 / 产能配置属于 self account + domain 的 production_policy，而不是写死在领域文件中。改变上限或滚动目标只影响每天可创建的 production_task 数量，不改变候选、研究、文案和经验流程。**
  -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

# **6 多领域配置包**

核心程序只认识
domain_id、配置版本和通用接口。任何"音乐排除八卦""泛科普排除硬核技术"都必须位于领域配置中。新增领域时，应当调整明确文件，而不是修改核心状态机。

  **文件/目录**                         **必须配置的内容**                                                                      **新增领域是否必改**
  ------------------------------------- --------------------------------------------------------------------------------------- ----------------------
  domains/\<domain_id\>/domain.yaml     领域名称、边界、允许/排除内容类型、目标、默认内容类型；不写运行时 account_id 和产能。   是
  domains/\<domain_id\>/sources.yaml    启用来源、热点转化规则、活跃标签、外部视频过滤、线索/验证 TTL、日报抑制规则。           是
  domains/\<domain_id\>/research.yaml   证据要求、事实风险、主张支持规则、需要交叉核验的说法、研究停止条件。                    是
  domains/\<domain_id\>/review.yaml     脚本审核重点、禁用内容、相似风险、领域特殊错误。                                        是
  domains/\<domain_id\>/prompts/        只保存领域补充片段，不复制通用 Skill 正文。                                             按需
  domains/\<domain_id\>/tests/          应判入、应排除、边界、选题形成、研究和风险样本。                                        是
  config/model_profiles.yaml            模型档位、成本、上下文、超时和降级；领域只允许覆盖预算，不复制模型配置。                否

## **6.1 新增领域原则上不修改的内容**

-   账号、视频、快照、D/P 时间模型和数据库唯一约束。

-   成熟历史基线、正式 D 基线、自营 P 基线的核心结构。

-   评论采样、标准样本包、经验、证据、任务、Skill 运行和审计对象。

-   飞书网关、任务队列、Obsidian 同步和备份机制。

-   通用 Skill 的输入输出字段；只增加 domain_context 或领域扩展 Schema。

+----------------------------------------------------------------------+
| **新增领域检查点**                                                   |
|                                                                      |
| **只有当新领域真实样本证明现有通用对象无法表达必要差异时，才新增扩展 |
| Schema；不能因为预想可能不同就先增加字段。**                         |
+----------------------------------------------------------------------+

## 6.2 多领域账号与内容绑定

  **对象**                  **实现方式**                                                      **当前两个领域的使用**
  ------------------------- ----------------------------------------------------------------- ----------------------------------------------------------------
  account                   只表示平台账号身份，不把单一 domain_id 写进账号主表。             两个自营账号各有独立记录
  account_domain_binding    绑定 account_id、domain_id、role、status 和 production_policy。   每个自营账号绑定一个领域；对标账号可关联一个或多个领域
  video_domain_assignment   保存视频与领域的分类结果、版本和证据。                            同一外部视频可被多个领域发现，但每个 topic_item 只属于一个领域
  内容类型/source_type      使用配置键或注册表字符串，不使用数据库硬枚举。                    新增领域或来源不需要迁移核心表

# **7 账号类型、状态与纳入**

  **类型**     **定义**                                               **用途**
  ------------ ------------------------------------------------------ -----------------------------------------------
  self_owned   由使用者运营，能够确定发布时间、创作稿和最终发布稿。   精确 P+ 跟踪、流量复盘和经验验证。
  benchmark    长期跟踪的公开对标账号。                               D0---D7、基线、高/普通/低表现样本和选题来源。

  **状态**   **系统动作**
  ---------- -------------------------------------------------------
  active     每日发现新视频；已有 D/P 任务继续执行。
  paused     停止创建新的发现任务；已创建的 D/P 任务默认继续完成。
  archived   不再创建任务，历史数据只读保留。

## **7.1 标签搜索发现账号的复查触发**

系统不再判断"有持续研究价值"，只报告客观重复出现事实。

  **触发条件**                                                              **系统动作**                                                                                   **最终决定**
  ------------------------------------------------------------------------- ---------------------------------------------------------------------------------------------- --------------------------------------------------------------
  同一非对标账号在 30 天内至少有 3 条不同视频通过当前领域的外部视频筛选。   创建账号复查任务，获取最近 10 条可访问视频，统计领域相关、口播适配、独立选题和排除内容数量。   通过飞书展示客观统计，由用户选择加入、忽略 30 天或永久忽略。

领域相关和排除规则来自当前 domain
配置；系统不自动加入对标池，也不建立无法解释的账号总分。

**第三部分　采集、时间模型、基线、候选与评论**

保留 V0.3 的稳定数据口径，修正"所有候选必须等待 D7"的僵化设计。

# **8 历史回填与三类视频**

+----------------------------------------------------------------------+
| **固定历史窗口**                                                     |
|                                                                      |
| **新对标账号首次接入只获取最近 90 天内最多 50                        |
| 条视频，不为凑数量无限向更早历史扩展。**                             |
+----------------------------------------------------------------------+

  **类别**         **判定**                               **采集方式**                                                 **基线用途**
  ---------------- -------------------------------------- ------------------------------------------------------------ -----------------------------------------------
  历史成熟样本     接入时已发布满 7 天。                  保存一次当前累计值；进入音频、ASR、选题和历史高信号判断。    进入成熟历史基线，不进入正式 D 基线。
  接入期过渡样本   接入时已发布 1---7 天。                接入时保存一次；发布满 7 天后再保存成熟值；不补造 D 序列。   成熟后可进入成熟历史基线，不进入正式 D 基线。
  正式新视频       账号正式跟踪后，在日常批次首次发现。   保存 D0，创建 D1---D7，进入 ASR 和各 D 点候选判断。          完整 D0---D7 后可成为后续正式 D 基线成员。

# **9 视频唯一性与内容关系**

+----------------------------------------------------------------------+
| **唯一规则**                                                         |
|                                                                      |
| **视频主记录只使用 UNIQUE(platform,                                  |
| platfo                                                               |
| rm_video_id)。标题、发布时间、文案指纹和相似度不参与写入唯一判断。** |
+----------------------------------------------------------------------+

  **情况**                            **处理**
  ----------------------------------- ----------------------------------------------------------------
  同一平台视频重复采集                不新增主记录，只写入对应快照或允许变化的基础字段。
  不同视频 ID 使用相同/高度相似文案   全部保留为独立发布行为；深度分析可复用，机制证据按内容簇去重。
  同一选题但文案独立                  不去重，建立 same_topic 关系。
  来源视频与拓展选题                  通过 parent_topic_id、source_refs 和 derivation_type 追溯。

# **10 对标账号 D0---D7 时间模型**

D 表示 Discovery
Day，以系统首次发现视频的固定日采集批次为锚点，不伪装成发布后精确时长。

  **标记**   **定义**
  ---------- -----------------------------------------------------
  D0         系统第一次发现并采集数据的固定批次。
  D1         首次发现后的下一个固定日批次。
  D2---D6    首次发现后的第 2 至第 6 个固定日批次。
  D7         首次发现后的第 7 个固定日批次，完成后停止常规追踪。

漏采不顺延：D1 失败、D2 成功时保留 D1 缺失并写入 D2。保存
publish_time、first_discovered_at 和
discovery_delay_hours，但不据此补造发布后快照。

## 10.1 正式 D 基线与候选资格

  **条件**                **默认规则**                               **不满足时**
  ----------------------- ------------------------------------------ --------------------------------------------------
  publish_time 可用性     平台时间可解析且 adapter 标记为可信        仍保存 D 序列，但不进入正式基线
  discovery_delay_hours   默认 ≤36 小时；按平台配置                  不进入正式 D 基线，也不使用正式 D 倍数自动判候选
  快照完整性              D0---D7 全部有效                           不进入正式基线
  异常事件                删除、补采、账号停更、平台指标异常需标记   保留样本，排除基线资格

该规则解决"首次发现过晚的视频与正常每日发现视频混入同一 D
基线"的数据污染。36
小时是每日扫描条件下的当前默认，必须通过真实运行校准。所有时间以 UTC
存储，飞书和 Obsidian
按用户时区展示；公开指标允许下降，不假设累计值严格单调。

# **11 自营账号 P+ 时间模型**

  **观察点**    **目标时间**
  ------------- -------------------
  P+6h          发布后 6 小时
  P+1d          发布后 24 小时
  P+2d          发布后 48 小时
  P+3d          发布后 72 小时
  P+4d---P+6d   发布后第 4---6 天
  P+7d          发布后第 7 天

任务同时保存 target_age_hours 和
actual_age_hours。自营优先保存原始稿、修改稿、最终发布稿和实际口播偏差，ASR
不替代最终发布稿。

# 12 采集调度与 CollectorAdapter（MediaCrawler 为当前候选）

核心程序只依赖 CollectorAdapter。MediaCrawler 当前能够通过 Playwright
浏览器自动化采集抖音等平台公开信息，但项目许可证仅授予"非商业学习目的"使用权；本系统用于内容账号运营，即使没有直接成交目标，也不能默认满足该许可。MediaCrawler
因此只能作为开发验证候选，生产启用前必须获得明确授权或完成合规替换。\[R1\]\[R15\]

  ---------- --- ------------------ --- ---------------- --- ---------- --- -------------
  业务任务   →   CollectorAdapter   →   已批准采集实现   →   标准化器   →   数据库/文件
  ---------- --- ------------------ --- ---------------- --- ---------- --- -------------

  **职责**                                                          **放置位置**
  ----------------------------------------------------------------- -----------------------------------------
  账号准入、D/P 编号、基线、候选、评论策略、选题、经验和 Obsidian   核心程序
  登录、浏览器上下文、平台请求、原始字段获取                        具体采集实现（MediaCrawler 或合规替代）
  平台原始字段到标准对象映射                                        标准化器

## 12.1 采集器上线门禁

  **状态**       **允许动作**                                                                                                                                                          **禁止动作**
  -------------- --------------------------------------------------------------------------------------------------------------------------------------------------------------------- ----------------------------------------------------------------------
  开发/测试      使用 fixtures、人工导入、脱敏样本或在许可范围内验证适配器                                                                                                             不得把测试可用等同于生产许可
  生产待确认     完成许可证、平台条款、账号授权、访问频率和数据留存评审                                                                                                                不得自动开启长期采集
  生产已批准     锁定 commit/版本、频率、字段映射和失败恢复测试                                                                                                                        不得绕过适配器直连项目内部对象
  替代路径       人工导出导入、用户授权浏览器流程、合法数据服务商或自研合规适配器                                                                                                      不得为保持流程而补造数据
  字段能力契约   CollectorAdapter 在启动时登记 capabilities/version：publish_time、like/comment/collect/share、comments、search、media。缺失指标写 NULL，并记录 unavailable_reason。   不得把缺失指标写成 0；对应基线和候选规则必须自动禁用，不能补造数据。

## 12.2 每日对标任务

9.  扫描全部 active 对标账号并获取新视频。

10. 依据 platform + platform_video_id 写入或识别已有视频。

11. 新视频保存 D0、创建 D1---D7 任务，进入音频、ASR、清理和选题提取。

12. 执行当天到期的 D1---D7 任务并保存快照。

13. 在每一个可判断 D 点执行对应基线比较；命中后立即进入候选处理，不等待
    > D7。

14. D7
    > 完成确定性收尾；仅当视频满足深度分析资格时，才采集成熟评论并执行完整分析。无论是否进入深度分析，D7
    > 跟踪完成后都转为已完成历史样本。

# **13 音频、ASR 与文本清理**

正式跟踪账号的历史、过渡和日常新视频都进入音频与本地 ASR
队列；这表示不丢失转写能力，不表示账号接入当天必须把全部历史视频处理完。日常新视频、正式候选和已选生产任务优先，历史回填按
worker
资源预算分批执行；标签搜索外部视频只对通过低成本筛选、进入完整处理名额的对象执行。默认
ASR 适配器优先评测 faster-whisper/CTranslate2，但模型大小、量化和 VAD
参数必须用中文口播样本测试后锁定。\[R17\]

  **版本**             **用途**                             **处理要求**
  -------------------- ------------------------------------ ----------------------------------------------------------------
  raw_transcript       保留 ASR 直接输出，便于追溯。        不可覆盖。
  cleaned_transcript   供分类、关系判断、选题和分析使用。   补标点、修断句、删除明显重复、修正常见识别错误；不得改写原意。

## 13.1 转写清理分层

  **层级**                 **对象范围**                                          **处理方式**                                                   **LLM**
  ------------------------ ----------------------------------------------------- -------------------------------------------------------------- ----------------
  raw_transcript           全部已下载音频                                        保存 ASR 原始结果、时间戳和置信信息                            否
  cleaned_transcript v1    全部 ASR 完成视频                                     确定性空白、标点、重复片段和常见格式清理；可使用本地标点模型   默认否
  cleaned_transcript v2+   正式候选、外部完整处理、正式生产或质量异常            语义修错但不改写原意，生成新版本                               按需低成本模型
  质量异常判断             空文本、重复率高、语言概率低、压缩率/无语音概率异常   触发重跑或升级处理                                             规则判断

处理状态只保留
pending、running、completed、failed。重新处理的明确条件：原任务失败、结果为空/不可读、正式生产研究被文本质量阻塞、或用户明确要求。转写版本不可覆盖，必须记录
asr_model、参数、处理方法和输入音频哈希。

## 13.2 ASR 队列优先级与资源门禁

  **优先级**   **对象**                                                       **处理原则**
  ------------ -------------------------------------------------------------- -------------------------------------------------------
  1            已选 production_task、用户明确研究对象                         立即处理；失败会阻塞生产并提示。
  2            formal D/P 提前候选及深度分析合格样本                          在评论/分析前完成可用转写。
  3            当日正式跟踪新视频 D0                                          按每日 source_to_topic 上限处理；超额排队，不丢视频。
  4            历史高信号、普通对照和低表现反例                               按分析需要和回填预算处理。
  5            其余历史/过渡样本                                              空闲资源分批完成；不因暂未 ASR 影响指标基线。
  资源边界     并发、GPU/CPU、每日音频分钟数属于 deployment resource_policy   不写入领域配置，不使用 LLM 决定队列优先级。

# **14 基线体系**

  **基线**             **数据来源**                                                                                   **用途**                                                                  **限制**
  -------------------- ---------------------------------------------------------------------------------------------- ------------------------------------------------------------------------- ----------------------------------------------------------------------
  成熟历史基线         最近 90 天、最多 50 条、发布满 7 天视频的一次累计值中位数；每项指标至少 5 个非空样本才计算。   账号接入时识别 history_high_signal，并在正式 D 基线形成前提供背景参照。   不是 D0---D7；不得用于 D0---D6 自动候选；样本不足 5 时只展示原始值。
  冷启动 D7 粗略参照   正式新视频 D7 当前累计值与成熟历史中位数比较。                                                 正式 D 基线不足 20 条时，提供弱信号和选题证据。                           只标记 rough；不能用于早期增长结论或经验升级。
  对标正式 D 基线      当前视频之前最近 20 条符合发现延迟与完整性要求的视频，各 D 点分别计算中位数。                  相同 D 点发现异常表现。                                                   累计不足 20 条不得启用。
  自营正式 P 基线      当前视频之前最近 20 条完整 P 序列，各 P 点分别计算中位数。                                     评价自营流量实验。                                                        不足 20 条时只显示粗略统计，不作正式实验结论。

  -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  正式 D 固定口径｜账号累计至少 20 条完整且合格的 D0---D7 视频后启用。每个 D 点使用当前视频之前最近 20 条完整视频的对应 D 数据；当前视频禁止参与自己的基线。account_baseline 保存 scope_type=account_all\|account_domain，默认 account_all；只有混合账号在某领域累计至少 20 条合格序列且真实数据证明分布差异时，才启用 account_domain，不为预想场景提前拆分。
  -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

同一观察点倍数 = 当前视频指标 ÷ 对应同口径基线中位数。NULL
指标不参与样本和计算，每项倍数保存实际非空样本数；中位数为 0
时跳过该指标。mature_history 只用于历史高信号和冷启动 D7
弱参照；formal_d\_series/formal_p\_series 才能在对应 D/P
点触发正式候选。

## 14.1 历史高信号与冷启动 D7 粗略规则

  **规则**       **安全默认**                                                               **输出与限制**
  -------------- -------------------------------------------------------------------------- ----------------------------------------------------------------------
  单指标高信号   点赞 ≥ 2.5 倍，或评论/收藏/分享任一 ≥ 3.0 倍成熟历史中位数                 history_high_signal 或 cold_start_rough；judgment_confidence=rough。
  多指标高信号   可用指标中至少两项 ≥ 2.0 倍                                                只形成选题/研究弱信号。
  样本门槛       该指标成熟历史非空样本数 ≥ 5；中位数为 0 或字段 unavailable 时跳过         不得补 0、不得换算为正式 D/P 倍数。
  经验边界       不进入 eligible_tactic_evidence，不触发 L2+ 自动升级                       只有人工指定或正式生产需要时才进入深度分析。
  校准方式       rough_candidate_rules 独立配置；初期数值与 formal 规则相同只是低成本起点   真实数据证明分布不同后单独调整，不改状态机。

# **15 候选视频：提前触发与双阶段处理**

## **15.1 候选触发**

  **规则**     **当前默认参数**                        **适用范围**
  ------------ --------------------------------------- ----------------------------------------------------
  点赞异常     点赞倍数 ≥ 2.5                          仅 formal_d\_series / formal_p\_series；上线后校准
  多指标异常   四项中至少两项倍数 ≥ 2.0                仅正式同口径基线；上线后校准
  强评论       评论倍数 ≥ 3.0                          仅正式同口径基线
  强收藏       收藏倍数 ≥ 3.0                          仅正式同口径基线
  强分享       分享倍数 ≥ 3.0                          仅正式同口径基线
  冷启动 D7    使用成熟历史中位数，仅生成 rough 信号   不提前采评论、不自动进入经验深度分析

正式 D 基线成熟且当前视频满足发现延迟资格后，D0---D7
任一点均可触发正式候选。正式基线不足时，新视频仍在 D0 提取选题，但
D0---D6 不使用成熟历史累计值判候选；D7 可生成 cold_start_rough
信号。首次正式触发后保留记录，即使后续倍数回落也不删除。

## **15.2 一条候选记录的最小字段**

  **字段**                    **说明**
  --------------------------- -----------------------------------------------------------------------------------------------------------------------
  first_trigger_observation   首次命中的 D/P 观察点。
  first_trigger_at            首次触发时间。
  trigger_rules               累计命中的规则列表。
  peak_observation            相对基线表现最强的观察点。
  baseline_mode               mature_history / formal_d\_series / formal_p\_series。
  judgment_confidence         rough / formal。
  tracking_completed          D7/P+7d 是否完成。
  light_process_status        pending / completed / failed；只表示视频级轻量处理。深度分析按 sample_id + domain_id 在 content_analysis 中单独跟踪。
  deep_eligibility            pending / eligible / not_eligible / manual；D7/P+7d 收尾后确定。

## **15.3 提前轻量处理与 D7 成熟处理**

  **阶段**             **触发**                                             **执行内容**                                                                          **目的**
  -------------------- ---------------------------------------------------- ------------------------------------------------------------------------------------- ------------------------------
  提前轻量处理         D0---D6 首次触发 formal D/P 候选，或用户明确指定。   采集早期评论；提取原选题、追问、争议和拓展问题。                                      及时利用高可信早期信号。
  D7/P+7d 轻量收尾     所有曾触发候选的视频。                               固定完整数据、更新峰值和最终状态；不默认调用高质量 LLM。                              保留完整轨迹并判断深度资格。
  成熟完整处理         满足表 15.4 的 deep_eligibility 条件。               采成熟评论、对照、深度分析、经验提取或反例标记。                                      把高成本模型限制在合格样本。
  最终点首次正式触发   D7 或 P+7d 才命中 formal D/P。                       采 mature_analysis 评论并按表 15.4 判断 deep_eligibility；eligible 才执行深度分析。   避免重复采样与无效模型调用。
  冷启动粗略信号       无 formal D，D7 对成熟历史异常。                     保存 rough 信号；可形成选题，不自动提炼经验。                                         不混淆数据口径。

## 15.4 深度分析资格

  **条件**                                                           **是否进入完整深度分析**
  ------------------------------------------------------------------ ----------------------------------
  D7/P+7d 仍命中正式候选                                             是
  早期峰值达到 strong_peak（默认 5 倍，配置化）                      是
  该视频已关联用户选中的 production_task，或用户明确标记为研究样本   是
  被明确选为普通对照、低表现反例或人工研究样本                       是
  仅早期轻微触发、D7 回落且未产生选题                                否；只做确定性收尾
  冷启动 rough 信号                                                  默认否；人工或正式选题需要时例外

# **16 评论采集**

  **视频类型/时点**                   **采样策略**                                                   **用途**
  ----------------------------------- -------------------------------------------------------------- ----------------------------
  formal D/P 在早期首次触发           立即采集热度排序前 60 条一级评论，purpose=early_topic。        识别追问、争议、拓展方向。
  同一候选到 D7/P+7d 且需要成熟处理   再次采集前 60 条，purpose=mature_analysis。                    成熟分析和标准样本包。
  D7/P+7d 才首次正式触发              采一次 mature_analysis。                                       避免重复。
  冷启动 rough / 历史高信号           只有选题验证或人工研究需要时采一次，purpose=mature_history。   弱证据，不等同正式 D7。
  标签搜索外部视频                    进入完整处理或选题验证时采一次，purpose=external_snapshot。    外部信号，不称为 D7。

-   实际不足 60 条时保存实际数量；第一阶段关闭二级评论。

-   保留采集器热度顺序和 sample_rank；空文本不入库。

-   不建立评论者画像，不把热门评论样本占比写成观众占比。

-   同一视频、purpose、观察点和采样策略建立唯一约束，保证重试幂等。

# **17 对照视频与标准样本包**

  **对照优先级**   **匹配方式**
  ---------------- --------------------------------------------------
  1                同一账号、同一或高度接近选题、表现接近正常水平。
  2                其他对标账号、同题或高度接近、表现普通。
  3                同一账号、主题接近但不完全相同的普通视频。

对照不是必需条件，匹配不到不阻塞分析；不同 D
点、历史累计和外部快照只能比较内容，不能直接比较绝对数据或增长。

## 17.1 普通与低表现对照按需选择

  **步骤**           **确定性规则**                                                                                                               **边界**
  ------------------ ---------------------------------------------------------------------------------------------------------------------------- ------------------------------------------------
  候选召回           优先同账号；从 same_topic、相同 core_question 或全文/pg_trgm top-K 中召回。语义关系不明确时才调用 content_relation_judge。   不对全库两两比较。
  普通对照默认带     相同最终观察点主指标处于账号基线 0.75---1.25 倍，且未命中候选规则；再按题目接近度排序。                                      阈值配置化；只用于比较，不代表内容质量"正常"。
  低表现反例默认带   相同观察点主指标 ≤0.5 倍，或至少两项可用指标 ≤0.7 倍；必须与候选存在同题/近题关系。                                          低表现不自动否定机制，只增加反例或边界证据。
  跨口径             没有相同 D/P 点时，只比较内容结构和表达，不比较绝对数据、增长或倍数。                                                        匹配不到不阻塞深度分析。
  人工覆盖           用户可以指定普通、低表现或反例样本，记录选择理由。                                                                           人工样本仍需标注数据口径与可比性。

  **样本包信息组**   **必须固定的内容**
  ------------------ ------------------------------------------------------------------------------------------------------
  账号与视频         account_id、video_id、发布时间、首次发现、时长、标题、领域。
  数据               D/P 序列、快照 ID、对应基线、倍数、缺失点和触发规则。
  文案               raw/cleaned 版本及版本 ID。
  评论               实际存在的早期/成熟评论批次 ID、实际数量、进入模型上下文数量和 omitted_count；没有某批次时明确为空。
  比较关系           重复内容、同题关系、对照视频。
  数据口径           baseline_mode、发现延迟、样本数、采集时间。
  运行版本           domain_config、Skill、提示词、模型和 payload_hash。

**第四部分　选题来源、选题流转、拓展调研与飞书汇报**

把"从哪里来、什么时候处理、如何验证、如何退出"写成可执行规则。

# **18 选题来源总表**

  **来源类型**         **触发时间**                                             **原始输入**                                                                **输出**
  -------------------- -------------------------------------------------------- --------------------------------------------------------------------------- ------------------------------------
  hotspot_conversion   每日热点批次或飞书人工提交热点。                         已配置热点源中的事件、榜单项或明确人工事件。                                转化后的领域选题或关闭。
  benchmark_daily      对标账号日常新视频 D0 及后续候选处理。                   标题、清理文案和对应 D 数据；评论只在候选策略触发后加入。                   原视频同题、衍生线索或无选题。
  benchmark_history    新账号 90 天历史回填后。                                 历史高信号视频、成熟历史倍数和文案；评论仅在选题验证/人工研究触发后加入。   存量高信号选题及拓展问题。
  tag_search           活跃领域标签轮换搜索。                                   非对标账号视频和当前公开数据。                                              外部来源选题、重复账号事实或关闭。
  hit_expansion        正式候选、历史高信号或自营高表现内容出现未解决问题时。   父视频、评论、原文、已有资料。                                              经有限调研支持的拓展选题。
  manual_task          用户通过飞书提交。                                       人物、歌曲、社会现象、问题、视频或资料。                                    定向线索、研究任务或正式选题。

## 18.1 来源进入选题状态的确定条件

  **来源**                              **初始 stage**       **进入 validated / supported 的最低条件**                                                                                                          **不满足时**
  ------------------------------------- -------------------- -------------------------------------------------------------------------------------------------------------------------------------------------- --------------------------------------------------------
  hotspot_conversion                    lead 或 validation   热点来源可追溯；领域转化得到具体 core_question；time_sensitive 必填 valid_until；无排除命中和 exact_duplicate。                                    关闭或保留 lead，不能把事件标题直接当选题。
  benchmark_daily / benchmark_history   lead                 标题/转写足以说明具体问题；domain_match 通过；来源视频与数据快照可追溯；无 exact_duplicate。数据表现决定证据强弱，不是"能否成为选题"的唯一门槛。   文本不足则等待 ASR/修订；宽泛对象进入 validation。
  tag_search                            lead                 通过四层过滤；具体 core_question、来源标签/搜索批次/视频可追溯；无排除命中和 exact_duplicate。                                                     关闭最小发现记录或等待完整处理。
  hit_expansion                         validation           待验证问题有合法依据，有限调研 result=supported，且与父选题存在明确增量。                                                                          uncertain 等待证据；rejected 关闭。
  manual_task                           lead 或 validation   用户给出具体问题时可直接 validation；至少有可追溯人工指令。允许 material_status=research_required，但 known_risk_status 不能 blocked。             只有人物/主题名称时保留 lead，先执行 source_to_topic。
  共同门槛                              ---                  validation_result=supported、source_evidence_status 至少 traceable、领域/账号匹配。完整事实核查在选中后的 production research 完成。               不得为了进日报伪造"资料已充分"。

+----------------------------------------------------------------------+
| **明确删除的来源**                                                   |
|                                                                      |
| **系统不为了补满候选池或完成日产量进行无目标关键词搜索。搜           |
| 索只存在于受控标签渠道、明确对象的定向研究和正式选题后的资料研究。** |
+----------------------------------------------------------------------+

# **19 热点转化**

热点本身不是选题。系统先保存
hotspot_event，再由所属领域规则判断它与目标受众及内容边界的关系，形成明确问题或故事角度。系统和模型不得凭主观感觉宣布某件事是热点；热点来源必须来自已配置数据源或用户在飞书中的明确指令。

  -------------- ------- ---------------- ------- -------------- ------- -------------- ------- --------------
  **热点事件**   **→**   **领域相关性**   **→**   **受众关系**   **→**   **具体角度**   **→**   **候选选题**
  -------------- ------- ---------------- ------- -------------- ------- -------------- ------- --------------

  **规则**       **实施口径**
  -------------- ----------------------------------------------------------------------------------------------
  合法热点来源   已配置 HotspotSourceAdapter 返回的事件，或用户通过飞书明确提交。
  泛科普转化     从事件中提取普通人生活影响、原因、规则或认知增量，禁止纯新闻复述。
  音乐娱乐转化   热点只作为人物、歌曲、作品或背景故事入口，禁止转成娱乐新闻和八卦。
  时效           必须设置 valid_until；超过有效期自动关闭或经重新策划转成常青角度。
  新增领域       读取 domains/\<domain_id\>/sources.yaml 的 hotspot_transform；核心代码不得增加领域 if/else。

自动热点数据源当前属于未完成节点；MVP 可先通过飞书人工热点输入和通用
hotspot_event 接口运行，不阻塞其他四类来源。

# **20 对标日常与历史高信号选题**

## **20.1 日常追踪**

-   D0 完成 ASR
    > 后即可提取原视频核心选题，但此时只说明"有人发布"，不自动称为爆款。

-   任一 D
    > 点触发正式候选后，立即补充来源数据、早期评论和拓展问题，可进入当日选题判断。

-   D7
    > 补充成熟表现并完成确定性收尾；满足深度资格时再执行成熟评论采样和完整分析。新增证据可能提高、降低或重新激活选题的推荐依据。

## **20.2 存量高信号**

历史视频只能通过成熟历史基线粗略识别，数据库类型使用
history_high_signal，用户阅读时可以简称"历史爆款"，但不能与正式 D
候选混用证据口径。

# **21 领域话题标签搜索**

## **21.1 标签来源与轮换**

-   初始活跃标签来自领域 sources.yaml。

-   对标高信号样本中出现的新标签只能进入 suggested
    > 状态，不能自动成为活跃标签。

-   每个活跃标签当前默认 7
    > 天内至少轮换搜索一次；热点或人工指定标签可临时提高优先级。

-   每次最多读取前 20 个尚未处理的新结果，但先执行不需要 LLM/ASR
    > 的元数据过滤；只有排序靠前且通过资格判断的最多 5
    > 个对象才进入音频、ASR 和低成本选题提取。所有数值配置化。

## **21.2 四层过滤标准**

  **层级**           **过滤规则**                                                                                                                           **失败后的保存**
  ------------------ -------------------------------------------------------------------------------------------------------------------------------------- ---------------------------------------------
  确定性过滤         平台视频 ID 已处理；不可访问；命中黑名单；批次重复；标题/描述/标签均未命中当前标签、配置同义词或领域实体映射且账号无该领域先验。       只存视频 ID、标签、排除原因和最后发现时间。
  元数据领域过滤     用标题、描述、标签和账号已知属性执行配置规则，不调用 LLM。                                                                             保留最小外部发现记录。
  批次完整处理排序   在剩余结果中按搜索位置、同一查询批次内的公开互动分位、元数据完整度排序；同题签名完全重复者只保留排序最高的一条。取配置上限进入 ASR。   未入选不下载音频，保留最小发现记录。
  ASR 后低成本判断   每个标签每批最多 5 个，同时受每领域每日完整处理上限约束；提取 topic_signature、domain_match 和 exclusion_hits。                        不合格则关闭，不调用高质量模型。
  外部数据排序       搜索位置和绝对互动仅作弱排序；不同账号不可直接比较倍数。                                                                               保留原始口径。

## 21.3 标签预算与自动降频

  **规则**                                                 **处理**
  -------------------------------------------------------- ---------------------------------------------------------------------------------------
  单标签连续 3 个轮换周期没有形成 stage=validated 的选题   自动设为 suggested_pause，停止轮换并在维护报告中提示；周期数来自 sources.yaml。
  同一账号反复出现                                         按客观次数触发账号复查，不额外调用 LLM 评分
  当日 ASR/模型预算不足                                    保留结果并延后完整处理，不丢失搜索记录
  新增标签                                                 只进入 suggested；通过人工确认或领域测试后才 active
  每领域标签完整处理总量                                   安全默认每天最多 10 个外部视频进入 ASR/低成本判断；超出部分保留记录并顺延，不丢数据。

外部视频没有该账号正式 D
基线，只能显示当前绝对数据、搜索位置和来源标签；不得给出正式倍数或直接作为
L2/L3 经验升级证据。

# **22 爆款选题拓展：解决方向与调研的循环**

+----------------------------------------------------------------------+
| **核心规则**                                                         |
|                                                                      |
| **拓展不能凭空生成相邻题。调研前                                     |
| 确定的是"有来源依据的待验证问题"，调研后才形成"有支撑的拓展选题"。** |
+----------------------------------------------------------------------+

  ---------------- ------- ---------------- ------- ------------------ ------- -------------- ------- ----------------- ------- --------------
  **父爆款来源**   **→**   **未解决问题**   **→**   **有限调研计划**   **→**   **方向调研**   **→**   **支持/不支持**   **→**   **拓展选题**
  ---------------- ------- ---------------- ------- ------------------ ------- -------------- ------- ----------------- ------- --------------

## **22.1 待验证问题的合法依据**

-   原视频明确提及但没有展开的人物、事件、歌曲、原因或后果。

-   热门评论中的集中追问、质疑、反例或另一种解释。

-   正式资料研究过程中发现、且与当前主题存在明确内容关系的新方向。

-   同一主题的多个独立高信号样本出现一致空缺。

评论只提供问题线索，不提供事实结论。传闻、评论观点或高赞说法必须经过外部资料验证。

## **22.2 有限方向调研**

  **输入**                                                              **输出**
  --------------------------------------------------------------------- ----------------------------------------------------------------------------
  父视频/父选题、来源数据、原文位置、评论位置、待验证问题、领域配置。   需要验证的核心判断、证据类型、搜索方向、放弃条件、最大检索轮次和时间预算。

  **结果**    **处理**
  ----------- -------------------------------------------------------------------------------------------------------
  supported   核心方向有独立资料和内容增量，生成拓展选题。
  uncertain   线索存在但支撑不足；保留证据，按 validation_ttl（安全默认 30 天）等待新证据，到期后关闭，可显式重开。
  rejected    事实不成立、内容太薄、与父选题高度重复或风险不可控；关闭。

方向调研默认最多 3 轮搜索、10
个候选来源；达到支持或否定结论即可停止，不做整个主题的全面研究。

## **22.3 研究中发现新方向**

正式选题研究中发现的新方向只创建 research_discovery
记录，不中断当前任务，也不自动递归研究。后续由每日选题批次判断是否进入拓展验证，避免
A→B→C 无限分支。

# **23 选题对象与生命周期**

为减少状态同步，线索、待验证问题和可生产选题统一使用 topic_item 表，通过
stage 区分；正式生产由 production_task 引用，不在 topic_item 中再增加
selected 状态。

  **字段/状态**                                        **当前口径**
  ---------------------------------------------------- --------------------------------------------------------------------------
  stage                                                lead / validation / validated。
  status                                               active / closed；关闭不删除，可重新激活。
  source_type                                          注册表字符串：五类自动来源或 manual_task；新增来源不迁移核心表。
  derivation_type                                      direct / expanded / independent。
  time_class                                           time_sensitive / evergreen。
  source_refs / evidence_refs                          必须追溯视频、热点、评论、父选题或资料。
  valid_until                                          时效题必填。
  next_eligible_at / last_reported_at / report_count   控制日报资格。
  auto_report_suppressed                               达到配置的重复汇报次数后停止自动推荐，但仍可搜索和被新证据激活。
  last_evidence_at                                     判断是否需要重新验证，不因时间单独关闭常青题。
  close_reason                                         published / expired / duplicate / unsupported / user_rejected / merged。

## 23.1 选题最小证据与排序字段

  **字段**                 **通用枚举**                                                                                **含义与日报资格**
  ------------------------ ------------------------------------------------------------------------------------------- -------------------------------------------------------------------------------------------------------------------------
  validation_result        supported / uncertain / rejected                                                            只有 supported 可自动进入日报；它表示选题方向成立，不表示所有文案事实已核验。
  source_evidence_status   missing / traceable / corroborated / verified                                               traceable=来源对象和位置可追溯；corroborated=至少两个独立来源/内容簇；verified=相关关键事实已核验。日报最低 traceable。
  material_status          unknown / research_required / prepared                                                      只影响排序和预计成本；research_required 仍可选，选中后由 production research 补齐。
  known_risk_status        clear / review / blocked                                                                    blocked 不进日报；review 必须在卡片展示风险；领域只能增加 reason，不得修改核心含义。
  report_count             整数                                                                                        仅当该 topic_item 实际显示在飞书卡片中时 +1；生成日报但未展示不计数。
  硬资格                   supported + source_evidence_status≥traceable + known_risk_status!=blocked + 未过期/未抑制   不得把 research.yaml 的 blocking_claim 门禁提前到选题日报阶段。

### 23.1.1 选题重复判定与确定性召回

重复判定分为"确定性完全重复"和"候选关系判断"。系统不得对全库选题做 LLM
两两比较。先生成标准化核心问题与签名，用索引召回少量近似候选；只有关系仍不明确时，才调用
content_relation_judge。

  **步骤/字段**                          **执行规则**                                                                                                                                             **结果与边界**
  -------------------------------------- -------------------------------------------------------------------------------------------------------------------------------------------------------- ------------------------------------------------------------------------------------------------------
  core_question_normalized               对 core_question 执行 Unicode NFKC、大小写统一、空白与常规标点归一化；保留人物、歌曲、机构、数字、时间和否定词。领域可增加规范化词典，但不得覆盖原文。   保存原值、标准化值、规范化版本；规则变化时可离线重算。
  core_question_hash / topic_signature   对 domain_id + core_question_normalized 生成稳定哈希；topic_signature 另保存主要对象、问题关系和内容类型，用于索引召回。                                 哈希相等只表示核心问题完全相同，不自动删除来源证据。
  exact_duplicate                        同一 domain_id 下哈希相等，且不存在不同目标受众、不同关键结论或不同创作承诺时，判为完全重复。                                                            新对象关闭为 duplicate，来源/证据挂接到保留对象；如差异影响可独立成篇，则建立 same_topic，不判重复。
  near/same_topic 召回                   先用对象字段、全文检索和 pg_trgm 从 active、在产及近期已发布选题中召回 top-K；阈值只负责召回，不直接形成关系结论。                                       确定性规则可确认时直接写关系；仍模糊才调用低成本关系判断。
  LLM 边界                               禁止全库两两比较、禁止仅凭相似度自动合并、禁止模型覆盖原始选题。                                                                                         模型失败时保留 unknown，允许入 validation，但不得作为 exact_duplicate 自动关闭。

## 23.2 防止选题积压的退出规则

  **对象**                                  **自动处理**                                                                                                          **是否丢失数据**
  ----------------------------------------- --------------------------------------------------------------------------------------------------------------------- ----------------------------
  lead                                      自动来源安全默认 lead_ttl=7 天；到期仍无法形成具体 core_question 则关闭。manual_task 不自动关闭，只停止自动日报。     不丢失，保留来源。
  validation                                任务应在创建后 24 小时内进入 running，安全默认 7 天内得出 supported/uncertain/rejected；未执行属于任务异常。          不丢失，可重试或重开。
  time_sensitive validated                  超过 valid_until 关闭，或重新策划为常青角度。                                                                         不丢失。
  evergreen validated                       未采用后 next_eligible_at 默认 +3 天；无新增证据且累计自动汇报 2 次后设置 auto_report_suppressed=true，不自动关闭。   不丢失且不长期占日报位置。
  unsupported / duplicate / user_rejected   关闭并记录原因。                                                                                                      完整保留。
  published                                 关闭并关联 production/experiment，避免短期重复。                                                                      完整保留。

候选视频不是人工池。视频完成轻量/D7 分析后进入历史样本；只有形成的
topic_item 参与日报和生产选择。

# **24 每日生产计划与飞书汇报**

## **24.1 每日运行**

> 1\.
> 汇总当天新增热点转化、对标新视频、历史高信号、标签搜索、已完成拓展验证和人工任务。
>
> 2\. 去除重复、已发布、已过期、证据不足和 next_eligible_at 未到的内容。
>
> 3\. 按 self account + domain 独立执行确定性排序：硬资格 → valid_until
> → 来源证据口径 → 内容去重 → 生产准备度 → 新证据/last_reported_at →
> topic_id。建议数量不得超过 production_policy.max_daily。日报排序不调用
> LLM，不产生黑盒综合分。
>
> 4\. 生成 daily_topic_report 和 report_item 快照，通过飞书发送。
>
> 5\. 没有收到用户明确指令时，只保存报告，不自动创建正式生产任务。
>
> 6\. 用户选择后创建
> production_task；被明确拒绝、暂缓或换角度的选题按指令处理。

## **24.2 汇报必须显示来源数据**

  **来源**           **飞书展示口径**
  ------------------ ------------------------------------------------------------------------------------------------------------------------------------------------
  正式 D 候选        来源账号/视频、首次触发 D 点、当前 D 点、可用指标绝对数、对应 D 基线倍数、每项基线样本数、发现延迟；不可用指标明确显示 unavailable，不显示 0。
  历史高信号         成熟累计绝对数、相对成熟历史中位数倍数、历史样本数，并注明"粗略口径，不等于 D7"。
  标签搜索外部视频   来源标签、搜索位置、可用的当前绝对数据、账号、是否采评论；缺失指标显示 unavailable，并明确"无正式 D 基线"。
  热点转化           热点来源、榜单/事件位置、首次发现时间、有效期、是否有视频数据共同支持。
  爆款拓展           父视频数据表现、原文/评论依据、方向调研独立来源数量和当前结论。
  人工任务           人工来源、已有资料、当前缺口；不得虚构数据表现。

### 24.2.1 日报排序顺序

  **顺序**   **规则**         **说明**
  ---------- ---------------- -------------------------------------------------------------------------------------------------------------------------
  1          硬资格           未过期、validated、未抑制、validation_result=supported、来源至少 traceable、known_risk_status!=blocked、账号/领域匹配。
  2          时效窗口         仅保留 valid_until 晚于目标发布时间的时效题；按 valid_until 升序。
  3          来源证据口径     formal D/P \> 至少 2 个独立账号或内容簇的高信号 \> history/外部弱信号；不同口径不换算成同一分数。
  4          内容去重         与最近已发布/在产内容存在 same_topic 时后置；exact_duplicate 直接排除。
  5          生产准备度       material_status=prepared 优先，其次 research_required，再到 unknown；review 风险后置但可展示，blocked 已在硬资格排除。
  6          展示与稳定排序   有新证据者优先，其次 last_reported_at 更早者；最终以 topic_id 作为稳定 tie-breaker。

### 24.2.2 日报展示数量与稳定性

  **项目**           **规则**
  ------------------ -------------------------------------------------------------------------------------------------
  max_report_items   按 account_domain_binding.production_policy 计算：max(3, min(5, max_daily + 2))；也可显式覆盖。
  没有合格选题       发送 0 条推荐及来源/任务健康摘要，不用低质量题补位。
  超出展示上限       保留在 active 队列，只有实际显示的项目才更新 last_reported_at/report_count。
  稳定性             同一输入快照必须得到相同顺序；最终以 topic_id tie-break，不调用 LLM 改排序。

飞书卡片文字由结构化字段和模板直接生成；只有用户用自然语言提出开放任务且规则无法解析时，才调用
command_parse。

## **24.3 未选中后的处理**

  **类型**           **处理**
  ------------------ ---------------------------------------------------------------------------------------------
  时效选题           valid_until 前每天重新参与排序；只有仍排在前列才再次显示。
  常青选题           未采用后 next_eligible_at 默认 +3 天；无新增证据且自动汇报累计 2 次后抑制自动汇报，不关闭。
  有新增证据         清除 auto_report_suppressed，重置 next_eligible_at，重新参与排序。
  用户"暂缓到某日"   直接设置 next_eligible_at。
  用户"保留"         保持 active，按正常资格处理。
  用户"不做"         关闭，reason=user_rejected。
  用户"换个角度"     保留来源，创建新 lead/validation，不覆盖旧题。

## **24.4 飞书指令示例**

  **用户自然语言**             **受限系统动作**
  ---------------------------- ------------------------------------------------------------------------------
  "泛科普今天做第 1 个"        校验当日产能和时效后创建一条 production_task；选择多条时必须满足 max_daily。
  "音乐今天暂停"               当日该领域报告标记 skipped，不创建任务。
  "第 2 个等 D7 再看"          设置 next_eligible_at 到 D7 成熟分析完成后。
  "这个人物换成创作转折角度"   创建新的选题策划任务，保留原来源链路。
  "研究一下某首歌的创作背景"   创建 manual_task 和定向研究线索。
  "看看今天任务做到哪了"       查询 production_task、research、script 和 job 状态。

**第五部分　深度分析、经验原子与自营验证**

经验系统仍是核心，但经验服务具体决策，不是强制塞进每篇文案的固定步骤。

# **25 单条口播内容分析框架**

## **25.1 行为链路**

分析按获得关注、产生停留、持续观看、看完、点赞、评论、收藏、转发和关注承接判断文案可能作用；涉及留存、完播和关注转化的结论只能写为机制推断。

## **25.2 分析维度**

  **维度**         **必须回答**
  ---------------- ------------------------------------------------------------------------------
  背景校准         平台、时间、热点、账号资产、人物知名度、目标观众、相对基线、文案贡献置信度。
  选题与标题       具体问题、受众关系、观看损失/收益、内容承诺和是否兑现。
  开头             第一句承担什么任务；使用结果、冲突、损失、身份、反常识或证据中的哪一种机制。
  结构与信息释放   整体结构、每段功能、信息提前/延后、注意力重置点和理解难度。
  情绪与冲突       情绪变化、冲突来源、悬念问题、兑现时间和反转是否服务主题。
  价值与信任       知识、观点、方法、情绪、谈资、风险提醒；身份、经历、数据、案例和限制披露。
  传播             评论接口、转发关系对象、关注承接。

## **25.3 可复现输出与反伪规则**

  **组成**     **要求**
  ------------ ----------------------------------------------
  原文表现     说明原文具体做了什么、位于哪里。
  底层机制     说明可能触发什么认知、情绪或行为。
  有效证据     引用原文位置、数据、评论、对照或外部资料。
  适用条件     领域、受众、选题、内容目标和前提。
  失效边界     无效、反感、机械或风险过高的条件。
  新内容复现   如何换事实、人物和叙事保留机制，不复制原句。

-   禁止只写"节奏紧凑、情绪价值强、引发共鸣、抓住痛点"。

-   观察事实、机制解释和经验状态分开保存。

-   禁止爆款因素百分比；只使用主要因素、辅助因素、背景因素和不可判断因素。

-   必须指出不可复制因素、明显短板和至少一个失效条件。

# **26 经验原子与证据等级**

  **等级**   **名称**       **证据含义**                                                                                                                     **生产用途**
  ---------- -------------- -------------------------------------------------------------------------------------------------------------------------------- --------------------------------
  L0         内容现象       单篇样本中的可核对写法或结构。                                                                                                   只记录，不进入正式生产决策。
  L1         候选机制       至少 1 个内容簇，具有机制解释、动作和初步失效假设。                                                                              用于继续找相似样本。
  L2         重复模式       当前默认至少 3 个独立内容簇重复出现；复制/转载文案按同一簇计 1。                                                                 高度匹配时低置信参考。
  L3         条件经验       达到 L2，且至少有 1 个普通/低表现/对照或反例参与校准；适用条件和失效边界完整，无未解决强冲突。                                   可进入相关决策上下文。
  L4         自营支持经验   当前版本至少 1 次 formal P 实验 supported；P 基线未成熟时的 provisional 结果不升级。                                             提高排序权重，保留失败记录。
  L5         稳定打法       当前版本至少 3 次 formal P supported，覆盖至少 2 个不同 core_question，recommendation_status=active 且无 unresolved_conflict。   账号级优先打法，仍受条件限制。

## **26.1 经验原子颗粒度**

  **判断**       **合格要求**
  -------------- ----------------------------------------------------------
  一个主要问题   主要解决选题、停留、理解、信任、价值或互动中的一个问题。
  一个主机制     不把选题、开头、情绪和结构混成一个大经验。
  可独立理解     不读取整篇原文也能理解怎样使用。
  可证伪         能找到失败条件和反例。
  证据可追溯     支持和反对证据都能定位到样本包、评论、实验或资料。

## **26.2 决策作用域**

  **decision_scope**   **解决的问题**
  -------------------- --------------------------------------------
  topic                主题如何形成具体选题，观众为什么愿意关注。
  opening              观众为什么继续听。
  overall_structure    内容如何持续推进。
  paragraph_function   某一段承担什么任务。
  expression           同一信息怎样更具体、可信或有力。
  interaction_spread   评论、转发和关注动机。

每条经验同时保存
trigger_problem，描述它解决的具体问题。例如"人物资料跨度长，容易写成流水账"，而不是只保存"人物传记结构"。

L2 及以上的自动升级只统计 eligible_tactic_evidence：正式 D/P
样本、明确选中的普通/低表现对照、或人工核验为可比的研究样本。mature_history
rough 和 external_snapshot
只能帮助发现或补充边界，不计入自动升级所需的独立内容簇。

# **27 经验怎样进入内容生产**

  -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  核心口径 / 当宽泛对象必须形成具体角度时，source_to_topic 条件检查 topic 作用域成熟经验；正式 content_plan 必查相关成熟经验。匹配结果可以为空，模型可以拒绝采用。script_review 只核对本稿已考虑/采用经验的失效条件，不重新检索全库。
  -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

经验上下文不是所有语义 Skill
的通用前置步骤。系统只在两类明确决策中检查经验：其一，宽泛对象或主题需要形成具体角度时，source_to_topic
条件触发 topic 作用域匹配；其二，正式 content_plan
形成内容策略时检查相关成熟经验。script_review 不重新全库检索，只核对
content_plan
已考虑或采用经验的失效条件。转写、分类、关系判断、深度分析、经验提取、研究计划和命令解析默认不加载经验。

  **系统保证**                                                                                                                           **系统不保证**
  -------------------------------------------------------------------------------------------------------------------------------------- ------------------------------------
  通过 decision_scope、domain、适用/排除条件筛选，并按 level、recommendation_status、freshness 排在上下文上限内的 L3+ 经验不会被遗漏。   每篇文案必须使用经验。
  当前能力能看到精简的动作、适用条件、失效边界和反例。                                                                                   必须组合固定数量经验。
  实际采用、拒绝和原因被记录。                                                                                                           经验优先于事实、素材和选题逻辑。
  经验上下文有数量和 token 上限。                                                                                                        为提高经验使用率而扩大无关上下文。

冲突优先级：事实真实性 ＞ 选题核心承诺 ＞ 创作路线与材料逻辑 ＞
领域与账号规则 ＞ 历史经验。

## 27.1 经验上下文注入边界

  **调用点**        **是否检查经验**   **实现**
  ----------------- ------------------ ------------------------------------------------------------------------------------------------------------------------------------
  source_to_topic   条件触发           仅当输入仍是人物/歌曲/宽泛主题、尚无具体 core_question 时，匹配 L3+、decision_scope=topic，最多 3 条摘要；具体问题已明确时不加载。
  content_plan      是                 SQL 元数据/条件过滤，最多 5 条简版经验卡；不额外调用 LLM 做匹配
  script_generate   否                 沿用 content_plan 已确定的 used/considered，不重复检索
  script_review     不重新全库匹配     只加载 content_plan 的 considered/used/rejected 记录及相关失效条件，避免重复 token 和事后强塞经验。
  其他 Skill        否                 避免无关上下文、额外 token 和机械套用
  向量检索          初期关闭           经验数量和召回评测证明必要后，才启用 pgvector

经验匹配使用
decision_scope、trigger_problem、domain、适用/排除条件和状态进行确定性过滤，不额外调用模型。注入的是短摘要，不注入完整证据链；没有匹配结果时直接继续。considered_tactic_ids
只记录真正进入模型上下文的经验。

## 27.2 经验使用记录

  **字段**                        **说明**
  ------------------------------- ----------------------------------------------------------
  considered_tactic_ids           当前决策实际看见过的经验。
  used_tactic_ids                 最终采用的经验及作用位置。
  rejected_tactic_ids             明确拒绝的经验。
  rejection_reason                不适用、与事实冲突、与路线冲突、重复功能、证据不足等。
  decision_scope / decision_ref   发生在选题、内容计划、段落、文案版本或审核中的具体位置。

# **28 经验维护**

系统自动更新证据统计、新鲜度和风险，但 MVP
不允许模型自动重写经验定义、合并/拆分经验或建立复杂父子树。

  **自动执行**                                        **只生成建议**
  --------------------------------------------------- ------------------------------------------------------------------------------------------------------------------------------------------------------
  新增支持/反例、更新时间、证据数、使用数、失败数。   经验合并、拆分、机制定义修改、跨领域升级。
  产生 stale 信号、watch 风险和推荐降权。             只有达到重复/冲突阈值或用户发起时才调用维护建议 Skill；不做固定周期 LLM 扫描。
  有新有效支持时从 watch 恢复 active。                自动删除历史经验或失败证据。
  当前默认状态阈值                                    stale：180 天无新增支持；watch：当前版本出现 1 次正式自营 not_supported 或 2 个独立反例；paused：当前版本跨不同选题连续 3 次正式自营 not_supported。
  恢复规则                                            watch 出现 1 次新的正式自营 supported 可恢复 active；paused 需要 2 次跨选题 supported 或人工解除。deprecated 只允许人工批准。

时间只产生陈旧信号，不能单独证明经验失效。manual_lock
后系统继续记录风险，但不自动改变受保护状态。

# **29 自营实验与 P 基线**

## **29.1 当前默认**

  **项目**       **当前口径**
  -------------- ----------------------------------------------------------------------------------
  正式启用门槛   每个自营账号至少 20 条完整 P 序列。
  正式窗口       当前视频之前最近 20 条完整 P 序列。
  不足 20 条     使用当前全部完整样本的中位数粗略展示，必须显示样本数。
  评价目标       流量：点赞、评论、收藏、分享；如能人工导入播放、完播、粉丝增长，可作为扩展指标。
  内容类型分组   MVP 不分组；只有真实数据证明分布长期不同才新增分组基线。

## **29.2 实验记录**

  **字段**                  **说明**
  ------------------------- ----------------------------------------------------
  primary_hypothesis        一次实验只确定一个主要验证假设。
  supporting_tactics        辅助机制不因一次成功自动升级。
  planned / actually_used   计划与最终稿实际使用的经验和位置。
  expected_metrics          重点观察的 P+ 流量指标。
  confounders               热点、画面、投流、人物、发布时间、账号异常等干扰。
  result                    supported / not_supported / inconclusive。

## 29.3 确定性指标信号与语义复盘边界

  **步骤**       **实施规则**
  -------------- --------------------------------------------------------------------------------------------------------------------------------------------------------------
  发布前         experiment 必须保存 primary_metric、success_rule、failure_rule 和 evaluation_observation；规则在看到结果前冻结。
  安全默认       有 formal P 基线时，若未单独配置：主指标 ≥1.5 倍为 metric_supported，≤0.8 倍为 metric_not_supported，中间为 metric_inconclusive；阈值配置化。
  P 基线未成熟   仍计算 provisional_metric_signal 并显示样本数，但不能升级 L4/L5。
  最终 result    先由程序生成 metric_signal；无重大 confounder 时直接映射 supported/not_supported/inconclusive。存在干扰、指标冲突或会影响 L4/L5 时才调用 experiment_review。
  模型权限       模型只能根据已记录 confounders 调整为 inconclusive，或提出人工复核；不得无理由把确定性失败改成成功。

**第六部分　研究、内容策划、文案生成与审核**

选题确定后，研究内容由本次核心结论和创作路线动态生成，不套固定资料清单。

# **30 选题与来源关系、创作路线**

## **30.1 选题与来源关系**

  **关系**      **定义**
  ------------- --------------------------------------------------
  direct        保留来源视频的核心问题，形成同题内容。
  expanded      从父爆款的未解决问题经调研形成新的独立选题。
  independent   来自多个资料、热点转化或人工任务，没有单一原稿。

## **30.2 正式创作路线**

  **路线**                      **适用场景**                                         **要求**
  ----------------------------- ---------------------------------------------------- -----------------------------------------------------------------------
  same_topic_original_rebuild   同一核心问题已经被来源视频验证，但希望低成本进入。   重新核查事实、组织材料、确定观点和结构；必须做措辞/结构相似风险审核。
  derived_original              来源只提供出发点，选题核心问题已经改变。             建立新承诺、新材料和新结构。
  independent_research          人物、歌曲、社会问题或热点由多份资料建立。           无单一原稿，完整保留证据链。

+----------------------------------------------------------------------+
| **明确排除**                                                         |
|                                                                      |
| **系统不设置"逐句洗稿"                                               |
| 路线。低成本路线的正式名称和要求是"同题原创重构"，不是同义词替换。** |
+----------------------------------------------------------------------+

# **31 正式选题研究计划**

素材与事实搜索不按领域列固定清单。系统从正式选题、暂定核心结论、创作路线和准备讲出的关键事实反推出本次研究问题。production_research_plan
只生成问题、主张、证据要求和搜索方向；核心 worker 再调用
SearchAdapter/FetchAdapter 获取候选来源，保存原始快照，并用
research_evidence_extract 将相关片段关联到
claim。搜索、抓取和证据判断不是一个不可审计的单体 Agent。

  -------------- ------- -------------- ------- -------------- ------- -------------- ------- --------------- ------- ------------
  **正式选题**   **→**   **暂定结论**   **→**   **关键问题**   **→**   **证据需求**   **→**   **搜索/读取**   **→**   **证据包**
  -------------- ------- -------------- ------- -------------- ------- -------------- ------- --------------- ------- ------------

  **research_plan 输出**   **说明**
  ------------------------ ----------------------------------------------
  research_questions       本次必须回答的问题。
  claim_plan               准备讲出的核心事实、观点和不确定项。
  evidence_requirements    每个问题需要的证据类型和可信度。
  blocking_claims          无法证实时会阻止写稿的内容。
  search_directions        与问题直接相关的搜索方向，不是领域固定清单。
  stop_conditions          何时资料足够、何时应放弃或缩小选题。
  budget                   最大轮次、来源数量和时间预算。

## 31.1 证据属性与主张映射

  **属性**                      **建议值**                                                                                **作用**
  ----------------------------- ----------------------------------------------------------------------------------------- ----------------------------------------------------------------
  source_kind                   official / primary_interview / original_research / reputable_secondary / user_generated   描述来源类型，不直接等同可信度。
  directness                    direct / indirect / commentary                                                            判断是否直接支持当前主张。
  claim_relation                supports / contradicts / context                                                          保留支持和反对证据。
  independence_group            同源转载共享同一组                                                                        避免把转载次数误认为多个独立来源。
  published_at / retrieved_at   时间戳                                                                                    处理时效和网页变化。
  location / excerpt            页码、段落、时间戳或必要短摘录                                                            保证可核对；不保存无必要的大段版权内容。
  content_hash / snapshot_ref   哈希或文件引用                                                                            来源变更后仍能追溯本次使用版本。
  verification_status           unverified / checked / disputed                                                           由领域 research.yaml 决定达到何种状态才可用于 blocking claim。

## 31.2 主张---证据对象

  **对象**              **关键字段**                                          **规则**
  --------------------- ----------------------------------------------------- ------------------------------------------------------------------------
  research_claim        claim_text、claim_type、blocking、status、domain_id   每个准备写入文案的关键事实/强因果必须成为独立主张。
  research_evidence     来源属性、URL/文件、摘录、哈希、时间                  证据自身不预设支持哪个结论。
  claim_evidence_link   claim_id、evidence_id、relation、notes                一条主张可有支持、反对和背景证据；同源转载按 independence_group 去重。
  审核门禁              blocking claim 全部 checked，争议主张明确披露         否则 research_blocked、缩小结论或取消。

领域 research.yaml
可以提高要求。例如人物传闻必须有当事人/合作方一手资料或多个可靠独立来源；泛科普的强因果需要研究或官方数据支持。

## 31.3 研究停止规则

-   所有 blocking_claims 均达到 research.yaml 规定的 verification_status
    > 和最小独立来源数，且核心叙事不依赖未证实传闻。

-   所有 core_claims 已达到 research.yaml
    > 的最小独立来源数；连续两轮搜索没有新增 supports/contradicts
    > 证据时触发停止。

-   达到预算仍无法支持核心结论时，缩小选题、修改路线或取消任务。

-   研究过程发现新选题时只保存 research_discovery，不自动展开。

## 31.4 分阶段研究预算与上下文

  **阶段**   **安全默认**                                                                                                              **升级条件**
  ---------- ------------------------------------------------------------------------------------------------------------------------- ---------------------------------------------------------
  首轮召回   最多 5 个候选来源；每个来源先做确定性正文抽取和 claim 关键词/实体窗口定位，单来源最多 3,000 字符进入 evidence_extract。   足以支持/否定即停止。
  扩展召回   仅 unresolved blocking_claim 或 disputed claim 可扩展到总计最多 10 个来源、单来源最多 6,000 字符。                        必须记录为什么首轮不足。
  模型档位   evidence_extract 默认 low_cost；结构化直接证据可由规则/人工确认。                                                         来源互相冲突、间接证据或高风险主张才升级 high_quality。
  人工路径   用户提供 URL/文件可直接进入 Fetch/Extract，不强制再次全网搜索。                                                           同样保存快照、位置和哈希。
  禁止       把整篇网页、整份人物生平或所有搜索结果一次性塞入模型。                                                                    由 ModelGateway 字符上限和 job 校验拒绝。

# **32 内容计划、文案与版本**

## **32.1 生产任务状态**

  ------------- ------- ----------------- ------- ------------- ------- ---------------------- ------- --------------- ------- --------------
  **created**   **→**   **researching**   **→**   **writing**   **→**   **ready_to_publish**   **→**   **published**   **→**   **reviewed**
  ------------- ------- ----------------- ------- ------------- ------- ---------------------- ------- --------------- ------- --------------

异常状态只保留 blocked 和 cancelled。blocked
必须记录阻塞原因和恢复条件；cancelled 不删除研究、文案或来源。

## **32.2 content_plan**

  **字段**             **说明**
  -------------------- --------------------------------------
  core_promise         标题/开头向观众承诺解决什么。
  target_audience      具体受众和观看动机。
  core_claims          准备讲出的关键事实和观点。
  narrative_route      同题原创重构、衍生原创或独立研究。
  section_functions    各段承担的功能，不是固定三段式。
  evidence_map         每个关键事实对应 research_evidence。
  experience_context   相关经验、适用/失效条件及是否采用。
  risk_notes           事实、传闻、误导、相似度和领域风险。

## **32.3 script_version**

  **字段**              **说明**
  --------------------- ---------------------------------------------------------
  version_type          draft / revision / final / published_text。
  parent_version_id     版本继承关系。
  change_reason         事实修正、结构调整、风格、用户反馈或风险处理。
  source_snapshot       使用的 content_plan、研究证据、经验上下文和提示词版本。
  actual_tactic_usage   最终实际使用经验和位置。
  review_result         审核问题、严重程度和修复状态。

# **33 文案审核**

## 33.1 两阶段审核与 token 控制

  **阶段**       **检查内容**                                                                                                                                                                                **LLM 调用**
  -------------- ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- ----------------------------
  确定性预检     缺失 claim 引用、字数/时长、禁用字段、未解决 blocking claim、版本完整性；同题原创对来源做标准化连续文本匹配，安全默认单段连续 20 个汉字命中即标记 revise_local（专名/标题白名单配置化）。   否
  语义审核       承诺兑现、结构自然度、因果边界、领域风险、相似结构和表达问题；核对已采用经验的失效条件，不重新检索经验库。                                                                                  高质量模型一次
  局部修订复审   只传入变更段落、相关 claim 和原审核问题                                                                                                                                                     低成本或高质量，按严重程度
  结构重写复审   重新执行完整语义审核                                                                                                                                                                        是
  用户确认       最终稿必须人工确认后才能 ready_to_publish                                                                                                                                                   否

MVP 只保留一个 script_review
Skill，根据领域和创作路线加载不同检查规则，不拆成多个审核 Agent。

  **确定性检查**                                                                                             **语义/条件检查**
  ---------------------------------------------------------------------------------------------------------- -----------------------------------------------------------------------------------------------------------------------------------------------------------
  claim 引用完整；blocking claim 已核验；长度/格式；禁用内容；与来源逐句/n-gram 重合；版本与来源 ID 有效。   核心承诺、结构、因果边界和相似风险为通用检查；人物时间线、传闻、泛科普适用范围、盘点标准等仅作为当前领域 review.yaml 示例，新增领域不得修改核心审核代码。

  **审核结果**       **处理**
  ------------------ -------------------------------------------------
  pass               进入 ready_to_publish，等待用户确认和人工发布。
  revise_local       只修改指定段落或事实。
  revise_structure   返回 content_plan，重新组织结构。
  research_blocked   返回研究，补证据或缩小结论。
  cancel             核心事实不成立、风险不可接受或与来源过度相似。

# **34 发布与实际口播**

-   当前不接入抖音自动发布，最终发布由人完成。

-   发布后录入
    > platform_video_id、publish_time、最终标题、最终文案和必要的实际口播差异。

-   系统据此创建 P+ 任务并关联 experiment。

-   如果 published_text 与最终稿的标准化文本差异率超过 review.yaml
    > 配置阈值（安全默认
    > 10%），实验分析必须保存差异段落并说明；未超过阈值只记录版本哈希。

第七部分　LLM 成本治理、技术架构、飞书交互、数据模型与部署

确定性核心程序掌握状态；飞书使用官方接口直连；LLM 只通过受预算控制的
ModelGateway 服务语义任务。Hermes/OpenClaw 与编码 Agent
均不进入核心运行事实链。

# 35 LLM 调用、预算与故障降级

## 35.1 ModelGateway

  **能力**         **实施要求**
  ---------------- -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  统一入口         所有云端/本地模型通过 ModelGateway；业务代码不得直接调用厂商 SDK。
  模型档位         none / local_low_cost / cloud_low_cost / cloud_high_quality；Skill 声明默认档位和允许降级。
  缓存             input_hash + skill_version + prompt_version + model_profile；相同输入默认复用。
  预算             按日、领域、Skill 设置 token/费用上限；超限时低优先级任务延期。
  记录             输入/输出 token、费用估算、延迟、cache_hit、重试、失败原因。
  上下文           只传当前任务必要字段；经验、评论和证据均有数量/字符上限。
  评论上下文       数据库保留全部采样。低成本早期处理默认取去除精确重复后的前 20 条且最多 8,000 字符；成熟深度分析默认前 30 条且最多 12,000 字符，记录 included_count/omitted_count。扩大上限必须由 Skill 预算显式允许。
  研究来源上下文   完整抓取内容保存为 file_asset。首轮每来源最多 3,000 字符、最多 5 个来源；只有 unresolved blocking/disputed claim 才扩展到 6,000 字符和 10 个来源。先确定性截取，再进入 evidence_extract。
  上线开关         每个 Skill 的 llm_enabled 默认 false；只有回归集、预算和输出 Schema 门禁通过后启用。

## 35.2 LLM 故障模式

  **故障**          **系统行为**
  ----------------- ----------------------------------------------------------------------------------------------------------
  模型 API 不可用   采集、D/P 快照、基线、标签元数据过滤、日报模板和按钮命令继续；语义 job 延后。
  预算耗尽          先保留已创建的正式 production_task；深度分析、拓展验证按证据口径和创建时间排队，普通 D0 选题提取可延迟。
  结构化输出失败    同模型重试一次；再失败切换允许的备用档位；仍失败则标记 failed，不让自由文本入库。
  上下文过长        按固定裁剪策略选择评论/证据并记录 omitted_count；禁止模型自行忽略来源。
  供应商切换        保持 Skill Schema 不变，重新跑回归集后切换。

# 36 当前技术选择

  **层**            **选择**                                                        **理由**
  ----------------- --------------------------------------------------------------- -------------------------------------------------------------------------------------------
  核心语言/接口     Python 3.12（锁定补丁）+ FastAPI                                与采集、ASR 和 LLM 生态一致；提供 OpenAPI。升级 minor 前跑完整回归。
  ORM/迁移          SQLAlchemy 2.x + Alembic                                        事务、模型和可审计迁移成熟。
  数据库            PostgreSQL 18.x（锁定补丁版本）                                 当前受支持版本；满足并发任务、唯一约束、JSONB、全文检索和审计。\[R10\]\[R13\]\[R14\]
  向量              MVP 不启用索引；保留 SimilarityProvider                         先用哈希、pg_trgm/全文和 top-K；经验/选题规模与召回评测证明必要后再启用 pgvector。\[R11\]
  任务执行          PostgreSQL job 表 + scheduler + worker                          个人负载足够；避免 Redis/Celery/Temporal 运维。
  ASR               ASRAdapter；优先评测 faster-whisper                             本地运行、支持时间戳/VAD；具体模型和参数需中文样本基准。\[R17\]
  采集              CollectorAdapter；MediaCrawler 为待合规候选                     隔离平台变化，并保留人工导入/替换路径。\[R1\]\[R15\]
  文件              本地内容寻址文件 + file_asset                                   MVP 不引入 MinIO/S3；原始 JSON、音频和快照有哈希与清单。
  知识工作          Obsidian Vault                                                  阅读、分析、创作和复盘；数据库为状态真相。
  飞书              FeishuDirectAdapter + 官方 SDK/API                              按钮/卡片确定性操作；自然语言按需解析。\[R16\]
  模型              ModelGateway + 模型档位                                         统一预算、缓存、降级和供应商替换。
  可选 Agent 网关   Hermes/OpenClaw 后置                                            只有多渠道或复杂宿主需求出现后评估。
  开发工具          Codex / Claude Code / 常规 IDE 均可                             只是开发效率工具，不属于运行架构和交付门禁；按订阅、代码质量和团队偏好选择。
  研究执行          SearchAdapter + FetchAdapter + ExtractorAdapter；提供商待评测   搜索/抓取/正文抽取与业务主张分离；保留人工 URL 路径。

## 36.1 为什么选择 PostgreSQL 而不是 SQLite

  **考虑**     **结论**
  ------------ -----------------------------------------------------------------------------------------------------------------
  并发         系统同时存在采集、调度、ASR、评论、LLM、飞书和同步任务，PostgreSQL 更适合多进程领取任务和事务。
  迁移成本     SQLite 前期简单，但后续切 PostgreSQL 会影响锁、JSON、并发和任务实现；当前直接使用 PostgreSQL 的额外成本可接受。
  向量与全文   pgvector 可与业务数据同库，避免新服务。
  部署         Docker Compose 可一键启动，个人使用仍可维护。

## 36.2 为什么不使用 Celery/Temporal

当前任务量低、业务状态必须留在数据库、任务大多是可重试的离线任务。使用
PostgreSQL job 表、SKIP LOCKED 领取、幂等唯一约束和 heartbeat
已能满足。只有出现大量并发、跨服务长事务或复杂补偿流程时，再评估
Celery/Temporal。

# 37 飞书交互与可选 Agent 网关

## 37.1 飞书官方接口直连（MVP 默认）

-   飞书 MVP
    > 使用自建应用直接接入官方消息、事件订阅和交互卡片接口。可以采用长连接或
    > Webhook；固定选择、暂停、暂缓、换角度等动作使用卡片按钮和结构化回调，不经过
    > LLM。\[R16\]

-   常用文本命令先由规则解析器处理；无法匹配且确实需要开放语义理解时，调用
    > command_parse 低成本模型，输出白名单 action、参数和
    > confidence。低置信度或高风险动作必须回问确认。

-   日报定时由核心 scheduler 创建并通过 message_outbox
    > 发送，不使用第三方 Agent 的
    > Cron。飞书会话只保存消息引用和命令审计，不作为业务状态来源。

-   飞书 adapter 只调用核心服务层，不获得数据库任意 SQL、文件系统或
    > Shell 权限；重复回调通过 event_id/idempotency_key 去重。

-   当后续确实需要多渠道会话、复杂工具宿主或更强沙箱时，可以接入 Hermes
    > 或 OpenClaw 作为 ConversationGateway；它们只能调用相同的受限核心
    > API，不能接管状态机。

## 37.2 Hermes/OpenClaw 的可选位置

Hermes 具备飞书、会话和 Skill；OpenClaw 具备 Gateway、沙箱和 exec
approvals，飞书插件目前由社区维护。两者都增加升级、安全和上下文成本，当前单用户飞书场景没有足够收益，因此不作为
MVP 必选依赖。\[R2\]\[R4\]\[R6\]\[R7\]

## 37.3 Codex 与 Claude Code

Codex CLI/App 和 Claude Code 都是软件工程编码
Agent，可以用于开发、测试、迁移和维护，但不是系统运行依赖。项目不得把任一编码
Agent 的私有会话、订阅额度或内部 Skill 当作生产状态、模型 API
或部署门禁；开发团队可自由替换常规 IDE、Codex 或 Claude
Code。\[R8\]\[R9\]

# 38 运行架构

  ------ --- --------------------- --- ---------- --- ------------ --- --------------------
  飞书   →   FeishuDirectAdapter   →   核心 API   →   PostgreSQL   →   Worker / Scheduler
  ------ --- --------------------- --- ---------- --- ------------ --- --------------------

  **组件**                        **职责**                                           **部署建议**
  ------------------------------- -------------------------------------------------- -------------------------------------------------
  core-api                        业务 API、校验、状态机、飞书回调和 Skill 入口。    Docker Compose/WSL2。
  scheduler                       按时间创建 D/P、标签、日报、维护和备份 job。       独立进程，数据库 advisory lock 保证单实例。
  worker                          执行采集、ASR、语义、同步和研究任务。              初期 1---2 个进程；按任务类型限流。
  postgres                        业务数据、任务、审计和全文查询。                   Docker 持久卷，定时备份。
  collector-adapter               调用已批准的采集实现并标准化。                     可在 Windows/WSL 主机运行；生产前通过合规门禁。
  feishu-adapter                  长连接/Webhook、卡片、回调去重、message_outbox。   核心服务或独立轻量进程。
  model-gateway                   模型路由、缓存、预算、用量记录和降级。             核心模块；凭证隔离。
  obsidian-sync                   数据库对象单向生成/更新 Markdown。                 worker 任务，Vault 挂载到宿主目录。
  optional-conversation-gateway   Hermes/OpenClaw，仅调用受限 API。                  默认不部署。

## 38.1 推荐仓库结构

  **目录**              **内容**
  --------------------- -------------------------------------------------------------------------------
  apps/api              FastAPI、路由、鉴权、服务层。
  apps/worker           job 领取和任务执行。
  apps/scheduler        定时规则和 job 创建。
  adapters/collectors   CollectorAdapter 接口、人工导入适配器及经批准的具体采集实现。
  adapters/feishu       飞书事件接收、签名校验、卡片、出站消息、回调幂等和受限核心 API 调用。
  skills                业务 Skill 定义、Schema、实现和测试。
  domains               领域配置包。
  prompts               通用提示词和版本。
  models                SQLAlchemy 数据模型。
  migrations            Alembic 迁移。
  obsidian              模板与同步器。
  tests                 单元、集成、回归和领域样本测试。
  deploy                Docker Compose、环境模板、备份脚本。
  adapters/research     搜索、网页/文件读取、正文抽取、快照和引用定位适配器。
  domain_schemas        通用 topic/evidence/risk 枚举及各领域 JSON Schema；防止领域配置改变核心语义。

# 39 业务 Skill 与确定性能力

## 39.1 当前最小业务 Skill 清单

  **skill_id**                 **职责**                                                                                    **阶段/模型策略**
  ---------------------------- ------------------------------------------------------------------------------------------- -----------------------------------------------------------
  transcript_clean             必要时修正 ASR，不改原意；确定性 v1 不属于 Skill。                                          阶段1；low_cost，条件触发
  content_classify             领域、内容类型和排除规则判断。                                                              阶段1；low_cost，元数据规则优先
  content_relation_judge       只判断确定性检索 top-K 中仍无法确认的重复/同题/衍生关系。                                   阶段1；local/cloud_low_cost，禁止全量两两比较
  source_to_topic              把合格来源形成线索/候选；只有宽泛对象成角度时条件加载 topic 经验。                          阶段3；low_cost，可批处理，受每日数量上限
  command_parse                仅解析规则无法识别的飞书自然语言为白名单动作。                                              阶段3；low_cost，低置信度回问
  sample_deep_analyze          固定样本包完整内容分析，并输出可为空的 tactic_candidates。                                  阶段4；high_quality，仅 deep_eligibility=eligible
  tactic_extract               可选恢复/归并能力：只在 deep_analyze 未给出合格原子、结构校验失败或多份分析需合并时运行。   阶段4；默认不单独调用，low_cost/high_quality 按异常复杂度
  tactic_maintenance_suggest   只生成经验结构变化建议。                                                                    默认关闭周期调用；达到重复/冲突阈值或用户发起时 low_cost
  topic_expansion_validate     有依据的问题有限调研并输出结论。                                                            阶段5；high_quality，受研究预算
  production_research_plan     为正式选题生成动态研究计划。                                                                阶段5；high_quality
  content_plan                 根据证据、路线和精简经验上下文形成计划。                                                    阶段5；high_quality
  script_generate              从 content_plan 和证据生成文案。                                                            阶段5；high_quality
  script_review                确定性预检后执行语义审核。                                                                  阶段5；high_quality/局部降级
  experiment_review            只解释 confounders、指标冲突或会影响 L4/L5 的实验；确定性 metric_signal 清楚时不调用。      阶段6；low_cost 默认，复杂/高影响时 high_quality
  research_evidence_extract    对确定性截取的来源窗口提取可核对证据，并关联目标 claim；不负责全网搜索。                    阶段5；low_cost，blocking/disputed 可升级 high_quality

## 39.2 不做成 Skill 的能力

-   定时调度、D/P 编号、基线计算、阈值判断、唯一约束和状态流转。

-   数据库写入、任务重试、文件移动、备份和 Obsidian 同步。

-   飞书消息发送和产能硬上限检查。

-   CollectorAdapter 调用、音频下载和 ASR 执行。

## 39.3 Skill 契约

  **项目**                       **要求**
  ------------------------------ -----------------------------------------------------------------------------------------------------------
  职责                           只完成一个明确语义任务，不越权修改其他对象。
  input_schema / output_schema   固定字段、类型、枚举、最大长度、证据引用和错误结构。
  domain_context                 domain_id、配置版本、风险和扩展 Schema。
  uses_experience                默认 false；source_to_topic 仅在宽泛对象成角度时条件 true，content_plan=true，review 只继承本稿经验记录。
  model_profile                  none/local_low_cost/cloud_low_cost/cloud_high_quality，以及允许降级。
  budget                         max_input_tokens、max_output_tokens、daily_cost_class、超限行为。
  tools                          允许的只读数据、搜索、文件或研究工具。
  validation                     JSON Schema、ID、证据位置、事实引用和安全检查。
  failure_policy                 重试、降级、排队、阻塞、人工提示或停止。
  audit                          输入快照、模型、token、费用、提示词、领域版本、缓存和哈希。
  tests                          正常、边界、失败、跨领域、成本和回归样本。

# 40 数据模型

下列为 MVP 物理表建议。高频稳定字段使用正式列；领域特殊字段使用
domain_id + schema_version + JSONB，并由领域 Schema 校验。

  **数据域**   **MVP 核心表**
  ------------ ----------------------------------------------------------------------------------------------------------------------------------
  领域与账号   domain、account、account_domain_binding、video_domain_assignment
  视频与采集   video、video_metric_snapshot、file_asset、collection_raw_payload
  文案与评论   transcript、comment_sample、comment_sample_item
  基线与候选   account_baseline、baseline_member、analysis_candidate、content_relation
  分析与经验   analysis_input_snapshot、content_analysis、tactic、tactic_version、tactic_evidence；维护事件统一记录在 audit_event，不另建专表。
  选题与日报   topic_item、topic_evidence、daily_topic_report、daily_topic_report_item
  研究与生产   production_task、research_plan、research_claim、research_evidence、claim_evidence_link、content_plan、script_version
  实验         experiment、experiment_result；保存冻结的 primary_metric、success/failure rule、metric_signal、confounders 和最终 result。
  运行         skill_run、model_usage、job、audit_event
  交互与同步   feishu_command_log、message_outbox、obsidian_note_link
  来源发现     hotspot_event、tag_search_observation；保存外部 ID/批次/标签/排名/原始 payload 引用、过滤结果和最后发现时间。

Skill、提示词、领域正文以 Git 仓库文件为唯一来源；数据库只在
skill_run、analysis_input_snapshot 和 script_version
中保存版本号与内容哈希。MVP 不建立可在线编辑的
skill_definition、prompt_template 或独立 sync_job 表。

## 40.1 关键唯一约束

  **对象**       **唯一约束**
  -------------- ----------------------------------------------------------------------------------------------------------------------------
  账号领域绑定   UNIQUE(account_id, domain_id, role)
  视频           UNIQUE(platform, platform_video_id)
  视频领域分类   UNIQUE(video_id, domain_id, classifier_version)
  快照           UNIQUE(video_id, observation_type, observation_index)
  到期任务       UNIQUE(task_type, target_object_id, observation_type, observation_index, task_version)
  候选           同一 video_id 一条主候选记录，trigger_rules 累计。
  评论采样       UNIQUE(video_id, purpose, observation_type, observation_index, strategy_version)
  日报           UNIQUE(report_date, account_id, domain_id)
  生产任务       UNIQUE(account_id, production_date, daily_sequence)
  Skill 运行     run_id 唯一；可缓存输入通过 input_hash + skill_version + model_profile 查询。
  内容分析       UNIQUE(analysis_input_snapshot_id, domain_id, skill_version, prompt_version, payload_hash)；同一视频可按领域形成独立分析。
  热点事件       UNIQUE(provider, external_event_id)；无稳定外部 ID 时使用 provider + normalized_title_hash + event_date。
  标签搜索观察   UNIQUE(search_batch_id, tag_key, platform, platform_video_id)；同视频可在不同标签/批次出现，但不会重复建 video 主记录。

## 40.2 PostgreSQL 任务队列

  **字段**                                  **说明**
  ----------------------------------------- ------------------------------------------------------
  job_id / task_type                        任务标识与类型。
  status                                    pending / running / completed / failed / cancelled。
  scheduled_at / priority                   到期时间与优先级。
  attempts / max_attempts / next_retry_at   重试。
  locked_by / locked_at / heartbeat_at      领取和失联恢复。
  payload / result_ref                      结构化输入和结果对象引用。
  idempotency_key                           防止重复创建和执行。
  error_code / error_detail                 可统计失败。

Worker 使用 SELECT ... FOR UPDATE SKIP LOCKED
领取任务。所有任务必须依赖数据库唯一约束和幂等键，不能只靠"先查有没有"。

# 41 API 与飞书权限

  **API 组**                 **能力**
  -------------------------- ------------------------------------------------------------------
  /accounts / domains        账号、领域绑定、启停、production_policy、复查建议和人工纳入。
  /videos / candidates       视频、快照、领域分类、候选和分析状态查询；不允许任意改原始数据。
  /topics                    添加线索、改角度、暂缓、关闭、恢复自动汇报和查询。
  /daily-reports             生成、读取、选择、跳过；内部采用确定性排序。
  /production-tasks          创建、查询、阻塞、取消、推进受控状态。
  /research / scripts        读取主张/证据、创建修订请求、确认最终稿。
  /feishu/events / actions   事件/卡片回调、幂等校验和白名单动作。
  /jobs / usage              查询和受限重试；查看模型 token/费用和预算。
  /conversation/commands     自然语言只映射为白名单动作并记录审计，不开放任意工具执行。

FeishuDirectAdapter 以及未来可选的 ConversationGateway 只通过受限核心
API 执行业务动作。数据库、文件系统和 Shell
默认不可见；高风险动作必须经过白名单校验，并在需要时要求用户确认。

# 42 Obsidian 职责与同步

  **组件**            **负责**                                                           **不负责**
  ------------------- ------------------------------------------------------------------ ------------------------------------------
  数据库/程序         快照、任务、基线、状态、关系、版本和审计。                         主要长文阅读和手工创作界面。
  Obsidian            完整候选分析、经验卡片、正式选题、研究摘要、文案版本和实验复盘。   高频快照、唯一约束、定时任务和正式状态。
  轻量 CLI/API docs   账号启停、失败重试、队列和数据健康。                               完整 Web 内容后台。

## 42.1 只生成必要笔记

-   账号研究页和基线摘要。

-   完成深度分析的候选视频。

-   L2 以上经验和证据摘要；L1
    > 仅在被人工关注、进入对照研究或与正式生产任务关联时生成笔记。

-   进入正式生产的选题、研究、文案和复盘。

-   周期维护与系统健康报告。

普通视频、原始快照、标签搜索淘汰记录和任务状态只保存在数据库，避免
Obsidian 变成自动文件垃圾场。MVP 使用数据库→Obsidian
单向生成；人工正文不自动回写结构化状态。

# 43 安全、监控与备份

## 43.1 安全

-   采集器登录态或 Cookie、飞书凭证、模型 API Key
    > 与业务数据分离，存入环境变量或专用密钥文件，不进入日志和
    > Obsidian。

-   飞书使用应用 allowlist、事件签名/加密、最小权限、回调幂等和业务 API
    > 令牌；高风险动作必须二次确认。\[R16\]

-   若以后部署 Hermes/OpenClaw，必须关闭非必要工具、启用写入审批/exec
    > approvals，并把业务配置目录只读；当前直连飞书不加载平台自学习
    > Skill。\[R4\]\[R7\]

-   外部文案、评论和网页均视为不可信输入，与系统指令分隔；输出强制
    > Schema 校验。

-   模型不能生成数据库主键、绕过状态机或直接修改正式经验定义。

## 43.2 最低监控

  **指标**                                         **用途**
  ------------------------------------------------ ----------------------------------------------
  最近 7 天任务按时完成率 / 最老 pending           判断调度和队列阻塞。
  采集/登录失败率                                  发现平台风控、Cookie、许可替换或适配器问题。
  D/P 快照完整率 / 发现延迟合格率                  发现漏采和基线污染风险。
  ASR 失败率与质量异常率                           发现音频/模型问题。
  早期/最终评论成功率                              发现评论接口变化；D7/P+7d 分开统计。
  Skill 结构化输出失败率                           发现提示词/模型问题。
  LLM 输入/输出 token、费用、cache hit、队列延期   控制日常成本和重复调用。
  飞书事件/卡片回调/日报送达率                     发现交互故障。
  备份成功、文件清单一致性和剩余存储               防止数据库与文件资产脱节。

## 43.3 备份与保留

  **数据**                    **当前默认**
  --------------------------- -----------------------------------------------------------------------------
  PostgreSQL                  每日逻辑备份保留 14 天；每周完整备份保留 8 周。
  配置/代码仓库               每次发布打 tag；密钥不入库；恢复时必须与数据库版本匹配。
  Obsidian Vault              每日增量/版本化备份。
  原始 JSON、文案和研究摘录   长期保留并维护 file_asset 清单；每周校验哈希。
  普通视频音频                ASR 成功并过 30 天追溯期后可清理；文件记录保留。
  候选、失败和自营音频        至少保留 180 天；自营最终素材由用户长期保存。
  关闭/淘汰对象               不物理删除；保留来源、状态和原因。
  恢复演练                    至少恢复数据库 + 配置版本 + file_asset 清单 + Vault；只恢复数据库不算通过。

**第八部分　实施阶段、验收、未完成节点与简化审计**

从基础链路开始逐阶段验收，未完成节点明确阻塞范围，不把未知问题伪装成已解决。

# 44 分阶段实施

  **阶段**           **主要建设**                                                                                    **最低验收**
  ------------------ ----------------------------------------------------------------------------------------------- --------------------------------------------------------------------------------
  0 门禁与基础设施   仓库、PostgreSQL、迁移、job、日志、备份、配置、fixtures；采集许可评审；ModelGateway 骨架。      任务可创建/领取/重试；迁移可回滚；备份可恢复；未批准采集器不能切 production。
  1 数据链路         账号/领域绑定、人工导入或批准采集适配器、历史/D/P、音频、ASR、确定性清理、Obsidian 基础同步。   用 fixtures/合规数据跑通历史回填、一条 D0 和一条 P+ 任务。
  2 基线/候选/评论   发现延迟资格、成熟历史、20 条正式 D、对应 D/P 点候选、早期+最终评论、深度资格。                 不合格延迟样本被排除；D2 formal 候选采早期评论；D7/P+7d 只对合格对象深度分析。
  3 选题/飞书        来源对象、标签低成本过滤、topic_item、确定性日报、飞书直连、按钮动作、产能。                    两个领域收到带来源数据的卡片；无 LLM 也可选择、暂缓、关闭和查询。
  4 分析/经验        深度分析、经验原子、证据、有限经验注入、Obsidian 卡片。                                         一条候选生成可追溯分析；经验只在指定决策点出现。
  5 研究/创作        主张---证据、拓展验证、研究计划、内容计划、文案、两阶段审核。                                   音乐与泛科普各完成一条；blocking claim 门禁和来源相似预检有效。
  6 自营验证         P 基线、实验、主要假设、实际经验使用和复盘。                                                    一条发布视频完成 P+7d 并输出结论。
  7 稳定化           阈值/预算校准、领域测试、监控、恢复、性能和成本。                                               连续 14 天运行；LLM 故障不影响快照；费用不超过配置预算。

# 45 当前未完成节点

  **节点**               **当前现实**                                                                                      **是否阻塞**                                             **下一步**
  ---------------------- ------------------------------------------------------------------------------------------------- -------------------------------------------------------- -----------------------------------------------------------------------------
  采集器许可与平台合规   MediaCrawler 许可证限定非商业学习；账号运营用途不能默认合规。                                     阻塞生产自动采集，不阻塞 fixtures/人工导入和核心开发。   取得授权、完成合规评审或替换 CollectorAdapter。
  自动热点数据源         尚未确定稳定合法的热点适配器。                                                                    只阻塞自动热点。                                         保留人工热点入口并评测数据源。
  网页研究提供商         搜索、抓取、正文抽取和引用记录方案未冻结。                                                        阻塞全自动研究，不阻塞人工 URL/半自动研究。              用真实选题比较召回、中文质量、稳定性和成本。
  运行 LLM/提示词        具体模型易变。                                                                                    阻塞语义质量上线，不阻塞接口。                           建立固定测试集、模型档位和预算。
  ASR 模型/参数          faster-whisper 为候选，中文口播质量和硬件成本未测。                                               阻塞正式批量 ASR。                                       用 20---30 条样本比较模型、量化、VAD 和速度。
  实际产能配置           用户未正式冻结 1/日或 3/日+滚动目标。                                                             不阻塞开发；生产计划上线前必须确认。                     安全默认 max_daily=1、rolling_target=null，飞书可修改。
  候选阈值/发现延迟      2.5/2.0/3.0 和 36h 为默认。                                                                       不阻塞。                                                 30---50 条后校准。
  领域标签与回归样本     初始标签和系统化样本不足。                                                                        标签/语义 Skill 上线前必须补。                           每领域至少 30 分类、20 选题、10 风险样本。
  飞书应用               App 权限、长连接/Webhook 和卡片回调未实测。                                                       阻塞飞书上线。                                           按官方 SDK 完成签名、幂等、allowlist 和回调测试。
  运行时区与批次时间     内部统一 UTC，但用户尚未确认 business_timezone、每日账号扫描时间、标签批次和飞书日报时间。        阻塞正式调度，不阻塞开发。                               生产配置前由用户确认；开发安全默认 Asia/Shanghai，扫描/日报时间只作测试值。
  账号与来源种子         两个自营账号平台 ID、首批对标账号、初始标签和黑名单尚未录入。                                     阻塞真实数据运行，不阻塞 schema/fixtures。               通过配置导入并保留来源与生效日期。
  来源相似度阈值         确定性连续文本默认 20 个汉字，仅为安全起点；中文专名、歌词/标题引用和结构相似仍需领域样本校准。   不阻塞开发，阻塞自动审核 hard-pass。                     用同题原创正反样本校准 review.yaml。

# 46 僵化与过度设计审查

## 46.1 已删除或简化

  **原设计/风险**                                **V0.5 处理**
  ---------------------------------------------- ------------------------------------------------------------------------------------------
  Hermes 作为必选 MVP 网关                       删除；飞书官方接口直连，Hermes/OpenClaw 后置为可替换 ConversationGateway。
  所有语义 Skill 自动检索经验                    删除；宽泛对象成角度时条件检查，content_plan 必查，script_review 只继承本稿经验记录。
  日报由 LLM 排序/解释                           删除；资格和字典序由程序完成，模板零 LLM。
  成熟历史用于 D0---D6 提前候选                  修复；仅 formal D/P 可早期触发，成熟历史只做历史和冷启动 D7 弱参照。
  所有早期候选 D7 必做高成本深度分析             改为确定性收尾 + 深度资格。
  全量转写都用 LLM 清理                          改为 raw ASR + 确定性 v1；仅正式候选、正式生产或表 13.1 定义的质量异常才语义修订。
  标签 20 个结果都进入语义判断                   改为元数据规则过滤，最多 5 个 ASR/低成本判断。
  常青题两次未选/30天即关闭                      改为抑制自动汇报，只有明确过期、无支撑或拒绝才关闭。
  A/B/C 单一证据等级                             改为来源类型、直接性、独立性、时效和主张关系多维属性。
  account 单域、domain.yaml 写账号/产能          改为 account_domain_binding + production_policy。
  Skill/提示词数据库双份正文                     删除；Git 文件为唯一来源，运行只存版本和哈希。
  独立向量库/初期 pgvector 索引                  后置；先用哈希、全文、trigram 和 top-K。
  production_slot、完整后台、Obsidian 双向同步   继续删除/后置。
  每次深度分析再单独调用 tactic_extract          删除常规重复调用；deep_analyze 同时给出候选原子，tactic_extract 只处理异常或跨分析归并。
  每条自营视频都调用高质量实验复盘               改为程序先生成 metric_signal；只有干扰、冲突或影响 L4/L5 时调用模型。
  全部历史视频接入时立即 ASR                     改为全部入队但按生产/候选/新视频/历史优先级分批，不影响指标基线。

## 46.2 不能继续简化的基础设计

-   platform + platform_video_id 唯一约束。

-   成熟历史、D、P 和外部快照分离。

-   当前视频禁止参与自己的基线。

-   raw/cleaned 文案双版本。

-   早期/成熟评论批次和采集口径。

-   固定样本包和 Skill/提示词/模型版本。

-   观察事实、机制解释、经验状态分离。

-   重复文案按内容簇去重计算独立机制证据。

-   任务幂等、重试、审计、备份和关闭不删除。

# 47 实施就绪审查

  **审查项**      **V0.5 结论**
  --------------- ------------------------------------------------------------------------------------------------------------------------
  业务与产能      目标清楚；产能模型清楚，具体数值未误写为已确认。
  数据口径        历史/D/P/外部快照分开；发现延迟资格和指标下降已处理。
  冷启动          不会用成熟累计值对 D0---D6 做无效倍数比较。
  选题来源/退出   来源进入 stage 的条件、排序字段枚举、标签过滤、拓展依据、日报上限、抑制和关闭条件清楚。
  经验            服务具体决策；topic 条件检查、content_plan 必查、review 不重复检索；数量、拒绝和失效条件清楚。
  多领域          账号/视频/领域关系、配置文件和新增领域改动范围清楚。
  研究证据        研究计划、Search/Fetch/Extract 分层、主张---证据映射、独立来源、原始快照和反对证据可保存。
  LLM 成本        每日主链可无模型运行；普通来源、评论、研究窗口、高质量后台任务均有限额；深度分析不重复调用经验提取，实验先确定性评价。
  飞书            官方接口直连，确定性按钮优先；不依赖第三方 Agent 平台。
  技术选择        核心依赖可实施；MediaCrawler 生产许可被正确列为门禁。
  数据完整性      热点与标签搜索观察有持久化对象；关闭不删除；文件/配置/数据库恢复边界补齐。
  仍未落实        采集合规、自动研究、模型/ASR评测、真实阈值、领域样本和飞书凭证明确列出。

  --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  专业判断 / V0.5 可以直接交付开发启动阶段 0---3，并可同步完成阶段 4 的 Schema、接口、fixtures 和回归框架。阶段 4 的真实语义运行必须在模型/提示词和领域测试门禁通过后开启。采集器未批准时，可通过 fixtures、人工导入或合规替代源完成核心建设；生产自动采集、全自动研究和正式模型运行均不得绕过附录门禁。
  --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

**附录**

固定参数、核心字段、领域配置示例、Skill 契约、飞书模板、任务/API
和外部技术资料。

# **附录 A　当前固定参数与默认参数**

  **项目**              **V0.5 当前口径**                                                                                                     **状态**
  --------------------- --------------------------------------------------------------------------------------------------------------------- --------------------------------
  历史窗口              最近 90 天、最多 50 条                                                                                                已确认
  成熟历史              发布满 7 天的一次累计值；单指标至少 5 个非空样本才计算；仅历史/冷启动 D7 弱参照                                       已确认原则；最小样本为当前默认
  过渡样本              接入一次 + 满 7 天一次；不建 D 序列                                                                                   当前默认
  对标快照              D0---D7                                                                                                               已确认
  自营快照              P+6h、P+1d---P+7d                                                                                                     已确认
  正式 D/P 启用         至少 20 条完整且合格序列                                                                                              D 已确认，P 当前默认
  发现延迟资格          默认 ≤36 小时                                                                                                         当前默认，待校准
  候选阈值              2.5 / 2.0 / 3.0，仅正式同口径基线                                                                                     当前默认
  评论                  formal 早期触发 60 条；成熟处理再 60 条；一级评论                                                                     已确认原则
  标签轮换              每活跃标签 7 天至少一次；连续低产出可暂停                                                                             当前默认
  标签结果              最多读 20 个元数据；最多 5 个完整处理                                                                                 当前默认
  外部账号复查          30 天至少 3 条通过领域过滤                                                                                            当前默认
  产能                  安全默认 max_daily=1；rolling_target=null                                                                             实际值待用户配置
  常青日报              默认 +3 天重新资格；无新增证据且累计自动汇报 2 次后抑制，不关闭                                                       当前默认
  时效题                有效期内重新排序，过期关闭                                                                                            已确认原则
  方向调研              默认最多 3 轮、10 个候选来源                                                                                          当前默认
  经验注入              宽泛对象成角度时 topic 条件检查（最多3）；content_plan 必查（最多5）；review 不重新检索                               当前默认
  LLM 日预算            按 Skill/领域配置；初始值待模型评测                                                                                   未完成节点
  无回复                不自动创建生产任务                                                                                                    安全默认
  线索/验证 TTL         自动 lead 7 天；validation 任务 24h 启动、7 天完成；uncertain 30 天                                                   当前默认，领域可覆盖
  标签每日完整处理      每领域最多 10 个外部视频进入 ASR/低成本判断                                                                           当前默认
  高质量后台任务上限    sample_deep_analyze 与 topic_expansion_validate 各每领域每天最多 2 个；正式生产任务不受此后台上限但受产能与费用预算   当前默认，待成本评测
  业务时区/批次时间     内部 UTC；业务时区和每日批次尚未由用户确认                                                                            未完成节点
  来源文本重合          同题原创默认连续 20 个汉字命中即要求局部修订；白名单和最终阈值由 review 样本校准                                      当前默认
  普通 D0 选题提取      每领域每天安全默认最多 20 个 source_to_topic；formal 候选和人工任务不受该普通队列上限                                 当前默认
  模型评论上下文        早期前20条/8000字符；成熟前30条/12000字符；全部60条仍保存数据库                                                       当前默认
  实际口播差异          标准化差异率默认 \>10% 保存差异说明                                                                                   当前默认，待样本校准
  历史/冷启动粗略信号   成熟历史同口径：点赞2.5倍、两项2.0倍或评论/收藏/分享3.0倍；只标 rough                                                 当前默认，待校准
  日报展示数量          max(3, min(5, max_daily+2))；仅实际显示才累计 report_count                                                            当前默认
  普通/低表现对照       普通主指标0.75---1.25倍；低表现主指标≤0.5或两项≤0.7；仅同口径且同题/近题                                              当前默认，待校准
  研究首轮上下文        5个来源×3000字符；仅 unresolved blocking/disputed 扩展至10×6000                                                       当前默认
  实验指标信号          formal P 默认成功≥1.5倍、失败≤0.8倍；发布前可按实验覆盖                                                               当前默认，待校准
  选题重复判定          domain_id + 规范化 core_question 哈希只判完全重复；对象/全文/pg_trgm 只做 top-K 召回；模糊关系才调用 LLM              当前执行口径

# **附录 B　核心字段摘要**

## **B.1 topic_item**

  **字段**                                                          **说明**
  ----------------------------------------------------------------- ------------------------------------------------------------------------------------------------------------------------------------------------------
  topic_id / domain_id                                              内部主键与单一生产领域。
  stage / status                                                    lead\|validation\|validated；active\|closed。
  title / core_question                                             当前标题和标准化核心问题。
  source_type / source_refs                                         注册表来源和视频/热点/评论/人工引用。
  parent_topic_id / derivation_type                                 父选题和 direct\|expanded\|independent。
  time_class / valid_until                                          时效类型和截止时间。
  evidence_summary / evidence_refs / last_evidence_at               来源数据和调研证据。
  recommended_route                                                 三类创作路线。
  last_reported_at / next_eligible_at / report_count                日报控制；report_count 仅在飞书实际展示时递增。
  auto_report_suppressed                                            停止自动推荐但保留搜索和重新激活能力。
  closed_at / close_reason                                          只用于发布、过期、重复、无支撑、合并或用户拒绝。
  validation_result / validation_due_at                             supported / uncertain / rejected 与验证期限；防止 validation 无限积压。
  source_evidence_status / material_status / known_risk_status      missing\|traceable\|corroborated\|verified；unknown\|research_required\|prepared；clear\|review\|blocked。日报最低 traceable，blocked 禁止自动推荐。
  core_question_normalized / core_question_hash / topic_signature   确定性重复判定与近似召回字段；保存规范化版本，不能替代原始 core_question。

## **B.2 production_task**

  **字段**                                               **说明**
  ------------------------------------------------------ ----------------------------------------------------------------------------------------------------
  task_id / account_id / domain_id                       任务、账号和领域。
  topic_id                                               正式选题。
  production_date / daily_sequence                       产能和唯一约束。
  status                                                 created / researching / writing / ready_to_publish / published / reviewed / blocked / cancelled。
  research_plan_id / content_plan_id / final_script_id   生产产物。
  block_reason / resume_condition                        阻塞处理。
  created_by / selected_report_item_id                   用户或系统来源。
  planned_publish_at                                     由 production_policy 和 business_timezone 计算；时效题必须满足 valid_until \> planned_publish_at。

## **B.3 tactic**

  **字段**                                    **说明**
  ------------------------------------------- --------------------------------------------------------
  tactic_id / tactic_version                  稳定 ID 与版本。
  name / mechanism                            名称与底层机制。
  decision_scope / trigger_problem            作用域和解决的具体问题。
  usage_action                                可执行动作。
  applicable / failure_conditions             适用和失效条件。
  scope_type / domains                        通用、跨域或领域专属。
  level / recommendation_status / freshness   L0---L5、active/watch/paused/deprecated、fresh/stale。
  manual_lock                                 是否阻止自动正式状态变化。

## B.4 experiment

  **字段**                                               **说明**
  ------------------------------------------------------ ---------------------------------------------------------------------------------------------
  experiment_id / production_task_id / video_id          连接生产、发布视频和 P+ 结果。
  primary_hypothesis / primary_metric                    一次只验证一个主要假设与主要指标。
  success_rule / failure_rule / evaluation_observation   发布前冻结的确定性评价规则和观察点。
  metric_signal                                          metric_supported / metric_not_supported / metric_inconclusive；P 基线不足时加 provisional。
  confounders                                            热点、投流、画面、人物、发布时间和账号异常等。
  result / result_reason                                 supported / not_supported / inconclusive；与 metric_signal 不一致必须有可审计原因。

# **附录 C　领域配置示例**

## **C.1 social_life/domain.yaml（示意）**

domain_id: social_life

name: 泛科普---社会生活

account_binding: database

objective: traffic

production_policy: database \# 安全默认 max_daily=1, rolling_target=null

allowed_types: \[生活现象, 社会规则, 消费, 职场, 家庭, 人际, 教育,
养老\]

excluded_types: \[硬核技术教学, 纯新闻搬运, 与大众生活弱相关知识,
专业诊断\]

## **C.2 music_entertainment/domain.yaml（示意）**

domain_id: music_entertainment

name: 音乐娱乐

account_binding: database

objective: traffic

production_policy: database \# 安全默认 max_daily=1, rolling_target=null

allowed_types: \[音乐人物传记, 歌曲赏析, 创作背景, 背景故事, 歌曲盘点\]

excluded_types: \[娱乐新闻, 综艺八卦, 粉圈争议, 私生活猎奇, 未证实传闻\]

mvp_exclusions: \[旋律分析, 和声分析, 编曲结构分析\]

# **附录 D　业务 Skill 契约模板**

  **项目**                               **模板要求**
  -------------------------------------- ------------------------------------------------------------
  skill_id / version                     稳定名称和语义版本。
  responsibility                         只描述完成什么，不越权修改其他对象。
  preconditions                          输入对象和状态要求。
  input_schema / output_schema           结构、枚举、最大长度、证据引用和错误字段。
  domain_context                         领域配置版本和扩展字段。
  decision_scope / current_problem       当前决策位置和问题。
  uses_experience / experience_context   默认 false；允许为空；限制数量和摘要长度。
  model_profile / fallback               模型档位和允许降级。
  budget                                 输入/输出上限、日预算类别、超限动作。
  tools                                  允许调用的只读/研究工具。
  validation                             Schema、ID、引用、事实和安全检查。
  failure_policy                         重试、缓存、降级、排队、阻塞或停止。
  test_cases                             正常、边界、失败、跨域、成本和回归。
  audit                                  输入快照、模型、token/费用、提示词、领域版本、缓存和哈希。

# **附录 E　飞书日报模板**

  -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **每日选题汇报｜示例 / 泛科普---社会生活 / 最近 7 天已安排：6 条｜今日硬上限：1（可配置）｜滚动软目标：未启用｜今日建议：1 条 / / 推荐 1：为什么便宜正在失去吸引力？ / 来源：对标账号 A，D2 首次触发；D2 点赞 3.8 倍、收藏 4.1 倍；正式 D 基线 20 条；发现延迟 11 小时；当前跟踪至 D4。 / 来源关系：direct｜建议路线：same_topic_original_rebuild｜时效：evergreen / 证据：原文核心问题明确；早期热门评论 60 条中 7 条提及品牌溢价（热门样本，不代表观众比例）。 / 风险：需要核查消费趋势数据。 / / 按钮：采用｜暂缓｜等 D7｜换角度｜不做。自然语言仅在按钮无法表达时使用。**
  -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

# **附录 F　任务类型**

  **任务类型**                                             **触发**
  -------------------------------------------------------- --------------------------------------------------------------------------------------------------------------------------------------------------------
  benchmark_account_scan                                   每日固定批次。
  benchmark_day_snapshot                                   D1---D7 到期。
  self_publish_snapshot                                    按发布时间精确调度。
  history_backfill / transition_maturity                   账号接入和过渡成熟。
  tag_search                                               领域标签轮换；元数据过滤后再建 ASR 任务。
  hotspot_ingest                                           已批准热点适配器或人工事件。
  audio_download / asr / transcript_normalize              前置任务成功后。
  transcript_llm_refine                                    候选/生产/质量异常时。
  candidate_light_process                                  formal D/P 对应观察点首次触发或人工指定。
  comment_collect_early / mature / external                候选时点、D7/P+7d 深度资格和外部验证。
  baseline_rebuild                                         有效快照或成员变化。
  deep_analysis_eligibility                                D7/P+7d 收尾或人工指定。
  deep_analysis / tactic_extract                           资格通过且样本包完成；deep_analysis 默认同时输出 tactic_candidates，只有异常/归并时再建 tactic_extract。
  topic_expansion_validate                                 有依据的待验证问题。
  daily_topic_report                                       每日配置时间；确定性排序和模板。
  feishu_event / command_parse                             事件回调；规则无法解析时才调用 command_parse。
  production_research / content_plan / script / review     用户确认生产后。
  experiment_metric_evaluate / experiment_review           到达发布前冻结的 evaluation_observation 时先由程序计算 metric_signal；仅当指标冲突、存在重大 confounder，或结果将影响 L4/L5 时创建 experiment_review。
  obsidian_sync / backup / maintenance / usage_reconcile   对象变化或周期任务。
  research_search / fetch / extract                        research_plan 生成后；搜索与抓取由适配器执行，evidence_extract 只处理候选来源窗口。

# **附录 G　未完成节点上线前清单**

-   选择并验证自动热点源；未完成时保留人工热点入口。

-   选择自动网页搜索/正文抽取提供商并完成中文真实选题评测。

-   为两个领域填写初始活跃标签和判入/排除/边界测试集。

-   确定生产采集器：取得 MediaCrawler 合法授权并锁定版本，或选择其他合规
    > CollectorAdapter；完成登录、字段、评论、标签搜索、限流和失败恢复测试。

-   选择运行时 LLM 与提示词版本，建立固定回归集。

-   运行 30---50 条视频后校准候选阈值和小样本保护。

-   完成选题规范化、exact_duplicate、same_topic 召回与"LLM
    仅处理模糊关系"的回归测试，验证重复来源合并不丢证据。

-   完成飞书用户
    > allowlist、事件签名/加密校验、回调幂等、卡片按钮白名单和核心 API
    > 权限测试；只有实际部署可选 ConversationGateway 时，才增加其 Skill
    > 写入审批和工具权限测试。

-   完成 PostgreSQL 恢复演练和 14 天连续运行验收。

# **附录 H　外部技术事实参考**

[[\[R1\] MediaCrawler
项目与功能]{.ul}](https://github.com/NanmiCoder/MediaCrawler)

[[\[R2\] Hermes Feishu/Lark
接入]{.ul}](https://hermes-agent.nousresearch.com/docs/user-guide/messaging/feishu)

[[\[R3\] Hermes
Cron]{.ul}](https://hermes-agent.nousresearch.com/docs/user-guide/features/cron)

[[\[R4\] Hermes Skill
写入审批]{.ul}](https://hermes-agent.nousresearch.com/docs/user-guide/configuration)

[[\[R5\] Hermes
安全与权限]{.ul}](https://hermes-agent.nousresearch.com/docs/user-guide/security/)

[[\[R6\] OpenClaw Feishu
插件（社区维护）]{.ul}](https://docs.openclaw.ai/plugins/reference/feishu)

[[\[R7\] OpenClaw Exec
approvals]{.ul}](https://docs.openclaw.ai/tools/exec-approvals)

[[\[R8\] OpenAI Codex
CLI/SDK]{.ul}](https://developers.openai.com/codex/cli)

[[\[R9\] Claude Code / Agent
SDK]{.ul}](https://docs.anthropic.com/en/docs/claude-code/overview)

[[\[R10\] PostgreSQL 18
当前文档]{.ul}](https://www.postgresql.org/docs/current/index.htm)

[[\[R11\] pgvector]{.ul}](https://github.com/pgvector/pgvector)

[[\[R12\] FastAPI]{.ul}](https://fastapi.tiangolo.com/)

[[\[R13\] SQLAlchemy
2.0]{.ul}](https://docs.sqlalchemy.org/en/20/intro.html)

[[\[R14\] Alembic]{.ul}](https://alembic.sqlalchemy.org/docs/)

外部技术资料核验日期：2026 年 6 月 28
日。开源项目和平台能力可能变化，部署时应锁定版本并保留适配器测试。

\[R15\] MediaCrawler LICENSE：NON-COMMERCIAL LEARNING LICENSE
1.1，仅授予非商业学习目的使用。https://github.com/NanmiCoder/MediaCrawler/blob/main/LICENSE

\[R16\] 飞书开放平台：消息
API、事件订阅（长连接/Webhook）与交互卡片回调。https://open.feishu.cn/document/server-docs/event-subscription-guide/overview

\[R17\] faster-whisper：基于 CTranslate2 的 Whisper 转写实现，支持
VAD、时间戳和批处理。https://github.com/SYSTRAN/faster-whisper

# **附录 I　核心原则汇总**

-   数据重复只按平台视频 ID 判断，内容重复不阻塞入库。

-   对标账号按发现日 D0---D7，自营账号按发布时刻 P+。

-   成熟历史、正式 D、自营 P 和外部快照不混算。

-   只有发现延迟合格且拥有正式同口径 D/P 基线的视频，才允许在对应 D/P
    > 观察点提前触发候选；提前信号服务选题，D7
    > 负责确定性收尾，并只对满足深度资格的对象执行成熟分析。

-   视频自动完成研究，用户选择的是有来源证据的生产选题。

-   拓展先有来源依据的问题，再有限调研，不凭空想象。

-   经验服务具体决策，保证可见但不强制采用。

-   领域差异进入配置包，新增领域不复制系统。

-   数据库掌握运行事实，Obsidian 掌握阅读和知识工作。

-   FeishuDirectAdapter
    > 负责飞书交互，核心程序负责状态、数据和业务规则；Hermes/OpenClaw
    > 仅在后续确有多渠道或复杂工具宿主需求时作为可替换网关。

-   关闭、未采用、失败和替代都不删除历史数据。

-   任何复杂设计必须证明会改变后续动作、提高结果质量或降低维护成本。

# 附录 J　LLM 调用矩阵与预算门禁

  **能力**                    **默认模型档位**                           **触发范围**                                                                  **经验上下文**                                   **预算超限**
  --------------------------- ------------------------------------------ ----------------------------------------------------------------------------- ------------------------------------------------ --------------------------------------
  transcript_clean            cloud_low_cost                             候选/生产/质量异常                                                            否                                               延期或保留确定性 v1
  content_classify            local/cloud_low_cost                       规则无法判定的完整处理对象                                                    否                                               延期
  content_relation_judge      local/cloud_low_cost                       确定性 top-K 仍模糊的候选对                                                   否                                               保留 unknown 关系，不阻塞入库
  source_to_topic             cloud_low_cost                             合格来源；普通 D0 每领域每日最多20个，单次批处理最多5个独立对象               宽泛对象成角度时 topic 最多3；具体问题不加载     延期，不丢来源
  command_parse               cloud_low_cost                             规则无法解析的自然语言                                                        否                                               要求用户使用按钮/固定命令
  sample_deep_analyze         cloud_high_quality                         deep_eligibility=eligible；同时输出可为空 tactic_candidates                   否                                               延期
  tactic_extract              cloud_low_cost/high_quality                仅 deep_analyze 原子结构失败、缺失或跨分析归并                                否                                               保留分析，进入人工/后续恢复队列
  topic_expansion_validate    cloud_high_quality                         有依据的 validation                                                           否                                               延期或人工研究
  production_research_plan    cloud_high_quality                         正式生产任务                                                                  否                                               阻塞并提示
  content_plan                cloud_high_quality                         研究门禁通过                                                                  是，最多5                                        优先保留
  script_generate             cloud_high_quality                         内容计划确认                                                                  沿用计划，不重查                                 优先保留
  script_review               cloud_high_quality                         确定性预检通过                                                                仅继承本稿 considered/used/rejected 与失效条件   优先保留/人工审核
  experiment_review           cloud_low_cost；复杂时 high_quality        metric_signal 冲突、存在重大 confounder 或影响 L4/L5                          否                                               保留确定性 metric_signal，延期解释
  后台高质量任务              cloud_high_quality                         sample_deep_analyze、topic_expansion_validate 各每领域每天安全默认最多 2 个   按各 Skill 规则                                  超额排队；不丢任务
  research_evidence_extract   cloud_low_cost；blocking/disputed 可升级   首轮5个来源×3000字符；必要时扩展至10×6000                                     否                                               超额来源顺延或人工核查；不丢原始快照

## J.1 成本验收

-   每个 skill_run 必须记录输入/输出
    token、费用估算、cache_hit、延迟和模型档位。

-   连续 14 天稳定化阶段应按来源、领域和 Skill
    输出日均成本；没有成本数据不得宣称系统可长期运行。

-   任何新增 LLM
    调用必须说明它改变了哪个后续动作；仅用于"润色说明"且可由模板完成的调用不得进入日常任务。

-   sample_deep_analyze 已能输出候选经验时，不得为了流程形式再调用
    tactic_extract；experiment_review 在确定性 metric_signal
    清楚且无重大干扰时不得调用。

-   批处理只能用于彼此独立、输出可逐项校验的低成本任务；正式
    production_task、通过 deep_eligibility 的样本和 blocking claim
    研究不得为节省调用而混入同一上下文。

# 附录 K　核心状态机与上线门禁

  **对象**             **状态**                                                                            **允许迁移**                                                                                   **迁移责任**
  -------------------- ----------------------------------------------------------------------------------- ---------------------------------------------------------------------------------------------- ---------------------
  job                  pending/running/completed/failed/cancelled                                          pending→running→completed；失败按策略重试；失联回 pending                                      worker + 数据库租约
  topic_item           lead/validation/validated + active/closed                                           stage 只能前进；新角度新建对象；closed 可显式 reopen                                           服务层
  production_task      created/researching/writing/ready_to_publish/published/reviewed/blocked/cancelled   按前置产物推进；blocked 需 resume_condition                                                    服务层 + 用户确认
  analysis_candidate   active/completed/failed                                                             首次 formal 触发创建；D7/P+7d 确定性收尾后 completed；多领域深度结果由 content_analysis 跟踪   候选服务
  tactic               active/watch/paused/deprecated                                                      证据更新可产生 watch；定义修改需人工批准                                                       维护服务 + 人工

## K.1 生产上线门禁

  **门禁**            **通过标准**                                                                                         **未通过处理**
  ------------------- ---------------------------------------------------------------------------------------------------- --------------------------------------------------
  采集许可/平台合规   书面授权或批准的替代采集方案；访问频率和数据留存明确                                                 仅 fixtures/人工导入
  飞书安全            allowlist、签名/加密、幂等、卡片按钮和高风险确认测试通过                                             不开启对话入口
  模型与提示词        固定回归集达到结构化、事实和领域标准；预算已设置                                                     语义任务不上线
  ASR                 中文口播样本质量、速度和成本达到门槛                                                                 只允许人工文本导入
  领域配置            边界、标签、research/review、测试集齐全                                                              该领域不进入自动流程
  备份恢复            数据库、配置、文件清单和 Vault 完整恢复                                                              不得连续运行生产任务
  14天稳定运行        快照、任务、成本、日报和备份达到目标                                                                 继续测试，不转正式
  采集字段能力        adapter capabilities 测试覆盖每项指标/评论/搜索/发布时间；缺失值为 NULL，规则自动禁用                不得把缺失写 0 或继续计算倍数
  研究执行            Search/Fetch/Extractor 的中文召回、正文完整性、引用定位、失败重试和成本测试通过；人工 URL 路径可用   只允许人工/半自动研究，不运行全自动 research job
  运行配置            business_timezone、扫描/日报时间、自营账号、首批对标账号、标签和产能已确认                           只运行 fixtures，不启动真实每日调度
  选题状态/日报       六类来源的 stage 迁移、最小证据枚举、时效、抑制、max_report_items 和稳定排序回归测试通过             只允许人工建 production_task，不开启自动日报

**爆款口播内容经验库系统**

**经验自动演进增量设计**

**CR-002 R01｜系统核心细化与三轮循环审查版**

基于：V0.5 执行审计修订版 + V0.5-plus R02 增量控制文档

  **项目**   **当前口径**
  ---------- ----------------------------------------------------------------------
  文档性质   结构化增量设计；不替代 V0.5 全量基线，后续累计并入 V0.6
  当前范围   经验自动演进、规则治理、最小数据模型、版本与证据继承、运行触发和验收
  明确排除   Hermes、飞书、交互审批界面、自动发布、跨领域共享经验状态
  设计原则   确定性优先、语义版本化、可逆状态自动化、不可逆语义需提案、不过度设计
  审查状态   已完成 3 轮循环审查；修正项已合并到正文
  编制日期   2026 年 6 月 29 日

本文件只保留最终方案，不保留讨论与纠错过程正文。

# 0 文档定位与使用规则

本文件用于冻结经验自动演进的系统设计。V0.5 继续承担全量执行基线；R02 对
V0.5
的增量校正仍然有效。本文件只覆盖经验自动演进及其直接依赖，不重写采集、基线、选题、研究、创作和运行时整体架构。

-   与 V0.5
    冲突时，仅本文件明确写出的经验演进规则覆盖旧口径；未涉及部分继续沿用
    V0.5。

-   本文件不讨论
    Hermes、飞书及交互审批载体；审批在系统层只表现为"提案未发布／已发布"。

-   所有阈值先作为版本化默认值运行；真实样本只能触发校准，不得由系统自动改写规则。

-   新增表、状态和任务必须满足：真实问题存在、会影响后续动作、简单方案不足以处理。

+----------------------------------------------------------------------+
| **最终权限原则**                                                     |
|                                                                      |
| 可重复计算、可逆、不会改变经验含义的变化自动执行                     |
| ；会改变经验含义、作用范围或经验身份关系的变化，只能形成版本化提案。 |
+----------------------------------------------------------------------+

# 目录

> 1 设计目标、边界与最终结论
>
> 2 经验对象的正交模型
>
> 3 L0 的处理与经验创建门槛
>
> 4 最小数据模型
>
> 5 证据资格与独立性
>
> 6 L1---L5 证据成熟度
>
> 7 推荐状态状态机
>
> 8 manual_lock 与人工干预边界
>
> 9 fresh / stale
>
> 10 经验进入生产的规则
>
> 11 语义修订提案
>
> 12 版本发布与证据继承
>
> 13 自动维护、任务触发与 LLM 成本
>
> 14 多领域与多账号预留
>
> 15 规则配置、版本与校准
>
> 16 实施阶段与验收
>
> 17 明确排除的伪需求和过早需求
>
> 18 三轮循环审查与最终结论
>
> 附录 A 经验规则配置示例
>
> 附录 B 推荐状态判断表
>
> 附录 C 关键验收用例

# 1 设计目标、边界与最终结论

## 1.1 目标

-   让经验能够随着新证据和自营结果持续变化，而不是成为静态卡片库。

-   让日常证据、等级、新鲜度和可逆风险状态自动维护，避免把经验库巡检工作转嫁给人。

-   让语义正文、适用边界、拆分、合并和退役保持版本化、可审计、不可静默覆盖。

-   让两个初始领域可以使用同一套核心状态机，同时不把领域差异写死在核心代码中。

-   控制 LLM
    调用范围：确定性变化不调用模型；只有达到明确语义触发条件才生成提案。

## 1.2 不在本增量范围

-   Hermes、飞书、消息卡片、工具契约和交互审批流程。

-   经验自动发布到创作 Prompt 之外的任何外部平台。

-   自动调参、自动改 Prompt、多模型投票和定期全库 LLM 巡检。

-   跨领域共享同一经验状态、账号级独立经验状态和经验知识图谱。

## 1.3 最终权限矩阵

  **变化对象**                   **系统权限**           **LLM**
  ------------------------------ ---------------------- ----------------
  新增支持证据、反例、实验结果   自动写入               否
  证据资格与独立性去重           自动计算               否
  L1---L5 等级                   自动计算并生效         否
  fresh / stale                  自动计算并生效         否
  active / watch / paused        确定性状态机自动生效   否
  deprecated                     只能通过正式提案发布   否
  机制、动作、条件和失效边界     只生成版本化提案       生成提案时调用
  split / merge / restore        只生成版本化提案       生成提案时调用
  跨领域复用                     目标领域新建独立经验   按需
  规则阈值自动调整               禁止                   否

# 2 经验对象的正交模型

经验只保留四个独立维度。四者分别回答"经验说什么、证据成熟到什么程度、当前是否建议使用、最近是否仍有新支持"。任何一个维度都不能替代另一个维度。

  **维度**                **回答的问题**               **变化方式**
  ----------------------- ---------------------------- --------------------------
  tactic_version          当前经验具体表达什么         版本化发布，不覆盖旧版本
  level                   当前版本积累了多少合格证据   确定性计算
  recommendation_status   当前是否适合进入生产决策     确定性状态机
  freshness               最近是否仍有合格支持         时间规则计算

## 2.1 正交约束

-   失败不会直接修改经验正文。

-   陈旧不等于失效；stale 只影响排序。

-   高等级经验可以处于 watch 或 paused。

-   发布新版本后，等级可以因继承证据减少而重新下降。

-   deprecated 不删除证据、版本或历史使用记录。

-   等级不再依赖 recommendation_status=active，也不依赖"无未解决冲突"。

+----------------------------------------------------------------------+
| **覆盖旧口径**                                                       |
|                                                                      |
| L5 不再要求                                                          |
| recommendation_status=active。等级只表达证据成熟度；当前风险由       |
| recommendation_status 表达，避免双重降级和状态冲突。                 |
+----------------------------------------------------------------------+

# 3 L0 的处理与经验创建门槛

L0 是单篇内容中的观察事实、写法或候选机制，不建立正式 tactic。L0
继续保存在 content_analysis 的结构化分析结果中，只有达到 L1
原子结构要求时才创建正式经验。

## 3.1 L1 创建门槛

-   一个明确的 decision_scope。

-   一个具体 trigger_problem。

-   一个主要 mechanism。

-   一个可执行 usage_action。

-   至少一个初步 applicable_condition。

-   至少一个初步 failure_condition。

-   至少一条可追溯、非 excluded 的支持来源。

+----------------------------------------------------------------------+
| **伪需求判定**                                                       |
|                                                                      |
| 为每个单篇现象都创建 L0                                              |
| 经验，会                                                             |
| 快速制造大量无法复用、无法验证、无法维护的半成品对象，因此明确排除。 |
+----------------------------------------------------------------------+

# 4 最小数据模型

沿用 V0.5 已有
tactic、tactic_version、tactic_evidence、experiment_result、skill_run、job
和 audit_event。只新增 experience_revision_proposal
一张表，不建立状态历史表、证据图谱表、提案明细表或审批记录表。

## 4.1 tactic：稳定身份与当前派生结果

  **字段**                   **说明**
  -------------------------- --------------------------------------
  tactic_id                  稳定经验 ID
  domain_id                  所属领域；MVP 一条经验只属于一个领域
  current_version_id         当前正式语义版本
  level                      L1---L5，确定性物化结果
  recommendation_status      active / watch / paused / deprecated
  freshness                  fresh / stale
  manual_lock                仅冻结推荐状态自动迁移
  status_changed_at          当前推荐状态生效时间
  last_eligible_support_at   最近合格支持时间
  computed_rule_version      当前派生结果使用的规则版本
  created_at / updated_at    创建与更新时间

level、freshness 和自动 recommendation_status
是物化计算结果，允许持久化以提高生产查询效率，但普通业务接口不得直接任意修改。

## 4.2 tactic_version：不可变语义正文

  **字段**                  **说明**
  ------------------------- ------------------------------------------
  version_id                版本 ID
  tactic_id / version_no    稳定经验和单调递增版本号
  name                      简短名称
  decision_scope            topic / opening / structure 等通用注册键
  trigger_problem           解决的具体内容决策问题
  mechanism                 机制解释
  usage_action              实际可执行的创作动作
  applicable_conditions     required_tags、excluded_tags、notes
  failure_conditions        risk_tags、notes
  based_on_version_id       修订来源版本
  content_hash              语义正文哈希
  created_by_skill_run_id   生成来源，可为空
  created_at                创建时间

适用条件不建设复杂规则引擎。核心程序只识别通用标签集合：明确不满足
required_tags 或命中 excluded_tags 时硬排除；notes 继续由后续语义 Skill
阅读。

## 4.3 tactic_evidence：经验版本与来源对象的链接

  **字段**                  **说明**
  ------------------------- ----------------------------------------------------------
  evidence_id               证据链接 ID
  tactic_version_id         证据针对的语义版本
  source_type               content_analysis / experiment_result / research_evidence
  source_id                 原始来源对象 ID
  relation                  support / counterexample
  eligibility               eligible / provisional / excluded
  eligibility_reason_code   资格或排除原因码
  independence_key          独立证据去重键
  observed_at               证据发生时间
  rule_version              资格规则版本
  created_by_skill_run_id   语义判断来源，可为空

  ---------------------------------------------------
  UNIQUE(tactic_version_id, source_type, source_id)
  ---------------------------------------------------

同一来源不能同时对同一版本既是支持又是反例。后续确认判断错误时可以重新分类，但必须写
audit_event，不能静默修改。

## 4.4 experience_revision_proposal：唯一新增表

该表只承载具有正式生命周期的语义修订。字段定义见第 11 章。

# 5 证据资格与独立性

## 5.1 证据资格矩阵

  **来源**                           **资格**      **能否支持 L2+**
  ---------------------------------- ------------- ------------------
  合格正式 D 样本                    eligible      是
  明确选中的普通、低表现或对照样本   eligible      是
  人工或规则确认可比的研究样本       eligible      是
  成熟正式 P 实验                    eligible      是
  P 基线不足的结果                   provisional   否
  mature_history rough               provisional   否
  external_snapshot                  provisional   否
  主要假设未实际使用的实验           excluded      否
  supporting_tactic                  excluded      否
  重大干扰或 inconclusive 实验       excluded      否
  considered / rejected 使用记录     非证据        否

## 5.2 正式 P 实验资格

一个 P 实验只有同时满足下列条件，才可以影响经验等级和推荐状态：

**1.** P 基线已经达到正式成熟门槛。

**2.** 该经验被记录为 primary_hypothesis。

**3.** 最终发布稿中 actually_used=true。

**4.** 不存在重大 confounder。

**5.** 最终结果为 supported 或 not_supported；inconclusive 不计。

## 5.3 independence_key

MVP 不新增 content_cluster 表。independence_key 由现有对象关系生成：

  **证据类型**               **independence_key**
  -------------------------- -------------------------
  复制、转载或完全相同内容   规范根内容 ID
  没有复制关系的外部视频     自身 video_id
  正式自营实验               core_question_hash
  人工确认可比的研究样本     研究样本组 ID
  same_topic 但文案独立      分别使用自身 ID，不合并

该设计只解决重复计数，不建设复杂内容图谱。

# 6 L1---L5 证据成熟度

level 是当前 tactic_version
的派生结果。每次证据、版本或资格规则变化后，由确定性任务重新计算。

  **等级**   **确定性条件**
  ---------- -----------------------------------------------------------------------------------------
  L1         Schema 完整，且至少有 1 条非 excluded 支持证据。
  L2         达到 L1，且至少有 3 个不同 independence_key 的 eligible 支持证据。
  L3         达到 L2，且至少有 1 条 eligible 普通/低表现/对照/反例参与边界校准；适用和失效条件完整。
  L4         达到 L3，且当前版本至少 1 次 formal P supported。
  L5         达到 L4，且当前版本至少 3 次 formal P supported，覆盖至少 2 个不同 core_question_hash。

## 6.1 等级变化规则

-   满足门槛后自动升级，不逐级人工审批。

-   普通新增反例和失败不直接使等级下降。

-   发布语义新版本后，按实际继承证据重新计算；允许下降。

-   证据被确认重复、不合格、来源错误或规则资格修正时，允许重新计算下降。

-   每次变化必须写 audit_event：旧值、新值、规则版本、证据摘要和原因码。

+----------------------------------------------------------------------+
| **边界说明**                                                         |
|                                                                      |
| "等级永远只升不降"会在证据                                           |
| 资格被纠正后继续保留错误成熟度，因此不采用。等级可重算，但反例本身由 |
| recommendation_status 表达风险，不触发机械降级。                     |
+----------------------------------------------------------------------+

# 7 推荐状态状态机

recommendation_status 只表达"当前是否建议进入生产决策"。状态包括
active、watch、paused、deprecated。前三者由确定性状态机管理；deprecated
只能通过正式提案。

## 7.1 合格风险信号

  **信号**      **必要条件**
  ------------- --------------------------------------------------------------------------------------
  正式 P 失败   formal P；primary_hypothesis；actually_used=true；result=not_supported；无重大干扰。
  外部反例      eligible；适用条件匹配；独立内容簇；不是 history rough 或 external_snapshot。

## 7.2 active → watch

满足任一条件即自动进入 watch：

-   当前版本出现 1 次新的正式 P 失败。

-   出现 2 个独立 eligible 外部反例，且反例晚于最近一次正式 P 支持。

## 7.3 active / watch → paused

从"最近一次正式 P supported 之后"计算合格失败窗口，必须同时满足：

-   至少 3 次 formal P not_supported。

-   覆盖 3 个不同 core_question_hash。

-   同一核心问题的重复失败只计 1 次。

-   inconclusive 不计数，也不打断窗口。

达到条件后自动 paused。外部反例不能直接触发
paused，因为公开数据无法证明单一文案机制导致失败。

## 7.4 watch → active

出现 1 条晚于最近风险证据的 formal P supported。最近风险证据包括最近正式
P 失败，以及导致 watch 的最近外部反例。

## 7.5 paused → active

从 status_changed_at 开始重新计算：至少 2 次 formal P supported，覆盖 2
个不同 core_question_hash；两次支持之间若出现 formal P
not_supported，则恢复计数重新开始。

## 7.6 deprecated

-   只能通过 deprecate 提案发布。

-   长期无使用、180
    天无支持、一次或多次失败、模型判断"过时"均不得自动触发。

-   进入 deprecated
    后不再自动召回，但版本、证据、历史计划与使用记录全部保留。

-   晚到的实验结果仍可写入，但只能通过 restore 提案恢复。

## 7.7 状态迁移优先级

一次重算若同时满足多个迁移条件，按风险优先级处理：deprecated（仅人工发布）\>
paused \> watch \>
active。程序只生成一个最终状态和一条状态变化审计，避免同一事务连续跳转。

# 8 manual_lock 与人工干预边界

manual_lock 只冻结 recommendation_status 的自动迁移，不冻结
level、freshness、证据写入或语义提案。

  **锁定后仍执行**         **锁定后不执行**
  ------------------------ ----------------------------------
  追加证据                 active / watch / paused 自动迁移
  重新计算 level           
  重新计算 fresh / stale   
  计算建议状态并记录风险   
  生成语义修订提案         

当建议状态与当前锁定状态不同，不新增 pending_status
字段，只写去重审计事件 status_change_blocked_by_manual_lock。

-   人工直接修改 recommendation_status 时，默认同时开启 manual_lock。

-   解锁后立即用当前证据重新计算一次状态。

-   人工不能直接修改 level 和 freshness；需要修正来源或规则后重算。

# 9 fresh / stale

  ----------------------------------------------------------------------------
  freshness_base = max(current_version.created_at, last_eligible_support_at)
  ----------------------------------------------------------------------------

  **条件**                          **结果**
  --------------------------------- ------------
  距 freshness_base \< stale_days   fresh
  距 freshness_base ≥ stale_days    stale
  新增 eligible support             恢复 fresh

-   默认 stale_days=180，配置化但不允许每条经验单独设置。

-   stale 不改变 level。

-   stale 不改变 recommendation_status。

-   stale 不触发语义修订提案。

-   每日一次 SQL 批量计算，不为每条经验创建到期任务。

# 10 经验进入生产的规则

## 10.1 硬过滤

  -----------------------------------------------
  domain_id 匹配\
  AND level \>= L3\
  AND recommendation_status IN (active, watch)\
  AND 未命中明确 excluded condition

  -----------------------------------------------

## 10.2 排序

**1.** 适用条件完全匹配。

**2.** active 优先于 watch。

**3.** 当前自营账号已有正式支持的优先。

**4.** level 高到低。

**5.** fresh 优先于 stale。

**6.** 最近有效支持时间。

**7.** tactic_id 作为稳定 tie-breaker。

不建立不透明综合分数。排序元组可解释、可复现，也方便后续校准。

## 10.3 上下文上限

  **环节**           **经验上限与规则**
  ------------------ ----------------------------------------------------
  宽泛对象形成角度   最多 3 条。
  content_plan       最多 5 条。
  script_generate    使用计划已选经验，不重新检索。
  script_review      只核对本稿已 considered / used / rejected 的经验。

watch 经验可以单独出现，但必须携带风险原因。禁止规定"watch
不能成为唯一经验"，因为系统可能确实只有一条相关经验，强行隐藏会造成信息缺失。

# 11 语义修订提案

所有会改变经验含义、边界、身份关系或正式退役状态的动作，均需形成提案。MVP
不设置"低风险语义自动发布"。

## 11.1 提案类型

  **类型**    **处理**
  ----------- --------------------------------------
  revise      修改机制、动作、适用条件或失效边界。
  split       一条经验实际包含多个独立机制。
  merge       多条经验实质重复。
  deprecate   正式停用。
  restore     基于历史版本生成新的恢复版本。

expand_scope 和 narrow_scope 不单独作为提案类型，统一作为 revise 的
changed_fields。

## 11.2 experience_revision_proposal 字段

  **字段**                    **说明**
  --------------------------- ----------------------------------------------------
  proposal_id                 提案 ID
  proposal_type               revise / split / merge / deprecate / restore
  base_versions               涉及的 tactic_id 与 current version_id 映射
  proposed_payload            候选新版本或新经验的完整 Schema
  changed_fields              实际修改字段
  trigger_evidence_ids        触发证据
  evidence_mapping            新版本或新 tactic 如何继承证据
  related_tactic_ids          split / merge 涉及的经验
  change_summary              面向人工查看的简明差异
  skill_run_id                模型、Prompt、Token、费用和输入快照引用
  proposal_hash               防重复生成
  status                      pending_review / published / rejected / superseded
  reviewed_at / review_note   审核时间与说明
  published_refs              发布后的版本或新 tactic
  created_at                  创建时间

## 11.3 提案状态

  -----------------
  pending_review\
  ├─ published\
  ├─ rejected\
  └─ superseded

  -----------------

批准和发布必须在同一数据库事务完成，不保留 draft 或 approved
中间状态，避免"已经批准但未发布"的悬空记录。

## 11.4 并发与过期检查

**1.** 发布前检查 base_versions 仍是各 tactic 的当前版本。

**2.** 检查 proposal_hash 尚未被同一发布事务消费。

**3.** 校验 proposed_payload 和 evidence_mapping。

**4.** 创建新版本或新 tactic。

**5.** 更新 current_version_id 或 recommendation_status。

**6.** 重新计算 level、status 和 freshness。

**7.** 提案改为 published，写 audit_event。

任一基础版本已变化，提案改为 superseded，不允许在旧语义上继续发布。

# 12 版本发布与证据继承

## 12.1 字段影响分类

  **影响类型**   **字段**                                                   **证据继承**
  -------------- ---------------------------------------------------------- ------------------------------------------------
  表达性变化     name、说明性表达                                           全部证据链接自动继承
  边界变化       applicable_conditions、failure_conditions                  只继承 evidence_mapping 明确仍符合新边界的证据
  实质变化       mechanism、decision_scope、trigger_problem、usage_action   默认不继承；必须逐条映射

影响分类由 changed_fields 确定，不让模型自行声明"影响很小"。

## 12.2 revise

-   在同一个 tactic 下创建 version_no+1。

-   旧版本永久保留。

-   按 evidence_mapping 创建新版本的 evidence link。

-   重新计算 level、recommendation_status 和 freshness。

## 12.3 deprecate

deprecate 不创建空语义版本，只把 recommendation_status 改为
deprecated，并写正式审计。

## 12.4 restore

  --------------------------------------
  v1 → v2 → v3\
  恢复 v1 的内容\
  生成 v4，记录 based_on_version_id=v1

  --------------------------------------

不直接把 current_version_id
指回旧版本，避免历史生产记录和审计失去时间顺序。

## 12.5 split

-   创建多个新的 tactic，每个从 v1 开始。

-   证据按 proposal 的 evidence_mapping 分配。

-   未映射证据继续保留在原 tactic 历史版本。

-   原 tactic 在同一事务中 deprecated。

## 12.6 merge

-   创建一个新的 tactic。

-   旧 tactic 全部 deprecated。

-   同一 source_type + source_id 的证据去重。

-   历史 content_plan、script 和 experiment 仍引用原
    tactic，不改写历史。

统一创建新
tactic，比选择某个旧经验作为"胜者"更容易审计，也避免旧经验语义被静默扩大。

# 13 自动维护、任务触发与 LLM 成本

## 13.1 确定性重算任务

以下事件创建幂等 recompute_tactic(tactic_id) Job：

-   新增或重新分类 tactic_evidence。

-   experiment_result 最终完成。

-   发布新 tactic_version。

-   manual_lock 改变。

-   experience_rule_version 改变。

-   每日 freshness 批处理。

同一 tactic 同时只允许一个 pending 或 running 的
recompute_tactic。任务只执行 SQL 和规则计算，不调用 LLM。

## 13.2 语义提案触发

只在下列条件调用一次 experience_revision_propose：

**1.** 状态首次进入 paused。

**2.** 因 2 个独立外部反例首次进入 watch。

**3.** 现有 tactic_extract 产生明确的 split / merge / boundary 信号。

**4.** 人工主动要求修订。

+----------------------------------------------------------------------+
| **成本控制**                                                         |
|                                                                      |
| 一次正式 P 失败进入 watch 时不调用 LLM。watch                        |
| 是风险标记，不足以证明经验语义需要修订。                             |
+----------------------------------------------------------------------+

## 13.3 提案去重

  --------------------------------
  proposal_hash = hash(\
  proposal_type,\
  base_versions,\
  sorted(trigger_evidence_ids),\
  sorted(related_tactic_ids)\
  )

  --------------------------------

-   相同 hash 存在 pending_review：不重复生成。

-   已 rejected：没有新增触发证据时不再生成。

-   已 published：版本已变化，新触发按新版本重新计算。

## 13.4 LLM 上下文上限

  **输入项**        **上限**
  ----------------- ---------------------------
  当前经验版本      完整 1 份
  聚合统计          1 份结构化摘要
  代表性支持证据    最多 6 条
  代表性反例        最多 6 条
  最近正式 P 实验   最多 5 条结构化摘要
  相关经验摘要      split / merge 时最多 3 条
  单条证据摘录      不超过 600 个中文字符

禁止输入整个经验库、全部历史文案、全部评论、完整研究网页和与本次变化无关的领域资料。提案任务继续使用
V0.5 已有后台高质量任务预算，不新增独立预算类别。

# 14 多领域与多账号预留

## 14.1 多领域

-   MVP 每条 tactic 只属于一个 domain_id。

-   同一机制在另一个领域出现时，在目标领域创建新 tactic。

-   目标领域必须有自己的来源证据，不继承原领域 level 和 P 实验。

-   可以在 proposal 或 audit 中记录参考来源，但不建立共享状态。

跨领域共享经验会要求领域级成熟度、状态和证据分账，当前只有两个领域，额外表和状态的维护成本明显高于少量语义重复，因此暂不建设。

## 14.2 多账号

-   正式 P 证据保留 account_id。

-   L4/L5 默认统计同领域且满足适用条件的自营实验。

-   生产排序优先当前账号已有正式支持的经验。

-   当前账号无支持时，其他账号证据只作为次级参考。

-   暂不建立
    tactic_account_state。只有真实数据证明同一经验长期跨账号分化时再引入。

+----------------------------------------------------------------------+
| **多领域适配边界**                                                   |
|                                                                      |
| 核心代码只认识                                                       |
| domain_                                                              |
| id、通用标签和通用证据接口。领域内容边界、标签词表和研究风险继续留在 |
| domain 配置中，不在经验状态机中写"音乐"或"泛科普"分支。              |
+----------------------------------------------------------------------+

# 15 规则配置、版本与校准

## 15.1 配置位置

新增 config/experience_rules.yaml。MVP
的经验成熟度和状态阈值全局统一，不允许两个领域分别覆盖一套阈值。领域配置只负责内容边界、标签、研究风险和审核规则。

## 15.2 当前默认

  **参数**                             **默认值**   **含义**
  ------------------------------------ ------------ -------------------------------
  l2_min_independent_support           3            L2 独立支持数
  watch_external_counterexamples       2            外部反例触发 watch
  pause_distinct_p\_failures           3            paused 所需不同核心问题失败数
  paused_recovery_p\_supports          2            paused 恢复所需正式支持数
  paused_recovery_distinct_questions   2            恢复支持覆盖的核心问题数
  l5_p\_supports                       3            L5 正式自营支持数
  l5_distinct_questions                2            L5 覆盖核心问题数
  stale_days                           180          新鲜度阈值

## 15.3 规则修改流程

**1.** 在当前全部合格数据上运行旧规则。

**2.** 使用新规则进行 dry-run。

**3.** 输出受影响 tactic、等级下降、状态变化和生产召回变化。

**4.** 形成明确 CR，确认调整理由。

**5.** 更新 experience_rule_version。

**6.** 创建批量确定性 recompute Job。

**7.** 记录旧值、新值、配置 hash 和原因。

规则修改不重新运行历史
ASR、深度分析或研究。系统禁止根据近期数据自动调节阈值。

# 16 实施阶段与验收

## 16.1 阶段 A：确定性经验引擎

-   证据资格与 independence_key。

-   L1---L5。

-   fresh / stale。

-   active / watch / paused。

-   manual_lock。

-   audit_event。

-   生产过滤与稳定排序。

该阶段不依赖 LLM。

## 16.2 阶段 B：影子运行

  ------------------------------------
  apply_recommendation_status: false
  ------------------------------------

系统计算建议状态并记录审计，但不正式迁移。影子运行通过后再打开自动 watch
/ paused。

-   历史 fixtures 全部符合预期。

-   重复运行结果一致。

-   相同证据重复写入不增加计数。

-   规则版本变化可完整重算。

-   任务重试不产生重复状态事件。

-   领域 A 的证据不能进入领域 B。

## 16.3 阶段 C：语义提案

-   experience_revision_proposal。

-   experience_revision_propose。

-   revise / split / merge / deprecate / restore。

-   evidence_mapping。

-   并发版本检查。

-   同事务发布。

模型或预算不可用时，确定性经验引擎继续运行，提案任务排队，不影响证据、等级和状态维护。

## 16.4 阶段 D：正式启用

-   开启 apply_recommendation_status。

-   在真实数据中保留规则版本和变更审计。

-   达到校准样本后只生成 dry-run 报告，不自动调参。

# 17 明确排除的伪需求和过早需求

  **需求**                                **结论**
  --------------------------------------- -------------------------------------
  为每个 L0 现象创建 tactic               排除；会造成经验库膨胀。
  定期全库 LLM 扫描经验                   排除；成本高且重复。
  每次 watch 都生成语义修订               排除；一次失败不足以证明边界变化。
  经验自动跨领域升级                      排除；会混淆证据。
  每个账号单独一套经验状态                暂缓；当前没有真实分化数据。
  自动修改经验阈值                        排除；历史结果将不可解释。
  独立经验知识图谱                        排除；现有关系、版本和提案足够。
  proposal diff / review / history 多表   排除；proposal + audit_event 足够。
  paused 后必须人工恢复                   排除；会重新把日常维护交给人。
  fresh / stale 自动删除经验              排除；陈旧不等于失效。
  跨领域共享同一 tactic 状态              暂缓；边际成本过高。
  多模型投票判断经验变化                  排除；成本高且不提高确定性。

# 18 三轮循环审查与最终结论

## 18.1 第一轮：结构与状态冲突审查

  **发现的问题**                           **修正**
  ---------------------------------------- ----------------------------------------------
  L0 全量物化导致经验对象膨胀              L0 只保存在分析结果，达到 L1 才创建 tactic。
  L5 依赖 active，等级和状态耦合           删除 active 和 unresolved_conflict 条件。
  manual_lock 同时冻结等级、新鲜度和状态   缩小为只冻结 recommendation_status。
  跨领域共享经验需要额外成熟度表           MVP 改为领域独立 tactic。
  considered / rejected 被误当作证据       只保留为使用统计。

## 18.2 第二轮：运行逻辑与边界审查

  **发现的问题**                             **修正**
  ------------------------------------------ -----------------------------------------------
  watch / paused 恢复缺少时间顺序            使用 status_changed_at 和"晚于最近风险证据"。
  3 次失败是否可能来自同一选题不明确         使用不同 core_question_hash 去重。
  等级永不下降会保留错误资格                 允许版本、规则或证据纠正导致重算下降。
  proposal 状态过多                          删除 draft 和 approved。
  deprecate 无语义变化却创建新版本           只改推荐状态并审计。
  merge 选择旧 tactic 作为存续对象污染历史   统一创建新 tactic。
  同一事务可能连续跳转多次状态               按风险优先级只生成一个最终状态。

## 18.3 第三轮：成本、扩展和边际收益审查

  **发现的问题**                     **修正**
  ---------------------------------- --------------------------------------------
  一次 P 失败也触发 LLM，调用过多    进入 watch 但不生成提案。
  proposal 可能重复生成              增加基于版本和触发证据集的 proposal_hash。
  proposal 重复保存模型和费用数据    只保存 skill_run_id。
  每领域覆盖经验阈值会造成配置分叉   MVP 阈值全局统一。
  自动调参解释成本高                 完全取消自动调参。
  多账号状态表属于预想性设计         延后到真实分化出现。
  逐条 freshness 到期任务成本高      每日 SQL 批处理。

## 18.4 最终审查结论

+----------------------------------------------------------------------+
| **审查通过**                                                         |
|                                                                      |
| 第三轮修正                                                           |
| 后，当前范围内未发现仍未处理的结构性冲突、状态歧义、跨领域污染、重复 |
| LLM                                                                  |
| 调用、明显过度设计或不可接受的边际维护成本。剩余不                   |
| 确定性仅是阈值在真实数据中的表现，已经被隔离为版本化配置、影子运行和 |
| dry-run 校准问题，不要求修改数据结构。                               |
+----------------------------------------------------------------------+

## 18.5 建议冻结的核心结论

+----------------------------------------------------------------------+
| **CR-002 冻结口径**                                                  |
|                                                                      |
| 证据、L1---L5、fresh/stale、active/watch/paused                      |
| 由确定性规则自动维护；经验语义正文、split、merge、deprecated 和      |
| restore 只能通过版本化提案发布。                                     |
+----------------------------------------------------------------------+

# 附录 A｜经验规则配置示例

  ----------------------------------------
  version: 1\
  \
  l2_min_independent_support: 3\
  \
  watch_external_counterexamples: 2\
  pause_distinct_p\_failures: 3\
  \
  paused_recovery_p\_supports: 2\
  paused_recovery_distinct_questions: 2\
  \
  l5_p\_supports: 3\
  l5_distinct_questions: 2\
  \
  stale_days: 180\
  \
  apply_recommendation_status: false

  ----------------------------------------

apply_recommendation_status 在影子运行通过后改为
true。修改其他阈值必须按第 15.3 节执行 dry-run 和规则版本升级。

# 附录 B｜推荐状态判断表

  **当前状态**        **信号**                                                                   **目标状态**                          **是否自动**
  ------------------- -------------------------------------------------------------------------- ------------------------------------- --------------
  active              1 次 formal P not_supported                                                watch                                 是
  active              2 个晚于最近支持的独立 eligible 外部反例                                   watch                                 是
  active / watch      最近支持后 3 个不同 core_question 的 formal P 失败                         paused                                是
  watch               1 次晚于最近风险证据的 formal P supported                                  active                                是
  paused              暂停后 2 次、覆盖 2 个 core_question 的 formal P supported，且中间无失败   active                                是
  任意非 deprecated   deprecate 提案发布                                                         deprecated                            否
  deprecated          restore 提案发布                                                           active / watch / paused，由证据重算   否
  manual_lock=true    任何自动状态建议                                                           保持当前状态并写审计                  否

# 附录 C｜关键验收用例

  **用例**                          **预期**
  --------------------------------- -----------------------------------------
  3 条转载视频                      只计 1 个独立证据。
  3 个独立 eligible 支持            自动 L2。
  达到 L2 但无边界样本              不进入 L3。
  provisional P supported           不进入 L4。
  一次正式 P 失败                   自动 watch；不调用修订 LLM。
  同一 core_question 失败 3 次      不自动 paused。
  3 个不同 core_question 正式失败   自动 paused。
  manual_lock 下出现失败            状态不变，但写 blocked 审计。
  新版本只继承部分证据              按新版本证据重新计算 level。
  领域 A 的证据                     不能提升领域 B 的经验。
  提案基于旧版本                    发布时改为 superseded。
  LLM 服务中断                      证据、等级、状态和 freshness 继续运行。
  同一 recompute 重试               不产生重复状态事件。
  规则版本升级                      dry-run 后批量重算，不重跑 ASR/研究。

# 附录 D｜依据文件与增量关系

  **文件**                                                              **作用**
  --------------------------------------------------------------------- -------------------------------------------------------------------------
  《爆款口播内容经验库系统_当前阶段技术设计文档_V0.5_执行审计修订版》   完整执行基线；本文件未覆盖的规则继续有效。
  《爆款口播内容经验库系统_新对话交接控制文档_V0.5-plus_R02》           V0.5 后的增量控制与经验演进方向；本文件细化并冻结其尚未明确的权限边界。

本文件作为结构化增量项保存。达到阶段节点后，与其他已确认增量统一合并生成
V0.6，不在本文件中提前重写全量系统。

**爆款口播内容经验库系统**

**新对话交接控制文档**

**V0.5-plus R05｜完整无损继承修订版 · TECH 启动版**

  ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **文件定位：**本文件重新执行 R02→R03→R04→CR-003F→CR-005 的逐章继承审计，完整保留仍有效的细粒度控制点，纠正旧版"已取代但未逐项继承"的问题。它取代先前存在压缩缺陷的 R05，并可作为进入 TECH 的最新增量控制文档；它仍不替代 V0.5 完整执行基线与 CR-002 经验自动演进正式增量。
  ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

  **项目**         **当前口径**
  ---------------- -----------------------------------------------------------------------------
  完整执行基线     《当前阶段技术设计文档 V0.5 执行审计修订版》
  经验正式增量     《经验自动演进增量设计 CR-002 R01》
  最新交接控制     本文件 R05 完整无损继承修订版
  完整继承来源     R02 Hermes 增量；R03 CR-004/CR-006；R04 CR-003A---E；本对话 CR-003F、CR-005
  已完成逻辑节点   CR-003A---F、CR-004A---C、CR-005、CR-006A---B
  下一唯一节点     TECH：物理对象/版本/追溯、状态、Job、Runner/Port、Materializer 与恢复机制
  Codex 实施状态   现有控制包 R01 已落后；TECH 完成后统一更新，不得先宣布完整实施

编制日期：2026 年 6 月 30 日

**最高优先级：端到端可追溯、逐章无损继承、Skill
可移植独立、历史不可静默覆盖、核心功能可在无真实数据条件下验证。**

# **目录**

0 使用方式、冲突优先级与继承审计规则

1 当前状态分类与全局已确认边界

2 R02 有效增量：Hermes、飞书、工具、权限、调度与故障隔离

3 R03 上位规则：用户流程、端到端追溯、验证与上下文治理

4 CR-004A：选题到 production_task

5 CR-004B：研究、计划、稿件与审核版本链

6 CR-004C：实际发布、P+ 与经验归因

7 CR-006A：研究来源资格与独立来源

8 CR-006B：正式资料研究执行与音乐作品评论边界

9 CR-002 与其他章节的权限衔接

10---19 R04 CR-003A---E 完整有效契约及跨章节审计

20---23 CR-003F：experiment_review 与 experience_revision_propose

24---29 CR-005：数据纠错、失效传播与重算

30 当前状态、待校准、未完成 TECH 规格和外部门禁

31 下一唯一节点：TECH

附录 A 新对话启动提示词

附录 B 逐章继承与覆盖清单

附录 C Portable Skill 职责矩阵

附录 D TECH 启动检查表

附录 E 后续文档无损继承门禁

# **0 使用方式、冲突优先级与继承审计规则**

15. 上传《爆款口播内容经验库系统_当前阶段技术设计文档_V0.5_执行审计修订版》。

16. 上传本文件《爆款口播内容经验库系统_新对话交接控制文档_V0.5-plus_R05_TECH启动版_完整无损继承版》。

17. 上传《爆款口播内容经验库系统_经验自动演进增量设计_CR-002_R01》。

18. 粘贴附录 A 或单独提供的《新对话启动提示词_R05_TECH_完整继承版》。

19. 新对话先确认完整读取三份文件，并用不超过 10
    条列出有效控制点、已完成节点和 TECH 第一子节点。

20. 随后直接进入 TECH；除非发现明确冲突，不重新讨论已经冻结的业务边界。

  ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **冲突优先级：**用户在新对话中的最新明确指令 ＞ 本 R05 完整无损继承版 ＞ CR-002 ＞ V0.5 ＞ R04、R03、R02 及更早内容。旧文档只作来源审计；本 R05 对其仍有效内容逐章继承，并明确标注已覆盖、已完成或移交 TECH 的事项。
  ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

## **0.1 本次修订纠正的问题**

  **问题**       **旧版风险**                                            **本版处理**
  -------------- ------------------------------------------------------- ----------------------------------------------
  R02→R03        Hermes 工具、权限、调度和故障隔离被压缩为"架构已确认"   恢复完整有效控制点，并标记当前暂缓实施细节
  R03→R04        CR-004A---C 与 CR-006A---B 的细粒度规则没有逐项迁移     完整重建生产、版本、发布、研究来源与执行章节
  R04→旧 R05     CR-003A---E 被缩成摘要，却宣称完整取代 R04              直接继承 R04 详细契约，不再用摘要替代
  未来版本治理   "取代"缺少逐章证据，容易再次丢失内容                    新增逐章继承清单和无损继承门禁

## **0.2 结论状态分类**

  **分类**           **含义**
  ------------------ -------------------------------------------------------------------------------------
  已确认             业务边界或逻辑契约已经冻结，除非发现明确冲突不得重开。
  当前默认           可执行专业默认；可由固定回归或真实运行校准，不代表业务设计缺失。
  待校准             模型、Prompt、阈值、正常输入规模、性能、成本和真实兼容性参数。
  未完成 TECH 规格   物理对象、表/索引、事务、API、Job、锁、租约、Runner/Port、Materializer 和恢复机制。
  外部门禁           必须用真实供应商、凭证或平台测试确认，但不得阻塞 fixture/Replay 的核心逻辑验证。

# **1 当前全局边界**

-   两个初始领域为"泛科普---社会生活"和"音乐娱乐"；各自当前绑定一个自营账号，核心结构预留多领域和多账号。

-   两个自营账号均只以流量为目标，不设计成交、线索、私信、商品或咨询转化。

-   PostgreSQL 是状态与业务事实唯一来源；Obsidian
    只作单向派生阅读、分析、创作和复盘层。

-   确定性规则由 Core API、Scheduler 和 Worker 执行；LLM
    只承担分类、分析、研究、策划、创作和语义审核。

-   平台采集、搜索、Fetcher、ASR、模型和外部来源通过 Adapter 或 Port
    隔离，不能掌握业务状态。

-   用户选择正式选题后，系统自动完成研究、计划、初稿、审核与限制内修订；用户只在最终稿确认和明确异常时介入。

-   系统当前不自动发布抖音；用户确认稿与实际发布稿必须分开保存。

-   每条自营发布内容均跟踪
    P+；只有发布前冻结单一主要经验假设且实际使用的内容才是 formal
    experiment。

-   端到端可追溯是全系统最高级约束。

-   每个模块必须能够独立运行和验证；验证资产删除后不得影响生产系统。

  -------------------------------
  用户选择正式选题\
  → 自动资料研究\
  → 自动内容计划\
  → 自动初稿\
  → 确定性预检\
  → 自动语义审核与限制内修订\
  → 用户确认最终稿\
  → 用户人工发布\
  → 保存实际发布稿\
  → P+ 跟踪、实验复盘与经验演进

  -------------------------------

流程只在 blocking claim
无法核验、可靠证据重大冲突、核心结论必须改变或缩小、研究预算需明显扩大、自动修订超限，或系统建议取消/换角度/补人工资料时暂停。

# **2 R02 有效增量：Hermes、飞书、工具、权限、调度与故障隔离**

  -------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **当前状态：**Hermes 与飞书架构已经确认。当前进入核心 TECH 时继续忽略 Hermes 的部署细节，但以下职责、权限和故障隔离规则仍是有效上位约束，不能因"暂缓讨论"而丢失。
  -------------------------------------------------------------------------------------------------------------------------------------------------------------------

## **2.1 已确认运行架构**

  -----------------------------------------------------------------------------
  飞书\
  ↓\
  Hermes Messaging Gateway（会话、消息、附件、主动回传）\
  ↓\
  Hermes Agent Runtime（意图、追问、工具选择、多步编排、等待/恢复）\
  ↓ 只调用白名单工具 / Core API\
  Core API（权限、参数校验、状态机、幂等、审计）\
  ↓\
  PostgreSQL + Job/Scheduler + Worker\
  ↓\
  Collector / ASR / Search-Fetch / ModelGateway / 业务 Skill / Obsidian 派生\
  ↑\
  执行结果与待确认动作 → Hermes → 飞书

  -----------------------------------------------------------------------------

这不是"飞书直连＋自建 Agent"。系统仍需开发业务 API、数据库、Worker
和业务 Skill，但不再自行开发通用会话、Agent
循环、工具选择、对话上下文管理和飞书消息网关。

## **2.2 Hermes、核心系统与 ModelGateway 的责任边界**

  **层级**               **负责**                                                                                  **明确不负责**
  ---------------------- ----------------------------------------------------------------------------------------- ----------------------------------------------------------------------------
  Hermes                 飞书连接、会话、自然语言理解、澄清、工具发现与调用、多步编排、等待/恢复、结果组织与回传   不计算基线；不直接写数据库；不直接改 D/P；不自行升级经验；不绕过业务状态机
  Core API               业务权限、Schema 校验、状态迁移、幂等、审计、工具结果格式、创建/查询 Job                  不负责开放式对话和 Agent 推理
  PostgreSQL             账号、视频、快照、基线、候选、选题、经验、生产任务、审计和版本的唯一事实来源              不以 Hermes 会话、记忆或 Skill 正文替代
  Job/Scheduler/Worker   D/P 到期任务、重试、采集、ASR、评论、基线、研究和生产任务执行                             不依赖用户在线；不把精确时间任务交给 Agent 临场判断
  ModelGateway           业务 Skill 的模型路由、预算、缓存、降级、Token/费用记录                                   不是 Agent Runtime，不管理飞书会话和工具循环
  Obsidian               展示完整分析、经验卡、研究、稿件和复盘                                                    不作为任务、状态和审批的唯一来源

## **2.3 Hermes 如何驱动执行**

-   用户通过飞书发送自然语言任务；Hermes
    识别目标，信息不足时追问，信息充分时选择一个或多个白名单工具。

-   每次业务动作仍由 Core API
    校验当前状态、账号、领域、产能、风险和幂等键。

-   长任务只返回 task_id 与阶段进度；Worker
    完成后，核心系统产生结果事件，由 Hermes 主动回传飞书。

-   "继续刚才的研究""查看音乐任务"等后续会话必须从 Core API
    读取正式任务，不得只依赖聊天记忆。

-   每日选题报告内容由 Core 业务数据确定；Hermes 负责触发、接收与投递。

-   卡片按钮、固定命令和简单选择走确定性工具路径，不需要模型重新理解整个会话。

-   无回复时只保存日报，不自动创建正式生产任务，除非用户以后明确修改该安全默认。

## **2.4 Hermes Tool、Hermes Skill 与业务 Skill**

  **类型**       **位置与职责**                                                                           **版本/权限规则**
  -------------- ---------------------------------------------------------------------------------------- -----------------------------------------------------------------
  Hermes Tool    调用 Core API 的可执行薄封装，例如选题、研究任务、状态查询、请求写稿和审批经验修订       只暴露白名单 JSON 参数；不得任意 SQL、Shell、文件写入或通用执行
  Hermes Skill   描述意图识别、调用顺序、何时追问、怎样呈现结果                                           正式目录只读；Agent 自动生成/修改内容进入草稿或待审批区
  业务 Skill     真正受 Schema、领域配置、Prompt、模型、预算、缓存、回归和 skill_run 审计控制的语义能力   由核心仓库版本控制；不与 Hermes Skill 复制可变业务逻辑

## **2.5 MVP 白名单工具动作**

  **工具组**   **最小动作**                                                                            **高风险确认**
  ------------ --------------------------------------------------------------------------------------- -----------------------------------------------
  选题         list_daily_topics、select_topics、defer_topic、close_topic、request_new_angle           永久关闭、批量关闭需要确认
  来源与研究   create_manual_source、create_research_task、get_research_status                         扩大研究预算或访问高风险来源时确认
  生产         create_content_plan、generate_script、review_script、request_revision、approve_script   批准最终稿需要确认；当前不自动发布抖音
  运行查询     get_task_status、get_account_status、get_baseline_status、get_usage_cost                只读，无需确认
  经验         list_revision_proposals、approve_revision、reject_revision、lock_experience             发布新经验语义版本、解除 manual_lock 必须确认

工具接口名称在 TECH 中可以调整，但动作边界不得扩大；Hermes
不得通过"通用执行工具"绕过白名单。

## **2.6 Hermes Cron 与核心 Scheduler**

  **任务类型**                                        **执行者**                                                                  **原因**
  --------------------------------------------------- --------------------------------------------------------------------------- -----------------------------------------------------------
  D0---D7、P+6h---P+7d、重试、基线、评论、ASR、备份   核心 Job/Scheduler/Worker                                                   需要数据库唯一约束、精确状态、可重试并且不依赖 Agent 在线
  每日选题报告触发与飞书投递                          当前默认：Hermes Cron 或核心事件触发 Hermes；内容仍由 Core API 生成         固定报告可走无 LLM 脚本路径
  用户提醒、待审批提醒、长任务完成通知                Hermes                                                                      属于会话和用户触达
  经验周期维护                                        核心 Scheduler 计算证据/状态；语义变化创建 proposal Job，再调用业务 Skill   避免 Agent 按聊天状态决定经验维护

## **2.7 故障隔离、权限与安全**

  ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **故障隔离：**Hermes 停止时，后台采集、快照、基线、到期任务、Worker 和数据库继续运行；受影响的只有飞书对话、主动消息和人工审批。恢复后由 Hermes 查询未投递结果和待审批项。
  ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-   当前是单用户系统：飞书只允许明确 allowlist
    用户；默认不开放普通群聊成员执行业务工具。

-   Hermes
    只启用本项目需要的工具；terminal、任意文件写入、任意数据库、浏览器自动操作等默认关闭。

-   正式业务 Skill 目录只读；启用 skills.write_approval，任何 Agent
    自建/修改 Skill 必须进入待审批区。

-   生产配置、Prompt、领域配置和业务 Skill 由核心仓库版本控制；Hermes
    程序性记忆不是正式版本。

-   所有工具调用写入
    agent_action_log：用户、会话、工具、参数摘要、结果、关联对象、时间、模型用量和确认记录。

-   Core API 必须再次执行权限和状态校验，不能只相信 Hermes
    已经判断正确。

-   高风险动作遵循"提案 → 飞书确认 → Core API 执行"，不得由 Agent
    静默完成。

## **2.8 Hermes 上下文与成本治理**

  **路径**           **成本控制**
  ------------------ ---------------------------------------------------------------------------------------
  固定按钮/命令      使用确定性工具或脚本路径，不启动开放式推理
  普通自然语言指令   只给 Hermes 当前任务摘要、对象引用和少量状态；不把 V0.5、完整经验库或全部视频塞入会话
  工具选择           暴露有限工具集和按需工具说明；一次会话不暴露不相关工具
  业务语义任务       由 Core API 触发业务 Skill，经 ModelGateway 执行预算、缓存、分层和审计
  Hermes 自身模型    单独记录会话 Token/费用；MVP 不强求先经 ModelGateway 代理
  长会话             以 Core 正式状态和结构化任务摘要恢复；聊天历史定期压缩，不依赖无限上下文
  日报/状态查询      模板化、SQL/程序化生成；Hermes 只投递或改写极短说明

## **2.9 R02 对 V0.5 的明确覆盖**

  **V0.5 旧口径**                            **当前有效口径**
  ------------------------------------------ -------------------------------------------------------------------------------------
  FeishuDirectAdapter 为 MVP 主路径          飞书通过 Hermes Messaging Gateway/Runtime，再调用白名单工具和 Core API
  Hermes/OpenClaw 后置比较                   Hermes 是已确认的 MVP Agent Runtime；不再比较或自建 Runtime
  飞书直连阶段验收                           Hermes Gateway 接入、白名单工具可执行、自然语言多轮任务可继续、固定动作走确定性路径
  不部署平台自学习 Skill                     部署 Hermes，但正式业务 Skill 只读，开启写入审批
  "移除自动经验重写"可能被理解为经验不进化   禁止静默覆盖；保留证据/状态确定性演进和版本化语义修订提案

# **3 R03 上位规则：用户流程、端到端追溯、验证与上下文治理**

## **3.1 用户使用流程与暂停条件**

  ---------------------------
  用户选择正式选题\
  → 系统自动完成资料研究\
  → 自动生成内容计划\
  → 自动生成初稿\
  → 自动审核并在限制内修订\
  → 用户确认最终稿\
  → 用户人工发布\
  → 系统执行 P+ 跟踪与复盘

  ---------------------------

中间不要求用户逐节点确认。仅在以下异常情况下暂停：

-   关键事实或 blocking claim 无法核验。

-   可靠证据发生重大冲突。

-   研究证明原选题核心结论必须改变或缩小。

-   需要明显扩大研究预算。

-   自动修订循环达到上限仍不能通过。

-   系统建议取消、换角度或补充人工资料。

最终稿必须由用户确认；系统当前不自动发布到抖音。

## **3.2 端到端可追溯**

  -------------------------------------
  source\
  → topic_item\
  → production_task\
  → research_plan / claim / evidence\
  → content_plan_version\
  → script_version\
  → script_review\
  → approved_script_version\
  → publication_script_version\
  → P+ snapshot / baseline\
  → experiment_result\
  → tactic_evidence

  -------------------------------------

-   每个关键产物必须具有稳定
    ID、版本、上游对象引用、输入快照或哈希、skill_run_id、Prompt/模型/配置版本和审计记录。

-   内容计划记录经验 used / considered / rejected 及原因。

-   稿件记录实际使用的 claim、evidence、tactic 和对应句段。

-   实际发布稿与用户确认稿分开保存；任何正文变化都生成新版本。

-   摘要和压缩必须保留来源对象、版本、哈希和生成记录。

-   任一最终稿无法反查资料、经验、计划、审核和实际发布版本时，相关 Codex
    Goal 不得完成。

## **3.3 独立验证与实施上位规则**

-   每个模块先完成契约、独立运行、fixture、边界故障、资源测试和多视角审查，再进入集成。

-   所有原子 Skill 必须可以通过统一 runner 单独调用；生产环境仍必须经过
    Core API 与状态机。

-   D0---D7、P+、TTL、stale、租约和重试等长周期逻辑使用 FakeClock
    验证，不等待真实时间。

-   测试和生产使用同一份业务代码，只替换数据、时间、模型和外部 Adapter。

-   真实数据只用于参数校准、代表性确认和外部兼容性，不是核心功能第一次验证前提。

-   每个模块至少从使用者、维护者、领域专家、数据可靠性、多领域扩展、成本与资源六个视角检查；设计和验收至少循环两轮。

## **3.4 上下文、Prompt 与 Skill 长度治理**

-   模型支持的最大上下文不是日常输入目标；长度必须由固定评测和执行质量决定。

-   Codex Skill 采用渐进式披露；SKILL.md 尽量小于 5,000 Token、500
    行，详细材料按需放入 references、scripts、assets 和 fixtures。

-   AGENTS.md、Goal 和业务 Prompt
    不设无依据的统一长度上限；以目标是否单一、能否独立验收、是否重复规则和上下文稀释来判断。

-   每个业务 Skill 从最小输入开始，经消融确定
    minimum_passing、normal_budget 和 maximum_verified_input。

-   单次输入达到 20K Token 触发长上下文专项评测，但 20K 不是通用硬上限。

-   新增 Prompt
    段落或上下文类型，不能在回归集证明收益则不进入日常上下文。

-   每次 skill_run 保存 context manifest、各部分
    Token、舍弃资料及原因、Prompt/模型/配置版本和溢出处理。

-   超出已验证范围时只能去重、结构化摘要、拆分调用或明确阻塞，不能静默截断必要证据。

# **4 CR-004A：选题到 production_task**

-   用户确认正式选题即授权创建 production_task，不再重复确认。

-   同一 account_id + topic_item_id
    同时只能存在一个有效任务；使用部分唯一约束，不建立 topic lock 或
    reservation 表。

-   blocked 仍占用该选题的有效任务关系，必须恢复或取消；cancelled
    后允许创建关联重试任务。

-   已发布内容默认禁止直接重复生产；明确 remake 时允许，并关联
    previous_task_id。

-   核心角度变化必须创建派生 topic_item，保留 parent_topic_id 和
    derivation_type，不能覆盖原选题。

-   创建 production_task 时保存不可变 topic_snapshot 与
    hash；以后选题修改不改变旧任务依据。

-   max_daily
    只限制业务日内新建任务数，不建立跨日排产占位；任务取消不返还当日额度。

  **任务类型**         **处理**
  -------------------- ----------------------------------
  initial              首次从正式选题创建
  retry_after_cancel   取消后重新启动，引用原任务
  remake               已发布内容明确重制，引用原任务
  angle_replacement    原任务取消，创建派生选题和新任务

# **5 CR-004B：研究、计划、稿件与审核版本链**

-   research_plan、content_plan、script_version、script_review
    均不可覆盖；修改生成新版本或新执行结果。

-   production_task 保存
    current_research_plan_id、current_content_plan_id、current_script_version_id、candidate_final_script_version_id、approved_script_version_id。

-   每个下游产物必须引用具体上游版本，不能只引用 production_task_id。

-   所有 blocking claim 达到 checked
    且核心主张满足领域证据要求后，才自动进入内容计划。

-   内容计划必须包含核心承诺、核心结论、内容路线、段落、主张---证据映射、经验决策、不确定性和风险控制。

-   任何稿件正文变化都创建
    script_version；稿件不能自行增加计划之外的新事实，需要新事实时返回补研究。

-   script_review 只做审核判断，不在内部隐藏生成修订稿；Core 编排
    script_generate 的 revision 模式。

-   审核 pass 只设置候选最终稿；用户确认后才进入 ready_to_publish。

-   局部修订最多 2 轮、结构重写最多 1 轮作为当前配置化安全默认，超限进入
    blocked。

-   新增普通背景资料不自动重跑；只有当前计划实际引用的核心 claim 或
    evidence 变化才触发下游处理。

-   产生新版本只是正常历史；invalidated
    专指上游依据后来被确认有问题，不能混用。

# **6 CR-004C：实际发布、P+ 与经验归因**

-   每条发布内容都跟踪 P+；只有发布前冻结单一主要经验假设的内容才是
    formal experiment，其他为 observational。

-   用户确认稿与实际发布稿分开保存；发布时始终创建 publication_capture
    类型的 script_version。

-   实际发布内容可声明 same_as_approved、modified_text_provided 或
    unknown_pending_check。

-   只有 formal experiment 的 primary tactic 且
    actual_use_status=used，才可能产生正式经验支持或反例。

-   辅助经验只记录实际使用，不因一条内容成功或失败自动升级。

-   P+7d 是当前统一最终评价点；早期点只用于过程观察，不改变经验状态。

-   主要指标在发布前冻结；程序先计算 metric
    signal，模型只能在重大干扰时把结论降为
    inconclusive，不能把确定性失败改成成功。

-   P 基线按 account_id 使用当前视频发布前最近 20
    条合格完整序列；当前视频不得进入自身基线，未来视频不能反向正式化旧结果。

-   缺失、迟到、基线为 0、异常采集只能得到 provisional、ineligible 或
    inconclusive，不能伪装成失败。

# **7 CR-006A：研究来源资格与独立来源**

-   搜索结果标题、摘要、AI 摘要和未打开 URL
    只能作为候选线索，不能成为正式证据。

-   正式证据链为 research_source → source_snapshot → research_evidence →
    claim_evidence_link → research_claim。

-   来源类型与可信度分离；是否 usable 必须针对具体 claim 判断，不建立
    0---100 黑盒可信分。

-   同一通稿、采访、公告、报告、翻译版或多站转载只计一个底层独立来源。

-   保存 origin_group，并针对具体 claim 计算 independence_group。

-   权威记录型事实可由一个直接 verified official record
    核验；普通事实通常需要一个直接一手来源或两个独立 verified secondary
    source。

-   强因果主张必须有适配范围的原始研究或官方分析。

-   评论只能证明评论中出现某类表达、问题或争议，不能证明外部事实、观众整体比例或强因果。

-   网页更新时创建新
    snapshot，旧版本不覆盖；网页后来失效不自动撤销已保存快照的历史证据。

# **8 CR-006B：正式资料研究执行与音乐作品评论边界**

  -----------------------------------------------------------------
  research_plan\
  → SearchProvider\
  → 候选筛选、去重和平台能力排除\
  → PageFetcher / 专用来源 Adapter\
  → source_snapshot\
  → 确定性正文抽取与片段定位\
  → research_evidence_extract\
  → Core 重算 claim\
  → ready / needs_scope_change / insufficient / technical_blocked

  -----------------------------------------------------------------

-   正式资料研究与对标视频学习链严格分开；正式研究结构上没有视频平台搜索、读取、下载、ASR、分析或视频评论链路。

-   该限制不影响抖音对标账号/标签视频发现、指标快照、ASR、标题与口播分析、早期/成熟评论、普通/低表现对照、深度分析、经验提取和选题证据。

-   普通网页搜索保持开放，不使用排他平台白名单；平台配置只表达明确排除能力、偏好路由和专用能力。

-   音乐娱乐领域允许豆瓣、网易云音乐等专用来源，但不限制只能使用这些平台。

-   豆瓣可以覆盖与选题相关的音乐、影视、综艺、娱乐、书籍和其他文化条目及评论。

-   研究按 claim 组织搜索请求；每轮由 Core
    根据证据缺口、独立来源、冲突、技术失败和预算决定继续或停止。

-   搜索、抓取和证据提取分层；不把开放式自主研究 Agent
    当作正式事实来源。

## **8.1 音乐作品评论采集的严格触发条件**

-   外部文化平台评论只在音乐娱乐领域启用。

-   当前内容明确准备提及某个具体作品，且 mentioned_work_target 已解析。

-   研究计划明确要求该评论材料及用途。

-   不能因为研究某位人物就采集其全部作品评论。

-   网易云使用已完成技术测试的独立采集脚本，后续薄封装为
    NetEaseMusicCollectorAdapter，不属于 MediaCrawler。

-   评论只可作为 audience_signal、interpretation_clue、context 或
    topic_clue，不能证明作者意图、发行事实、创作背景、整体观众比例或强因果。

# **9 CR-002 与其他章节的权限衔接**

  ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **正式增量边界：**经验等级、证据资格、freshness、active/watch/paused/deprecated、manual_lock、修订提案状态与发布事务以 CR-002 为准。本 R05 只补充与生产、Skill、实验和纠错传播的接口，不复制 CR-002 正文。
  ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-   确定性证据和状态可以自动重算；经验语义变化必须通过版本化修订提案，不能静默覆盖。

-   Portable Skill 不暴露 tactic ID、版本 ID、L1---L5
    编码、内部状态码或数据库结构。

-   Materializer 负责把 local_ref
    和语义候选恢复为宿主对象、版本、哈希和事务。

-   manual_lock 只冻结 recommendation status
    的自动迁移，不冻结证据写入、level/freshness 重算或修订提案触发。

-   deprecated 经验不能因纠错自动复活，只能走 restore
    proposal，并创建新语义版本。

  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **R04 无损继承说明：**以下 CR-003A---E 正文直接继承 R04 的详细表格、枚举、职责边界、验证和跨章节审计；只移除了已经失效的"下一节点 CR-003F"状态与旧启动说明。未把任何 Skill 契约压缩成摘要。
  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

# **10 CR-003 全局统一边界：五层责任与禁止重叠**

## **10.1 五层责任**

> 获取执行层\
> 采集 / 搜索 / 抓取 / ASR\
> ↓\
> Core 输入准备\
> 召回 / 筛选 / 去重 / 冻结本次材料\
> ↓\
> Host Binding\
> 确定性格式转换 / 局部引用映射\
> ↓\
> Portable Skill\
> 完成一个语义工作\
> ↓\
> Core / Materializer\
> 版本化写入 / 状态迁移 / 下游编排

  **层级**              **唯一职责**                                          **明确禁止**
  --------------------- ----------------------------------------------------- --------------------------------------------------
  获取执行层            视频与评论采集、ASR、网页搜索抓取、文化平台专用采集   语义结论、状态推进
  Core 输入准备         从已有数据召回、选择、去重、预算控制并冻结输入        代替 Skill 作语义判断
  Host Binding          把已选材料转换成 Skill 自有 Schema，并映射局部引用    选择材料、扩大上下文、调用模型
  Portable Skill        对固定输入完成一个自解释语义任务                      搜索、采集、读库、调用其他 Skill、写库、推进状态
  Core / Materializer   校验、保存版本、恢复追溯、状态迁移和显式编排          静默改写 Skill 业务结论

## **10.2 禁止重新定义上游能力**

-   不得重新定义采集、搜索、抓取、ASR、评论获取、平台选择、Adapter
    路由和数据库召回能力。

-   Skill
    使用某类数据，只表示它可以消费已准备好的材料，不代表它拥有获取能力。

-   若确实要改变上游能力，必须形成对既有 CR 的明确冲突修订，不能在某个
    Skill 字段中静默增加。

-   同一份已保存数据可以被不同 Skill
    复用；禁止的是重复采集和职责重复，不是数据复用。

-   Core 记录 Input Assembly Record；Binding Manifest
    只记录转换与追溯映射。两者不能合并为一个隐含决策层。

## **10.3 职责快速检查**

  **检查问题**                  **正确答案**
  ----------------------------- --------------------------
  数据是谁取得的？              已冻结的工作流或 Adapter
  数据是谁选择并冻结的？        Core 输入准备
  数据是谁转换的？              Host Binding
  语义判断是谁完成的？          当前 Portable Skill
  谁能写业务事实？              Core / Materializer
  谁能推进状态？                Core 状态机
  是否重复定义上游能力？        必须为否
  是否与相邻 Skill 输出重叠？   必须为否

# **11 CR-003A：可移植原子 Skill、Binding、Runner、Model Port 与可抹除验证**

## **11.1 Portable Skill 正式定义**

  --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **正式定义：**业务 Skill 不依赖当前系统数据库表、ORM、内部编码、状态机或专用数据结构；只凭自身公开输入契约即可完成单一工作。换到其他系统时，仅替换 Host Binding，不修改 Skill 本体。
  --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-   输入和输出必须自解释；枚举必须由 Skill
    契约定义，不能引用宿主隐含字典。

-   Skill
    可以依赖业务语义，例如稿件生成需要内容计划；不能依赖宿主产物结构，例如
    content_plan_version 数据库行。

-   Skill 不读取数据库、不调用 Core API
    补资料、不推进状态、不调用其他业务 Skill。

-   Skill 可以通过统一 Runner 调用，但不能只有在特定 Runner
    或宿主系统中才可运行。

-   逻辑批处理只能调度多个隔离
    Run；不得把多个独立对象放入一个共享模型上下文。

## **11.2 原子边界**

原子不是"一次模型调用"，而是"一个主要业务判断或一个主要产物"。同一工作内部的结构化输出、有限格式修复和必要模型调用可以保留；不能把搜索、分析、计划、写稿和审核隐藏在一个
Skill 中，也不能把一个完整工作机械拆成大量无独立业务价值的微 Skill。

## **11.3 Input/Output Binding 与 Binding Manifest**

  **对象**           **职责**                                                            **不负责**
  ------------------ ------------------------------------------------------------------- --------------------------------------------
  Input Binding      校验 Core 已选对象版本与哈希；转换为 Skill Schema；建立 local_ref   材料召回、相关性判断、补充上下文、语义判断
  Output Binding     校验输出和 local_ref；转换成宿主候选结构；恢复追溯关系              正式写入、覆盖版本、推进状态、改写结论
  Binding Manifest   记录 local_ref 与宿主对象/版本/哈希、转换规则、输入输出哈希         作为模型上下文、参与业务判断

Binding 必须是确定性转换。给定相同宿主对象版本、内容哈希、Skill
契约版本和 Binding 版本，应产生内容等价的 Skill 输入。

## **11.4 Input Assembly Record**

材料选择不属于 Binding。Core
负责从已有数据中召回、筛选、去重、控制上下文预算并冻结输入，同时记录包含、舍弃及原因。该逻辑审计对象暂称
Input Assembly Record；当前只冻结逻辑责任，不提前建立物理表。

## **11.5 Runner 与 Model Port**

  **组件**     **允许**                                                                    **禁止**
  ------------ --------------------------------------------------------------------------- --------------------------------------------------------------
  Runner       加载契约、校验输入、注入端口、超时取消、技术重试、校验输出、记录用量        补业务上下文、读库、串联其他 Skill、状态推进、隐藏完整工作流
  Model Port   隔离供应商协议、返回响应/模型/Token/耗时/错误，支持真实/Fake/Replay/Fault   搜索资料、改写任务、判断业务状态、补充上下文

执行状态与业务结果必须分离。模型或网络失败不能伪装成"证据不足"或"内容无法分类"；业务证据不足也不能写成
Runner 失败。

## **11.6 Skill 可见上下文边界**

-   每个 Skill 的输入 Schema 就是其全部可见上下文；未声明字段必须拒绝。

-   只有对当前工作有直接作用、契约明确允许、且经该 Skill
    消融测试证明有收益的内容才可进入。

-   追溯元数据、宿主
    ID、任务状态和数据库版本关系不得污染模型业务上下文。

-   超出最大已验证范围时只能确定性去重、带来源映射的结构化摘录、版本化派生摘要、拆分调用或明确阻塞；不得静默截断必要证据。

## **11.7 可抹除验证与 Clean-room**

  ----------------------------------------------------------------------------------------------------------------------------------
  **硬门禁：**删除 fixtures、Replay、测试数据库、验证工具、验证报告和验证专用依赖后，生产系统和 Skill 必须仍能启动并完成正式调用。
  ----------------------------------------------------------------------------------------------------------------------------------

-   验证资产与生产运行资产物理和逻辑隔离；生产代码不得导入 validation。

-   验证数据不得通过 is_test 长期混入正式业务表。

-   Shadow
    可只读真实快照，但输出必须写入独立可销毁存储，不创建正式版本、不推进状态。

-   共同硬门禁包括 standalone、fixture、boundary、fault、ablation 和
    clean-room；Replay、live model、binding、shadow 按 Skill 阶段启用。

-   验证报告是实施验收证据，不是生产业务事实，不能成为系统启动或业务判断依赖。

# **12 CR-003B：来源与选题类 Skill**

  **Skill**                **唯一职责**                                             **单次对象**         **关键边界**
  ------------------------ -------------------------------------------------------- -------------------- -----------------------------------------------
  content_classify         判断一份内容相对一个给定边界的语义范围、类型和排除命中   一份内容＋一个边界   不输出业务资格，不自由选择全系统领域
  content_relation_judge   判断两份已提供内容或选题的语义关系                       一对内容             不检索全库、不合并或关闭对象
  source_to_topic          从一份已准备的来源材料形成零个或多个具体选题提案         一份来源             不搜索、不验证事实、不去重、不创建 topic_item

## **12.1 content_classify**

输出只允许使用纯语义结果：in_scope、exclusion_matched、out_of_scope、uncertain、insufficient_content。是否进入候选、等待
ASR、关闭来源或触发下游由 Core 决定。

-   允许输入：标题、描述、标签、清理后的正文/口播、当前分类边界、当前边界下的内容类型和排除定义。

-   禁止输入：流量表现、账号历史、经验库、评论集合、研究证据、内容计划、其他领域无关规则。

-   同一内容可由 Core 对多个边界分别形成独立调用；Skill
    不在一个上下文中自由比较整个领域库。

## **12.2 content_relation_judge**

确定性规范化、哈希、全文检索和 pg_trgm top-K 召回全部在 Skill 外。Skill
仅判断一对自解释内容，输出
duplicate、same_topic_distinct、derived、related、unrelated 或
uncertain；derived 才需要方向。

-   duplicate 是语义关系，不等于 Core 的 exact duplicate。

-   同一人物/作品但核心问题不同，应优先判
    same_topic_distinct，而不是仅凭实体重合判重复。

-   输出不能包含"关闭右侧""合并对象""保留高流量对象"等系统动作。

## **12.3 source_to_topic**

提案至少包含具体核心问题、受众关系、可交付的信息增量、来源依据和待核验问题。只有人物名、歌曲名、宽泛主题、热点标题改写或"聊聊某人"不能单独成为有效提案。

-   具体问题规范化模式不加载经验。

-   宽泛人物、歌曲或主题尚未形成具体问题时，Core 可预筛少量成熟 topic
    经验，经 Binding 转为自解释 angle_guidelines；Skill 可
    used/considered/rejected。

-   评论只能作为 audience_signal、topic_clue
    或问题来源，不能证明外部事实。

-   输出提案后，由 Core 做规范化、确定性召回，必要时调用
    content_relation_judge。

# **13 CR-003C：深度分析与经验提取类 Skill**

  **Skill**             **唯一职责**                                                                 **经验输入**                           **输出权限**
  --------------------- ---------------------------------------------------------------------------- -------------------------------------- ---------------------------------------------------
  sample_deep_analyze   分析一个 Core 已冻结的固定样本包，形成完整 content analysis 与 L0 机制发现   不加载动态经验卡；使用稳定分析方法     不创建正式 tactic，不判等级/状态
  tactic_extract        把已选机制发现规范化、拆分或归并为原子候选                                   关系判断模式可接收少量预召回经验摘要   只给 possible duplicate/boundary/split/merge 信号

## **13.1 sample_deep_analyze 的固定样本包边界**

sample_deep_analyze 不搜索相似视频、不采评论、不调用 MediaCrawler 或
ASR。Core
可以从已经采集入库的视频中召回并冻结相似样本、普通对照、低表现反例和已选评论。这属于既有对标视频学习闭环，不属于正式资料研究。

-   匹配不到对照不阻塞分析；不同 D/P
    口径只能比较内容结构，不能直接比较绝对数据或增长倍数。

-   固定分析包可包含目标文案、表现摘要、已选评论、普通/低表现对照、背景因素和数据缺口说明。

## **13.2 稳定分析框架**

Skill
必须拥有稳定分析框架：标题、开头、结构、信息释放、情绪、价值、信任、互动、观察与推断分离、因果限制、适用和失效条件。这些通过
Skill 契约、Prompt 版本、Schema 和回归集管理。系统积累的动态 tactic
不进入首次深度分析，避免已有经验用自己的结论反向证明自身。

## **13.3 reusable_mechanism_findings 与 tactic_extract**

sample_deep_analyze 输出统一称 reusable_mechanism_findings，不再称
tactic_candidates。它们属于 content_analysis 内的
L0。若某条发现已经完整满足 L1 Schema，Core 可直接检查证据资格并创建
L1；只有结构不完整、纠缠、跨样本归并或与少量既有经验关系不清时才调用
tactic_extract。

## **13.4 一作用域、一主机制与 usage_action**

  -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **一个主机制的准确含义：**一条经验原子只能在一个 decision_scope 内解决一个主要决策问题、表达一个主要机制；不要求该机制贯穿整篇内容。反常识开头通常是 opening 经验，不应被扩大成 overall_structure。
  -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

usage_action 的逻辑内容必须包括
action、placement、span、handoff、stop_or_avoid，用于明确使用位置、持续范围、承接方式和停止边界。当前只冻结语义要求，物理字段留到
TECH。

## **13.5 tactic_extract 与经验语义修订边界**

tactic_extract 可以输出
possible_duplicate、boundary_signal、possible_split、possible_merge，但不能生成正式经验新版本、证据重新分配方案或
deprecated
决定。真正改变机制、动作、适用条件、失效边界、split、merge、deprecate、restore
的，只能是 CR-002 的 experience_revision_propose 版本化提案。旧名称
tactic_maintenance_suggest 正式失效。

# **14 CR-003D：研究类 Skill**

  **Skill**                   **唯一职责**                                                             **不负责**
  --------------------------- ------------------------------------------------------------------------ -------------------------------------------------------
  topic_expansion_validate    判断一个已有来源的问题及当前证据包是否有实质增量和初步依据               搜索、生成新角度、正式生产研究、创建/关闭选题
  production_research_plan    把正式选题转换为 claim-centric、可停止、可审计的研究计划                 搜索、抓取、证据提取、选择 Provider/Adapter、扩大预算
  research_evidence_extract   从一份已打开并保存的来源快照中提取与给定 claims 相关的证据、限制和关系   搜索来源、判最终 claim 状态、判独立来源、写稿

## **14.1 topic_expansion_validate**

> 候选问题与证据缺口\
> → Core 生成研究请求\
> → SearchProvider / Fetcher\
> → source snapshot\
> → research_evidence_extract\
> → topic_expansion_validate\
> → Core 决定候选状态

topic_expansion_validate 的 supported
只表示方向有来源、有增量且有继续生产研究的基础，不表示所有 blocking
claims 已 checked。技术失败或未找到资料不能自动产生 rejected。

## **14.2 production_research_plan**

-   输出研究问题、原子
    claims、blocking、来源性质、直接性、独立支持、时间范围、停止条件、预算内优先级和
    scope change triggers。

-   每个 claim
    必须可以独立支持或反驳；出生时间、发行成绩、创作背景和行业影响等不同主张不能打包。

-   blocking
    仅用于无法核验会破坏核心承诺、核心结论或整条内容路线的主张，不能把所有事实都标
    blocking。

-   Skill 只声明是否需要具体文化作品的评论材料和用途，不选择平台或
    Adapter。平台解析由 Core 补充到宿主计划。

-   内容经验不进入研究计划，避免为了既定创作技巧筛选证据。

## **14.3 research_evidence_extract**

-   一次处理一份已保存快照和一组相关 claims；多个来源应并发多个独立
    Run。

-   搜索标题、搜索摘要、AI 摘要、未打开 URL
    和无正文候选不能作为正式证据输入。

-   exact_excerpt 必须能在输入片段中逐字定位；normalized_meaning
    与原文摘录分开保存。

-   输出
    supports、contradicts、context、directness、适用范围、限定和来源线索；最终
    checked/disputed/ready 由 Core 重算。

-   来源类型不等于对所有 claim 都可信；是否 usable 必须针对具体 claim
    判断。

## **14.4 正式研究结构上无视频平台能力**

  -----------------------------------------------------------------------------------------------------------------------------------------------------
  **最终边界：**正式资料研究的能力集合中没有视频平台搜索、读取、下载、ASR、分析、视频评论抓取和 MediaCrawler 接口。不是"默认禁用"，而是结构上不存在。
  -----------------------------------------------------------------------------------------------------------------------------------------------------

已入库的父视频或视频评论可以解释选题和问题从哪里来，作为
origin_context、topic_clue 或
audience_question；不能自动替代正式事实证据，也不能触发新的外部视频操作。

# **15 CR-003E：内容计划、稿件生成与审核类 Skill**

  **Skill**         **唯一职责**                                                       **主要输出**         **关键禁止**
  ----------------- ------------------------------------------------------------------ -------------------- ------------------------------------
  content_plan      把一个已完成必要研究的选题组织成可执行、可追溯、可审核的内容计划   一份计划候选         不补研究、不写完整稿、不改经验
  script_generate   按指定计划生成一份初稿或修订稿候选                                 一份稿件候选         不加计划外事实、不自审、不定最终稿
  script_review     对一份指定稿件作计划、事实、经验、风格和风险审核                   一份结构化审核结论   不改稿、不搜索、不推进候选最终状态

## **15.1 显式编排链**

> Core 准备计划输入包\
> → content_plan\
> → Core 保存 content_plan_version\
> → script_generate\
> → Core 保存 script_version\
> → 确定性预检\
> → script_review\
> → Core 决定 pass / revision / plan revision / research

三个 Skill 不直接调用彼此，Runner 不得隐藏串联。所有正文变化都生成新的
script_version；审核只设置候选最终稿条件，用户确认后才进入
ready_to_publish。

## **15.2 content_plan**

-   只消费当前选题、verified claims、必要 evidence
    摘要与限制、少量背景/受众材料和 Core
    预筛的经验候选，不重新阅读全量来源。

-   输出核心承诺、核心问题、核心结论、内容路线、段落功能、claim/evidence
    映射、经验 used/considered/rejected、风险和不确定性处理。

-   计划不是稿件半成品，不应输出大段完整口播正文。

-   一份计划可使用多条不同 decision_scope
    的经验，但不能把开头经验扩张为全文机制，也不能为用经验而改变已核验事实。

-   主要经验假设在计划中只作为候选；formal experiment
    必须在发布前结合实际稿件、实际发布稿、主要指标和单一假设再次冻结。

## **15.3 经验状态的可移植语义转换**

Portable Skill 不接收 tactic ID、L1---L5 编码、内部
recommendation_status 或数据库结构；但不能因此丢失 active/watch
的业务含义。Core/Binding 必须把它转换成自解释语义，例如
usage_recommendation=recommended 或 use_with_caution，并附
risk_reason。content_plan 才能在不依赖宿主编码的前提下正确考虑 watch
风险。

content_plan 对每条候选经验记录 used、considered 或 rejected
及原因；script_generate 只接收计划相关的精简经验决策；script_review
读取本稿对应的全部 used/considered/rejected
决策，以检查计划声称使用但实际未用、拒绝经验被误用或经验作用域越界。

## **15.4 script_generate**

-   initial 和 revision 是同一 Skill
    的两种模式，因为主要输出始终是一份新稿件；revision 的范围和问题由
    Core 明确提供。

-   只能使用计划安排和 allowed claims
    中提供的事实；模型记忆、常识猜测、传闻、计划外数字和新因果都不得进入正文。

-   必须按句段记录 claim/evidence/tactic 实际落点；纯过渡句不要求
    claim，但承载外部事实、数字、人物经历和核心结论的片段必须可追溯。

-   若自然表达必须依赖新事实，返回
    blocked_by_missing_basis，而不是自行补充。

-   revision 不能修改 protected content，不能自行决定
    local/structural，也不能顺带审核自己。

## **15.5 确定性预检与 script_review**

V0.5 的确定性预检继续保留，并位于 script_review
之前：Schema、必需引用、长度硬限制、明显禁用表达、未解析 local_ref
等可以由程序先拦截，以降低 LLM 成本。预检不承担语义审核，也不改变 Skill
原子边界。

-   script_review 一次只审核一份稿件，对照一份具体计划、允许
    claims/evidence、计划经验决策和审核规则。

-   审核维度包括计划一致性、事实与证据、经验落实、风格表达、风险和不确定性。

-   每个 issue 必须原子化，严重度为
    blocking、major、minor、advisory；不用黑盒总分代替具体门禁。

-   审核结果为
    pass、revision_required、plan_revision_required、research_required
    或 inconsistent_input。

-   审核器只指出问题、影响片段、依据、必须改什么和保护内容；不得隐藏生成完整替换稿。

-   script_generate 的 tactic_usage 是生成器自述；script_review 的
    observed_tactic_usage 是独立审核结果，Core 保存差异。

## **15.6 修订循环、批准稿与实际发布稿**

局部修订最多 2 轮、结构重写最多 1 轮继续作为当前配置化安全默认；超限进入
blocked。审核 pass 只形成候选最终稿，用户确认后才成为 approved
script。实际发布时始终创建 publication_capture 类型的新
script_version，不能默认等同于用户确认稿。

# **16 视频、评论、正式研究与内部数据复用的最终边界**

## **16.1 工作流能力边界**

  **工作流**     **获取能力**                                              **主要语义 Skill**                                                       **禁止渗透**
  -------------- --------------------------------------------------------- ------------------------------------------------------------------------ ----------------------------------
  对标视频学习   MediaCrawler、视频/指标快照、音频/ASR、早期/成熟评论      content_classify、source_to_topic、sample_deep_analyze、tactic_extract   不得被"正式研究无视频"规则削弱
  来源到选题     消费已有视频、评论、网页、人工输入等来源                  source_to_topic、content_relation_judge                                  不得重复采集或完成正式核验
  拓展验证       受控网页研究执行；已有视频/评论只作问题来源               topic_expansion_validate、research_evidence_extract                      不得触发新视频搜索或视频评论采集
  正式资料研究   SearchProvider、Fetcher、文件、受限文化作品评论 Adapter   production_research_plan、research_evidence_extract                      结构上无视频平台能力

## **16.2 评论类型边界**

  **评论类型**   **获取责任**                                                 **允许用途**                                                **不能证明**
  -------------- ------------------------------------------------------------ ----------------------------------------------------------- ----------------------------------------------------
  对标视频评论   视频采集工作流 / MediaCrawler                                追问、争议、共鸣、拓展选题、深度分析、互动机制              人物经历、作品背景、外部事实、总体观众比例
  自营视频评论   P+ 跟踪与复盘工作流                                          实际反应、干扰因素、经验反例、后续选题                      外部事实核验
  文化作品评论   音乐娱乐正式研究的受限专用分支；网易云独立 Adapter、豆瓣等   audience_signal、interpretation_clue、context、topic_clue   作者意图、发行事实、创作背景、观众整体比例、强因果

普通网页评论区当前不建立专项采集能力。文化作品评论只有在音乐娱乐领域、当前内容明确准备提及具体作品、mentioned_work_target
已解析且研究计划明确需要时才可采集；不能因研究某位人物而抓取其全部作品评论。

# **17 上下文、Prompt、验证与成本治理**

## **17.1 上下文与 Prompt**

-   模型最大上下文不是日常输入目标；不凭空设置全系统统一 Token 范围。

-   每个 Skill 从最小输入开始，通过逐字段/逐材料类型消融确定
    minimum_passing、normal_budget 和 maximum_verified_input。

-   达到 20K Token 时触发长上下文专项评测，但 20K 不是通用硬上限。

-   新增 Prompt
    段落或上下文类型，若不能在固定回归集证明收益，不进入日常上下文。

-   每次 Skill Run 保存 context
    manifest、各部分用量、被舍弃资料及原因、Prompt/模型/配置版本和溢出处理。

## **17.2 多领域回归与待校准参数**

每个 Skill
的回归集必须覆盖两个初始领域和至少一个合成第三领域，以证明没有写死当前领域。语义质量阈值、误报漏报、正常输入规模和模型档位均通过固定回归集确定，不把待校准参数误写成业务设计缺失。

## **17.3 验证报告**

验证报告至少记录
Skill/Schema/Prompt/模型/配置/数据集/验证工具版本、用例、输入哈希、预期、实际、通过状态、失败原因、Schema/引用结果、耗时、上下文、Token/成本和副作用。物理存储位置、保留期和格式留到
TECH；删除报告不得影响生产。

# **18 端到端追溯在 CR-003 中的落点**

> source / video / comment / file\
> → topic proposal / topic_item\
> → research plan / claim / evidence\
> → content plan version\
> → script version\
> → script review\
> → approved script version\
> → publication capture version\
> → P+ snapshot / baseline / experiment result\
> → tactic evidence / revision proposal

  **对象**                **必须可反查**
  ----------------------- -----------------------------------------------------------------
  Input Assembly Record   本次候选材料、纳入/舍弃原因、预算处理和冻结哈希
  Binding Manifest        local_ref 对应的宿主对象、版本、片段、转换规则和前后哈希
  content_plan            每个段落的 claim/evidence、经验 used/considered/rejected 和原因
  script_version          每个承载事实或经验的句段对应计划、claim/evidence/tactic
  script_review           每个 issue 对应稿件片段、计划要求、claim/evidence/tactic
  人工修改                父版本、修改者、时间、差异和修改后新版本
  实际发布稿              与用户确认稿分开保存；任何正文变化创建新版本
  P+ 与经验               冻结假设、实际使用、指标口径、基线、干扰、结果和证据资格

Skill 本体只使用局部 ref；宿主系统通过 Binding Manifest 恢复到稳定
ID、版本和哈希。摘要或压缩不能破坏来源映射。

# **19 CR-003 跨章节审计**

## **19.1 已修复问题**

  **问题**                                     **修复**
  -------------------------------------------- --------------------------------------------------------
  Skill 章节重复定义采集/搜索/评论能力         增加全局硬规则；具体 Skill 只声明消费已准备材料
  Binding 同时承担材料选择                     Core 负责 Input Assembly；Binding 只做确定性转换
  Runner 可能补上下文和隐藏编排                Runner 只校验、执行和技术重试
  content_classify 输出 eligible/excluded      改为纯语义 in_scope/exclusion_matched 等
  source_to_topic 与 expansion validate 混淆   候选生成和证据验证彻底分开
  深度分析重新讨论视频搜索/评论采集            只接收 Core 冻结的固定样本包
  不加载经验被误解为没有分析方法               稳定分析方法与动态 tactic 分离
  tactic_extract 与经验语义修订靠近            前者只发信号，后者生成正式版本提案
  production research plan 选择平台/Adapter    Portable Skill 只给业务研究意图，Core 补执行路由
  正式研究反复写视频排除项                     视频能力从研究结构中完全移除
  所有评论混成 audience_comments               拆分对标、自营、文化作品评论
  CR-003E watch 风险语义可能丢失               Binding 转为自解释 usage recommendation 与 risk reason
  script_review 与确定性预检未对齐             确定性预检位于语义审核之前且不替代审核
  formal experiment 可能在计划阶段过早确定     计划只给候选，发布前基于实际使用和指标冻结

## **19.2 仍需警戒项**

-   Codex 不能因为某个业务字段未物理化，就自行增加业务状态、表、Agent
    或复杂图谱。

-   技术层为了批处理、缓存或减少调用，不能把多个原子对象合并到一个互相污染的上下文。

-   ContextAssembler 不能以 Token
    仍有空间为由自动加入经验、评论、来源或历史稿件。

-   Binding
    转换不能悄悄做摘要或删减业务含义；任何派生摘要都必须有版本、来源映射和哈希。

-   审核器不能因缺少输入证据而调用外部搜索；只能返回 research_required。

-   文化作品评论不能逐步扩张成普通网页评论、社交评论或视频评论的通用研究入口。

-   正式研究中不能因为某个选题资料难找而临时启用视频平台路径。

## **19.3 两轮审查结论**

第一轮从职责、状态权限、相邻 Skill 和既有 CR
对齐检查，没有发现需要新增业务 Skill
或改变用户使用方式的必要性。第二轮从多领域、上下文成本、过度设计和维护成本检查，没有引入微服务、经验图谱、兼容矩阵或新的采集链。CR-003A---E
可作为阶段检查点。

  ---------------------------------------------------------------------------------------------------------------------------------------------------------
  **本对话新增正式增量：**以下 CR-003F 与 CR-005 使用本对话两轮审查和跨章节最终复核后的版本。它们覆盖 V0.5/R04 中与其明确冲突的旧口径；其他部分继续有效。
  ---------------------------------------------------------------------------------------------------------------------------------------------------------

# 20 CR-003F 总体边界

  **Skill**                     **唯一语义职责**                                                                                                      **不承担**
  ----------------------------- --------------------------------------------------------------------------------------------------------------------- -------------------------------------------------------
  experiment_review             对一份已冻结并完成 P+ 观测的实验，在 Core 确定性 metric signal 之上判断主要经验是否真实被测试及是否存在不可归因因素   计算指标、选择基线、创建实验、修改经验等级或状态
  experience_revision_propose   根据已触发的经验问题和 Core 选定证据包，形成零个或一个语义修订提案候选                                                判断是否触发、计算等级/状态、发布提案、创建数据库版本

  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **执行与业务结果分离：**Runner 的 succeeded/failed 是技术执行结果；Skill 的 supported/counterexample/inconclusive/ineligible 或 proposal/no_proposal 是业务结果。两者不得混用。
  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-   两个 Skill 必须脱离宿主系统独立运行，使用自解释 Schema 和
    local_ref。

-   experiment_review 不覆盖 Core 的确定性 metric signal。

-   experience_revision_propose 不与 tactic_extract、recompute_tactic
    或人工发布权限重叠。

-   仅仅跨越 L4/L5 门槛不再调用
    experiment_review；等级始终由确定性规则重算。

# 21 experiment_review 正式逻辑契约

## 21.1 调用边界

Core
可以直接完成的清洁实验不调用模型：批准稿中主要经验已被独立确认使用；实际发布稿与批准稿一致或差异不影响经验；正式
P 基线、观察点和主要指标完整；没有重大干扰；metric signal 可直接映射。

只有出现下列情况才调用 experiment_review：

-   实际发布稿发生可能影响主要经验的变化。

-   主要经验是否真实保留无法确定。

-   存在已记录的重大干扰或归因冲突。

-   多项记录相互矛盾，需要语义判断是否只能降为 inconclusive。

技术失败、基线缺失、P+ 数据无效等确定性问题由 Core
直接处理，不交给模型解释。

## 21.2 独立输入

  **输入组**            **内容**
  --------------------- -------------------------------------------------------------------------------
  frozen_experiment     冻结的主要假设、主要经验完整语义、主要指标、观察点、成功/失败规则
  expected_usage        计划使用的 action、placement、span、handoff、stop_or_avoid
  approved_script       用户批准稿或与主要经验相关的完整可核对片段
  publication_capture   实际发布稿及与主要经验相关的片段
  publication_diff      Core 确定性生成的差异块及影响范围；Binding 不得私自摘要
  metric_packet         Core 已计算的 metric signal、实际值、基线值、样本数、规则命中过程和观察有效性
  confounder_packet     已记录的热点、投流、人物事件、发布时间异常、账号异常、画面变化等事实
  minimal_context       判断经验适用条件所必需的领域、受众和选题语义

默认不输入评论、完整历史 P
数据、辅助经验、经验等级和推荐状态。只有固定回归证明额外信息能改善归因判断时才可增加。

## 21.3 独立输出

  ------------------------------------------------------------------------------
  primary_tactic_use: used \| partially_used \| not_used \| uncertain\
  publication_change_effect: tactic_preserved \| tactic_altered \| unclear\
  attribution_assessment: attributable \| materially_confounded \| uncertain\
  business_outcome: supported \| counterexample \| inconclusive \| ineligible\
  findings: 原子问题 + 影响范围 + 依据 local_ref + 限制\
  evidence_candidate: support \| counterexample \| none

  ------------------------------------------------------------------------------

-   只有 primary_tactic_use=used 才可能形成正式经验依据。

-   partially_used 不视为完成冻结假设，业务结果为 ineligible。

-   辅助经验只能记录实际使用，不产生正式支持或反例。

-   所有判断必须引用稿件片段、diff、指标包或干扰记录。

-   输出不得包含 level、freshness、active/watch/paused/deprecated。

## 21.4 结果映射硬约束

  **Core metric signal**            **主要经验使用与归因**                 **业务结果**
  --------------------------------- -------------------------------------- ----------------
  metric_supported                  used、保留、可归因                     supported
  metric_not_supported              used、保留、可归因                     counterexample
  任意 signal                       重大干扰导致不可归因                   inconclusive
  metric_inconclusive               任意                                   inconclusive
  任意 signal                       未使用、部分使用或经验被发布修改破坏   ineligible
  provisional、基线缺失、观察无效   不进入语义翻案                         ineligible

  --------------------------------------------------------------------------------------------------------------------------------------------------------------
  **禁止翻案：**模型不得把 metric_not_supported 改成 supported，也不得把 metric_supported 改成 counterexample；只能在重大干扰时把可归因结论降为 inconclusive。
  --------------------------------------------------------------------------------------------------------------------------------------------------------------

# 22 experience_revision_propose 正式逻辑契约

## 22.1 调用触发

触发条件完全服从 CR-002，并由 Core 确定：

-   首次满足跨不同选题的 repeated formal failures。

-   两个独立 eligible 外部反例首次形成语义维护触发。

-   tactic_extract 产生明确 boundary、split 或 merge 信号。

-   人工明确要求修订。

Portable Skill 不读取 recommendation_status；Core 直接提供
repeated_formal_failures、independent_external_counterexamples、structural_revision_signal
或 human_requested_revision 等自解释触发语义。

## 22.2 宿主持久化字段与 Skill 契约分离

  ------------------------------------------------------------
  Portable Skill 输出 local_ref 与语义提案候选\
  → Output Binding 恢复宿主对象映射\
  → Materializer 生成 base_versions、宿主 ID、proposal_hash\
  → Core 执行并发检查与事务发布

  ------------------------------------------------------------

CR-002 中的
proposal_id、base_versions、trigger_evidence_ids、related_tactic_ids、skill_run_id、proposal_hash
等属于宿主持久化字段，不能进入 Portable Skill 的公开契约。

## 22.3 独立输入

  **输入组**                       **内容**
  -------------------------------- -------------------------------------------------------------
  revision_trigger                 自解释触发类型、触发事实与 local_ref
  current_experience               当前正式经验完整语义 Schema
  evidence_aggregate               合格支持、反例、正式实验、provisional/excluded 的结构化统计
  representative_supports          Core 选定的代表性支持证据及限制
  representative_counterexamples   Core 选定的代表性反例及适用关系
  formal_experiments               已完成正式 P 实验摘要、实际使用、结果与干扰
  structural_signals               可选的 tactic_extract boundary/split/merge 信号
  related_experiences              仅 merge 等场景，由 Core 预召回并选定的少量完整经验
  historical_candidate             仅 restore 场景，由 Core 提供的历史语义版本
  human_constraints                人工要求修订时的意图和禁止范围

不得输入整个经验库、全部历史稿件、完整评论集合、全部网页或未筛选的相似经验。

## 22.4 独立输出

  ------------------------------------------------------------------
  decision: proposal \| no_proposal\
  \
  当 decision=proposal：\
  proposal_type: revise \| split \| merge \| deprecate \| restore\
  changed_fields\
  proposed_experiences（完整自解释 Schema）\
  field_change_basis\
  evidence_mapping\
  unmapped_evidence\
  change_summary\
  risks_and_open_questions\
  history_preservation_requirements

  ------------------------------------------------------------------

-   no_proposal 只是 Skill Run 的业务结果，不创建新的提案状态。

-   一次 Run 最多输出一个提案，不能同时生成多个互斥方向。

-   proposed_experiences 必须是完整 Schema，不能只输出 diff。

-   每个被修改字段都必须有依据；每条继承或不继承的证据都必须说明。

-   输出不包含 tactic ID、version ID、proposal ID 或事务命令。

## 22.5 五类提案边界

  **类型**    **必须输出**                                       **业务限制**
  ----------- -------------------------------------------------- -------------------------------------
  revise      一个完整新语义版本、changed_fields、证据继承建议   不能静默扩大 decision_scope
  split       至少两个满足"一作用域、一主机制"的完整经验         原经验由发布事务统一 deprecated
  merge       一个完整新经验及各旧经验的证据映射                 不选择旧经验作为"胜者"直接扩写
  deprecate   退役依据及为何 revise/split 不能保留有效部分       两个外部反例本身不足以 deprecate
  restore     基于历史候选生成的完整新版本                       不得把 current version 指回历史版本

-   independent_external_counterexamples 只允许 revise、split 或
    no_proposal。

-   repeated_formal_failures 可允许 revise、split、deprecate 或
    no_proposal。

-   structural_revision_signal 可允许 revise、split、merge 或
    no_proposal。

-   restore 在 MVP 中只接受人工明确请求，并必须提供历史候选。

## 22.6 上下文与默认数量

CR-002 中"最多 6 条支持、6 条反例、5 条实验、3
条相关经验"改为当前验证起点，不是永久契约，也不是统一 Token 上限。每个
Skill 分别通过固定回归与消融冻结 minimum_passing、normal_budget 和
maximum_verified_input。

# 23 CR-003F 审查结论与 Codex Goal

## 23.1 两轮循环审查结论

  **发现的问题**                                   **修正**
  ------------------------------------------------ ------------------------------------------------------
  L4/L5 门槛变化可能触发模型                       等级变化不再单独触发 experiment_review
  CR-002 宿主 ID 字段可能污染 Skill 契约           定位为 Materializer 持久化结果，Skill 只用 local_ref
  counterexample 与 not_supported 可能重复建状态   前者为语义/证据关系，后者为宿主持久化映射
  触发即强制修改经验                               增加 no_proposal 业务出口
  技术成功与业务结果混用                           Runner 状态与 Skill 结论严格分离

第二轮未引入新采集链、经验知识图谱、多模型投票、账号级经验状态、新审批中间状态或周期性全库
LLM 扫描。

## 23.2 Codex Goal

  **Goal**                                  **完成定义**                                                               **硬门禁**
  ----------------------------------------- -------------------------------------------------------------------------- --------------------------------------------------------------------
  CR-003F-G01 experiment_review             接收自解释实验包，输出主要经验实际使用、发布差异影响、归因判断和业务结果   不得计算 metric signal、修改经验或使用宿主 ID；失败不得翻成成功
  CR-003F-G02 experience_revision_propose   接收当前经验和有限证据包，输出零个或一个完整语义修订提案候选               不得发布、改状态、重算等级；split/merge 必须完整 Schema 与证据映射
  CR-003F-G03 跨层门禁                      保证两个 Skill standalone、可追溯、可抹除验证                              本 Goal 不实现数据库、Job、搜索、采集、ASR、自动发布或未冻结状态

# 24 CR-005 定位、变化分类与纠正记录

  ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **模块定位：**CR-005 不是新的 Portable Skill，也不是通用纠错 Agent。它属于宿主 Core 的确定性治理能力：记录纠正、解析显式依赖、执行确定性重算或调用原有语义 Skill，并保护历史与生命周期。
  ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

## 24.1 五类变化必须分开

  **变化类型**    **定义**                                           **处理**
  --------------- -------------------------------------------------- ----------------------------------------------
  数据纠错        原先保存的值、关系、资格或身份判断被确认错误       创建纠正记录并传播影响
  新观察到达      P+ 数据迟到、新评论批次、新网页快照、新视频指标    正常追加，不否定旧观察
  外部来源更新    网页后来修改、平台数值增长、作者发布新说法或撤回   保存新快照或新证据；旧快照仍代表当时内容
  规则/配置升级   阈值、规范化规则、Prompt、模型或领域配置换版本     按版本向前生效或限定迁移，不自动认定历史错误
  业务语义修订    经验机制、适用条件、计划或稿件含义变化             创建新版本并走既有流程

## 24.2 历史与当前可用性的四个概念

  **概念**                  **含义**
  ------------------------- ----------------------------------------------------
  superseded                已有更新版本，旧版本仍是合法历史。
  invalid for current use   旧产物输入存在重大错误，不能继续作为当前决策依据。
  stale                     长期没有新支持，不等于错误或失效。
  deprecated                经验经正式提案退役，不等于历史数据错误。

## 24.3 Correction Record 逻辑内容

  **信息**           **要求**
  ------------------ ------------------------------------------------------------
  被纠正对象         对象、具体版本或快照、原内容哈希
  纠正范围           哪些字段、片段、关系或资格发生变化
  纠正前后           原值和新值，或旧版本与替代版本
  纠正原因           抓取、解析、人工录入、身份合并、来源错误等
  依据               人工说明、原始响应、权威来源、重新解析结果或确定性规则
  发起者与工具版本   人工、Adapter、Core 检查器或迁移任务；解析器/规则/配置版本
  时间语义           原数据发生时间、发现时间、纠正生效时间
  传播根节点与报告   从哪个对象/版本开始；影响对象、动作、结果和停止点

Correction Record 只是逻辑审计概念；是否使用独立表、audit_event 结构化
payload 或版本关系组合，留到 TECH。

# 25 CR-005 依赖解析、传播算法与业务等价

## 25.1 依赖来源

-   正式对象版本引用关系。

-   Input Assembly Record 中 included / discarded 材料与冻结哈希。

-   Binding Manifest 中模型实际可见 local_ref、版本、片段和转换规则。

-   claim/evidence、content plan 段落、script 句段、review
    issue、experiment 假设与 tactic evidence 映射。

不新建全系统知识图谱；依赖传播只使用明确版本关系与追溯记录。

## 25.2 最终传播算法

  ------------------------------------------------------
  1\. 确认变化类型\
  2. 固定原对象、纠正版、原因和时间语义\
  3. 从 Input Assembly / Binding / 正式引用找直接依赖\
  4. 判断纠正内容是否进入模型可见输入\
  5. 重算或重跑直接消费者\
  6. 比较业务等价结果\
  7. 结果未变：停止该分支\
  8. 结果变化：继续传播\
  9. 保护任务、批准稿、发布稿、实验和经验生命周期\
  10. 形成完整传播报告并检查收敛

  ------------------------------------------------------

## 25.3 "模型看见什么"是重跑边界

  **情况**                                                               **处理**
  ---------------------------------------------------------------------- -----------------------------------
  纠正内容未进入 Input Assembly included 集合，或只在 discarded 材料中   不受影响
  Skill 只看固定窗口/派生摘要，纠正发生在未见部分                        不受影响；保留追溯
  重新 Binding 后模型可见业务哈希不变                                    不受影响
  纠正字段或片段进入过 Skill 输入                                        直接 Skill 需要重跑
  派生摘要、Binding 转换或截断因纠正发生变化                             直接 Skill 需要重跑
  无法证明模型只看过未变化窗口                                           采用安全重跑，不用 LLM 再判断影响

  ------------------------------------------------------------------------------------------------------------------
  **关键修正：**不能因为输出没有引用某段材料就跳过重跑。LLM 可能受模型可见上下文影响，但没有在最终输出中逐条引用。
  ------------------------------------------------------------------------------------------------------------------

## 25.4 业务等价投影

重跑后只有业务等价结果变化才继续传播。时间戳、Run ID、Token
数和供应商响应元数据不进入业务等价判断。

  **对象**           **业务等价投影示例**
  ------------------ --------------------------------------------------------------
  claim              claim meaning + scope + gate result
  content analysis   findings + mechanism boundaries + evidence refs
  content plan       核心承诺/结论 + 段落功能 + claim/evidence/tactic 决策
  script review      outcome + issues + protected content
  experiment         eligibility + metric signal + semantic outcome
  tactic             当前语义版本 + level + freshness + recommendation suggestion

## 25.5 传播动作

  **动作**                      **适用场景**
  ----------------------------- ------------------------------------------------------------
  no_action                     修改未命中实际输入或业务结果未变
  deterministic_recompute       基线、指标、资格、计数、状态、排序等
  semantic_rerun                原语义 Skill 的模型可见输入发生变化
  replace_current_candidate     尚未批准的当前候选被新版本替代
  require_user_reconfirmation   已批准但未发布的用户可见内容发生实质变化
  historical_annotation         已发布或已完成的历史对象只追加纠正关系
  human_attention_required      已发布内容出现重大事实或风险问题，系统不能自动处置平台内容

# 26 CR-005 生命周期保护

## 26.1 production_task

  ---------------------------------------------------
  当前状态\
  → 上游纠错使必要前置条件失效\
  → blocked（记录原因和恢复条件）\
  → 重算或重跑最早变化节点\
  → 满足 resume_condition\
  → 恢复到 researching / writing / ready_to_publish

  ---------------------------------------------------

-   不得直接把 writing 倒回 researching，也不得把 ready_to_publish 倒回
    writing。

-   纠错未影响当前前置条件时，任务状态保持不变，不制造无意义 blocked。

-   published 或 reviewed 永远不因纠错倒退。

-   cancelled 只用于明确放弃，不作为通用纠错状态。

## 26.2 单向生命周期对象

  **对象**             **保护规则**
  -------------------- -----------------------------------------------------------------------------------------------------------
  topic_item           stage 不倒退；validated 被证明不成立时关闭、建立 duplicate/unsupported 关系或创建新 topic，不改写核心问题
  analysis_candidate   completed 不恢复为 active；创建新的分析 Run 和 content_analysis 版本
  Job                  completed Job 不重开；纠错创建新工作单元并引用原 Job
  已完成 Skill Run     保留原输入、模型/Prompt/配置与输出；纠错生成新的 Run

## 26.3 批准稿与用户重新确认

  **情况**                                                                                        **是否重新确认**
  ----------------------------------------------------------------------------------------------- -------------------------------------------------------------------------------
  标题和正文完全不变；核心承诺/结论/风险披露不变；blocking claims 仍通过；新预检与审核通过        不需要，原批准继续有效
  标题或正文变化；事实强度/限定变化；主要经验使用变化；风险披露或计划路线变化；原稿不再通过审核   必须重新确认
  已经发布                                                                                        不重新确认历史稿，也不自动编辑平台内容；重大问题生成 human_attention_required

## 26.4 已完成语义结果不可覆盖

-   content analysis、research evidence extraction、content plan、script
    version、script review、publication capture、experiment
    evaluation、tactic version、已发布 revision proposal
    均不得静默覆盖。

-   纠错生成替代输出、新版本或纠正关系；物理上采用新行、版本列还是审计关系留给
    TECH。

-   允许改变的是当前派生值，如 claim 当前门禁、基线当前值、tactic
    level/status/freshness 和未来召回顺序，但必须保留旧值、新值与原因。

# 27 CR-005 主要对象与四条重点传播链

## 27.1 视频指标、滚动基线与冻结实验基线

-   正常新快照只建立新的滚动基线，不改变过去候选、计划或实验使用的基线快照。

-   正式实验发布前已经冻结观察时间、基线口径、成员、指标和成功/失败规则；后来新数据不得改变该冻结基线。

-   冻结基线内部数据被确认错误时，使用原冻结时点、原规则版本和原成员集合重算纠正版。

-   若错误本身是成员选择错误，可以修正成员，但必须解释原成员为何不合格、新成员为何应进入，并保持冻结时点不变。

-   数据纠错与规则升级不得在同一批次混合，否则结果变化无法归因。

## 27.2 ASR、评论与来源证据

  **对象**                 **传播范围**
  ------------------------ ---------------------------------------------------------------------------------------------------------
  ASR / 清理稿             只传播到真实消费该文本版本/片段的分类、关系、选题、深度分析、经验提取及其直接下游；不触发新搜索或新采集
  对标/自营/文化作品评论   只影响实际消费该评论批次的选题线索、audience signal、深度分析、互动机制和复盘；仍不能升级为外部事实证据
  来源快照                 抓取或解析错误时重跑对应 evidence extraction；网页后来更新通常创建新快照或新证据，不回写旧快照
  research evidence        重新提取后由 Core 重算 claim 关系和门禁；未被当前 claim 使用的变化不传播到生产链

## 27.3 publication capture 与 P+ 实验链

  ------------------------------------------
  publication capture 纠正\
  → 创建新的 capture 版本或纠正关系\
  → 重新比较 approved script 与实际发布稿\
  → 重算 primary tactic actual use\
  → 重算实验资格\
  → 用纠正版冻结基线重算 metric signal\
  → 必要时重新运行 experiment_review\
  → 生成替代 experiment evaluation\
  → 重分类 tactic evidence\
  → recompute_tactic

  ------------------------------------------

-   原 publication capture、原 experiment result 和原 tactic evidence
    分类不得删除或覆盖。

-   如果主要经验实际未使用，原正式 support/counterexample 资格必须撤销。

-   P+ 数值成功不能覆盖"经验未实际使用"的 ineligible。

-   辅助经验仍不得从单一正式实验产生正式支持。

## 27.4 经验链

  -------------------------------------------------------
  tactic evidence 纠正\
  → 重算 independence 与 eligibility\
  → 重算 level / freshness / recommendation suggestion\
  → manual_lock 门禁\
  → 检查是否首次触发 CR-002 修订条件\
  → 必要时调用 experience_revision_propose

  -------------------------------------------------------

  **经验变化**               **当前未发布生产**                      **历史已发布内容**
  -------------------------- --------------------------------------- ------------------------
  level 变化但使用建议不变   不自动失效；影响以后排序                不处理
  active → watch             重新考虑风险或复审当前计划              只保留历史
  active/watch → paused      当前使用该经验的任务需 blocked 并重评   不改写历史
  deprecate 发布             当前未发布任务重新规划                  历史引用继续指向旧版本
  freshness 变化             影响以后排序                            不处理

## 27.5 四条重点链路最终结论

  **链路**          **最终边界**
  ----------------- ---------------------------------------------------------------------------------------------------------
  production_task   必要前置条件失效才 blocked；修复后 resume；不倒退正常状态
  claim/evidence    研究证据与经验证据枚举分离；claim 含义不变只重算门禁，含义/范围变化创建新表达
  实际发布稿与 P+   capture、冻结基线、metric signal、experiment review 和 tactic evidence 全链可追溯且不覆盖历史
  经验              证据/等级/状态确定性重算；机制和边界变化只能进入版本化 proposal；manual_lock 与 deprecated 权限不被绕过

# 28 CR-005 跨章节最终复核修正

## 28.1 六项必须修正

  **编号**   **原风险**                                                                   **最终修正**
  ---------- ---------------------------------------------------------------------------- ----------------------------------------------------------------------
  F-01       "从最早节点继续"可能实现成 production_task 状态倒退                          使用 blocked/resume，正常状态不倒退
  F-02       research evidence 与 tactic evidence 的 eligible/provisional/excluded 混用   资格枚举只属于经验域；正式研究继续使用 claim-relative 关系和来源门禁
  F-03       "更新实验结果"等表述可能覆盖历史                                             已完成评价创建替代评价并保留纠正关系
  F-04       日常滚动基线与实验冻结基线混淆                                               新观察不改变冻结基线；错误数据按原时点/口径重算纠正版
  F-05       topic、analysis、Job 等单向生命周期未保护                                    不倒退、不重开；创建替代对象、版本或新工作单元
  F-06       只按输出引用判断是否受影响                                                   以模型实际可见输入为准；看过的内容变化即安全重跑

## 28.2 五项补强

  **补强项**           **最终规则**
  -------------------- ---------------------------------------------------------------------------------------------------
  manual_lock          仍追加/重分类证据并重算 level/freshness/建议状态；不执行 active/watch/paused 实际迁移；写风险审计
  deprecated           纠错不能自动复活；只能生成 restore proposal，且恢复形成新版本
  网页更新与纠错       作者更正/撤回是新证据，抓取器或解析器当时错误才是纠错
  正常升级与组件缺陷   Prompt/模型/Binding 正常升级默认向前生效；确认旧版本具体缺陷时才限定范围 replay/rerun
  时间语义             旧证据保留 observed_at，纠正另存 corrected_at；今天修正旧证据不能把它变成今天的新支持

## 28.3 运行版本默认

纠错重跑优先使用原 Skill contract、Prompt、模型档位、配置、Binding
和规则版本，以隔离输入纠正本身造成的变化。原版本不可运行或本身存在缺陷时，使用当前已验证版本，但必须记录原/新版本、原因，并声明输出变化不能完全归因于数据纠错。

## 28.4 五层能力归属复核

  **层级**              **CR-005 中允许**                                        **禁止**
  --------------------- -------------------------------------------------------- ---------------------------------------------
  数据获取层            报告抓取错误、生成新快照、重新执行已有采集/解析任务      决定 claim 失效、修改生产状态、决定下游重跑
  Core 输入准备         找出受影响对象、选择纠正版材料、冻结重跑输入             改写 Skill 语义结论
  Host Binding          使用纠正版对象重新生成自解释输入和 local_ref             判断纠错成立、选择额外材料、扩展摘要
  Portable Skill        在输入变化时重新执行原有语义任务                         创建纠错记录、扫描依赖、写库、推进状态
  Core / Materializer   保存纠正、重算、生成替代版本、blocked/resume、传播报告   静默修改原 Skill 输出

本轮明确不采用：通用 invalid 状态、每种对象 correction
status、data_correction Skill、LLM
影响评估器、全系统依赖图数据库、全量历史重跑、自动改写已发布内容、为纠错建立新的
Agent Runtime。

# 29 CR-005 验证与 Codex Goal

## 29.1 验证矩阵

  **用例**                                                 **必须满足**
  -------------------------------------------------------- ---------------------------------------------
  ASR 只修正空格，模型可见业务哈希不变                     不重跑
  ASR 修正否定词并进入过模型输入                           重跑直接消费者；业务结果未变即停止
  一条 research evidence 被纠正但 claim 仍有足够独立支持   在 claim 门禁重算处停止
  blocking claim 失去唯一有效支持                          production_task blocked，返回研究或缩小结论
  批准稿可见正文因此变化                                   生成新版本并要求重新确认
  已发布稿发现重大事实错误                                 不改历史稿；生成 human_attention_required
  冻结 P 基线成员值错误                                    按原冻结时点/口径重算，不采用当前滚动窗口
  publication capture 登记错误                             新 capture，旧 capture 保留；重算实验链
  manual_lock 下证据减少                                   level 可下降，状态不迁移，写风险审计
  deprecated 依据被纠正                                    不自动恢复，只能 restore proposal
  validated topic 被证明重复                               stage 不倒退，关闭或建立关系
  Prompt V2 上线                                           不自动重跑历史
  Binding V1 确认漏否定词                                  限定受影响版本与输入后 replay
  同一纠错重复执行                                         无重复版本、状态事件或证据
  删除 validation 资产                                     正式纠错、传播和重算仍可运行

## 29.2 Codex Goal

  **Goal**                            **完成定义**                                                                                          **门禁**
  ----------------------------------- ----------------------------------------------------------------------------------------------------- -------------------------------------------------------
  CR-005-G01 纠错登记与不可变历史     接收旧/新版本、变化范围和依据，保留纠错前内容并生成可追溯记录                                         同一纠错幂等；前后哈希可核对；不直接采集或语义判断
  CR-005-G02 依赖影响解析             基于 Input Assembly、Binding Manifest、正式引用与当前生产关系输出直接影响对象和动作                   不使用 LLM 判断影响；不扫描无正式依赖的全库
  CR-005-G03 确定性重算与收敛         类型化处理 baseline、claim gate、metric signal、experiment、tactic evidence、level/status/freshness   新观察与历史纠错分开；结果未变即停止传播
  CR-005-G04 语义重跑与生命周期保护   调用原 Portable Skill 生成新 Run/候选版本                                                             不建立通用纠错 Prompt；不倒退状态；不自动继承用户确认
  CR-005-G05 传播报告与恢复           记录影响、无影响原因、动作、前后结果、停止点、失败与人工事项                                          中断恢复不产生重复副作用；删除验证资产后仍运行

# **30 当前状态、待校准、未完成 TECH 规格和外部门禁**

  **分类**      **当前内容**
  ------------- ----------------------------------------------------------------------------------------------------------------------------------------
  已确认        R02 Hermes/飞书职责与安全；CR-003A---F；CR-004A---C；CR-005；CR-006A---B；五层责任；端到端追溯；历史不可覆盖；blocked/resume；冻结基线
  当前默认      局部修订 2 轮、结构重写 1 轮；纠错重跑优先保持原版本；可见正文不变且重审通过时沿用原批准
  待校准        模型与 Prompt、语义阈值、normal budget、maximum verified input、D/P 阈值、性能、批次、成本和真实供应商表现
  未完成 TECH   物理表/对象、stable ID、version/current pointer、事务、API、Job、锁/租约、依赖索引、业务等价哈希、恢复机制
  外部门禁      SearchProvider/Fetcher 真实评测、ASR/模型真实评测、平台字段兼容、凭证与部署；不阻塞 fixture/Replay 核心验证

## **30.1 当前外部执行口径**

  **事项**                  **状态**
  ------------------------- ----------------------------------------------------------------------------------------
  MediaCrawler              抖音等对标视频链路默认执行器；许可门禁按用户要求豁免，不再作为阻塞项
  网易云音乐                独立脚本已完成技术测试；后续薄封装为 NetEaseMusicCollectorAdapter，不属于 MediaCrawler
  SearchProvider            Tavily、AnySearch 等统一评测后冻结主供应商
  Fetcher/Extractor         供应商 Extract、Scrapling 等统一评测
  豆瓣专用能力              契约与 Fake/Replay 先行，真实评测后决定实现
  真实模型、Prompt 与阈值   用于校准；不作为状态机、追溯和错误处理第一次验证前提

# **31 下一唯一节点：TECH**

  --------------------------------------------------------------------------------------------------------------------------------------------------------
  **新对话起点：**直接进入 TECH。TECH 负责把已冻结业务逻辑物理化，但不得借"实现需要"重新定义业务状态、能力归属、Skill 输入输出、研究链路或用户使用方式。
  --------------------------------------------------------------------------------------------------------------------------------------------------------

## **31.1 TECH 应解决的物理规格**

-   核心对象、稳定
    ID、不可变版本、当前指针、内容哈希和正式引用的物理模型。

-   Input Assembly Record、Binding Manifest、Skill
    Run、Prompt/模型/配置、验证报告和审计事件的存储。

-   production_task、topic、analysis、script、experiment、tactic 和
    revision proposal 的状态/版本物理契约。

-   Job、Scheduler、Worker、幂等键、租约、重试、超时、取消、并发和故障恢复。

-   Core API、Materializer、Runner、Model Port、Adapter Port 与 Binding
    的接口和事务边界。

-   CR-005
    纠错登记、直接依赖索引、业务等价比较、传播收敛、blocked/resume
    与恢复报告。

-   测试/验证资产与生产资产的物理隔离及 Clean-room 删除门禁。

-   Hermes/飞书的物理接入继续暂缓，Core API
    只保留已确认的白名单工具边界。

## **31.2 推荐 TECH 子节点顺序**

  **TECH 子节点**                            **先解决的问题**
  ------------------------------------------ -----------------------------------------------------------------------------------------
  TECH-001 物理对象与版本/追溯骨架           对象边界、stable ID、version、current pointer、哈希、引用和不可变历史
  TECH-002 状态机与 Core/Materializer 事务   状态迁移、前置条件、幂等、并发检查、用户确认、提案发布与回滚
  TECH-003 Job/Scheduler/Worker              job 类型、payload、租约、重试、超时、取消、优先级、恢复和独立运行
  TECH-004 Portable Skill Runtime            Skill Package、Binding、Input Assembly、Runner、Model Port、输出物化和技术/业务结果分离
  TECH-005 CR-005 纠错与传播                 Correction Record、依赖解析、业务等价、重算/重跑、收敛、blocked/resume 和报告
  TECH-006 验证、删除门禁与 Codex Goal       fixture/Replay/Fault/FakeClock/Shadow/Clean-room 的物理隔离和验收包

## **31.3 TECH 中不得重新打开**

-   Hermes、飞书直连、自建 Agent Runtime 或 Runtime 比较。

-   正式资料研究的视频平台路径。

-   对标视频采集、ASR、评论、普通/低表现对照、深度分析和经验提取的必要性。

-   MediaCrawler 许可门禁或把网易云写成 MediaCrawler 能力。

-   Portable Skill 使用宿主 ID、ORM、状态码或数据库结构。

-   Binding 选择材料、Runner 补上下文或隐藏完整工作流。

-   统一 Token 上限或按模型最大窗口堆叠上下文。

-   Codex 自行补造未冻结业务状态、表、Agent、经验图谱或判断规则。

完成 TECH 后，再更新 Codex 可执行实施控制包，并把 V0.5、CR-002、本 R05
与 TECH 统一合并为 V0.6。

# **附录 A 新对话启动提示词**

  -----------------------------------------------------------------------------------------------------------------------------------------------------------------------
  请先完整读取我上传的三份文件：\
  1.《爆款口播内容经验库系统_当前阶段技术设计文档_V0.5_执行审计修订版》\
  2.《爆款口播内容经验库系统_新对话交接控制文档_V0.5-plus_R05_TECH启动版_完整无损继承版》\
  3.《爆款口播内容经验库系统_经验自动演进增量设计_CR-002_R01》\
  \
  使用规则：\
  - V0.5 是完整执行基线；本 R05 是经过 R02→R03→R04→CR-003F→CR-005 逐章继承审计后的最新增量控制文档；CR-002 是经验自动演进的详细正式增量。\
  - 冲突优先级：我在本对话中的最新明确指令 ＞ 本 R05 ＞ CR-002 ＞ V0.5 ＞ R04、R03、R02 及更早内容。\
  - 先区分已确认、当前默认、待校准、未完成 TECH 规格和外部门禁；不要把待校准参数当成业务设计缺失。\
  - Hermes 与飞书架构已确认，当前继续忽略实施细节。不要建议飞书直连、自建 Agent Runtime，或重新比较 Hermes 与其他 Runtime。\
  - 不要从头复述系统，不要重写完整文档，不要重新讨论 CR-003A---F、CR-004A---C、CR-005、CR-006A---B，除非发现明确冲突。\
  - 先确认已经读取三份文件，并用不超过 10 条列出当前有效控制点、已完成逻辑节点、未完成 TECH 规格和下一子节点。\
  - 随后直接从 R05 第 31 章进入 TECH，优先讨论 TECH-001：物理对象、稳定 ID、版本、current pointer、哈希、引用和不可变历史。\
  - 端到端可追溯是全系统最高级约束：资料、主张、证据、经验、计划、稿件、审核、模型/Prompt/配置版本、人工修改、实际发布稿和 P+ 结果都必须可反查。\
  - Skill 必须可脱离当前系统独立运行；不得使用宿主内部 ID、状态码、ORM 或数据库结构作为公开输入输出。\
  - 数据获取、Core 输入准备、Host Binding、Portable Skill、Core/Materializer 五层责任不得混用。后续 Skill 不得重新定义采集、搜索、ASR、评论获取、平台和 Adapter 路由。\
  - 每个模块必须能单独运行和验证；验证数据、工具、报告和依赖必须可抹除，删除后不得影响系统运作。\
  - 每项设计至少进行两轮循环审查，并增加跨章节能力归属检查，检查僵化、过度设计、冲突、未对齐、多领域、LLM/上下文成本、逻辑缺口、判断标准、边界和维护成本。\
  - 上下文和 Prompt 长度不得按模型最大窗口堆叠，也不得凭空设置统一 Token 范围；每个 Skill 通过固定回归和消融确定正常预算与最大已验证范围。\
  - 对标视频采集、ASR、评论和爆款文案分析完整保留；正式资料研究结构上没有视频平台搜索、读取、ASR、分析或视频评论链路。\
  - MediaCrawler 是抖音等对标视频链路默认执行器，许可门禁已豁免；网易云使用已完成技术测试的独立采集脚本。\
  - 正式搜索采用 SearchProvider/Fetcher 分层，Tavily、AnySearch、Scrapling 等先统一评测，不预先冻结供应商。\
  - 除非某项决定确实改变我的使用方式或业务边界，否则由你给出可执行专业默认，不要把大量技术细节重新抛给我。\
  - 已确认修改先形成结构化增量项；累计到阶段节点后再更新 Codex 实施控制包并生成 V0.6。\
  - 后续任何声称"新文档取代旧文档"的版本，必须附逐章继承清单：每一章标注完整保留、修改后保留、明确覆盖、移交 TECH、待校准或失效；不得仅用摘要替代旧版细粒度控制。

  -----------------------------------------------------------------------------------------------------------------------------------------------------------------------

# **附录 B 逐章继承与覆盖清单**

  **来源**   **原章节/主题**                                **本 R05 处理**                             **状态**
  ---------- ---------------------------------------------- ------------------------------------------- --------------------------------
  R02        Hermes 架构与责任                              第 2.1---2.3 节完整恢复                     已确认，实施细节暂缓
  R02        Tool / Hermes Skill / 业务 Skill、白名单动作   第 2.4---2.5 节完整恢复                     已确认边界
  R02        Cron/Scheduler、故障隔离、权限、安全、成本     第 2.6---2.8 节完整恢复                     已确认边界
  R02        对 V0.5 的架构覆盖                             第 2.9 节明确覆盖                           V0.5 旧直连口径失效
  R03        用户流程、追溯、验证、上下文                   第 3 章完整恢复                             已确认
  R03        CR-004A                                        第 4 章完整恢复                             已确认
  R03        CR-004B                                        第 5 章完整恢复                             已确认
  R03        CR-004C                                        第 6 章完整恢复                             已确认；部分由 CR-003F/005细化
  R03        CR-006A                                        第 7 章完整恢复                             已确认
  R03        CR-006B 与音乐评论                             第 8 章完整恢复并采用 R04 更严格视频边界    已确认
  R04        CR-003 全局边界及 CR-003A---E                  第 10---18 章直接继承详细正文、表格与枚举   已确认
  R04        跨章节审计                                     第 19 章直接继承                            已确认
  本对话     CR-003F                                        完整纳入；修正 L4/L5 触发与独立输入输出     已确认
  本对话     CR-005                                         纳入全面复核后的最终版本                    已确认
  旧 R05     TECH 起点                                      第 30---31 章重写，删除错误"完整吸收"声明   当前有效

# **附录 C Portable Skill 职责矩阵**

  **Skill**                     **只负责**                                 **核心输出**                                          **不得做**
  ----------------------------- ------------------------------------------ ----------------------------------------------------- ------------------------------
  content_classify              相对一个边界作语义分类                     范围/排除语义结果                                     业务资格、搜索、全领域选择
  content_relation_judge        判断一对内容关系                           duplicate/same_topic/derived/related 等               检索全库、合并/关闭对象
  source_to_topic               从一份来源形成具体问题提案                 零个或多个 topic proposal                             搜索、正式核验、创建 topic
  sample_deep_analyze           分析一个冻结样本包                         content analysis 与 L0 机制发现                       采集/搜索/ASR、正式经验状态
  tactic_extract                规范化、拆分或归并已选机制发现             原子候选及边界/重复/split/merge 信号                  发布语义版本、改等级/状态
  topic_expansion_validate      判断已有问题及证据包是否有增量和初步依据   supported/uncertain 等验证结果                        搜索、创建/关闭选题
  production_research_plan      把正式选题转为 claim-centric 研究计划      claims、blocking、缺口、停止条件                      搜索、选供应商、扩大预算
  research_evidence_extract     从一份已保存快照提取相关证据               excerpt、meaning、关系、限制                          搜索、判最终 claim、写稿
  content_plan                  组织可执行、可追溯内容计划                 段落、claim/evidence、经验决策、风险                  补研究、写完整稿
  script_generate               按计划生成初稿或修订稿候选                 新 script candidate 与句段映射                        加计划外事实、自审、定最终稿
  script_review                 审核指定稿件                               原子 issue 与业务结果                                 改稿、搜索、推进状态
  experiment_review             在确定性指标上判断主要经验实际使用和归因   supported/counterexample/inconclusive/ineligible      计算指标、改经验状态、翻案
  experience_revision_propose   根据已触发问题形成零个或一个语义提案候选   no_proposal 或 revise/split/merge/deprecate/restore   判触发、发布、生成宿主 ID

# **附录 D TECH 启动检查表**

-   □ 是否从 TECH-001 开始，而不是重讲已冻结 CR？

-   □ 是否把 R02/R03/R04 的详细控制点视为有效上位约束，而不是摘要？

-   □ 是否保留对标视频学习与正式资料研究的结构分离？

-   □ 是否保留 Skill 可移植独立、端到端追溯、历史不可覆盖和可抹除验证？

-   □ 是否把数据获取、Core 选材、Binding 转换、Skill 判断和 Materializer
    写入分开？

-   □ 是否禁止 Skill 暴露宿主 ID、ORM、数据库表和内部状态码？

-   □ 是否禁止 Binding 选材、Runner 补上下文、Adapter 推进状态？

-   □ 是否保护
    production_task、topic、analysis、Job、approved/published/experiment
    的生命周期？

-   □
    是否区分滚动基线、冻结实验基线、新观察、数据纠错、规则升级与组件缺陷？

-   □ 是否禁止 Codex 自行补造业务状态、图谱、Agent、判断规则和统一 Token
    上限？

# **附录 E 后续文档无损继承门禁**

  -----------------------------------------------------------------------------------------------------------------------------------------------------
  **禁止再次发生的情况：**后续 R06、TECH 汇总或 V0.6 不得仅因"新文档更短、更易读"就删除细粒度规则。摘要可以另做导航，但不能承担取代旧文档的唯一职责。
  -----------------------------------------------------------------------------------------------------------------------------------------------------

-   任何声称取代前一版本的文档，必须列出前一版本每个章节的处理状态。

-   处理状态只能是：完整保留、修改后保留、明确覆盖、移交
    TECH、待校准、外部门禁或明确失效。

-   "已吸收""已整合"不能作为无证据的笼统声明；必须能定位到新文档章节。

-   旧版细粒度枚举、状态、调用边界、错误处理、验证门禁和安全限制不得被上位原则摘要替代。

-   新结论与旧结论冲突时，正文必须写清旧规则、冲突点和新规则，而不是静默删除。

-   生成新文档后必须执行内容覆盖审计、冲突审计、逐页渲染检查和页数/字符量异常检查。

-   最终交接包应保留 V0.5、当前增量文档、独立正式
    CR、启动提示词和逐章继承清单。

**爆款口播内容经验库系统**

**V0.5-plus R06｜TECH 完成与运行宿主固化版 · 完整无损继承**

最新增量控制文档｜完成 TECH-001---006

生产宿主：Hermes｜开发执行：Codex｜OpenClaw：替代候选

编制日期：2026 年 6 月 30 日

# 0 文档定位、冲突优先级与本轮结论

本文件完整继承 R05、CR-002 与 V0.5 的仍有效控制，并把 TECH-001---006
物理化规格、运行宿主最终路线及实施就绪结论固化为最新增量。它取代 R05
作为最新增量控制文档，但不以摘要替代旧版细粒度正文；逐章继承状态见附录
A。

  **项目**       **当前有效口径**
  -------------- ---------------------------------------------------------------------------------
  完整业务基线   V0.5 执行审计修订版；后续由 V0.6 完整执行基线汇编版统一承载
  经验正式增量   CR-002 R01 全部有效，并已完成物理化衔接
  最新增量控制   本 R06，取代 R05
  运行宿主       Hermes production-host 单 Profile 为当前正式方案
  开发工具       Codex 只负责代码开发、测试、迁移与维护，不进入生产运行链
  替代宿主       OpenClaw 只作为外部兼容与故障替代候选，不并行建设
  Agent 边界     禁止自建通用 Agent Runtime；正式业务 Skill 由 Core Runner/ModelGateway 独立执行
  TECH 状态      TECH-001---006 已完成，可更新 Codex 实施控制包并进入实施

冲突优先级：用户在本对话中的最新明确指令 ＞ 本 R06 ＞ R05 ＞ CR-002 ＞
V0.5 ＞ 更早版本。

  --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **本轮最重要的路线修正\
  **Codex 不再作为生产 Host 候选；Hermes 负责飞书会话、意图理解、白名单工具调用、等待/恢复和结果回传。正式研究、策划、文案、审核和经验提案由 Core 通过独立 Runner 与 ModelGateway 调用，不继承 Hermes 的系统提示词或会话上下文。

  --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

# 1 当前状态分类

  **分类**           **当前内容**
  ------------------ --------------------------------------------------------------------------------------------------------------------------------------------------------
  已确认             业务边界、CR-003A---F、CR-004A---C、CR-005、CR-006A---B、CR-002、端到端追溯、历史不可覆盖、五层责任、Hermes 生产宿主、Codex 开发专用、TECH-001---006。
  当前默认           单个 Hermes production-host；高风险运维 Profile 暂不建设；局部修订最多 2 轮、结构重写最多 1 轮；纠错优先保持原执行组件版本；数据库任务队列。
  待校准             各 Skill 模型与 Prompt、normal budget、maximum verified input、语义阈值、D/P 数值、批次、并发、超时、成本和真实内容质量。
  外部门禁           Hermes/飞书真实接入、模型供应商、SearchProvider/Fetcher、ASR、MediaCrawler/网易云适配器、平台字段与凭证、备份恢复和连续运行。
  不再属于设计缺失   物理表、状态机、Core API、Job、Runner/Port、Materializer、纠错传播和 Clean-room 验证。

# 2 运行宿主与正式模型调用路线

## 2.1 最终运行结构

正式生产链固定为：

  -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **生产链\
  **飞书 → Hermes production-host → 白名单 Core API → PostgreSQL / Scheduler / Worker → Core Runner / ModelGateway → Portable Skill → Materializer → 版本、状态、审计与结果事件 → Hermes → 飞书

  -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

  **组成**                   **负责**                                                                      **明确不负责**
  -------------------------- ----------------------------------------------------------------------------- ------------------------------------------------------------------------------------
  Hermes production-host     飞书连接、会话、必要澄清、工具选择、多步编排、等待/恢复、结果组织和主动回传   不直接生成正式研究/计划/稿件；不计算基线；不写数据库；不改经验；不执行定时数据任务
  Core API                   权限、Schema、状态、幂等、并发前置、Job 创建、确认门禁、审计                  不承担开放式会话和创作文风
  Scheduler/Worker           D/P、采集、ASR、评论、基线、研究执行、生产执行、重试与恢复                    不依赖用户在线；不依赖 Hermes Cron 保证精确业务时点
  Core Runner/ModelGateway   独立加载 Portable Skill、装配专用 Prompt、选择模型、预算、缓存、输出校验      不是 Agent Runtime；不拥有业务状态
  Codex                      实现代码、迁移、测试、验证报告和维护                                          不作为生产 Host，不进入正式内容生成上下文
  OpenClaw                   仅保留同一 Core API 的可替代 Host Binding                                     当前不部署、不建设并行多 Agent 路由

## 2.2 为什么正式文案不由 Hermes 直接写

-   Hermes 仍有自己的系统提示词、工具清单、会话和记忆；仅靠加载 Skill
    不能保证完全隔离 Host 风格。

-   正式 script_generate 由 Core 建立独立 Input Assembly
    和独立模型调用，只接收选题、计划、证据、允许经验、账号风格和正反例。

-   独立创作调用看不到 Hermes SOUL、Hermes
    会话、代码仓库、数据库结构、TECH 文档、Job 或运维信息。

-   创作和审核分别调用：创作允许发散，审核负责事实、证据、风险、相似与计划一致性。

-   模型不按 Host 固定；相同 Portable Skill 可评测 GPT、Claude、Gemini
    或其他模型，结果由固定回归和人工修改量决定。

## 2.3 Hermes production-host 最小化与防自改

  **控制点**     **冻结规则**
  -------------- --------------------------------------------------------------------------------------------------------------------------
  Profile 隔离   使用独立 production-host Profile，拥有独立 config、SOUL、会话、记忆、Skill、Cron 和凭证。
  Agent 身份     SOUL 只定义中性的生产协调身份，不把它写成文案创作者或软件工程师。
  正式 Skill     正式 Portable Skill 位于核心仓库，由 Git 版本控制；Hermes 不加载其完整业务 Prompt，只看到薄 Core Tool 契约。
  Skill 写入     production-host 不开放 skill_manage；同时保留 skills.write_approval=true 作为第二道门禁。
  记忆写入       memory.write_approval=true；正式状态、业务规则和用户批准不得写进 Hermes 记忆作为事实来源。
  后台自改       curator.enabled=false；关闭不必要的自动学习、整理和 Skill 巡检。
  文件权限       正式 Skill 与配置只读挂载；非 root 运行；不开放任意 Shell、文件写入、数据库、浏览器自动化。
  工具白名单     只开放明确的 Core API 工具；固定按钮和简单查询走确定性路径。
  多 Profile     当前不提前建立 content/operations 多 Profile；普通查询和生产共用 production-host，高风险运维隔离在真实需要出现后再增加。

官方能力依据：Hermes Profile 可隔离
config、SOUL、记忆、会话、Skills、Cron 与 Gateway 状态；SOUL
可替换默认身份；Skill 与 Memory 写入支持审批；Curator 可按 Profile
关闭。具体配置名称在实施时须以锁定版本的官方文档和回归测试为准。

## 2.4 切换到 OpenClaw 的门禁

只有出现下列可测问题，才启动 OpenClaw 对照或迁移；不得因"多 Agent
更强"提前增加运维复杂度：

-   Hermes 飞书消息收发、附件、主动回传或长会话恢复无法达到验收。

-   Hermes 工具白名单、权限隔离或 Profile 故障隔离无法满足要求。

-   后续真实出现多个飞书群、多个用户或多个独立机器人身份的确定性路由需求。

-   Hermes 版本升级兼容或长期运行稳定性持续不合格。

迁移时只替换 Host Binding；Core API、数据库、Job、Portable Skill、Input
Assembly、Binding Manifest 和 Materializer 不变。

# 3 TECH-001：物理对象、版本、追溯与原子事务（完成）

## 3.1 三种物理对象模式

  **模式**                              **适用对象**                                               **规则**
  ------------------------------------- ---------------------------------------------------------- ---------------------------------------------------------------------------------
  稳定根＋不可变版本＋current pointer   topic、transcript、tactic 等长期身份对象                   根保存稳定 ID 和当前指针；正文变化创建新版本；历史引用固定到具体 version。
  父聚合＋不可变子版本流                production_task 下的 research_plan、content_plan、script   不增加空壳根表；production_task 保存显式当前、候选、批准与发布指针。
  不可变观察/结果链                     快照、评论批次、分析、审核、实验结果、审计                 每次观察或执行新增记录；通过 supersedes/纠正关系连接，不设置通用 current flag。

## 3.2 ID、版本、指针与哈希

  **主题**          **冻结规则**
  ----------------- ---------------------------------------------------------------------------
  内部 ID           应用侧生成 UUIDv7；不编码领域、状态、平台、日期或版本。
  外部 ID           平台视频 ID、URL、账号 ID 只作外部键，不作系统主键。
  版本号            同一根内单调递增，只供排序和阅读；正式引用使用 version_id。
  current pointer   只表示新动作应采用的当前版本；历史下游不得动态追随 current。
  restore           复制旧语义形成新版本，记录 based_on_version_id，不把 pointer 指回旧版本。
  blob_hash         原始字节校验和去重。
  content_hash      规范化完整对象内容，检测静默修改和精确缓存。
  business_hash     对象专属业务投影，用于纠错传播收敛；不能用全局文本清洗替代。
  哈希算法          SHA-256，并记录投影版本。

## 3.3 正式引用与 Skill local_ref

-   数据库关系引用具体 version/snapshot，不只引用稳定根。

-   Host ObjectRef 包含
    object_kind、stable_id、version_or_snapshot_id、content_hash、locator
    和 relation_role。

-   Portable Skill 只接收
    local_ref、自解释内容、对象类型和必要定位，不出现宿主
    UUID、表名、ORM、内部状态码。

-   Binding Manifest 保存 local_ref → ObjectRef → 转换规则 →
    转换前后哈希。

-   locator 使用网页段落/PDF
    页码/音频时间段/稿件段落与字符区间等类型化结构，不能只保存易漂移摘录。

## 3.4 不可变历史与表级边界

  **类别**     **允许操作**                                             **代表对象**
  ------------ -------------------------------------------------------- ------------------------------------------------------------------------------
  完全不可变   只 INSERT；业务角色禁止 UPDATE/DELETE                    版本、快照、分析、审核、实验结果、Input Assembly、Binding Manifest、审计事件
  受控可变     只更新白名单 pointer、状态和确定性物化值；每次写 audit   稳定根、production_task、tactic、experiment
  运行态可变   按正式状态机迁移；终态冻结                               job、skill_run、outbox

-   生产外键默认 ON DELETE RESTRICT；关闭、取消、deprecated、失败和
    superseded 都不删除。

-   验证数据使用独立数据库/Schema、存储前缀和 Vault，不给每张生产表添加
    environment 字段。

-   评论按一次 sample 的 rank 冻结，不建立无价值的跨批次平台评论 ID
    去重。

-   CR-002 经验域保持最小模型，只新增
    experience_revision_proposal，不建设经验图谱或状态历史表。

## 3.5 并发、幂等和版本创建

  **机制**          **规则**
  ----------------- -----------------------------------------------------------------------------------------------------
  row_revision      所有保存 pointer/状态的稳定根增加乐观并发版本；命令同时提交 expected revision 与 expected pointer。
  显式行锁          READ COMMITTED＋SELECT FOR UPDATE 锁定稳定根；不使用全局 SERIALIZABLE、Redis 锁或分布式锁。
  command_receipt   同步命令使用 command_scope＋idempotency_key 唯一；相同请求返回原结果，不同请求复用同 key 拒绝。
  版本事务          锁根→校验前置→分配 version_no→插入不可变版本和引用→切 pointer→审计→outbox→提交。
  过期异步输出      Skill Run 保留，但 stale basis 不得覆盖 current；默认不建立自动分支池。
  锁顺序            command receipt→父聚合→其他稳定根按 kind＋UUID→proposal/correction→关系行。

## 3.6 人工修改、批准与实际发布

  **动作**              **冻结规则**
  --------------------- --------------------------------------------------------------------------------------------------------
  人工改稿              基于 current 版本创建 user_edit；不自动合并并发版本；改后清除候选/批准并重新审核。
  审核 pass             只设置 candidate_final；旧稿审核晚到可保存但不改变 pointer。
  用户批准              不复制稿件；只把 approved pointer 指向当前 candidate，并写不可变确认审计。
  批准后变化            任何可见正文变化使批准失效；必须重新审核和确认。
  纠错批准继承          仅正式纠错、visible_text_hash 完全相同、引用复审通过且主张强度/范围/风险不变时允许。
  publication_capture   发布时始终创建独立稿件版本；same_as_approved、modified_text_provided、unknown_pending_check 三种关系。
  未知发布稿            正文可为空，不得复制批准稿伪装为实际发布内容。
  capture 纠错          创建新 capture，旧 capture、旧实验结果和旧经验资格保留。

# 4 TECH-002：状态机、Core API 与 Materializer（完成）

## 4.1 production_task 单向状态机

  **状态**           **进入条件**                                  **允许去向**                             **关键保护**
  ------------------ --------------------------------------------- ---------------------------------------- ----------------------------------------------------------------------------------------------
  created            用户选题创建任务和冻结 topic snapshot         researching / blocked / cancelled        短暂但持久化，便于崩溃恢复；不重复确认选题。
  researching        research plan 已激活，正在取得和核验 claims   writing / blocked / cancelled            所有 blocking claim checked 且计划门禁满足才进入 writing。
  writing            content plan、稿件、审核和限制内修订进行中    ready_to_publish / blocked / cancelled   普通修订不倒退状态；结构重写仍留在 writing。
  ready_to_publish   用户已确认 approved script                    published / blocked / cancelled          批准稿变化必须清除批准并通过受控动作回到 writing；普通纠错不直接倒退。
  published          实际 publication capture 和平台视频已登记     reviewed                                 状态永不因纠错倒退；只追加纠正与新评价。
  reviewed           P+ 最终观察与复盘完成                         终态                                     历史只读；remake 创建新 task 并关联 previous_task。
  blocked            当前必要前置失效或需人工资料/扩大预算         原 blocked_from_status / cancelled       记录 blocked_reason、resume_condition、blocked_from_status；修复后恢复，不把正常状态链倒放。
  cancelled          明确放弃且尚未发布                            终态                                     不删除任何研究、稿件或来源；可创建关联 retry task。

published 和 reviewed 不进入 blocked；已发布重大事实问题使用
historical_annotation /
human_attention_required，不改平台内容、不倒退任务。

## 4.2 其他状态注册表

  **对象**             **正式状态/阶段**                                         **规则**
  -------------------- --------------------------------------------------------- ----------------------------------------------------------------------
  topic_item           stage: lead→validation→validated；status: active/closed   stage 不倒退；证明重复/不成立时关闭、建关系或派生新 topic。
  analysis_candidate   active/completed                                          completed 不重开；纠错创建新 Run 和 content_analysis。
  claim                unverified/checked/disputed                               正文或范围变化创建新 claim revision；核验状态可受控重算。
  experiment           prepared→observing→completed；cancelled                   formal/observational 由 experiment_kind 区分；结果缺失不伪装为失败。
  tactic               active/watch/paused/deprecated                            严格遵守 CR-002；manual_lock 只冻结推荐状态实际迁移。
  revision proposal    pending_review/published/rejected/superseded              批准和发布同事务；基础版本变化即 superseded。
  skill_run            queued/running/succeeded/failed/cancelled                 技术状态与业务结果分离。

## 4.3 Core API 形态

Core API 只暴露业务动作和查询，不暴露任意 SQL、内部 ORM
或通用执行器。所有写命令采用统一 Command Envelope：

  **字段**                **用途**
  ----------------------- -----------------------------------------------------------------------
  actor                   用户或服务身份；Core 再次授权，不相信 Host 已判断。
  idempotency_key         重复按钮、网络重试和 Hermes 重试只生效一次。
  expected_row_revision   检测聚合发生过任何受控变化。
  expected_current_refs   确认命令基于明确业务版本。
  confirmation_token      仅高风险动作使用，绑定 actor、action、object、payload hash 和有效期。
  correlation_id          贯穿 Agent action、command、job、skill_run、audit 和 outbox。

  **工具组**   **最小 Core 动作**
  ------------ -------------------------------------------------------------------------------------------
  选题         list_daily_topics、select_topics、defer_topic、close_topic、request_new_angle
  生产         get_production_status、approve_script、register_publication、correct_publication_capture
  研究         create_manual_source、create_research_task、get_research_status、request_budget_extension
  经验         list_revision_proposals、publish/reject proposal、lock/unlock experience
  运行查询     get_task/account/baseline/job/usage status
  受控运维     retry/cancel job、register correction、resume blocked task；默认不暴露给普通会话路径

## 4.4 Materializer 责任

-   接收 Runner 的结构化 Run Envelope，不接收自由文本直接入库。

-   校验 Skill contract、Schema、Binding Manifest、local_ref
    白名单、对象归属、版本和哈希。

-   把业务结果映射为正式版本、关系、候选 pointer 或业务
    no_action；不得调用模型重新解释。

-   技术 succeeded 但业务 no_proposal/inconclusive/research_required
    仍属于成功执行，不得误记为技术失败。

-   业务前置已过期时保存 Run 但不激活结果，返回 stale_basis。

-   所有状态/pointer 变化与审计、command receipt、outbox 同事务。

## 4.5 确认、权限与错误

  **类别**   **规则**
  ---------- ----------------------------------------------------------------------------------------------------------------------------
  普通动作   用户选择选题后自动研究、计划、初稿、审核和限制内修订，不逐节点确认。
  必须暂停   blocking claim 无法核验、可靠证据重大冲突、核心结论需改变、预算明显扩大、修订超限、建议取消/换角度/补人工资料。
  必须确认   批准最终稿、永久关闭/批量关闭、发布经验语义版本、解除 manual_lock、高风险纠错。
  冲突错误   返回 stale_aggregate_revision、stale_current_pointer、stale_review_basis、proposal_superseded 等明确代码和当前可继续动作。
  授权       单用户 allowlist 是第一层；Core 服务角色和动作权限是第二层；所有高风险确认可反查。

# 5 TECH-003：Job、Scheduler 与 Worker（完成）

## 5.1 Job 最小物理模型

  **字段组**   **最小字段**
  ------------ ---------------------------------------------------------------------------------------------
  身份         job_id、job_type、payload_schema_version、idempotency_key、correlation_id、parent_job_id
  调度         queue_name、priority_class、available_at、deadline_at、timeout_seconds
  状态         queued/running/succeeded/failed/cancelled、attempt_count、max_attempts、cancel_requested_at
  租约         worker_id、lease_expires_at、heartbeat_at、started_at、completed_at
  结果         result_ref、last_error_code、last_error_detail、created_at

每次尝试另存不可变
job_attempt：attempt_no、worker、开始/结束、输入哈希、技术结果、错误分类和外部调用摘要。重试不覆盖失败历史。

## 5.2 领取、租约与崩溃恢复

21. Worker 使用 SELECT \... FOR UPDATE SKIP LOCKED 领取 status=queued 且
    available_at≤now 的 Job。

22. 同一事务把 Job 设为 running、增加 attempt_count、写 worker_id 和
    lease_expires_at。

23. 长任务按配置发送 heartbeat；租约只覆盖当前尝试，不等于业务锁。

24. 租约过期后恢复器检查幂等和尝试数：可重试则重新
    queued，不可重试或超限则 failed。

25. 业务副作用只能通过 Core/Materializer 的幂等事务提交；Worker
    崩溃重跑不得产生重复版本。

## 5.3 Job 类型注册与显式编排

  **类别**     **代表 job_type**
  ------------ -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  采集与快照   benchmark_account_scan、benchmark_day_snapshot、self_publish_snapshot、tag_search、hotspot_ingest、comment_collect\_\*
  媒体处理     audio_download、asr、transcript_clean、source_fetch、source_extract
  确定性计算   baseline_rebuild、candidate_evaluate、sample_pack_build、claim_gate_recompute、metric_signal_compute、tactic_recompute
  语义 Skill   content_classify、source_to_topic、sample_deep_analyze、tactic_extract、topic_expansion_validate、production_research_plan、research_evidence_extract、content_plan、script_generate、script_review、experiment_review、experience_revision_propose
  生产与经验   daily_topic_report、production_orchestrate、experiment_finalize、experience_maintenance
  纠错与维护   correction_resolve、correction_recompute、correction_semantic_rerun、correction_report、obsidian_sync、backup、outbox_deliver

-   Job 类型、payload
    Schema、超时、重试、资源池和错误分类必须进入版本化注册表。

-   不建设通用 DAG/工作流引擎；Core 根据业务状态和前置条件创建下一 Job。

-   多前置汇聚通过查询正式对象是否齐备完成，不用聊天记忆或 Job
    数量猜测。

-   精确 D/P、重试、备份和经验维护始终由 Core Scheduler；Hermes
    停止不影响后台。

## 5.4 重试、取消、优先级与资源

  **主题**   **规则**
  ---------- --------------------------------------------------------------------------------------------------------------------------
  错误分类   transient_external、rate_limited、timeout、technical_schema、non_retryable_input、business_result；仅前 3 类按策略重试。
  退避       按 job_type 配置指数/分段退避和抖动；数值由故障注入和真实供应商评测校准。
  取消       queued 可直接取消；running 采用 cooperative cancel；已提交业务事实不做技术回滚，只能走纠错。
  优先级     production_blocking ＞ formal observation/analysis ＞ daily discovery ＞ backfill ＞ maintenance；映射为配置化整数。
  资源池     collector、io、asr、model_low、model_high、maintenance 分池限流；并发属于部署策略，不写进领域规则。
  预算       预算耗尽只延迟高成本 Job；正式 production_task 优先于普通深度分析。

## 5.5 Scheduler 设计

-   Scheduler 周期扫描正式对象和领域/账号配置，按唯一幂等键物化 due
    Job；不依赖 Agent 在线。

-   日常固定批次、D0---D7、P+6h---P+7d、评论批次、备份和维护均可由
    FakeClock 验证。

-   不建立可自由编辑的通用 Cron
    业务表；静态时点来自版本化配置，动态时点来自业务对象。

-   Hermes Cron
    只可用于用户提醒或投递触发；不得成为精确业务调度唯一来源。

# 6 TECH-004：Portable Skill Runtime（完成）

## 6.1 Portable Skill Package

  **组成**                                 **内容**
  ---------------------------------------- ------------------------------------------------------------------------------------------------------------------------------
  skill.yaml                               skill_key、contract_version、职责、输入/输出 Schema、业务结果枚举、预算 profile、允许 model capability、错误分类和发布门禁。
  input.schema.json / output.schema.json   只包含自解释业务字段和 local_ref；不含宿主 ID、ORM、数据库状态码。
  prompts/                                 通用系统指令、任务模板和领域补充插槽；Prompt 内容、版本和哈希可追溯。
  examples/                                通过/失败/边界/跨领域样本；不把生产数据作为包依赖。
  tests/                                   Schema、引用完整性、固定回归、消融、预算和跨领域测试。
  README/contract                          独立运行方式、输入假设、输出语义、禁止副作用和失败处理。

Host Binding 不属于 Portable Skill 包；它位于核心仓库的
bindings/，负责将宿主对象转换为 Skill Schema。

## 6.2 五层执行链

  **层**                  **唯一责任**                                       **禁止**
  ----------------------- -------------------------------------------------- -----------------------------------------------
  数据获取                采集、搜索、Fetch、ASR、评论和外部原始响应         决定业务状态、直接调用业务 Skill 形成正式结果
  Core 输入准备           选择确切版本、included/discarded、预算和冻结哈希   改写来源、偷偷扩充上下文
  Host Binding            ObjectRef→local_ref、自解释格式和转换清单          选择材料、搜索、摘要删除业务含义
  Portable Skill/Runner   基于冻结输入产生结构化语义结果                     写库、生成宿主 ID、调工具、推进状态
  Core/Materializer       恢复引用、校验、持久化、状态/pointer/审计          静默改变 Skill 输出语义

## 6.3 Runner 与 Model Port

-   Runner 可用统一 CLI/库在 stub、replay、live 三模式执行同一
    handler、validator 和 Materializer contract。

-   Runner 只读取 Skill Package、Input Assembly 和 Binding
    Manifest；不访问生产数据库。

-   Model Port 提供结构化
    completion、流式可选、超时、取消、用量、供应商原始标识和错误映射。

-   ModelGateway 根据 skill_key、domain、risk、budget profile 选择
    provider/model；Host Runtime 不决定正式模型。

-   正式文案可使用与 Hermes
    完全不同的模型和系统提示词；审核也可使用另一档模型。

-   Provider 返回自由文本但 Schema
    失败时只允许一次同模型修复和受控备用档；仍失败则 technical
    failed，不让文本入库。

## 6.4 Input Assembly 与上下文预算

  **要求**         **规则**
  ---------------- ----------------------------------------------------------------------------------------------
  正常预算         每个 Skill 通过固定回归和消融得出 normal budget，不按模型最大窗口堆叠。
  最大已验证输入   超过 maximum verified input 直接拒绝、拆分或升级明确流程；不能让模型自行忽略。
  裁剪             确定性排序和选择；记录 omitted_count、舍弃对象、理由和原哈希。
  经验注入         Core 先做硬过滤和排序，只注入当前决策需要的少量经验；不把全经验库放入上下文。
  来源输入         research_evidence_extract 一次只处理已保存的一份 source_snapshot 与相关 claims。
  Creative 输入    script_generate 只见计划、已核验证据、允许经验、账号风格、正反例和用户约束，不见工程上下文。

## 6.5 Run Envelope、缓存与可追溯

每次执行返回统一 Run
Envelope：technical_status、business_result、output_payload、output_schema_version、local_ref_usage、warnings、model/provider、Prompt/Skill/Binding/config
版本与哈希、Token/费用、input/output hash、timing。

-   缓存键至少包含 skill contract、Prompt、模型
    profile、输入、领域配置和 Binding 哈希。

-   相同命令技术重试可复用；创作稿不得因"题目相近"跨任务复用。

-   执行 succeeded 与业务 no_proposal/inconclusive/rejected
    必须分开记录。

-   输出 local_ref 只能引用输入允许集合；幻觉引用必须为 0。

-   模型或 Prompt 正常升级只向前生效；除非确认组件缺陷，不自动重跑历史。

## 6.6 Hermes 与 Skill 的最终隔离

  ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **硬边界\
  **Hermes 只加载"何时调用哪个 Core Tool、需要哪些参数、如何呈现结果"的薄 Tool/Skill。正式 Portable Skill、完整 Prompt、证据包和模型调用只存在于 Core Runner；Hermes 的 SOUL、记忆、工程工具与对话上下文不会进入正式稿件调用。

  ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

# 7 TECH-005：纠错、依赖传播与恢复（完成）

## 7.1 物理对象

  **对象**            **作用**                                                                     **事实性质**
  ------------------- ---------------------------------------------------------------------------- ----------------------------------------------
  correction_record   登记目标、旧/新版本或哈希、变化范围、依据、原因、时间和发起者                正式不可变纠错依据；状态只反映处理进度
  dependency_index    从正式引用、Input Assembly 和 Binding Manifest 物化直接依赖边                可重建索引，不是业务事实来源，不建设图数据库
  correction_impact   记录每个目标的 action_kind、basis hash、处理状态、结果、停止原因和人工事项   可恢复、幂等的传播清单
  correction_report   汇总受影响/无影响、前后结果、停止点、失败、用户确认和 human_attention        完成后不可变报告

## 7.2 最终传播算法

26. 登记 correction_record；同一纠错幂等，不修改旧对象。

27. 从正式版本引用、Input Assembly included/discarded、Binding Manifest
    local_ref 和业务映射解析直接消费者。

28. 按对象类型和"模型是否实际看见"确定
    no_action、deterministic_recompute、semantic_rerun、replace_current_candidate、require_user_reconfirmation、historical_annotation
    或 human_attention_required。

29. 为每个目标创建 correction_impact 和幂等
    Job；每个聚合独立小事务处理。

30. 重算/重跑后比较 business_hash；业务等价且引用无变化即停止。

31. 业务结果不变但 provenance
    变化时创建新版本，按依赖类型决定是否继续，并检查批准继承。

32. 业务结果变化时创建替代版本/新一代结果、切 current、继续传播。

33. 全部处理完成后生成 correction_report；失败可从未完成 impact
    恢复，不重复副作用。

## 7.3 生命周期和用户保护

  **对象/场景**        **处理**
  -------------------- ---------------------------------------------------------------------------------
  production_task      必要前置失效才 blocked；修复后 resume；不倒退正常链。
  批准但未发布         可见正文变化必须新版本并重新确认；严格 provenance-only 纠错可在复审后沿用批准。
  published/reviewed   不倒退，不改历史稿；追加纠正关系、新结果和必要人工提醒。
  冻结实验基线         按原冻结时点、原规则和原成员重算纠正版；不使用当前滚动窗口。
  manual_lock          证据和 level/freshness 仍重算，推荐状态实际迁移冻结并写风险审计。
  deprecated           不自动复活；只能产生 restore proposal，发布后创建新版本。
  组件升级             正常 Prompt/模型/Binding 升级不重跑历史；确认具体缺陷后限定范围 replay。

## 7.4 收敛、防循环与安全上限

-   传播访问键为
    correction_id＋target_ref＋expected_basis_hash＋action_kind，重复执行返回原结果。

-   只沿明确直接依赖向下传播；不按文本相似或全库语义猜测影响。

-   同一目标的相同 basis 不重复处理；新 basis 才形成新 impact。

-   每次纠错允许配置技术安全上限（动作数、深度、总成本）；超过后停止为
    human_attention，不自动扩大。

-   纠错重跑优先使用原
    Skill/Prompt/模型档位/配置/Binding；无法运行时用当前已验证版本并声明混杂。

# 8 TECH-006：独立验证、Clean-room 与 Codex 实施门禁（完成）

## 8.1 验证模式

  **模式**          **目的**                                               **是否需要真实外部服务**
  ----------------- ------------------------------------------------------ --------------------------
  unit              纯状态、哈希、资格、基线、幂等和传播函数               否
  contract          Schema、Port、Adapter、Binding 和 Materializer 契约    否
  fixture           标准静态数据验证完整模块路径                           否
  replay            重放已保存的采集、搜索、模型或平台响应                 否
  fault injection   超时、限流、崩溃、租约过期、重复消息、乱序和部分失败   否
  FakeClock         D/P、TTL、stale、lease、重试和日报时点                 否
  shadow            真实输入但不改变生产 current/pointer；生成对照报告     需要可选真实数据
  live gate         供应商、飞书、平台、真实模型和长期运行                 是，属于外部门禁

## 8.2 物理隔离和删除门禁

-   验证使用独立 PostgreSQL 数据库或独立 Schema、独立文件前缀、独立
    Obsidian 测试 Vault 和独立凭证。

-   生产对象禁止引用 validation
    对象；验证可复制脱敏生产快照，但不能反向成为正式来源。

-   测试工具、fixture、Replay、FakeClock、故障注入器和报告不得成为生产启动依赖。

-   执行 Clean-room 验收：删除整个 validation
    数据库/Schema、测试存储、报告和辅助工具后，生产构建、迁移、启动和核心路径仍通过。

-   测试路径与生产路径使用同一 Core handler、validator、Runner 和
    Materializer；禁止只为测试造第二套业务实现。

## 8.3 标准验证报告

  **字段**     **要求**
  ------------ ---------------------------------------------------------------------
  目标与规格   对应 CR/TECH/Goal、输入版本、代码 commit 和配置哈希。
  执行环境     模式、数据库、FakeClock、Adapter/Model stub/replay/live、资源限额。
  用例结果     通过/失败、预期/实际、输出哈希、审计和副作用。
  追溯证明     从输入对象到输出、Skill Run、版本、pointer 和 audit 的反查路径。
  成本与性能   调用次数、Token、费用、延迟、峰值内存和批次。
  删除证明     验证资产清单、删除步骤和删除后生产健康检查。
  未通过项     明确是逻辑缺陷、待校准还是外部门禁，不把三者混合。

## 8.4 Codex 实施 Goal 最终顺序

  **Goal**   **范围**                                                    **状态**
  ---------- ----------------------------------------------------------- ------------------------------
  GOAL-00    仓库、开发环境、验证基础、FakeClock、报告模板               READY
  GOAL-01    数据库基础、UUID/版本/哈希、审计、command receipt、outbox   READY
  GOAL-02    Core API、状态机、Materializer 事务和权限                   READY
  GOAL-03    Job/Scheduler/Worker、租约、重试、恢复                      READY
  GOAL-04    领域/账号/视频/快照/D/P/文件与采集 Port                     READY WITH EXTERNAL GATES
  GOAL-05    基线、候选、评论、样本包和对照                              READY
  GOAL-06    来源、Search/Fetch/Extract Port、选题与研究执行             READY WITH EXTERNAL GATES
  GOAL-07    Portable Skill Runtime、Binding、Runner、ModelGateway       READY WITH MODEL CALIBRATION
  GOAL-08    研究→计划→稿件→审核→批准→publication capture                READY
  GOAL-09    P+、experiment、CR-002 经验确定性引擎和语义提案             READY
  GOAL-10    Correction、dependency index、传播、blocked/resume 和报告   READY
  GOAL-11    Hermes production-host 薄绑定、飞书白名单工具和消息恢复     READY WITH EXTERNAL GATES
  GOAL-12    端到端 staging、备份恢复、Clean-room 删除与连续运行         READY WITH EXTERNAL GATES

Codex 每完成一个 Goal 必须输出
ExecPlan、变更清单、测试、验证报告和未完成门禁，然后停止等待人工确认；不得因为当前设计已完成而一次性无监督实现全系统。

# 9 TECH 全局两轮审查与跨章节归属

## 9.1 第一轮：冲突、逻辑和可追溯

  **发现风险**                                      **最终处理**
  ------------------------------------------------- -------------------------------------------------------------------
  Codex 可能被当成生产 Host                         明确 Codex 只负责开发；生产 Host 固定为 Hermes。
  Hermes Skill 仍可能污染正式写作                   正式 Skill 和模型调用移到 Core Runner；Hermes 只调用薄工具。
  R05 说 Hermes 架构已确认，但未冻结单/多 Profile   当前一个 production-host；operations-admin 延后到真实高风险需求。
  状态纠错可能实现为倒退                            blocked/resume 独立，正常生命周期单向。
  异步旧结果覆盖新 current                          冻结 basis＋row_revision＋Materializer 前置检查。
  Job 重试重复副作用                                command/job 幂等＋不可变 attempt＋Core 事务。
  通用图谱过度设计                                  仅显式引用与可重建 direct dependency index。
  验证系统成为生产依赖                              物理隔离＋Clean-room 删除门禁。

## 9.2 第二轮：过度设计、多领域、成本和维护

  **检查项**                       **结论**
  -------------------------------- --------------------------------------------------------------------------------------------------
  是否开发独立 Agent Runtime       否；Hermes 提供通用会话/Agent 能力，Core 只做业务系统。
  是否提前建设多 Agent             否；单 production-host 足够，OpenClaw 和 operations Profile 只保留门禁。
  是否让 Host 决定模型/Prompt      否；由 ModelGateway 和 Skill profile 决定。
  是否统一 Token 上限              否；逐 Skill 回归与消融。
  是否写死两个领域                 否；核心只识别 domain_id、Schema 和配置版本。
  是否增加无收益表                 只新增 command_receipt、job_attempt、correction/dependency/impact 等有明确恢复与审计价值的对象。
  是否需要 Redis/Celery/Temporal   当前不需要；PostgreSQL Job 足够。
  是否把外部门禁伪装成设计缺失     否；真实供应商和参数仍待评测，但核心逻辑已可 fixture/replay 验证。
  是否增加用户确认                 否；仍只在最终稿、经验语义发布和明确高风险动作确认。

## 9.3 跨章节能力归属终检

  **能力**             **唯一归属**                                      **不得转移到**
  -------------------- ------------------------------------------------- -----------------------------
  采集/搜索/ASR/评论   Adapter/Port＋Worker                              Skill、Hermes、Materializer
  材料选择与预算       Core Input Assembly                               Binding、Runner、模型
  宿主转换/local_ref   Host Binding                                      Portable Skill
  语义判断与生成       Portable Skill＋独立模型调用                      Core 状态机、Hermes会话
  版本/状态/审计       Core/Materializer                                 Agent、Skill、Adapter
  精确定时/重试        Scheduler/Worker                                  Hermes Cron、聊天记忆
  正式内容风格         script_generate Skill、账号配置、样本与模型回归   Hermes SOUL、Codex AGENTS
  代码实现             Codex＋人工审查                                   生产 Agent

# 10 实施就绪、待校准与外部门禁

  **类别**                     **结论**
  ---------------------------- ----------------------------------------------------------------------------------------------------------------------------------------------------------------
  可以直接实施                 数据库与版本、Core API、状态机、Job、Scheduler/Worker、Input Assembly、Binding、Runner、Materializer、CR-002、生产链、纠错传播、验证基础和 Hermes 薄工具契约。
  需要 fixture/Replay 先实施   采集、ASR、搜索、Fetcher、模型调用和飞书回传均先以 Adapter/Port Fake/Replay 验证完整核心路径。
  待校准                       模型/Prompt、上下文预算、阈值、超时、重试次数、并发、质量、成本和人工修改量。
  外部门禁                     真实 Hermes/飞书权限与恢复、供应商凭证、平台兼容、MediaCrawler/网易云真实运行、搜索/抓取/ASR/模型评测、备份恢复和连续运行。
  没有剩余业务设计节点         后续遇到真实冲突以 CR/TECH 增量处理；不得继续无目的扩展表、状态、Agent 或能力。

  --------------------------------------------------------------------------------------------------------------------------------------
  **下一实际动作\
  **使用 Codex 可执行实施控制包 R02，从 GOAL-00 readiness 审计开始。Codex 只负责实现，不重新讨论运行宿主，也不得把自己接入生产内容链。

  --------------------------------------------------------------------------------------------------------------------------------------

# 附录 A｜逐章继承与覆盖清单

## A.1 V0.5 继承状态

  **V0.5 章节**   **状态**     **说明**
  --------------- ------------ -------------------------------------------------------------------------
  第 1 章         完整保留     未被更高优先级规则改变。
  第 2 章         完整保留     未被更高优先级规则改变。
  第 3 章         完整保留     未被更高优先级规则改变。
  第 4 章         完整保留     未被更高优先级规则改变。
  第 5 章         完整保留     未被更高优先级规则改变。
  第 6 章         完整保留     未被更高优先级规则改变。
  第 7 章         完整保留     未被更高优先级规则改变。
  第 8 章         完整保留     未被更高优先级规则改变。
  第 9 章         完整保留     未被更高优先级规则改变。
  第 10 章        完整保留     未被更高优先级规则改变。
  第 11 章        完整保留     未被更高优先级规则改变。
  第 12 章        修改后保留   MediaCrawler 为默认对标视频执行器，许可门禁豁免；网易云为独立 Adapter。
  第 13 章        完整保留     未被更高优先级规则改变。
  第 14 章        完整保留     未被更高优先级规则改变。
  第 15 章        完整保留     未被更高优先级规则改变。
  第 16 章        完整保留     未被更高优先级规则改变。
  第 17 章        完整保留     未被更高优先级规则改变。
  第 18 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 19 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 20 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 21 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 22 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 23 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 24 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 25 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 26 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 27 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 28 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 29 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 30 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 31 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 32 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 33 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 34 章        修改后保留   业务主线保留，CR-003/004/006、CR-002 与 TECH 提供更细契约。
  第 35 章        完整保留     未被更高优先级规则改变。
  第 36 章        完整保留     未被更高优先级规则改变。
  第 37 章        明确覆盖     飞书直连失效；Hermes production-host；Codex 只开发；OpenClaw 替代候选。
  第 38 章        修改后保留   由 TECH-001---006 物理化；Core API 和 Hermes 薄工具替代旧直连。
  第 39 章        修改后保留   由 TECH-001---006 物理化；Core API 和 Hermes 薄工具替代旧直连。
  第 40 章        修改后保留   由 TECH-001---006 物理化；Core API 和 Hermes 薄工具替代旧直连。
  第 41 章        修改后保留   由 TECH-001---006 物理化；Core API 和 Hermes 薄工具替代旧直连。
  第 42 章        完整保留     未被更高优先级规则改变。
  第 43 章        完整保留     未被更高优先级规则改变。
  第 44 章        明确覆盖     采用 GOAL-00---12 新实施顺序。
  第 45 章        修改后保留   TECH 已完成；剩余项改为待校准和外部门禁。
  第 46 章        完整保留     未被更高优先级规则改变。
  第 47 章        完整保留     未被更高优先级规则改变。

  **V0.5 附录**   **状态**     **说明**
  --------------- ------------ ------------------------------------------------------
  A 参数          修改后保留   数值仍为版本化当前默认/待校准。
  B 字段摘要      移交 TECH    由 TECH-001/002 物理化。
  C 领域配置      完整保留     新增领域接口不变。
  D Skill 契约    修改后保留   由 CR-003 与 TECH-004 扩展。
  E 飞书日报      修改后保留   内容不变，投递由 Hermes。
  F 任务类型      修改后保留   由 TECH-003 注册表扩展。
  G 上线清单      修改后保留   重新划分待校准/外部门禁。
  H 技术参考      历史参考     实施时使用锁定版本官方文档，不作为当前事实唯一来源。
  I 核心原则      完整保留     与 R06 一致。
  J LLM 调用      修改后保留   由 TECH-004 逐 Skill 预算。
  K 状态/门禁     修改后保留   由 TECH-002/006 细化。

## A.2 R05 继承状态

  **R05 章节**   **状态**     **说明**
  -------------- ------------ ------------------------------------------------------------------------------------
  第 0 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 1 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 2 章        修改后保留   Hermes 继续有效；补充 Codex 开发专用、单 production-host、正式 Skill 独立 Runner。
  第 3 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 4 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 5 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 6 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 7 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 8 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 9 章        完整保留     业务逻辑和细粒度契约继续有效。
  第 10 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 11 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 12 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 13 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 14 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 15 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 16 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 17 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 18 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 19 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 20 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 21 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 22 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 23 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 24 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 25 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 26 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 27 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 28 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 29 章       完整保留     业务逻辑和细粒度契约继续有效。
  第 30 章       明确覆盖     未完成 TECH 已完成，保留待校准与外部门禁。
  第 31 章       明确覆盖     TECH-001---006 已完成。

  **R05 附录**        **状态**   **说明**
  ------------------- ---------- ---------------------------------
  A 新对话提示词      失效       由 V0.6 实施版提示词替代。
  B 逐章清单          完整保留   本附录在其基础上继续继承。
  C Skill 矩阵        完整保留   由 TECH-004 物理化。
  D TECH 启动检查表   已完成     检查项已落实到 TECH-001---006。
  E 无损继承门禁      完整保留   继续约束未来文档。

## A.3 CR-002 继承状态

  **CR-002 章节**   **状态**     **说明**
  ----------------- ------------ --------------------------------------------------------------
  第 0 章           完整保留     经验语义和权限不变。
  第 1 章           完整保留     经验语义和权限不变。
  第 2 章           完整保留     经验语义和权限不变。
  第 3 章           完整保留     经验语义和权限不变。
  第 4 章           修改后保留   最小数据模型由 TECH-001 物理化，不增加经验图谱/状态历史表。
  第 5 章           完整保留     经验语义和权限不变。
  第 6 章           完整保留     经验语义和权限不变。
  第 7 章           完整保留     经验语义和权限不变。
  第 8 章           完整保留     经验语义和权限不变。
  第 9 章           完整保留     经验语义和权限不变。
  第 10 章          完整保留     经验语义和权限不变。
  第 11 章          修改后保留   并发、提案发布、证据继承、任务与验收由 TECH 细化，语义不变。
  第 12 章          修改后保留   并发、提案发布、证据继承、任务与验收由 TECH 细化，语义不变。
  第 13 章          修改后保留   并发、提案发布、证据继承、任务与验收由 TECH 细化，语义不变。
  第 14 章          完整保留     经验语义和权限不变。
  第 15 章          完整保留     经验语义和权限不变。
  第 16 章          修改后保留   并发、提案发布、证据继承、任务与验收由 TECH 细化，语义不变。
  第 17 章          完整保留     经验语义和权限不变。
  第 18 章          完整保留     经验语义和权限不变。

CR-002 提案状态正式为 pending_review / published / rejected /
superseded；基础版本变化时必须 superseded。

# 附录 B｜Hermes production-host 实施检查表

  **检查项**   **验收内容**
  ------------ -------------------------------------------------------------------------------
  Profile      独立 HERMES_HOME、config、SOUL、skills、memory、sessions、cron、gateway state
  身份         SOUL 为生产协调员；不包含工程写作腔或正式文案规则
  工具         只开放 Core API 薄工具；无任意 Shell/文件/DB/浏览器
  Skills       正式业务 Skill 仓库只读；skill_manage 不可用；write_approval 开启
  Memory       write_approval 开启；状态和批准只来自 Core
  Curator      关闭
  飞书         用户 allowlist、群聊策略、附件、长任务回传、重复事件幂等
  故障         Hermes 停止时后台继续；恢复后补投 outbox 和待确认项
  成本         Host Token 单独记账；固定按钮/查询不启动开放推理
  验收         真实版本锁定、官方配置核验、权限测试、故障恢复和升级回归

# 附录 C｜官方外部依据（实施时重新核验锁定版本）

  **主题**                   **官方文档**
  -------------------------- ------------------------------------------------------------------------
  Hermes Profiles            https://hermes-agent.nousresearch.com/docs/user-guide/profiles/
  Hermes SOUL.md             https://hermes-agent.nousresearch.com/docs/guides/use-soul-with-hermes
  Hermes Skills 与写入审批   https://hermes-agent.nousresearch.com/docs/user-guide/features/skills/
  Hermes Curator             https://hermes-agent.nousresearch.com/docs/user-guide/features/curator
  Hermes Feishu/Lark         https://hermes-agent.nousresearch.com/docs/user-guide/messaging/feishu
  OpenClaw Multi-Agent       https://docs.openclaw.ai/concepts/multi-agent
  OpenClaw Feishu            https://docs.openclaw.ai/channels/feishu

**爆款口播内容经验库系统**

**V0.5-plus R07｜学习治理与 Codex 续写固化版 · 完整无损继承**

最新增量控制文档｜修订 Hermes 学习治理｜补齐 Codex Goal 断点续写协议

生产宿主：Hermes｜开发执行：Codex｜正式内容模型调用与 Host 隔离

编制日期：2026 年 6 月 30 日

# 0 文档定位、冲突优先级与最终结论

本文件完整继承 R06、R05、CR-002 与 V0.5
的仍有效控制，仅对两项内容作增量修订：一是把"禁止 Hermes 自动创建
Skill"细化为"保留自动记忆和候选改进、禁止未经验证的正式 Skill
静默生效"；二是补齐 Codex 按 Goal
实施时遇到额度、进程、会话或设备中断后的可靠续写协议。除本文件明确覆盖的内容外，R06
TECH-001---006 与既有业务逻辑全部继续有效。

  **项目**       **当前有效口径**
  -------------- ----------------------------------------------------------------------------------------------
  完整执行基线   后续由 V0.6.1 完整执行基线汇编版统一承载。
  最新增量控制   本 R07，取代 R06 作为最新增量控制文档；R06 细粒度 TECH 正文完整继承。
  生产 Host      Hermes production-host；OpenClaw 只作为替代候选；不自建 Agent Runtime。
  开发执行       Codex 只负责代码开发、迁移、测试、验证和维护，不进入生产内容链。
  正式内容生成   由 Core Runner/ModelGateway 发起独立模型调用，不继承 Hermes/Codex 的系统提示词和工程上下文。
  学习治理       Hermes 可自动记忆、形成 Skill 候选；正式 Skill 只读、版本化、经验证后发布。
  Goal 续写      仓库状态、检查点、测试和进度文件是恢复依据；优先恢复原会话，但不把会话作为唯一事实来源。

+----------------------------------------------------------------------+
| **冲突优先级**                                                       |
|                                                                      |
| 用户在当前对话中的最新明确指令 ＞ 本 R07 ＞ R06 ＞ R05 ＞ CR-002 ＞  |
| V0.5 ＞ 更早内容。                                                   |
+----------------------------------------------------------------------+

# 1 当前状态分类

  **分类**           **当前内容**
  ------------------ -------------------------------------------------------------------------------------------------------------------------------------------------
  已确认             Hermes 为生产 Host；Codex 开发专用；正式文案独立模型调用；正式 Skill 不得静默修改；端到端追溯、不可变历史、五层责任和 TECH-001---006 全部有效。
  当前默认           一个 Hermes production-host；Curator 关闭；Memory 保留；Skill 自动改进进入候选区；内容偏好以版本化 Core 档案进入创作输入。
  待校准             偏好推断的重复支持门槛、候选自动发布条件、模型/Prompt/预算、真实文案质量、人工修改量、并发与成本。
  外部门禁           Hermes/飞书真实配置、权限、恢复和升级；模型、搜索、抓取、ASR、采集器和平台兼容。
  不再属于设计缺失   Hermes 如何学习但不污染正式 Skill；Codex Goal 如何在额度中断后续写。

# 2 Hermes 学习治理：保留"越用越懂"，禁止静默污染生产

## 2.1 四层学习结构

  **层级**            **作用**                                                     **自动化权限**                                                **正式事实来源**
  ------------------- ------------------------------------------------------------ ------------------------------------------------------------- ------------------------------------------------
  Hermes 会话记忆     理解用户沟通习惯、常用要求和长期偏好，改善对话与工具选择。   允许自动写入低风险偏好；可展示记忆更新摘要。                  不是业务状态、批准、任务或正式规则来源。
  Core 内容偏好档案   把明确要求、人工改稿、批准/拒绝和正反例转成正式创作输入。    明确长期指令可直接发布新版本；推断偏好先形成候选。            PostgreSQL 版本对象＋不可变来源引用。
  经验自动演进        根据外部样本、自营 P+、实验和反例判断什么方法有效。          按 CR-002 确定性维护证据/等级/状态；语义变化只形成提案。      tactic/tactic_version/tactic_evidence。
  Skill 改进候选      改进可复用工作方法、Prompt 结构、输入输出契约或执行步骤。    Hermes 可提出候选；固定回归、消融、兼容和追溯通过后才发布。   Git 版本化正式仓库；候选区不进入生产加载路径。

+----------------------------------------------------------------------+
| **核心判断**                                                         |
|                                                                      |
| "越来越懂用户"主要依靠                                               |
| Memory、内容偏好档案、批准稿/改稿差异和经验演进；正式 Skill          |
| 不需要每次写稿后自动变化。频繁静默改 Skill                           |
| 会把一次偶然修改误写成永久方法。                                     |
+----------------------------------------------------------------------+

## 2.2 Hermes production-host 权限修订

  **控制点**       **R07 冻结规则**
  ---------------- -----------------------------------------------------------------------------------------------------------------------------------
  Memory           保留启用。允许记录低风险沟通偏好和用户明确的长期习惯；不得存放正式任务状态、批准结果、经验正文或数据库事实作为唯一来源。
  内容偏好指令     当用户明确表达长期内容偏好时，Hermes 除记忆外必须调用白名单 Core 动作 record_content_preference_instruction，形成可追溯正式来源。
  Skill 管理       不再绝对禁用候选创建能力；允许写入独立 candidate/pending 工作区。正式 Skill 仓库只读，生产 loader 只加载 allowlist 的正式版本。
  Skill 写入审批   保持审批门禁；候选不得自动进入正式目录，不得被 production-host 当作正式业务 Skill 调用。
  Curator          默认关闭。正式 Skill 数量受控并由 Git/回归治理，不由 Hermes 自动合并、归档或重写。
  文件与工具       生产 Profile 无任意数据库、Shell、浏览器和正式仓库写入；候选区写入只允许受限工具和固定目录。
  发布责任         通过验证的 Skill 候选由 Codex/受控发布流程合并到正式仓库；发布记录关联 commit、验证报告和触发依据。

## 2.3 正式 Skill 与候选 Skill 的物理隔离

  **区域**                      **允许内容**                                                     **生产是否加载**                       **删除影响**
  ----------------------------- ---------------------------------------------------------------- -------------------------------------- ----------------------------------------------------------
  正式 Portable Skill 仓库      已发布 skill.yaml、Schema、Prompt、examples、tests、contract。   是，只读，按 commit/hash allowlist。   不得随意删除；按版本迁移。
  Hermes candidate/pending 区   候选 Skill、新 Prompt、修改 diff、触发依据和说明。               否。                                   可整体删除，不影响生产运行。
  validation 工作区             固定回归、消融、跨领域、成本、Schema 和兼容报告。                否。                                   可整体删除；正式发布只保留报告摘要、哈希和 commit 引用。

-   候选必须声明 base
    skill/version、修改原因、受影响输入输出、触发样本引用和预期收益。

-   候选不得扩大采集、搜索、ASR、评论、平台或 Adapter 路由职责。

-   候选不得新增用户确认、业务状态、数据库写入或 Host 内部 ID。

-   未通过固定回归的候选可保留、拒绝或删除；不会影响正式生产。

## 2.4 Core 内容偏好档案的最小物理对象

新增对象只解决"正式文案模型如何稳定获得用户偏好"，不建设复杂人格图谱或在线强化学习系统。

  **对象**                      **最小字段/关系**                                                                                                                                                                         **规则**
  ----------------------------- ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- --------------------------------------------------------------------------------------------------------
  content_preference_profile    profile_id、scope_type(global/domain/account)、scope_id、current_revision_id、row_revision、created_at/updated_at                                                                         同一 scope 只有一个稳定根；账号级覆盖领域级，领域级覆盖全局级。
  content_preference_revision   revision_id、profile_id、revision_no、base_revision_id、status(candidate/published/rejected/superseded)、preference_payload、evidence_refs、origin、content_hash、created_at/decided_at   不可变；current pointer 只指 published。候选与正式版本共用同一不可变修订流，避免增加独立 proposal 表。
  正式输入引用                  Input Assembly 记录使用的 preference revision、selected examples、included/discarded 和哈希                                                                                               稿件必须可反查当时实际使用的偏好版本和样本。

  **偏好来源**             **处理**
  ------------------------ -----------------------------------------------------------------------------------
  用户明确长期指令         经 Core 校验后直接创建 published revision；若与当前偏好冲突，保留旧版本并写审计。
  单次人工改稿             只作为 evidence_ref 和候选信号，不自动变成永久规则。
  多次一致改稿/批准/拒绝   形成 candidate revision；自动发布阈值属于待校准，未校准前默认候选不自动发布。
  批准稿与实际发布稿       作为正例候选；是否形成偏好必须区分临时选题需要和长期风格。
  P+结果                   影响经验有效性，不直接证明用户审美偏好；不得把流量结果自动写成风格偏好。

## 2.5 创作输入中的生效顺序

34. Core 根据 production_task 选择全局、领域、账号三个作用域的当前
    published 偏好修订。

35. 确定性合并：账号级明确覆盖领域级，领域级明确覆盖全局级；未声明字段继承上级。

36. 选择少量与当前任务相关的批准正例、拒绝反例和人工修改片段；不把全部历史塞入上下文。

37. Input Assembly 记录每项纳入/舍弃理由、内容哈希和预算。

38. script_generate 只看到自解释的偏好、样本和业务材料，不看到 Hermes
    Memory、技术文档或候选 Skill。

39. script_review
    检查事实、计划、风险和明显风格违背；不把所有偏好都变成机械硬规则。

## 2.6 "越用越好"的验收标准

  **指标**        **验证方式**
  --------------- --------------------------------------------------------------------
  偏好命中        固定选题在旧/新偏好版本下对照，人工判断是否更接近明确要求。
  人工修改量      比较初稿到批准稿的结构、句段和字符级修改，不以单一字符数代替质量。
  工程腔/模板腔   固定回归样本由人工评分＋禁用表达/重复结构检测；不得只靠模型自评。
  跨领域污染      音乐娱乐偏好不得无依据进入泛科普账号；账号级差异必须可追溯。
  事实与风险      偏好升级不能降低 claim/evidence、相似风险和审核通过率。
  成本            新增偏好和样本上下文必须通过消融证明收益，不能无限增长。

# 3 Codex Goal 断点续写协议

## 3.1 基本原则

+----------------------------------------------------------------------+
| **恢复事实来源**                                                     |
|                                                                      |
| Git 仓库状态＋Goal                                                   |
| 进度文件＋测试/验证报告＋最近提交，是断点恢复的正式依据。原 Codex    |
| 会话和 Goal 状态用于提高连续性，但不是唯一事实来源。                 |
+----------------------------------------------------------------------+

-   一个 Goal 对应一个明确验收单元，不用一个 Goal 覆盖 GOAL-00---12
    全部实现。

-   每个 Goal 内再拆原子检查点；检查点必须可以独立验证、提交和恢复。

-   额度耗尽、进程退出、会话丢失、设备重启都只属于执行中断，不改变 Goal
    规格。

-   优先恢复原会话/原
    Goal；无法恢复时允许新会话读取持久化检查点继续，不得从头重做。

-   任何恢复都必须先对账，不能仅凭上一条聊天回复判断文件是否写入或测试是否执行。

## 3.2 每个 Goal 必备的持久化恢复包

  **组成**                             **内容**                                                                                            **更新时点**
  ------------------------------------ --------------------------------------------------------------------------------------------------- -------------------------------------------
  Goal 规格                            目标、非目标、规格依据、验收命令、停止条件。                                                        Goal 启动前冻结；规格变更必须走决定请求。
  ExecPlan                             文件、迁移、测试、验证模式、回滚、Clean-room、风险和检查点。                                        启动时创建；发现事实后受控更新。
  implementation_progress/GOAL-XX.md   当前检查点、完成项、未完成项、修改文件、测试、最近 commit、未提交 diff 状态、下一动作、阻塞原因。   每个检查点完成后及暂停前。
  Git commit                           已通过当前检查点最小验收的代码和测试。                                                              每个原子检查点完成后。
  Validation report                    测试结果、输入/输出哈希、追溯、成本、删除证明和门禁。                                               检查点可留增量；Goal 完成时形成正式报告。

## 3.3 安全检查点规则

40. 开始一个检查点前，确认其范围能在当前可用时间和额度内形成可验证中间状态；无法判断时进一步拆小。

41. 先写或更新测试，再实现生产代码；测试和生产必须走同一
    handler/validator。

42. 完成最小验证后更新进度文件，再提交 Git；提交信息包含 Goal 和
    checkpoint 编号。

43. 不得把未验证的大范围重构与多个检查点混在一个提交中。

44. 预计额度不足时不启动下一大检查点；完成当前最小验证、记录未提交状态并安全暂停。

## 3.4 中断后的标准对账流程

45. 打开原仓库和原分支，优先恢复原 Codex 会话或 Goal。

46. 读取 AGENTS、Goal 规格、ExecPlan、implementation_progress 和最近
    validation report。

47. 执行 git status、git diff、git
    log，确认工作区、最近提交和未提交修改。

48. 运行当前检查点最小验证，确认代码真实状态；不得假定最后一次回复未发出就等于命令未执行。

49. 若未提交修改完整且验证通过，补齐进度并提交；若属于半成品，只完成当前检查点或按证据回退，不得直接丢弃。

50. 从第一个未完成检查点继续，不重新实现已经提交且验证通过的内容。

## 3.5 会话恢复与仓库恢复的关系

  **场景**                     **处理**
  ---------------------------- ---------------------------------------------------------------------------------------------
  原 App Goal 可恢复           继续原 Goal，先执行仓库对账，再恢复执行。
  CLI 会话可恢复               使用官方支持的 resume 入口恢复原会话；仍以仓库和进度文件核对。
  原会话不可访问               创建新会话，读取完整恢复包继续；不得把会话丢失当成重新设计理由。
  额度达到计划上限             等待重置或使用当前计划可用的 credits 选项；不得为了续写私自改用未经批准的 API/模型/供应商。
  同一 Goal 有另一实例在运行   停止新实例；同一分支同一 Goal 禁止并行写入。只读审计可在独立 worktree 进行。

## 3.6 未提交工作区的恢复判定

  **状态**                                **动作**
  --------------------------------------- --------------------------------------------------------------
  只有明确完整的测试/文档改动             运行最小验证，通过后提交并更新进度。
  生产代码已改、测试未完成                先完成该原子检查点的测试，不进入后续范围。
  迁移文件已生成但模型/测试不一致         不得另生成第二份冲突迁移；核对目标状态后修复同一检查点。
  存在未知来源的大量 diff                 暂停，生成差异清单和决定请求；不得自动覆盖或 reset \--hard。
  检查点实现失败且已有可验证基线 commit   只回退该检查点的明确修改；保留失败报告，不影响既有完成项。

## 3.7 Goal 完成与进入下一 Goal

-   只有 Goal 的所有验收命令、两轮审查、跨章节归属检查、Clean-room
    条件和验证报告完成后，才能标记 completed。

-   Goal 完成后停止等待人工确认；不得自动跨入下一个 Goal。

-   额度是否充足不改变验收标准；额度中断不能把"部分完成"包装成 Goal
    完成。

-   下一 Goal 开始时引用前一 Goal 的正式 commit
    和报告，不重新读取聊天摘要代替代码事实。

# 4 对 R06 与 TECH 的明确修订

  **R06 位置**               **原口径**                                               **R07 当前口径**
  -------------------------- -------------------------------------------------------- -----------------------------------------------------------------------------------------------------
  2.3 / 附录 B：Skill 写入   production-host 不开放 skill_manage，正式 Skill 只读。   正式仓库仍只读；允许受限 candidate/pending 区形成 Skill 候选，候选不得进入生产加载。
  2.3 / 附录 B：Memory       memory.write_approval=true，偏保守。                     Memory 保留自动学习低风险沟通偏好；具体审批/通知方式以锁定 Hermes 版本评测，正式状态仍只来自 Core。
  6.4 Creative 输入          账号风格、正反例和用户约束进入 script_generate。         补充版本化 content_preference_profile/revision，并记录实际使用版本和选例。
  8.4 Codex Goal             每个 Goal 输出 ExecPlan、验证报告并停止。                补充原子检查点、持久化进度、Git 提交、额度/会话中断对账和续写协议。
  附录 B：Curator            关闭。                                                   继续关闭；关闭 Curator 不影响 Memory、偏好档案、经验演进和 Skill 候选。

# 5 实施 Goal 归属调整

  **Goal**    **R07 新增实施内容**
  ----------- ----------------------------------------------------------------------------------------------------------------
  GOAL-01     content_preference_profile/content_preference_revision 的稳定根、不可变修订、current pointer、审计和引用。
  GOAL-07     偏好版本和精选正反例进入 Input Assembly；预算、消融、跨领域隔离和 standalone 回归。
  GOAL-08     从明确指令、人工改稿、批准/拒绝和 publication capture 记录 evidence refs；不把单次修改直接升为永久偏好。
  GOAL-09     偏好候选形成与发布门禁；阈值未校准前 inferred candidate 默认不自动发布；与 tactic 经验演进保持分离。
  GOAL-11     Hermes Memory、record_content_preference_instruction 薄工具、candidate Skill 区、正式仓库只读和故障/权限测试。
  全部 Goal   应用 Goal 断点续写协议和 implementation_progress/GOAL-XX.md。

# 6 两轮循环审查与跨章节能力归属

## 6.1 第一轮：冲突、逻辑和安全

  **发现风险**                                  **处理**
  --------------------------------------------- ------------------------------------------------------------
  完全禁用 Skill 管理会损失自我改进             改为候选可生成、正式版只读、验证后发布。
  仅让 Hermes Memory 学习，正式写稿模型看不到   增加 Core 版本化内容偏好档案，并进入 Input Assembly。
  把 P+ 高表现直接当作用户偏好                  明确 P+ 属于经验有效性，不直接证明审美偏好。
  单次人工改稿被永久化                          单次只形成 evidence/candidate；明确长期指令才可直接发布。
  恢复强依赖原会话                              会话优先但非唯一；仓库、进度、测试和 commit 才是事实依据。
  额度突然耗尽导致半写入                        原子检查点、未提交 diff 对账和幂等测试确保可恢复。

## 6.2 第二轮：过度设计、成本和维护

  **检查项**                        **结论**
  --------------------------------- -------------------------------------------------------------------------------------
  是否新建复杂用户画像/知识图谱     否；只增加 profile 根＋不可变 revision。
  是否每次改稿都调用 LLM 总结偏好   否；明确指令和差异先确定性记录，候选汇总按触发条件执行。
  是否无限注入历史样本              否；固定预算和相关性选择，记录 included/discarded。
  是否让 Hermes 候选直接影响生产    否；生产 loader 只加载正式 allowlist。
  是否要求用户审批每个偏好变化      否；明确长期指令直接生效，推断候选按校准门禁处理；只有改变使用方式/边界才要求用户。
  是否把 Codex 会话当状态数据库     否。
  是否需要并行 Agent/新 Runtime     否。

## 6.3 跨章节能力归属

  **能力**         **唯一归属**                                      **禁止**
  ---------------- ------------------------------------------------- ---------------------------------------
  会话偏好记忆     Hermes Memory                                     作为正式任务、批准、经验或业务状态。
  正式内容偏好     Core preference profile/revision                  只存 Hermes 记忆或由 Skill 静默改写。
  内容方法有效性   CR-002 tactic 经验域                              与用户审美偏好混为同一状态。
  Skill 候选生成   Hermes 候选区/受控工具                            进入正式 loader 或直接发布。
  Skill 正式发布   Git＋回归＋Codex/受控发布流程                     由 production-host 静默完成。
  Goal 执行状态    Git＋implementation_progress＋validation report   只依赖 Codex 聊天记忆。

# 7 实施就绪结论

+----------------------------------------------------------------------+
| **最终结论**                                                         |
|                                                                      |
| 完成本 R07 后，业务设计、TECH、运行宿主、学习治理、正式 Skill        |
| 发布边界和 Codex Goal 中断恢复已经闭合。后续不再扩写设计章节，使用   |
| Codex 实施控制包 R03 从 GOAL-00 开始逐项实现。                       |
+----------------------------------------------------------------------+

  **类别**       **内容**
  -------------- ------------------------------------------------------------------------------------------------
  可以直接实施   R06 全部 TECH；R07 preference profile、Skill candidate 隔离和 Goal 恢复协议。
  待校准         推断偏好发布阈值、样本数量/时间跨度、上下文预算、模型与 Prompt、质量和成本。
  外部门禁       Hermes/飞书真实能力、具体配置项、模型/搜索/采集/ASR/平台兼容和长期运行。
  下一动作       使用 Codex 可执行实施控制包 R03 执行 GOAL-00 readiness audit；不得重新讨论 Host 或自建 Agent。

# 附录 A｜逐章继承与覆盖清单

  **来源章节**   **状态**       **说明**
  -------------- -------------- ----------------------------------------------------------------------
  R06 第 0 章    完整保留       未被 R07 改变。
  R06 第 1 章    完整保留       未被 R07 改变。
  R06 第 2 章    修改后保留     Host 路线不变；Memory 和 Skill 候选治理由 R07 细化。
  R06 第 3 章    完整保留       未被 R07 改变。
  R06 第 4 章    完整保留       未被 R07 改变。
  R06 第 5 章    完整保留       未被 R07 改变。
  R06 第 6 章    修改后保留     Portable Skill Runtime 不变；增加 content preference revision 输入。
  R06 第 7 章    完整保留       未被 R07 改变。
  R06 第 8 章    修改后保留     Codex Goal 实施门禁增加断点续写协议。
  R06 第 9 章    完整保留       未被 R07 改变。
  R06 第 10 章   完整保留       未被 R07 改变。
  R06 附录 A     完整保留       逐章继承门禁继续有效。
  R06 附录 B     修改后保留     正式仓库只读与 Curator 关闭保留；Skill 候选和 Memory 规则按 R07。
  R06 附录 C     历史外部依据   实施时重新核验锁定版本。

  **其他来源**   **状态**     **说明**
  -------------- ------------ -----------------------------------------------------------------------
  R05 全文       完整保留     CR-003A---F、CR-004A---C、CR-005、CR-006A---B 细粒度契约继续有效。
  CR-002 全文    完整保留     经验演进与用户偏好学习保持分离；权限模型不变。
  V0.5 全文      修改后保留   所有已被 R05/R06 覆盖的旧 Host/飞书/TECH 口径继续按高优先级版本执行。

# 附录 B｜Hermes production-host 最终检查表

  **检查项**      **验收**
  --------------- --------------------------------------------------------------------------
  Profile         独立 config、SOUL、memory、sessions、cron、credentials 和候选工作区。
  正式仓库        只读；生产 loader 只加载 allowlist commit/hash。
  Candidate 区    可写、不可生产加载、可整体删除；候选有 manifest 和触发引用。
  Memory          可学习低风险用户偏好；不得替代 Core 状态；写入变化可审计/可见。
  Core 偏好工具   明确长期内容偏好必须落入 Core versioned profile。
  Curator         关闭。
  工具            只开放薄 Core Tool 和候选 Skill 受限工具；无任意 DB/Shell/正式仓库写入。
  故障            Hermes 停止不影响后台；恢复补投 outbox；候选区删除不影响生产。

# 附录 C｜GOAL 进度文件模板

\# GOAL-XX Progress\
\
\#\# Goal / branch / spec commit\
\#\# Current checkpoint\
\#\# Completed and verified\
\#\# Modified files / migrations\
\#\# Tests and results\
\#\# Last verified commit\
\#\# Uncommitted worktree state\
\#\# First unfinished action\
\#\# Blockers / external gates\
\#\# Resume instructions

# 附录 D｜实施时核验的官方外部依据

  **主题**                        **官方文档**
  ------------------------------- --------------------------------------------------------------------------------------------------------
  Codex App Goal                  https://developers.openai.com/codex/app/commands
  Follow a goal                   https://developers.openai.com/codex/use-cases/follow-goals
  Codex CLI resume                https://developers.openai.com/codex/cli/features
  Codex CLI reference             https://developers.openai.com/codex/cli/reference
  Non-interactive resume          https://developers.openai.com/codex/noninteractive
  Codex plan usage                https://help.openai.com/en/articles/11369540-using-codex-with-your-chatgpt-plan
  Flexible credits                https://help.openai.com/en/articles/12642688-using-credits-for-flexible-usage-in-chatgpt-freegopluspro
  Hermes Profiles/Memory/Skills   实施时按锁定 Hermes 版本的官方文档重新核验配置名和行为。

# 第六编：跨窗口最终固化修订与单文件治理（完整正式增量）

## 1. 本编定位

本编将 V0.6.1
形成后，在多个对话窗口中已经明确确认、但曾分散在聊天和核对文件中的结论一次性正式落盘。它不压缩、不替换前五编正文；前五编继续完整保留。本编只负责给出更高优先级的新增实施控制，并明确哪些历史判断失效。

本 V0.6.2 同时承担：

完整业务与 TECH 事实来源；

Hermes 生产宿主、内容学习和 Skill 治理事实来源；

Codex 代码实施、Goal 检查点和中断恢复规则；

现有真实代码仓库的审计、复用和迁移规则；

最终审核和正式实施的唯一日常输入文档。

除真实代码仓库本身外，不再要求日常同时上传
V0.5、CR-002、R05、R06、R07、R03
ZIP、跨窗口台账或其他中间说明。它们全部转为历史归档和来源审计材料。

## 2. 当前唯一优先级

用户在当前对话中的最新明确指令 ＞ 本 V0.6.2
第六编和第七编的明确新增/覆盖 ＞ 本文件第五编 R07 ＞ 第四编 R06 ＞
第三编 R05 ＞ 第二编 CR-002 ＞ 第一编 V0.5。

第七编是 Codex
实施控制的完整文本化版本。若第七编内部文件与第六编冲突，以第六编为准；若第七编与前五编冲突，以本节优先级判断。

## 3. 已确认且保持有效的运行路线

Codex
只负责代码开发、迁移、测试、验证和维护，不进入正式生产运行，不作为生产
Host，不直接生成正式稿件。

Hermes production-host 是当前生产 Host。OpenClaw 仅作为 Hermes
在飞书、权限、恢复或真实多 Agent 路由上验收失败后的替代候选。

禁止开发独立通用 Agent Runtime。系统只开发业务
Core、API、Scheduler、Worker、Port/Adapter、Portable
Skill、Runner、ModelGateway、Materializer 和验证设施。

飞书中的 Hermes 只负责会话、澄清、白名单 Core Tool
调用、等待/恢复和结果回传；不得直接写 PostgreSQL，不得绕过 Core 状态机。

正式研究、内容计划、稿件生成和语义审核由 Core 通过 Runner/ModelGateway
发起独立模型调用。该调用不继承 Codex 或 Hermes
的工程上下文、会话记忆、代码仓库和工具系统提示词。

## 4. 四层学习与正式 Skill 治理

### 4.1 Hermes Memory

用于学习低风险的沟通习惯、长期偏好和交互方式。它可以自动积累，但不是业务状态、经验、任务或正式内容规则的唯一事实来源。错误记忆不得改变
Core 数据。

### 4.2 Core 内容偏好档案

通过版本化 content_preference_profile 和不可变
content_preference_revision
承载正式个性化输入。明确长期指令可以形成发布版本；单次改稿只形成证据；多次一致反馈形成候选；未完成阈值校准前，推断候选不得自动发布。global、domain、account
作用域分离，account 覆盖 domain，domain 覆盖 global。

### 4.3 CR-002 经验自动演进

根据外部样本、反例、自营实验和 P+
结果判断内容方法是否有效。经验与用户审美偏好保持独立，P+
不能直接写入用户偏好。

### 4.4 Skill candidate

Hermes 可以在隔离候选区提出 Skill 创建或修改候选。正式 Portable Skill
仓库只读、Git 版本化，production loader 只加载
allowlist。候选必须通过固定回归、消融、Schema、跨领域、成本、追溯和
Clean-room 检查，再由 Codex/受控发布流程进入正式仓库。Hermes
不得静默发布。Curator 在生产 Profile 默认关闭。

## 5. Codex Goal 的定位

Goal 是 Codex 的持续工程执行循环，不是生产
Agent，也不意味着从零重建。每个 Goal
必须有独立规格、ExecPlan、原子检查点、Git
commit、进度文件、测试结果和正式验证报告。

额度耗尽、会话关闭、进程退出或设备重启只暂停执行，不改变 Goal
规格。恢复事实来源为 Git、implementation_progress、测试/验证报告、Goal
规格和模块复用矩阵。原会话优先恢复，但不是唯一依据。

## 6. 已有代码仓库的最终处理原则

### 6.1 禁止两个极端

不得默认清空仓库后全量重建，也不得默认直接沿用所有旧代码。现有代码必须先作为待审计资产接受
GOAL-00 检查。

### 6.2 GOAL-00 的唯一任务

GOAL-00
先冻结现场，真实运行现有安装、启动、migration、健康检查、测试和回放；检查数据库、状态、ID、版本、哈希、审计、Job、Adapter、旧飞书入口、自建
Agent 残留及生产依赖；再逐模块提出 keep、adapt、wrap、replace。

GOAL-00 不实施 GOAL-01---12 的生产功能。完成后必须生成：

CURRENT_REPOSITORY_BASELINE.md

LEGACY_CODE_AUDIT.md

MODULE_REUSE_MATRIX.yaml

MIGRATION_EXECUTION_PLAN.md

implementation_progress/GOAL-00.md

GOAL-00 validation report

用户确认复用矩阵后，才进入 GOAL-01。

### 6.3 四类处理定义

keep：已符合目标设计，行为明确、测试通过，只补必要追溯、测试或文档。

adapt：核心算法或实现可复用，但接口、数据对象、状态、版本、追溯或测试需要迁移。

wrap：第三方组件、采集脚本、ASR、搜索或外部能力可用，通过 Port/Adapter
隔离，不允许掌握业务状态。

replace：与目标结构根本冲突且 adapt/wrap
明显不足。必须给出证据、数据迁移、回滚、测试和成本比较。

即使 replace，也应保留可验证算法、fixtures、replay、样本、原始数据、外部
Adapter 和迁移工具。

## 7. 单文件治理与无损原则

本文件不使用摘要替代前五编。V0.5、CR-002、R05、R06、R07
的完整正文继续原样存在。

第七编完整收录 Codex R04
的所有唯一文本文件、Goal、Schema、模板和启动提示词；标题层级可能为汇编排版而调整，但正文内容不得删减。

R04 source_docs 中的 V0.6.1、R07、R06、CR-002
与前五编重复。重复副本不再二次粘贴，但其全部正文已经在本文件前五编逐字完整收录；附录提供文件映射和哈希，不构成细节省略。

历史错误的《跨窗口审核修订归档台账
V0.1》不再作为事实来源。其错误和纠正记录完整保留在附录的 V0.2
核对文本中。

日常最终审核只审本文件；正式实施只使用本文件和真实代码仓库。仓库内可根据第七编生成/保留分文件控制材料，但用户无需管理多份上位文档。

## 8. 当前仍待真实实施确定的事项

以下不是设计缺失，也不能在文档中提前猜测：

真实仓库每个模块最终属于 keep、adapt、wrap 还是 replace；

旧数据库是否需要一次性转换、阶段迁移或其他数据迁移；

现有采集、ASR、基线、评论、研究、飞书和文案代码的真实复用比例；

哪些模块确实需要完全替换；

模型、Prompt、正常预算、最大已验证输入、阈值、性能、成本和真实供应商兼容性。

这些分别由
GOAL-00、固定回归/消融和外部门禁测试确定，不得重新写成业务设计问题。

# 附录 A：跨窗口已确认修订统一核对结果 V0.2（完整原文）

**爆款口播内容经验库系统**

跨窗口已确认修订统一核对结果

**V0.2｜准确版｜替代错误的 V0.1 归档台账**

核对对象：V0.6.1 完整基线＋R07＋Codex R03＋当前已确认对话结论

核对结论

当前真正"已经确认但尚未落盘"的内容只有一组：现有代码仓库必须先审计、不得默认整体重建，以及
GOAL-00 的迁移审计和复用矩阵规则。Hermes 学习治理、内容偏好档案、正式
Skill/候选 Skill 分离、Codex 仅开发、Goal 断点续写等内容，已经落入
V0.6.1/R07 和 Codex R03。

重要纠错

此前《跨窗口审核修订归档台账 V0.1》中关于"Codex R04 已存在、R04
补丁验证已完成、下一版应为
R05"的表述没有当前文件证据，属于错误归档。当前可核验的最高实施包是
R03；下一次正式修订应命名为 R04。

# 1. 本次实际核对的文件

  **文件**                          **实际角色**                   **核对结论**
  --------------------------------- ------------------------------ ----------------------------------------------------------------------------------
  V0.6.1 完整执行基线汇编版         当前完整设计事实来源           包含 R06/R07 的 Host、学习治理、TECH 和 Goal 恢复修订。
  R07 学习治理与 Codex 续写固化版   V0.6.1 末期增量与继承审计      明确四层学习、正式 Skill/候选 Skill 分离、Core 内容偏好档案和 Goal 断点续写。
  Codex 可执行实施控制包 R03        当前实际存在的代码实施控制包   已包含运行边界、学习治理、Goal 恢复协议和 GOAL-00---12；尚未包含旧仓库迁移审计。
  跨窗口归档台账 V0.1               历史归档尝试                   因错误声称 R04/R05 已存在，不能继续作为事实依据。

# 2. 已经正式落盘的确认项

  **主题**        **已落盘口径**                                                                         **证据落点**
  --------------- -------------------------------------------------------------------------------------- ----------------
  生产宿主        Hermes production-host；OpenClaw 仅作替代；不自建通用 Agent Runtime。                  V0.6.1/R07/R03
  Codex 职责      只负责代码开发、迁移、测试、验证和维护，不进入生产内容链。                             V0.6.1/R07/R03
  正式内容生成    研究、策划、创作、审核由 Core Runner/ModelGateway 独立调用，不继承 Host 工程上下文。   V0.6.1/R07/R03
  四层学习        Hermes Memory、Core 内容偏好、CR-002 经验、Skill candidate 相互分离。                  V0.6.1/R07/R03
  正式 Skill      正式仓库只读、版本化；候选区可写但 production loader 不加载。                          V0.6.1/R07/R03
  内容偏好档案    content_preference_profile/revision 已进入数据与 Goal 设计。                           V0.6.1/R07/R03
  Curator         生产 Profile 默认关闭，不影响 Memory、偏好档案、经验和候选 Skill。                     V0.6.1/R07/R03
  Goal 断点续写   以 Git、progress、测试和验证报告恢复；原会话优先但不是唯一依据。                       V0.6.1/R07/R03
  上下文预算      逐 Skill 通过固定回归与消融确定，不按最大模型窗口堆叠。                                V0.6.1/R03

# 3. 已确认但尚未落盘的唯一修订组

范围判断

下面全部属于"已有仓库如何实施迁移"的控制规则，不改变业务流程、数据口径、Hermes/Core/Skill
边界或 TECH 设计。因此无需重写 V0.6.1，只需在下一版 Codex 实施包 R04
中落盘。

  **编号**   **应落盘规则**
  ---------- ----------------------------------------------------------------------------------------------------------------------
  M-01       Goal 是持续工程执行循环，不代表默认从零重建。
  M-02       现有代码先审计，再逐模块标记 keep / adapt / wrap / replace。
  M-03       GOAL-00 完成前，禁止清空仓库、批量重写、另建平行系统或默认整体替换。
  M-04       GOAL-00 必须记录仓库路径、分支、HEAD、未提交 diff、启动/测试状态、数据库迁移状态和外部依赖。
  M-05       GOAL-00 固定输出 CURRENT_REPOSITORY_BASELINE、LEGACY_CODE_AUDIT、MODULE_REUSE_MATRIX 以及 Goal progress/validation。
  M-06       GOAL-01---12 开始前必须读取模块复用矩阵，只实施当前 Goal 获准的 keep/adapt/wrap/replace 范围。
  M-07       Goal 中断恢复对账必须同时核对当前复用分类、迁移进度、数据迁移状态和未知 diff。
  M-08       旧代码不符合设计时调整代码；不得为了保留旧代码反向修改 V0.6.1。

# 4. 仍然不能提前确定的事项

真实仓库中每个模块最终属于 keep、adapt、wrap 还是 replace。

是否存在必须完全重写的模块；必须依据代码、测试、依赖和迁移成本判断。

旧数据库是否需要数据迁移、双写或一次性转换；必须先读取实际 migration
和数据状态。

已有采集、ASR、基线、评论、研究或飞书代码的真实复用比例。

禁止伪造完成状态

这些结论必须由正式 GOAL-00
检查真实代码仓库后产生。本次核对只能冻结审计方法，不能预先填写模块结论。

# 5. V0.1 归档台账的错误清单

  **错误表述**                                                            **核验结果**                                        **纠正**
  ----------------------------------------------------------------------- --------------------------------------------------- -----------------------------------------------------
  "Codex R04 已存在并取代 R03"                                            当前文件中只有 R03；无 R04 ZIP 或可靠说明。         删除该结论。
  "R04_PATCH_VERIFICATION.md 已证明四项修复"                              当前 R03 包内不存在该文件。                         删除该证据链。
  "下一版应为 R05"                                                        建立在 R04 已存在的错误前提上。                     下一版应为 R04。
  "R03 已失效"                                                            无更高版本实施包可核验。                            R03 仍是当前实施控制包。
  "13 个 Skill 唯一 Goal 映射、B1/B2、C1/C2、candidate manifest 已完成"   当前 R03 文件和 Schema 未完整包含这些声称的结构。   不得视为已完成；如后续确有必要，应在 R04 单独评估。

# 6. 下一版 Codex R04 的最小修订范围

  **文件**                        **最小变更**
  ------------------------------- ---------------------------------------------------------------------------------
  README.md                       声明在真实旧仓库上迁移实施；禁止默认空仓重建；GOAL-00 先审计。
  AGENTS.md                       增加未审计禁止删除/批量改写、设计不迁就旧代码、所有 Goal 读取复用矩阵。
  IMPLEMENTATION_PLAN.md          GOAL-00 改为 Existing Repository Readiness Audit & Validation Foundation。
  goals/GOAL-00.md                加入仓库现状、测试/启动、migration、依赖、模块审计和复用矩阵验收。
  CODEX_GOAL_RESUME_PROTOCOL.md   恢复对账增加复用分类、迁移状态、数据迁移和未知 diff。
  MIGRATION_EXECUTION_RULES.md    新增迁移总规则和 keep/adapt/wrap/replace 定义。
  templates/                      新增 CURRENT_REPOSITORY_BASELINE、LEGACY_CODE_AUDIT、MODULE_REUSE_MATRIX 模板。

# 7. 收敛后的唯一主线

**1.** 以 V0.6.1 作为唯一完整设计事实来源。

**2.** 以当前 R03 为基底生成 R04，只合并 M-01---M-08。

**3.** 对 R04 做一次控制层核对，不再重审整套业务设计。

**4.** 正式实施输入只保留：V0.6.1＋R04 ZIP＋真实代码仓库。

**5.** 先执行 GOAL-00，生成仓库基线、旧代码审计和模块复用矩阵。

**6.** 用户确认复用矩阵后，再进入 GOAL-01---12。

最终准确状态

设计基线：V0.6.1。当前实施包：R03。下一实施包：R04。当前唯一已确认未落盘修订：旧仓库迁移审计与防默认重建规则。R04
不需要改业务设计，只需要补齐实施控制。

# 第七编：Codex 可执行实施控制 R04（完整文本化汇编）

本编逐文件完整收录 R04
的所有唯一文本控制内容。为适应总文档目录，Markdown
标题层级只做排版性下移；正文、字段、规则、Schema 和模板不删减。

## R04 文件完整性清单

  文件                                                       字节数   SHA-256
  ---------------------------------------------------------- -------- ------------------------------------------------------------------
  README.md                                                  815      d7d93427ef04a791d1dc87b02b028679db9eaa4be3b719e372d05053557dafed
  AGENTS.md                                                  3257     bc2fde02e6a69ff6b0f95baacdb16ae188ae859976c846f2a65b975557e8cdd0
  SPEC_INDEX.md                                              1516     b299721c1c2c620e69f9ff3dbb1a948ef5c876a3de553a7865da73b193b24061
  RUNTIME_BOUNDARY.md                                        1619     5ddd07461d42c3156fe5f4f5264593ae47fa0a12ab43f195dddb143ddf0f095b
  LEGACY_CODE_MIGRATION_CONTROL.md                           4312     94cb38e8e51e9e81d2d7b8a5b15e4a61643f3e36ba320a1ac677275f328047f4
  CONTENT_LEARNING_GOVERNANCE.md                             1894     7b4eef0efa005db4a045fdf0b46edbe6254407a05d6878b18f872e125988bc35
  CODEX_GOAL_RESUME_PROTOCOL.md                              2402     2be46b4149c109c3369c5b2d758668837bd19c0902313f81b2d5c8ea0a9172c7
  DECISION_AND_STOP_RULES.md                                 1626     1cdd7a567e829384e5b2e4cdf17af15dd6c7d5304bfdd0e88067f40837e23e23
  IMPLEMENTATION_PLAN.md                                     4067     a83ccb1cf56560e01f00b6308c4b57ff52c18343bee36bd7a6275a0d9600ceee
  MODULE_MANIFEST.yaml                                       2046     4bfbe3b72ab59cd95f392e603ebdac892c7bf4000740c2caa2592b26235ab3a0
  UNRESOLVED_GATES.md                                        1218     d14a4f37eb9610cad7de7e1d8c4dcc9ec65fb9edff6223017a8fc43294aefc6a
  VALIDATION_POLICY.md                                       1362     83446ce0e7342731e30d88f310db00f7b67a4911e80aa2964e7a0415905f77f4
  MULTI_PERSPECTIVE_REVIEW.md                                1049     3cb94a0d1f701572179e2a2ce9f14a39136e6c47742c1c19fdaa5a6cfeb99f45
  goals/GOAL-00.md                                           2744     62e50874b00b60fcf887ad9bfc45273e516cfde00cf99b4a1dd691a9f718caae
  goals/GOAL-01.md                                           1164     fade82610de5a7bfcf85b5749103fde83a08596d9e12101f99c73856ec1b841d
  goals/GOAL-02.md                                           965      d059061f95de25af65166cf475dad87386fcc90819585c29c62e8afeaf5e03b4
  goals/GOAL-03.md                                           947      f58b6308384f8dfe6792bf3914f69943fa93b80c260343512823e3f24cf308c9
  goals/GOAL-04.md                                           960      cb1dad1260f8892e6f07a9a32856b3bbede22a775109fdaf64cc1f2bd72168d7
  goals/GOAL-05.md                                           917      caae90d3f425b976f02f865af22d0667d615962f0938459fdedacb43cb20cfc4
  goals/GOAL-06.md                                           947      ced44437e65a63c39304ab77b06e220ff3c16af2283b4f33449f28bff95c1fae
  goals/GOAL-07.md                                           1200     5fdbace649a0f5ce6d0b3c02b08ed2387e9681d5ce8e549b8391c40d271286d5
  goals/GOAL-08.md                                           1196     c12a6728214082daf1a6b48ba35c58421a5ed3cf558506e2a9897e3667dd91a2
  goals/GOAL-09.md                                           1207     400c55d538f2773753295f48018425f44661b4067c7ff2307aad82db127c7183
  goals/GOAL-10.md                                           926      f3b50bf97de4ef5dc64f6f5a86b31eade96ba0b9259aa5490d403de04f302de0
  goals/GOAL-11.md                                           1254     02d5ea5e6307a60671695e9b7eccd92f49e7d3f84c2865777a5e0f788415c2eb
  goals/GOAL-12.md                                           1159     246da2547a265186fe191055be834b526db276f1cddf75968ccb98474bcf653f
  schemas/command_envelope.schema.json                       648      ef91079c2e304010df854995d90c23265745ec3623f6ba7fed386e3272cb6f61
  schemas/decision_request.schema.json                       652      507f53528415b2457ccd795ee8259a63f4a581a37cf28be489050f2f6111d695
  schemas/run_envelope.schema.json                           705      aa26b0cb4d2b15b5699a71980f519bbe37097ff1aef88edc8e3bc7ac73b6e42c
  schemas/validation_report.schema.json                      599      9b7bd4ce8a1ec72e941a21057df0467060c92b77679b3b262e67c69f8135639d
  templates/EXEC_PLAN_TEMPLATE.md                            217      e9723da05cd787e50818f40434e8765149644d92900c07239d042d4dac264093
  templates/GOAL_PROGRESS_TEMPLATE.md                        725      44b3460f53a9b5e0383be073965b41fe9eab77fc67146f5fb5e2e1877520115c
  templates/VALIDATION_REPORT_TEMPLATE.md                    178      c71b2fb42e33be2ac8a6bf79ca614a6c432b348967261a41c2a6f15d7a0bd486
  templates/LEGACY_CODE_AUDIT_TEMPLATE.md                    232      2e951f792511b70b6db8bc781af6e4774073639b3580607377e54ea56f0a8e67
  templates/MIGRATION_EXECUTION_PLAN_TEMPLATE.md             171      b85914f67e65e9e8d95a58dd99771529f4bf61aff1a4c32ed7b32ca661883f8a
  templates/MODULE_REUSE_MATRIX_TEMPLATE.yaml                362      00a67267f677131a3df95624ba5a31f7c2fcb94aa9307d6008899804e01094b1
  implementation_progress/README.md                          185      0020d6e9661fcfb2fd66dc26066de51b24e1828a70d3f28d20adbb44479d132a
  validation/README.md                                       350      f1d09a70ed33535c1ad8c8080063947d88d68b245f2d0d07f99242bac135212a
  爆款口播内容经验库系统_新对话启动提示词_V0.6.1_实施版.md   801      737c5a774733126c444e03b6e22c3d5c7f6f63bbdc677951c1c90f64eb94f09f

## 文件：README.md

### Codex Control Package R04

本控制包面向**已有代码仓库的受控迁移实施**，不是空仓库全量重建指令。

Start with: 1. AGENTS.md 2. SPEC_INDEX.md 3. RUNTIME_BOUNDARY.md 4.
LEGACY_CODE_MIGRATION_CONTROL.md 5. CODEX_GOAL_RESUME_PROTOCOL.md 6.
CONTENT_LEARNING_GOVERNANCE.md 7. IMPLEMENTATION_PLAN.md 8.
MODULE_MANIFEST.yaml

先执行 GOAL-00 Existing Repository Readiness Audit & Validation
Foundation。 GOAL-00
只允许冻结现场、审计现有实现、建立验证基础并形成复用矩阵；不得实施后续生产功能，不得默认清空仓库或全量重建。

GOAL-00 输出经用户确认后，才进入 GOAL-01---12。后续每个 Goal
必须读取已确认的 MODULE_REUSE_MATRIX.yaml，严格按 keep / adapt / wrap /
replace 决定处理方式。

## 文件：AGENTS.md

### AGENTS.md --- 爆款口播内容经验库系统

#### 最高优先级

用户最新明确指令 \> V0.6.1 \> R07 \> R06 \> R05 \> CR-002 \> V0.5。

Codex 只负责代码开发、迁移、测试、验证和维护，不得作为生产 Host
或正式内容生成器。

禁止自建通用 Agent Runtime；生产 Host 是 Hermes，OpenClaw 仅为替代候选。

PostgreSQL 是业务状态唯一事实来源；Hermes、Obsidian、Codex 会话和 Memory
都不是正式状态。

端到端可追溯、历史不可静默覆盖、Portable Skill
可独立运行、验证资产可抹除是硬门禁。

#### 实施规则

一个 Goal 一个 ExecPlan、一个 progress
文件和一个正式验证报告；完成后停止等待确认。

每个 Goal 拆成可独立测试和提交的原子检查点；每个检查点通过最小验证后更新
progress 并提交 Git。

中断恢复必须先核对 git status/diff/log、progress、测试和最近
commit，从第一个未完成检查点继续。

优先恢复原 Codex
会话/Goal，但会话不是正式恢复事实来源；无法恢复时允许新会话基于持久化恢复包继续。

所有业务写入经过 Core/Materializer；Adapter、Runner、Skill 和 Host
不直接写正式业务状态。

Portable Skill 输入输出不得出现宿主
UUID、ORM、表名、内部状态码或数据库结构。

正式 Skill 仓库只读；Hermes 可在隔离 candidate 区提出 Skill
修改，但候选不得进入生产 loader。

Hermes Memory 可学习低风险沟通偏好；正式内容偏好必须写入 Core 的版本化
preference profile。

不新增未批准的业务状态、核心表、Agent、通用图谱、用户确认节点或 LLM
调用。

不按模型最大窗口堆上下文；逐 Skill 通过回归与消融确定预算。

测试和生产使用同一
handler/validator；真实数据用于校准和外部兼容，不负责第一次验证核心逻辑。

生产外键默认 RESTRICT；正式版本/快照/结果/审计不可 UPDATE/DELETE。

所有命令、Job、Skill Run、版本、审计和 outbox 必须可通过 correlation_id
关联。

#### 已有仓库迁移硬规则

当前任务是已有仓库迁移，不是默认空仓库重建。

GOAL-00 必须先冻结现场并形成
CURRENT_REPOSITORY_BASELINE.md、LEGACY_CODE_AUDIT.md、MODULE_REUSE_MATRIX.yaml
和 MIGRATION_EXECUTION_PLAN.md。

模块只允许 keep / adapt / wrap / replace 四类结论；replace 必须证明
adapt/wrap 不足。

GOAL-00 未完成并经用户确认前，禁止批量删除、全量重写、另建平行系统或重排
migration 历史。

后续 Goal 必须读取已批准复用矩阵，不得因工程偏好重写已验证 keep 模块。

旧代码若与 V0.6.2 冲突，调整代码；不得为了保留旧代码反向修改业务边界。

#### 必须停止并请求决定

规格冲突或缺失会改变业务结果。

需要新增业务状态、表、LLM、Agent、用户确认或跨领域共享状态。

需要真实密钥、生产账号、不可逆平台操作或扩大外部预算。

当前 Goal 无法通过 fixture/replay/fault/FakeClock 客观验证。

同一 Goal/分支已经被另一写入实例占用，或发现无法解释的大量未提交 diff。

## 文件：SPEC_INDEX.md

### SPEC_INDEX.md

#### 优先级

用户最新明确指令

爆款口播内容经验库系统_最终完整执行总控文档_V0.6.2_无损汇编版.docx

source_docs/R07_学习治理与Codex续写固化版.docx

source_docs/R06_TECH完成与运行宿主固化版.docx

source_docs/CR-002_R01.docx

R05/V0.5 的未被覆盖部分（已收录在 V0.6.1）

#### 当前覆盖

V0.5 第 37 章飞书直连失效：Hermes production-host。

Codex 只负责开发，不是生产运行依赖。

正式业务 Skill 由 Core Runner/ModelGateway 独立执行，Hermes 只调用薄
Core Tool。

Hermes Memory 保留；正式内容偏好由 Core 版本化 profile 承载。

正式 Skill 仓库只读；Hermes 可生成隔离候选，但不得静默发布。

每个 Codex Goal 必须按持久化检查点和恢复协议执行。

MediaCrawler 许可门禁已豁免；网易云为独立 Adapter。

TECH-001---006 已完成。

#### 事实来源

业务状态、版本、任务、批准、实验、经验、正式内容偏好：PostgreSQL。

Skill/Prompt/领域/规则正文：Git 仓库。

原始媒体/响应：file_asset + raw payload。

Goal 实施状态：Git commit + implementation_progress + validation
report。

Obsidian：单向派生。

#### 本轮新增覆盖

实施对象是已有真实代码仓库；禁止默认全量重建。

GOAL-00 先形成现状基线、旧代码审计、复用矩阵和迁移计划。

真实模块分类属于实施输出，不是业务设计缺失。

## 文件：RUNTIME_BOUNDARY.md

### RUNTIME_BOUNDARY.md

#### 生产运行

飞书 → Hermes production-host → 白名单 Core API →
PostgreSQL/Scheduler/Worker → Core Runner/ModelGateway → Portable Skill
→ Materializer。

#### Codex

仅代码开发、迁移、测试、验证和维护。

不进入生产运行，不作为 Host，不生成正式稿件。

按 CODEX_GOAL_RESUME_PROTOCOL.md 分 Goal、分检查点实施。

#### Hermes

负责会话、澄清、工具调用、等待/恢复和回传。

一个 production-host Profile；不提前建设多 Agent。

Memory 可学习低风险沟通偏好，但不作为业务状态或正式规则来源。

用户明确长期内容偏好必须通过薄 Core Tool 写入版本化 content preference
profile。

正式 Skill 仓库只读；允许隔离 candidate/pending 区生成候选，生产 loader
不加载候选。

Curator 默认关闭；Skill 正式发布必须经过固定回归、消融、兼容、追溯和 Git
发布流程。

不直接执行正式业务 Skill，不直接写数据库。

#### Core content preference

content_preference_profile：global/domain/account 稳定根。

content_preference_revision：不可变
candidate/published/rejected/superseded 修订流。

明确长期指令可发布新
revision；单次改稿只作证据；推断候选自动发布阈值未校准前默认不自动发布。

Input Assembly 冻结实际使用的 preference revision 和精选正反例。

#### OpenClaw

只在 Hermes 飞书/权限/恢复/路由验收失败或出现真实多 Agent
路由需求时评测。

替换 Host Binding，不改 Core 和 Portable Skill。

## 文件：LEGACY_CODE_MIGRATION_CONTROL.md

### LEGACY_CODE_MIGRATION_CONTROL.md

#### 目标

在不破坏已完成实现、历史数据、可验证算法和外部采集能力的前提下，将现有仓库迁移到
V0.6.2
目标状态。现有代码是待审计资产，不是默认可信实现，也不是默认应删除的旧物。

#### 最高规则

禁止默认全量重建、清空仓库或另建平行系统。

禁止默认直接沿用旧代码。所有模块先审计，再分类。

V0.6.2 决定目标状态；旧代码不能反向修改已冻结业务设计。

GOAL-00 完成前不得批量删除、重命名、重排核心目录或重写数据库迁移链。

任何 replace 都必须说明为什么 adapt 或 wrap
不足，并比较迁移成本、风险、测试与历史数据影响。

即使模块需要替换，也优先保留可验证算法、fixtures、replay、测试数据、原始数据、外部
Adapter 和迁移工具。

#### GOAL-00 必须先冻结现场

记录当前仓库路径、分支、HEAD commit、tag、远端、未提交 diff
和未跟踪文件。

创建独立实施分支或 worktree；不得在唯一现有工作区中直接大范围改写。

记录当前启动方式、依赖安装、Compose、环境变量、数据库版本与 migration
heads。

对未提交工作区做来源判断；未知大 diff 必须停止，禁止 reset \--hard。

对现有数据库和文件资产先备份或快照，再执行迁移测试。

#### 必须真实检查

项目能否安装、启动、迁移和运行健康检查。

现有测试、静态检查和样本回放的实际通过情况。

现有表、状态、ID、版本、哈希、审计、Job 和恢复实现。

飞书直连、自建 Agent Runtime、Codex 生产依赖等已失效路线是否仍残留。

Adapter、ASR、MediaCrawler、网易云、基线、评论、研究和文案链的真实耦合。

配置、密钥、路径、平台字段、外部依赖和数据迁移风险。

代码是否能在无真实服务时用 fixture/replay/FakeClock 验证。

#### 四类决策

##### keep

模块已符合
V0.6.2，行为明确、测试通过、没有越权写状态或旧架构依赖。只补缺失的追溯、测试或文档。

##### adapt

核心算法或业务实现可复用，但接口、数据对象、版本链、状态、追溯、权限或测试方式需要迁移。优先渐进改造，保留可验证行为。

##### wrap

第三方组件、脚本、采集器、ASR、搜索或其他外部能力本身可用，但不得掌握业务状态。通过
Port/Adapter/薄封装隔离，保留原执行能力。

##### replace

现有模块与目标架构根本冲突，无法安全迁移，或修补成本和风险显著高于替换。replace
必须有证据、影响分析、数据迁移与回滚方案。

#### 必须生成的仓库内文件

CURRENT_REPOSITORY_BASELINE.md：仓库、分支、提交、启动、依赖、数据库和测试基线。

LEGACY_CODE_AUDIT.md：逐模块现状、证据、冲突和风险。

MODULE_REUSE_MATRIX.yaml：每个模块的 keep/adapt/wrap/replace 决策。

MIGRATION_EXECUTION_PLAN.md：顺序、依赖、迁移、回滚、验证和停止条件。

implementation_progress/GOAL-00.md：检查点和中断恢复事实。

GOAL-00 validation report：包含两轮审查、跨章节归属和 Clean-room 结论。

#### MODULE_REUSE_MATRIX 最小字段

module_id: string\
path_patterns: \[\]\
current_responsibility: string\
target_responsibility: string\
decision: keep\|adapt\|wrap\|replace\
keep_assets: \[\]\
required_changes: \[\]\
forbidden_changes: \[\]\
data_migration: none\|review\|required\
tests_current: \[\]\
tests_required: \[\]\
dependencies: \[\]\
risks: \[\]\
evidence_refs: \[\]\
approval_status: proposed\|approved\|rejected\|superseded

#### 后续 Goal 的强制使用

每个 Goal 开始前读取已确认复用矩阵。

只能修改属于本 Goal 且已批准的模块范围。

从 adapt 扩大到
replace、跨模块扩大替换范围或改变数据迁移方案时必须停止。

已验证 keep 模块不得因代码风格、目录偏好或"重写更干净"而重写。

新实现必须与旧数据迁移、回滚和历史追溯同时验收。

#### 用户确认门禁

GOAL-00
完成后停止。用户只需确认复用矩阵和迁移边界，不需要逐行审查代码。未确认前不得进入
GOAL-01。

## 文件：CONTENT_LEARNING_GOVERNANCE.md

### CONTENT_LEARNING_GOVERNANCE.md

#### 目标

保留"越用越懂用户、产出越来越好"，同时禁止未经验证的 Skill
变化直接污染正式生产。

#### 四层学习

Hermes Memory：沟通习惯与低风险长期偏好；不是正式业务事实。

Core content preference
profile：明确指令、人工改稿、批准/拒绝和正反例；正式进入创作输入。

CR-002 experience：根据外部样本、P+、实验和反例判断内容方法有效性。

Skill candidate：Hermes 可生成候选；通过验证和 Git 发布后才成为正式
Skill。

#### 最小数据模型

##### content_preference_profile

profile_id

scope_type: global/domain/account

scope_id

current_revision_id

row_revision

created_at / updated_at

##### content_preference_revision

revision_id

profile_id / revision_no / base_revision_id

status: candidate/published/rejected/superseded

preference_payload

evidence_refs

origin: explicit/inferred/mixed

content_hash

created_at / decided_at

#### 生效规则

用户明确长期要求：Core 校验后可直接发布新 revision。

单次人工改稿：只记录 evidence，不自动形成永久规则。

多次一致反馈：形成 candidate；自动发布阈值待校准，未校准前不自动发布。

P+ 结果影响 tactic 经验，不直接证明用户审美偏好。

account 覆盖 domain，domain 覆盖 global；未声明字段继承上级。

#### Skill 候选治理

正式仓库只读、Git 版本化、production loader allowlist。

candidate/pending 区可写、不可生产加载、可整体删除。

候选包含 base version、diff、触发依据、输入输出影响和预期收益。

必须通过固定回归、消融、Schema、跨领域、成本、追溯和 Clean-room 检查。

发布由 Codex/受控流程完成，写 commit、验证报告和 audit；Hermes
不静默发布。

## 文件：CODEX_GOAL_RESUME_PROTOCOL.md

### CODEX_GOAL_RESUME_PROTOCOL.md

#### 最高规则

额度、会话、进程或设备中断只暂停执行，不改变 Goal 规格。恢复事实来源是：
Git + implementation_progress + tests/validation reports + Goal spec。
原 Codex 会话/Goal 优先恢复，但不是唯一事实来源。

#### 每个 Goal 必须具备

独立 Goal 规格与停止条件

ExecPlan

implementation_progress/GOAL-XX.md

原子检查点 Git commits

检查点测试结果

Goal 完成 validation report

#### 检查点流程

拆成可独立验证和提交的原子检查点。

先写/更新测试，再实现生产代码。

运行当前检查点最小验证。

更新 progress 文件。

创建 GOAL-XX checkpoint-N commit。

预计额度不足时，不启动下一大检查点。

#### 中断后对账

优先恢复原会话/Goal；无法恢复可开新会话。

读取 AGENTS、Goal、ExecPlan、progress、最近 validation report。

执行 git status、git diff、git log。

运行当前检查点最小验证。

判断未提交修改是完整、半成品、失败或未知来源。

从第一个未完成检查点继续；不得重做已提交且验证通过的内容。

#### 未提交工作区

完整且测试通过：补齐 progress 并提交。

生产代码已改、测试未完：只完成当前检查点。

迁移冲突：核对目标状态，禁止另造重复迁移。

未知大 diff：停止并提交决定请求，禁止 reset --hard。

失败检查点：只回退该检查点明确修改，保留失败报告。

#### 并发

同一 Goal、同一分支只允许一个写入实例。

只读审计可用独立 worktree；合并前重新验证。

不允许两个 Codex 会话同时修改同一迁移或同一检查点。

#### 已有仓库迁移恢复

恢复时同时读取 CURRENT_REPOSITORY_BASELINE.md、MODULE_REUSE_MATRIX.yaml
和 MIGRATION_EXECUTION_PLAN.md。

核对当前模块的批准分类、数据迁移阶段、旧实现与新实现的切换点。

不得因会话丢失把 adapt/wrap 改成 replace，也不得重新创建已存在的
migration。

GOAL-00 尚未批准时，只能继续审计与验证，不得进入后续实现。

#### Goal 完成

所有验收、两轮审查、跨章节归属、Clean-room 和 validation report
完成后，标记 completed 并停止等待人工确认。不得自动进入下一 Goal。

## 文件：DECISION_AND_STOP_RULES.md

### DECISION_AND_STOP_RULES.md

#### Codex 可自行决定

不改变业务含义的类名、文件拆分、函数名、日志格式和内部优化。

已冻结对象内的普通索引、测试辅助和错误处理实现。

符合 TECH 的 SQLAlchemy/Alembic/FastAPI 常规实现。

按 R07 已明确新增 content_preference_profile /
content_preference_revision，不得另造用户画像或偏好图谱。

按模板拆分原子检查点和内部测试文件。

在已批准复用矩阵范围内，决定 keep 模块的非语义修补、adapt 的内部拆分和
wrap 的薄适配实现。

#### 必须停止

新增/删除规格外业务状态、正式表、正式关系或用户确认。

修改 CR-002 权限、生产状态链、研究视频边界、对标视频链、经验资格或 P+
口径。

把用户偏好与 tactic 经验合并为同一对象，或让 P+ 直接写用户偏好。

让 Skill、Hermes、Adapter 或 Worker 绕过 Core/Materializer 写状态。

让 Hermes candidate Skill 进入生产 loader，或让 Memory
成为正式状态来源。

需要真实凭证、不可逆操作、自动发布或扩大预算。

无法构造可客观验证的完成定义。

同一 Goal/分支存在另一写入实例，或发现无法解释的未知 diff。

未完成 GOAL-00 就开始生产实现；默认全量重建；把 keep/adapt/wrap 扩大为
replace；批量删除旧代码；重写 migration 历史；为了复用旧代码反向修改
V0.6.2。

#### 决定请求模板

已确认事实 / 缺失决定 / 影响对象 / 最小方案 2-3 个 / 推荐默认 / 暂停范围
/ 可继续工作。

## 文件：IMPLEMENTATION_PLAN.md

### IMPLEMENTATION_PLAN.md

按 00→12 顺序执行。GOAL-00 是现有仓库迁移门禁，不是空仓库初始化。每个
Goal 必须单独 ExecPlan、progress 文件、原子检查点
commits、验证报告和人工放行。所有 Goal 遵守
CODEX_GOAL_RESUME_PROTOCOL.md。

#### GOAL-00 Existing Repository Readiness Audit & Validation Foundation

冻结并审计现有代码仓库，同时建立
Compose、测试数据库、FakeClock、fixture/replay/fault、progress/验证报告模板与
Clean-room 基线。GOAL-00 不实现后续生产功能。

**必须输出：**
CURRENT_REPOSITORY_BASELINE.md、LEGACY_CODE_AUDIT.md、MODULE_REUSE_MATRIX.yaml、MIGRATION_EXECUTION_PLAN.md。

**完成定义：** 现有仓库、启动、测试、数据库和 migration
状态已记录；每个模块已提出
keep/adapt/wrap/replace；无真实服务可验证；删除验证资产后生产骨架可启动；中断恢复演练通过；复用矩阵经用户确认。

#### GOAL-01 Persistence and Traceability

实现 UUIDv7、根/版本模式、哈希、引用、审计、command
receipt、outbox、不可变门禁，以及 content preference profile/revision。

**完成定义：** 版本不可覆盖；跨根 pointer 被 DB 拒绝；幂等通过；偏好
candidate 不会成为 current。

#### GOAL-02 Core State and Materializer

实现 production/topic/claim/experiment/tactic 状态、Core Command
Envelope、权限、Materializer 事务。

**完成定义：** 状态单向；stale basis 不激活；确认门禁可验证。

#### GOAL-03 Jobs and Scheduling

实现 Job/attempt、SKIP
LOCKED、lease、heartbeat、retry、cancel、FakeClock scheduler 和恢复。

**完成定义：** 崩溃重跑无重复副作用；Hermes 不在线仍运行。

#### GOAL-04 Acquisition and Time Models

实现领域/账号/视频/raw/file、D/P、Collector/ASR Port、MediaCrawler
与网易云薄 Adapter 接口。

**完成定义：** fixture/replay 通过；真实平台标记 external gate。

#### GOAL-05 Baseline Candidate Comments Samples

实现基线、候选、评论批次、对照和标准样本包。

**完成定义：** 当前视频不入自身基线；缺失值不补 0。

#### GOAL-06 Sources Topics Research

实现 SearchProvider/Fetcher/Extractor Port、source
snapshot、evidence/claim、topic 和研究停止。

**完成定义：** 正式研究无视频平台路径；replay 可完成。

#### GOAL-07 Portable Skill Runtime

实现 Skill Package、Input Assembly、Binding Manifest、Runner、Model
Port、Run Envelope、预算和回归；注入冻结的 preference revision
与精选正反例。

**完成定义：**
standalone；local_ref；技术/业务结果分离；偏好跨领域隔离和消融通过。

#### GOAL-08 Production Version Chain

实现 research/content plan、script、review、批准、publication
capture、人工修改和内容偏好 evidence refs。

**完成定义：**
批准与发布稿分离；每次正文变化新版本；单次改稿不自动发布永久偏好。

#### GOAL-09 Experiments and Experience

实现 P+、metric signal、experiment review、CR-002 确定性引擎、proposal
发布和 preference candidate 门禁。

**完成定义：** 只有正式 primary used 可形成经验资格；P+
不直接写用户偏好；未校准 inferred candidate 不自动发布。

#### GOAL-10 Correction Propagation

实现 correction、direct dependency
index、impact、收敛、blocked/resume、报告和恢复。

**完成定义：** 同一纠错无重复；历史不覆盖。

#### GOAL-11 Hermes Feishu Binding

实现薄 Core Tools、allowlist、消息 outbox、重复事件、长任务回传、Memory
边界、内容偏好指令工具和 Skill candidate 隔离。

**完成定义：** Fake Host 先通过；正式仓库不可写；candidate
不被生产加载；真实 Hermes/飞书为 gate。

#### GOAL-12 End-to-End Staging

全链 staging、备份恢复、Clean-room 删除、故障注入、连续运行和 Goal
中断恢复演练。

**完成定义：** 所有追溯可反查；验证/候选资产可删除；外部门禁单列。

## 文件：MODULE_MANIFEST.yaml

version: R04\
date: \'2026-07-01\'\
runtime_host: hermes\
codex_role: development_only\
goal_resume_protocol: required\
content_learning_governance: R07\
goals:\
- id: GOAL-00\
name: Existing Repository Readiness Audit & Validation Foundation\
status: READY\
checkpoint_resume: required\
external_gates: \[\]\
user_approval_gate: module_reuse_matrix\
- id: GOAL-01\
name: Persistence and Traceability\
status: READY\
checkpoint_resume: required\
external_gates: \[\]\
- id: GOAL-02\
name: Core State and Materializer\
status: READY\
checkpoint_resume: required\
external_gates: \[\]\
- id: GOAL-03\
name: Jobs and Scheduling\
status: READY\
checkpoint_resume: required\
external_gates: \[\]\
- id: GOAL-04\
name: Acquisition and Time Models\
status: READY_WITH_EXTERNAL_GATES\
checkpoint_resume: required\
external_gates:\
- live_provider_or_platform\
- id: GOAL-05\
name: Baseline Candidate Comments Samples\
status: READY\
checkpoint_resume: required\
external_gates: \[\]\
- id: GOAL-06\
name: Sources Topics Research\
status: READY_WITH_EXTERNAL_GATES\
checkpoint_resume: required\
external_gates:\
- live_provider_or_platform\
- id: GOAL-07\
name: Portable Skill Runtime\
status: READY_WITH_CALIBRATION\
checkpoint_resume: required\
external_gates:\
- model_prompt_budget_calibration\
- id: GOAL-08\
name: Production Version Chain\
status: READY\
checkpoint_resume: required\
external_gates: \[\]\
- id: GOAL-09\
name: Experiments Experience Preferences\
status: READY\
checkpoint_resume: required\
external_gates:\
- preference_inference_threshold_calibration\
- id: GOAL-10\
name: Correction Propagation\
status: READY\
checkpoint_resume: required\
external_gates: \[\]\
- id: GOAL-11\
name: Hermes Feishu Binding\
status: READY_WITH_EXTERNAL_GATES\
checkpoint_resume: required\
external_gates:\
- live_provider_or_platform\
- id: GOAL-12\
name: End-to-End Staging\
status: READY_WITH_EXTERNAL_GATES\
checkpoint_resume: required\
external_gates:\
- live_provider_or_platform\
existing_repository_migration: required

## 文件：UNRESOLVED_GATES.md

### UNRESOLVED_GATES.md

当前没有未完成的核心业务或 TECH 规格。剩余事项分为：

#### 待校准

各 Skill 模型、Prompt、normal budget、maximum verified input。

D/P 阈值、批次、并发、超时、重试、性能、费用、真实文案质量和人工修改量。

inferred content preference candidate
的重复支持、时间跨度、冲突和自动发布阈值；未校准前默认不自动发布。

#### 外部门禁

Hermes/飞书真实权限、附件、主动回传、恢复、Memory/Skill
候选配置和版本升级。

SearchProvider、Fetcher/Extractor、ASR、模型和采集器真实评测。

MediaCrawler/网易云 Adapter、平台字段、登录和风控。

备份恢复、连续运行和生产凭证。

Codex 当前版本的 Goal/resume/usage UI
或命令以官方文档和实际客户端为准；恢复协议不依赖某个单一命令。

不得把这些事项重新写成业务设计缺失。

#### GOAL-00 才能确定的实施事实

每个旧模块的 keep/adapt/wrap/replace。

真实代码复用比例、数据库迁移方式和需完全替换的模块。
这些是仓库审计输出，不得提前猜测，也不得重新定义为业务或 TECH 缺失。

## 文件：VALIDATION_POLICY.md

### VALIDATION_POLICY.md

#### 模式

unit / contract / fixture / replay / fault-injection / FakeClock /
shadow / live-gate。

#### 规则

验证与生产使用同一业务代码。

验证使用独立数据库或 Schema、存储前缀、Vault 和凭证。

生产不得引用 validation 对象。

每个模块必须能在没有真实外部服务时完成核心逻辑验收。

外部兼容、质量和参数只能在 live/shadow 阶段确认。

content preference 必须测试 global/domain/account 覆盖、candidate
不生效、跨领域隔离和消融。

Hermes candidate Skill 区必须测试不可被 production loader
发现、可整体删除和正式仓库只读。

每个 Goal 至少执行一次检查点恢复校验；GOAL-00/12 执行强制中断恢复演练。

GOAL-00
必须先记录旧系统实际行为，再验证迁移目标；不能用新测试替代对旧行为的事实审计。

keep/adapt/wrap/replace 每项都必须有可重复证据；replace
需包含数据迁移和回滚验证。

#### Clean-room

删除 validation 数据库/Schema、fixtures、replays、fault tools、FakeClock
辅助、报告、测试 Vault 和 Hermes candidate 区后： 1.
生产依赖安装成功；2. 迁移成功；3. 服务启动；4. 核心健康检查；5. 正式
Skill loader 只加载 allowlist；6. 无生产对象引用 validation/candidate。

## 文件：MULTI_PERSPECTIVE_REVIEW.md

### MULTI_PERSPECTIVE_REVIEW.md

每个 Goal 完成前执行两轮审查，并记录在 validation report：

#### 第一轮：正确性与冲突

规格、状态、权限、追溯、幂等、恢复和失败路径。

R07：Memory/Core preference/CR-002 experience/Skill candidate 是否混用。

中断后是否从正确检查点继续，是否重复迁移、版本或副作用。

GOAL-00 的模块分类是否有证据；是否把可 adapt/wrap 的模块无依据升级为
replace。

#### 第二轮：复杂度与长期维护

是否过度设计、写死领域、增加 LLM/上下文成本、制造双重事实来源。

是否把候选资产变成生产依赖，是否可以 Clean-room 删除。

是否引入无必要 Agent、Runtime、表、状态或用户确认。

是否为了代码整洁进行全量重写，浪费已验证算法、fixtures、数据或 Adapter。

#### 跨章节归属

数据获取 / Core Input Assembly / Host Binding / Portable Skill / Core
Materializer 五层不得混用；Hermes 只做 Host，Codex 只做开发。

## 文件：goals/GOAL-00.md

### GOAL-00 --- Existing Repository Readiness Audit & Validation Foundation

#### Scope

冻结并审计现有代码仓库，确认启动、测试、数据库、migration、外部依赖和模块耦合；建立
Compose、测试数据库、FakeClock、fixture/replay/fault、验证报告与
Clean-room 基线。不得实施 GOAL-01---12 的正式生产功能。

#### Mandatory outputs

CURRENT_REPOSITORY_BASELINE.md

LEGACY_CODE_AUDIT.md

MODULE_REUSE_MATRIX.yaml

MIGRATION_EXECUTION_PLAN.md

implementation_progress/GOAL-00.md

GOAL-00 validation report

#### Acceptance

当前分支、HEAD、tag、未提交 diff、未跟踪文件和工作区来源已记录。

现有安装、启动、迁移、健康检查、测试和回放结果已真实执行并记录。

数据库表、migration heads、数据资产和备份/回滚风险已记录。

每个模块均提出
keep/adapt/wrap/replace，并给出证据、保留资产、必要改动、测试和迁移影响。

不存在未经审计的批量删除、全量重建或平行系统。

无真实服务可完成核心验证；删除验证资产后生产骨架可启动。

强制中断后可依据 progress/Git/测试/复用矩阵继续。

用户确认复用矩阵后才可完成 GOAL-00。

#### Required process

Read V0.6.2、LEGACY_CODE_MIGRATION_CONTROL.md and
CODEX_GOAL_RESUME_PROTOCOL.md.

Create independent implementation branch/worktree; record repository
baseline before changes.

Initialize ExecPlan and implementation_progress/GOAL-00.md.

Execute existing install/start/migration/test paths before making
architecture changes.

Audit modules, dependencies, data and old architecture residues.

Build the reuse matrix; replace is allowed only after adapt/wrap are
shown insufficient.

Establish fixture/replay/fault/FakeClock using production handlers.

After every atomic checkpoint: run minimum validation, update progress,
and commit.

Run two review loops and cross-chapter ownership check.

Produce validation report and Clean-room proof.

Stop for user approval of reuse matrix; do not enter GOAL-01
automatically.

#### Hard stops

No reset \--hard, repository wipe, mass deletion, parallel rebuild,
migration-history rewrite, or unexplained large rename.

No decision to replace the whole system without module-level evidence
and approval.

No real credentials or irreversible external operations.

Unknown large diff, unclear repository ownership, missing backup for
data-changing tests, or conflicting migration heads must stop.

#### Resume additions

Recovery must also verify the current reuse-matrix decision, pending
data migration, worktree/branch and whether an interrupted action was
keep/adapt/wrap/replace.

## 文件：goals/GOAL-01.md

### GOAL-01 --- Persistence and Traceability

#### Scope

实现 UUIDv7、根/版本模式、哈希、引用、审计、command
receipt、outbox、不可变门禁。

#### Acceptance

版本不可覆盖；跨根 pointer 被 DB 拒绝；幂等通过；content preference
candidate 不能成为 current。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-01.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval. \#\#\#\# R07 additions Implement content_preference_profile
and immutable content_preference_revision; current pointer may reference
published only.

## 文件：goals/GOAL-02.md

### GOAL-02 --- Core State and Materializer

#### Scope

实现 production/topic/claim/experiment/tactic 状态、Core Command
Envelope、权限、Materializer 事务。

#### Acceptance

状态单向；stale basis 不激活；确认门禁可验证。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-02.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval.

## 文件：goals/GOAL-03.md

### GOAL-03 --- Jobs and Scheduling

#### Scope

实现 Job/attempt、SKIP
LOCKED、lease、heartbeat、retry、cancel、FakeClock scheduler 和恢复。

#### Acceptance

崩溃重跑无重复副作用；Hermes 不在线仍运行。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-03.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval.

## 文件：goals/GOAL-04.md

### GOAL-04 --- Acquisition and Time Models

#### Scope

实现领域/账号/视频/raw/file、D/P、Collector/ASR Port、MediaCrawler
与网易云薄 Adapter 接口。

#### Acceptance

fixture/replay 通过；真实平台标记 external gate。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-04.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval.

## 文件：goals/GOAL-05.md

### GOAL-05 --- Baseline Candidate Comments Samples

#### Scope

实现基线、候选、评论批次、对照和标准样本包。

#### Acceptance

当前视频不入自身基线；缺失值不补 0。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-05.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval.

## 文件：goals/GOAL-06.md

### GOAL-06 --- Sources Topics Research

#### Scope

实现 SearchProvider/Fetcher/Extractor Port、source
snapshot、evidence/claim、topic 和研究停止。

#### Acceptance

正式研究无视频平台路径；replay 可完成。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-06.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval.

## 文件：goals/GOAL-07.md

### GOAL-07 --- Portable Skill Runtime

#### Scope

实现 Skill Package、Input Assembly、Binding Manifest、Runner、Model
Port、Run Envelope、预算和回归。

#### Acceptance

standalone；local_ref；技术/业务结果分离；偏好注入可追溯、可消融且不跨领域污染。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-07.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval. \#\#\#\# R07 additions Input Assembly must freeze preference
revision(s), selected positive/negative examples, included/discarded
reasons and budget. Run cross-domain isolation and ablation tests.

## 文件：goals/GOAL-08.md

### GOAL-08 --- Production Version Chain

#### Scope

实现 research/content plan、script、review、批准、publication capture
和人工修改。

#### Acceptance

批准与发布稿分离；每次正文变化新版本；偏好证据可追溯且单次改稿不永久化。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-08.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval. \#\#\#\# R07 additions Record explicit preference instructions
and evidence refs from user edits, approvals/rejections and publication
captures. A single edit must not automatically become a durable
preference.

## 文件：goals/GOAL-09.md

### GOAL-09 --- Experiments and Experience

#### Scope

实现 P+、metric signal、experiment review、CR-002 确定性引擎和 proposal
发布。

#### Acceptance

只有正式 primary used 可形成经验资格；P+ 不直接成为用户偏好；未校准
inferred preference 不自动发布。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-09.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval. \#\#\#\# R07 additions Create inferred preference candidates
separately from CR-002 tactics. Until calibrated, inferred candidates
never auto-publish. P+ does not directly prove user style preference.

## 文件：goals/GOAL-10.md

### GOAL-10 --- Correction Propagation

#### Scope

实现 correction、direct dependency
index、impact、收敛、blocked/resume、报告和恢复。

#### Acceptance

同一纠错无重复；历史不覆盖。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-10.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval.

## 文件：goals/GOAL-11.md

### GOAL-11 --- Hermes Feishu Binding

#### Scope

实现薄 Core Tools、allowlist、消息 outbox、重复事件、长任务回传；生产
Skill 不进入 Hermes。

#### Acceptance

Fake Host 先通过；正式 Skill 仓库不可写；candidate 不被生产加载；Memory
不替代 Core；真实 Hermes/飞书为 gate。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-11.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval. \#\#\#\# R07 additions Validate Hermes Memory boundary,
record_content_preference_instruction, read-only formal Skill
repository, isolated candidate workspace, production loader allowlist
and candidate deletion safety.

## 文件：goals/GOAL-12.md

### GOAL-12 --- End-to-End Staging

#### Scope

全链 staging、备份恢复、Clean-room 删除、故障注入和连续运行。

#### Acceptance

所有追溯可反查；外部门禁单列；中断恢复、candidate/validation
删除和生产独立性通过。

#### Required process

Read relevant V0.6.1/R07 sections and CODEX_GOAL_RESUME_PROTOCOL.md.

Create ExecPlan and initialize implementation_progress/GOAL-12.md from
the template.

Write fixture/replay/fault/FakeClock tests first.

Implement using production handlers.

Run two review loops and cross-chapter ownership check.

After every atomic checkpoint: run its minimum test, update progress,
and create a checkpoint commit.

Produce validation report and clean-room proof.

Stop for approval; do not automatically enter the next Goal.

#### Hard stops

No new business states/tables/agents/LLM calls/user confirmations beyond
spec. No real credentials or irreversible external operations without
approval. \#\#\#\# R07 additions Perform a forced interruption/resume
exercise and prove that deleting Hermes candidate/validation assets does
not affect production startup or formal Skill loading.

## 文件：schemas/command_envelope.schema.json

{\
\"\$schema\": \"https://json-schema.org/draft/2020-12/schema\",\
\"type\": \"object\",\
\"required\": \[\
\"actor\",\
\"idempotency_key\",\
\"correlation_id\",\
\"payload\"\
\],\
\"properties\": {\
\"actor\": {\
\"type\": \"string\"\
},\
\"idempotency_key\": {\
\"type\": \"string\"\
},\
\"correlation_id\": {\
\"type\": \"string\"\
},\
\"expected_row_revision\": {\
\"type\": \[\
\"integer\",\
\"null\"\
\]\
},\
\"expected_current_refs\": {\
\"type\": \"object\"\
},\
\"confirmation_token\": {\
\"type\": \[\
\"string\",\
\"null\"\
\]\
},\
\"payload\": {\
\"type\": \"object\"\
}\
}\
}

## 文件：schemas/decision_request.schema.json

{\
\"\$schema\": \"https://json-schema.org/draft/2020-12/schema\",\
\"type\": \"object\",\
\"required\": \[\
\"confirmed_facts\",\
\"missing_decision\",\
\"impact\",\
\"options\",\
\"recommendation\",\
\"paused_scope\"\
\],\
\"properties\": {\
\"confirmed_facts\": {\
\"type\": \"array\"\
},\
\"missing_decision\": {\
\"type\": \"string\"\
},\
\"impact\": {\
\"type\": \"array\"\
},\
\"options\": {\
\"type\": \"array\",\
\"minItems\": 2,\
\"maxItems\": 3\
},\
\"recommendation\": {\
\"type\": \"string\"\
},\
\"paused_scope\": {\
\"type\": \"array\"\
},\
\"work_that_can_continue\": {\
\"type\": \"array\"\
}\
}\
}

## 文件：schemas/run_envelope.schema.json

{\
\"\$schema\": \"https://json-schema.org/draft/2020-12/schema\",\
\"type\": \"object\",\
\"required\": \[\
\"technical_status\",\
\"business_result\",\
\"output_payload\",\
\"input_hash\",\
\"output_hash\",\
\"component_manifest\"\
\],\
\"properties\": {\
\"technical_status\": {\
\"enum\": \[\
\"succeeded\",\
\"failed\",\
\"cancelled\"\
\]\
},\
\"business_result\": {},\
\"output_payload\": {\
\"type\": \"object\"\
},\
\"input_hash\": {\
\"type\": \"string\"\
},\
\"output_hash\": {\
\"type\": \"string\"\
},\
\"component_manifest\": {\
\"type\": \"object\"\
},\
\"usage\": {\
\"type\": \"object\"\
},\
\"warnings\": {\
\"type\": \"array\"\
}\
}\
}

## 文件：schemas/validation_report.schema.json

{\
\"\$schema\": \"https://json-schema.org/draft/2020-12/schema\",\
\"type\": \"object\",\
\"required\": \[\
\"goal\",\
\"commit\",\
\"mode\",\
\"cases\",\
\"traceability\",\
\"clean_room\"\
\],\
\"properties\": {\
\"goal\": {\
\"type\": \"string\"\
},\
\"commit\": {\
\"type\": \"string\"\
},\
\"mode\": {\
\"type\": \"string\"\
},\
\"cases\": {\
\"type\": \"array\"\
},\
\"traceability\": {\
\"type\": \"object\"\
},\
\"cost_performance\": {\
\"type\": \"object\"\
},\
\"clean_room\": {\
\"type\": \"object\"\
},\
\"unresolved\": {\
\"type\": \"array\"\
}\
}\
}

## 文件：templates/EXEC_PLAN_TEMPLATE.md

### ExecPlan

Goal / Spec basis / Scope / Non-goals / Files / Migrations / Atomic
checkpoints / Minimum test per checkpoint / Validation modes / Rollback
/ Clean-room / Risks / Stop conditions / Resume entry / Results.

## 文件：templates/GOAL_PROGRESS_TEMPLATE.md

### GOAL-XX Progress

#### Goal / branch / spec commit

Goal:

Branch/worktree:

Spec commit/hash:

#### Current checkpoint

ID:

Objective:

Status: not_started / in_progress / verified / blocked

## \#\#\#\# Completed and verified

## \#\#\#\# Modified files / migrations

#### Tests and results

Command:

Result:

Report/ref:

#### Last verified commit

SHA:

Message:

#### Uncommitted worktree state

git status summary:

Expected diff:

Unknown diff:

## \#\#\#\# First unfinished action

## \#\#\#\# Blockers / external gates

#### Resume instructions

Read this file and the Goal ExecPlan.

Run git status/diff/log.

Run the checkpoint minimum validation.

Continue from the first unfinished action; do not redo verified commits.

## 文件：templates/VALIDATION_REPORT_TEMPLATE.md

### Validation Report

Goal / Commit / Environment / Cases / Traceability proof / Fault results
/ Cost-performance / Clean-room deletion / Unresolved calibration /
External gates.

## 文件：templates/LEGACY_CODE_AUDIT_TEMPLATE.md

### LEGACY_CODE_AUDIT

#### Repository baseline

#### Startup and test evidence

#### Database and migration evidence

#### Module-by-module findings

#### Old architecture residues

#### Data migration risks

#### Proposed reuse matrix references

## 文件：templates/MIGRATION_EXECUTION_PLAN_TEMPLATE.md

### MIGRATION_EXECUTION_PLAN

#### Approved scope

#### Module order and dependencies

#### Data migration

#### Compatibility and cutover

#### Rollback

#### Validation

#### Stop conditions

## 文件：templates/MODULE_REUSE_MATRIX_TEMPLATE.yaml

modules:\
- module_id: example\
path_patterns: \[\]\
current_responsibility: \'\'\
target_responsibility: \'\'\
decision: keep\
keep_assets: \[\]\
required_changes: \[\]\
forbidden_changes: \[\]\
data_migration: none\
tests_current: \[\]\
tests_required: \[\]\
dependencies: \[\]\
risks: \[\]\
evidence_refs: \[\]\
approval_status: proposed

## 文件：implementation_progress/README.md

### implementation_progress

Create one GOAL-XX.md from templates/GOAL_PROGRESS_TEMPLATE.md when a
Goal starts. Update it after each verified checkpoint and before any
planned pause.

## 文件：validation/README.md

### Validation

Use unit/contract/fixture/replay/fault/FakeClock first. Live credentials
are external gates.

Each Goal validation report must include: - checkpoint commits and
resume entry - forced interruption result when required - traceability
and idempotency - clean-room deletion proof - R07 preference/candidate
isolation tests where applicable

## 文件：爆款口播内容经验库系统_新对话启动提示词_V0.6.1_实施版.md

### 爆款口播内容经验库系统｜V0.6.2 实施启动提示词

请完整读取《爆款口播内容经验库系统_最终完整执行总控文档_V0.6.2_无损汇编版》和当前真实代码仓库。

本次只执行 GOAL-00 Existing Repository Readiness Audit & Validation
Foundation： - 冻结并记录现有仓库现场； -
运行现有安装、启动、迁移和测试； - 形成
CURRENT_REPOSITORY_BASELINE、LEGACY_CODE_AUDIT、MODULE_REUSE_MATRIX 和
MIGRATION_EXECUTION_PLAN； - 对模块逐项提出 keep/adapt/wrap/replace； -
建立验证基础和中断恢复检查点； - 不实施
GOAL-01---12，不默认全量重建，不清空仓库，不重写 migration 历史。

完成后停止，提交审计结果、复用矩阵、阻塞项和推荐迁移顺序，等待我确认。

# 附录 B：R03 人工审阅说明（完整历史原文）

**爆款口播内容经验库系统**

**Codex 可执行实施控制包 R03｜学习治理修订 · Goal 可恢复实施**

人工审阅说明｜基于 V0.6.1 与 R07

正式生产 Host：Hermes｜Codex：仅代码开发｜一个 Goal 一个恢复包

编制日期：2026 年 6 月 30 日

# 1 文档定位

R03 取代 R02。R02 的 TECH 和 Goal 00---12 主体继续有效；R03 合并 R07 的
Hermes 学习治理、Core 内容偏好档案，以及 Codex
在额度、会话、进程或设备中断后的断点续写协议。

  **项目**    **当前口径**
  ----------- ----------------------------------------------------------------------
  执行基线    V0.6.1 完整执行基线汇编版
  最新增量    R07 学习治理与 Codex 续写固化版
  生产 Host   Hermes production-host
  开发工具    Codex 仅开发、测试、迁移、验证和维护
  正式内容    Core Runner/ModelGateway 独立模型调用
  学习        Memory＋Core preference＋CR-002 experience＋Skill candidate 四层分离
  恢复        Git＋progress＋tests/report＋Goal spec；会话优先但不是唯一依据

# 2 R02 到 R03 的主要变化

  **变化**            **R03 处理**
  ------------------- -------------------------------------------------------------------------------
  Hermes Skill 写入   由"完全不开放"细化为"正式仓库只读＋隔离 candidate 区可生成候选＋验证后发布"。
  Hermes Memory       保留低风险偏好学习；不作为任务、批准或业务规则事实来源。
  内容个性化          新增 content_preference_profile/revision，并进入 Input Assembly。
  偏好推断            单次改稿不永久化；推断候选阈值未校准前不自动发布。
  Codex Goal          每个 Goal 增加 progress 文件、原子检查点 commit、最小测试和恢复对账。
  恢复容错            原会话可恢复时优先使用；不可恢复时允许新会话基于仓库事实继续。

# 3 控制包目录

  **文件/目录**                    **用途**
  -------------------------------- ----------------------------------------------------
  AGENTS.md                        最高优先级、停止条件和实施边界。
  SPEC_INDEX.md                    V0.6.1/R07/R06/CR-002 规格优先级。
  RUNTIME_BOUNDARY.md              Hermes、Core、ModelGateway、Codex 与偏好学习边界。
  CONTENT_LEARNING_GOVERNANCE.md   四层学习和 Skill 候选治理。
  CODEX_GOAL_RESUME_PROTOCOL.md    额度/会话/进程中断后的检查点和恢复协议。
  IMPLEMENTATION_PLAN.md           GOAL-00---12 顺序与 R07 归属。
  goals/                           每个 Goal 范围、验收、R07 新增内容和强制流程。
  implementation_progress/         每个 Goal 的持久化进度文件。
  templates/                       ExecPlan、Goal progress、validation report 模板。
  source_docs/                     只读 V0.6.1、R07、R06、CR-002。

# 4 Codex 必须遵守的执行方式

先读取
AGENTS、SPEC_INDEX、RUNTIME_BOUNDARY、两个治理协议、IMPLEMENTATION_PLAN
和 MODULE_MANIFEST，只执行 GOAL-00 readiness 审计。

一个 Goal 一个 ExecPlan 和 progress 文件；Goal 内拆成原子检查点。

每个检查点先写/更新测试，完成最小验证后更新 progress 并提交 Git。

预计额度不足时不启动下一大检查点；安全记录当前状态后暂停。

恢复时先核对 git status/diff/log、progress
和测试，从第一个未完成检查点继续。

原会话不可恢复时可用新会话续写，但不得重新设计、重做已验证内容或覆盖未知
diff。

Goal 全部验收后生成正式 validation report 并停止等待确认。

# 5 Codex 绝对不得做

不得把 Codex 本身接入生产 Host、Scheduler、正式模型调用或飞书机器人。

不得自建通用 Agent Runtime、多 Agent 框架或会话记忆系统。

不得让 Hermes Memory 替代 Core 正式内容偏好或业务状态。

不得把 Skill candidate 放入生产 loader，或让 Hermes 静默发布正式 Skill。

不得把单次人工改稿直接写成永久偏好，或把 P+ 高表现直接当作用户审美。

不得在恢复时 reset 未知
diff、重复生成迁移、重做已通过检查点或并行修改同一 Goal/分支。

不得恢复飞书直连、改写视频/研究边界、绕过 Core/Materializer。

# 6 Goal readiness 调整

  **Goal**   **新增验收**
  ---------- --------------------------------------------------------------------
  GOAL-00    progress 模板、checkpoint commit 规则和强制中断恢复演练基础。
  GOAL-01    content preference 根/修订与 current pointer。
  GOAL-07    偏好输入、选例、预算、消融与跨领域隔离。
  GOAL-08    明确指令、改稿、批准/拒绝的 evidence refs。
  GOAL-09    推断偏好 candidate 门禁；与 tactic/P+ 分离。
  GOAL-11    Hermes Memory、Core 偏好工具、Skill candidate/正式仓库隔离。
  GOAL-12    端到端中断恢复与删除 candidate/validation 资产的 Clean-room 证明。

# 7 当前剩余的非设计事项

  **类别**         **内容**
  ---------------- -------------------------------------------------------------------------------
  待校准           模型/Prompt/预算、偏好推断阈值、真实质量、人工修改量、性能和成本。
  外部门禁         Hermes/飞书真实配置、模型/搜索/采集/ASR/平台、备份和连续运行。
  不阻塞核心实现   全部外部能力先用 fixture/replay/fake 验证；偏好推断未校准前只生成 candidate。

# 8 交付使用步骤

解压 R03 ZIP 到新 Git 仓库根目录。

把 source_docs 保留为只读规格依据。

确认同一分支没有其他 Codex 写入实例。

执行 GOAL-00 readiness audit，不写生产功能。

按 GOAL 顺序实施；每个 Goal 单独验收和人工放行。

额度或会话中断时按 CODEX_GOAL_RESUME_PROTOCOL 恢复，不新建重复 Goal。

GOAL-11 前不要求真实 Hermes/飞书凭证；GOAL-12 后才进入生产部署门禁。

# 附录 C：R04 source_docs 无损映射

  R04 内部来源文件                                 本总文档收录位置                       SHA-256
  ------------------------------------------------ -------------------------------------- ------------------------------------------------------------------
  source_docs/V0.6.1_完整执行基线汇编版.docx       本文件前五编及第六编整合后的主体来源   b160bbbfd3332c313b4ca40136608aaee47e709eae0339fc149a2131448e87fe
  source_docs/R07_学习治理与Codex续写固化版.docx   本文件第五编完整原文                   6924d0fefa404e84a5234ee77e9244b9d1cfe0a8926d995e2bccada6d6b459f6
  source_docs/R06_TECH完成与运行宿主固化版.docx    本文件第四编完整原文                   154aeefdef06f52798c8cb8c6bb9998ddf314ab643daef33d6bee86fcd68069b
  source_docs/CR-002_R01.docx                      本文件第二编完整原文                   30cb2eb7e508350e00f848ad88c4536c2b5f3c23c4b0db8a0d4cccd58216e05e

这些文件不在第七编再次重复粘贴，是因为相同正文已在前五编完整收录，而非摘要、删减或省略。
